// Generated from trusted autoV sources; do not edit.
#ifndef AVFX_STANDALONE
#include "/Engine/Public/Platform.ush"
#endif
ByteAddressBuffer AVFXVertices;
ByteAddressBuffer AVFXIndices;
ByteAddressBuffer AVFXData;
ByteAddressBuffer AVFXInstances;
Texture2D<float4> uTexture;
SamplerState _uTexture_sampler;
Texture2D<float4> uNoise;
SamplerState _uNoise_sampler;
Texture2D<float4> uAlphaTexture;
SamplerState _uAlphaTexture_sampler;
Texture2D<float4> uMask;
SamplerState _uMask_sampler;
Texture2D<float4> uNormalMap;
SamplerState _uNormalMap_sampler;
Texture2D<float4> uSites;
SamplerState _uSites_sampler;
Texture2D<float4> uTrail;
SamplerState _uTrail_sampler;
Texture2D<float4> tDepth;
SamplerState _tDepth_sampler;

static float4 gl_Position;
static int gl_VertexIndex;
static float3 vLobe;
static float3 vWp;
static float3 vN;
static float3 vV;

struct SPIRV_Cross_Input
{
    uint gl_VertexIndex : SV_VertexID;
};

struct SPIRV_Cross_Output
{
    float3 vN : TEXCOORD0;
    float3 vV : TEXCOORD1;
    float3 vWp : TEXCOORD2;
    float3 vLobe : TEXCOORD3;
    float4 gl_Position : SV_Position;
};

float3 safeDir(float3 v, float3 fallback)
{
    float l = length(v);
    float3 _406;
    if (l > 9.9999997473787516355514526367188e-06f)
    {
        _406 = v / l.xxx;
    }
    else
    {
        _406 = fallback;
    }
    return _406;
}

float3 mod289v3(float3 x)
{
    return x - (floor(x * 0.00346020772121846675872802734375f) * 289.0f);
}

float4 mod289v4(float4 x)
{
    return x - (floor(x * 0.00346020772121846675872802734375f) * 289.0f);
}

float4 permute289(float4 x)
{
    float4 param = ((x * 34.0f) + 1.0f.xxxx) * x;
    return mod289v4(param);
}

float4 taylorInvSqrtV(float4 r)
{
    return 1.792842864990234375f.xxxx - (r * 0.8537347316741943359375f);
}

float snoise(float3 v)
{
    float3 i = floor(v + dot(v, 0.3333333432674407958984375f.xxx).xxx);
    float3 x0 = (v - i) + dot(i, 0.16666667163372039794921875f.xxx).xxx;
    float3 g = step(x0.yzx, x0);
    float3 l = 1.0f.xxx - g;
    float3 i1 = min(g, l.zxy);
    float3 i2 = max(g, l.zxy);
    float3 x1 = (x0 - i1) + 0.16666667163372039794921875f.xxx;
    float3 x2 = (x0 - i2) + 0.3333333432674407958984375f.xxx;
    float3 x3 = x0 - 0.5f.xxx;
    float3 param = i;
    i = mod289v3(param);
    float4 param_1 = i.z.xxxx + float4(0.0f, i1.z, i2.z, 1.0f);
    float4 param_2 = (permute289(param_1) + i.y.xxxx) + float4(0.0f, i1.y, i2.y, 1.0f);
    float4 param_3 = (permute289(param_2) + i.x.xxxx) + float4(0.0f, i1.x, i2.x, 1.0f);
    float4 p = permute289(param_3);
    float n_ = 0.14285714924335479736328125f;
    float3 ns = (float3(2.0f, 0.5f, 1.0f) * n_) - float3(0.0f, 1.0f, 0.0f);
    float4 j = p - (floor((p * ns.z) * ns.z) * 49.0f);
    float4 x_ = floor(j * ns.z);
    float4 y_ = floor(j - (x_ * 7.0f));
    float4 x = (x_ * ns.x) + ns.yyyy;
    float4 y = (y_ * ns.x) + ns.yyyy;
    float4 h = (1.0f.xxxx - abs(x)) - abs(y);
    float4 b0 = float4(x.xy, y.xy);
    float4 b1 = float4(x.zw, y.zw);
    float4 s0 = (floor(b0) * 2.0f) + 1.0f.xxxx;
    float4 s1 = (floor(b1) * 2.0f) + 1.0f.xxxx;
    float4 sh = -step(h, 0.0f.xxxx);
    float4 a0 = b0.xzyw + (s0.xzyw * sh.xxyy);
    float4 a1 = b1.xzyw + (s1.xzyw * sh.zzww);
    float3 p0 = float3(a0.xy, h.x);
    float3 p1 = float3(a0.zw, h.y);
    float3 p2 = float3(a1.xy, h.z);
    float3 p3 = float3(a1.zw, h.w);
    float4 param_4 = float4(dot(p0, p0), dot(p1, p1), dot(p2, p2), dot(p3, p3));
    float4 norm = taylorInvSqrtV(param_4);
    p0 *= norm.x;
    p1 *= norm.y;
    p2 *= norm.z;
    p3 *= norm.w;
    float4 m = max(0.60000002384185791015625f.xxxx - float4(dot(x0, x0), dot(x1, x1), dot(x2, x2), dot(x3, x3)), 0.0f.xxxx);
    m *= m;
    return 42.0f * dot(m * m, float4(dot(p0, x0), dot(p1, x1), dot(p2, x2), dot(p3, x3)));
}

float lobeR(float3 n)
{
    float3 q = (n * asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 8))) + float3(asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 0)) * 7.30000019073486328125f, (asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 0)) * 3.099999904632568359375f) - (asfloat(AVFXData.Load(0)) * asfloat(AVFXData.Load(16))), asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 0)) * 11.69999980926513671875f);
    float3 param = q;
    float3 param_1 = (q * 2.099999904632568359375f) + 5.0f.xxx;
    float3 param_2 = (q * 4.30000019073486328125f) + 11.0f.xxx;
    float f = ((0.60000002384185791015625f * snoise(param)) + (0.300000011920928955078125f * snoise(param_1))) + (0.1500000059604644775390625f * snoise(param_2));
    return 1.0f + (asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 4)) * ((0.449999988079071044921875f * f) + (0.550000011920928955078125f * abs(f))));
}

float3 lobeP(float3 n)
{
    float3 param = n;
    float3 p = n * lobeR(param);
    float s = clamp((p.y * 0.5f) + 0.5f, 0.0f, 1.0f);
    float3 _554 = p;
    float2 _556 = _554.xz * lerp(1.0f, 1.0f - asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 4)), smoothstep(0.20000000298023223876953125f, 1.0f, s));
    p.x = _556.x;
    p.z = _556.y;
    float a = asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 0)) * p.y;
    float3 _585 = p;
    float2 _587 = mul(_585.xy, float2x2(float2(cos(a), -sin(a)), float2(sin(a), cos(a))));
    p.x = _587.x;
    p.y = _587.y;
    float3 _628 = p;
    float2 _630 = mul(_628.xy, float2x2(float2(cos(asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 8))), -sin(asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 8)))), float2(sin(asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 8))), cos(asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 8))))));
    p.x = _630.x;
    p.y = _630.y;
    p.y *= asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 3u) * 16 + 12));
    return p;
}

void vert_main()
{
    vLobe = asfloat(AVFXVertices.Load4(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 5u) * 16 + 0)).xyz;
    float3 param = asfloat(AVFXVertices.Load4((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) * 16 + 0)).xyz;
    float3 param_1 = float3(0.0f, 1.0f, 0.0f);
    float3 n = safeDir(param, param_1);
    float3 param_2 = n;
    float3 p = lobeP(param_2);
    bool3 _684 = (abs(n.y) < 0.89999997615814208984375f).xxx;
    float3 up = float3(_684.x ? float3(0.0f, 1.0f, 0.0f).x : float3(1.0f, 0.0f, 0.0f).x, _684.y ? float3(0.0f, 1.0f, 0.0f).y : float3(1.0f, 0.0f, 0.0f).y, _684.z ? float3(0.0f, 1.0f, 0.0f).z : float3(1.0f, 0.0f, 0.0f).z);
    float3 param_3 = cross(n, up);
    float3 param_4 = float3(1.0f, 0.0f, 0.0f);
    float3 t1 = safeDir(param_3, param_4);
    float3 param_5 = cross(n, t1);
    float3 param_6 = float3(0.0f, 0.0f, 1.0f);
    float3 t2 = safeDir(param_5, param_6);
    float e = 0.054999999701976776123046875f;
    float3 param_7 = n + (t1 * e);
    float3 param_8 = n;
    float3 param_9 = safeDir(param_7, param_8);
    float3 param_10 = n + (t2 * e);
    float3 param_11 = n;
    float3 param_12 = safeDir(param_10, param_11);
    float3 param_13 = cross(lobeP(param_9) - p, lobeP(param_12) - p);
    float3 param_14 = n;
    float3 nrm = safeDir(param_13, param_14);
    if (dot(nrm, n) < 0.0f)
    {
        nrm = -nrm;
    }
    p += (nrm * asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 4u) * 16 + 12)));
    p = (p * asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 6u) * 16 + 12))) + asfloat(AVFXVertices.Load4(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 7u) + 6u) * 16 + 0)).xyz;
    float4 wp = mul(float4(p, 1.0f), float4x4(float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))), float4(asfloat(AVFXData.Load4(672))), float4(asfloat(AVFXData.Load4(688)))));
    vWp = wp.xyz;
    float4x4 _846 = float4x4(float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))), float4(asfloat(AVFXData.Load4(672))), float4(asfloat(AVFXData.Load4(688))));
    float3 param_15 = mul(nrm, float3x3(_846[0].xyz, _846[1].xyz, _846[2].xyz));
    float3 param_16 = float3(0.0f, 1.0f, 0.0f);
    vN = safeDir(param_15, param_16);
    float3 param_17 = asfloat(AVFXData.Load4(976)).xyz - wp.xyz;
    float3 param_18 = float3(0.0f, 0.0f, 1.0f);
    vV = safeDir(param_17, param_18);
    gl_Position = mul(wp, mul(float4x4(float4(asfloat(AVFXData.Load4(704))), float4(asfloat(AVFXData.Load4(720))), float4(asfloat(AVFXData.Load4(736))), float4(asfloat(AVFXData.Load4(752)))), float4x4(float4(asfloat(AVFXData.Load4(832))), float4(asfloat(AVFXData.Load4(848))), float4(asfloat(AVFXData.Load4(864))), float4(asfloat(AVFXData.Load4(880))))));
}

SPIRV_Cross_Output MainVS(SPIRV_Cross_Input stage_input)
{
    gl_VertexIndex = int(stage_input.gl_VertexIndex);
    vert_main();
    SPIRV_Cross_Output stage_output;
    stage_output.gl_Position = gl_Position;
    stage_output.vLobe = vLobe;
    stage_output.vWp = vWp;
    stage_output.vN = vN;
    stage_output.vV = vV;
    return stage_output;
}
