// Generated from trusted autoV sources; do not edit.
#ifndef AVFX_STANDALONE
#include "/Engine/Public/Platform.ush"
#endif
ByteAddressBuffer AVFXData;
ByteAddressBuffer AVFXVertices;
ByteAddressBuffer AVFXIndices;
ByteAddressBuffer AVFXInstances;
Texture2D<float4> uTexture;
SamplerState _uTexture_sampler;
Texture2D<float4> uNoise;
SamplerState _uNoise_sampler;
Texture2D<float4> uAlphaTexture;
SamplerState _uAlphaTexture_sampler;
Texture2D<float4> uMask;
SamplerState _uMask_sampler;
Texture2D<float4> uNormalMap;
SamplerState _uNormalMap_sampler;
Texture2D<float4> uSites;
SamplerState _uSites_sampler;
Texture2D<float4> uTrail;
SamplerState _uTrail_sampler;
Texture2D<float4> tDepth;
SamplerState _tDepth_sampler;

static float4 gl_Position;
static int gl_VertexIndex;
static float vWin;
static float vS;
static float vQ;
static float vTipD;

struct SPIRV_Cross_Input
{
    uint gl_VertexIndex : SV_VertexID;
};

struct SPIRV_Cross_Output
{
    float vS : TEXCOORD0;
    float vQ : TEXCOORD1;
    float vWin : TEXCOORD2;
    float vTipD : TEXCOORD3;
    float4 gl_Position : SV_Position;
};

float crescentWidth(float s)
{
    float dt = asfloat(AVFXData.Load(112)) - s;
    float rise = pow(max(smoothstep(0.0f, asfloat(AVFXData.Load(224)) * 0.89999997615814208984375f, dt), 9.9999997473787516355514526367188e-05f), asfloat(AVFXData.Load(240)));
    float fall = 1.0f - smoothstep(asfloat(AVFXData.Load(224)), asfloat(AVFXData.Load(224)) * 2.599999904632568359375f, dt);
    float root = pow(max(smoothstep(0.0f, asfloat(AVFXData.Load(256)), s - asfloat(AVFXData.Load(128))), 9.9999997473787516355514526367188e-05f), 0.800000011920928955078125f);
    return ((asfloat(AVFXData.Load(96)) * rise) * (0.14000000059604644775390625f + (0.86000001430511474609375f * fall))) * root;
}

float vorHash11(inout float p)
{
    p = frac(p * 0.103100001811981201171875f);
    p *= (p + 33.3300018310546875f);
    p *= (p + p);
    return frac(p);
}

float vorHash21(float2 p)
{
    float param = dot(p, float2(127.09999847412109375f, 311.70001220703125f));
    float _51 = vorHash11(param);
    return _51;
}

float vorNoise(float2 p)
{
    float2 i = floor(p);
    float2 f = frac(p);
    float2 param = i;
    float a = vorHash21(param);
    float2 param_1 = i + float2(1.0f, 0.0f);
    float b = vorHash21(param_1);
    float2 param_2 = i + float2(0.0f, 1.0f);
    float c = vorHash21(param_2);
    float2 param_3 = i + 1.0f.xx;
    float d = vorHash21(param_3);
    float2 u = (f * f) * (3.0f.xx - (f * 2.0f));
    return lerp(lerp(a, b, u.x), lerp(c, d, u.x), u.y);
}

float vorFbm(float2 p)
{
    float2 param = p;
    float2 param_1 = (p * 2.0699999332427978515625f) + 11.30000019073486328125f.xx;
    float2 param_2 = (p * 4.110000133514404296875f) + 5.69999980926513671875f.xx;
    return ((0.550000011920928955078125f * vorNoise(param)) + (0.2800000011920928955078125f * vorNoise(param_1))) + (0.17000000178813934326171875f * vorNoise(param_2));
}

void vert_main()
{
    float ph = asfloat(AVFXData.Load(64)) + (asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0)) * asfloat(AVFXData.Load(80)));
    float3 rad = (asfloat(AVFXData.Load4(0)).xyz * cos(ph)) + (asfloat(AVFXData.Load4(16)).xyz * sin(ph));
    float param = asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0));
    float w = crescentWidth(param) * asfloat(AVFXData.Load(144));
    float2 param_1 = float2((asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0)) * 7.0f) - (asfloat(AVFXData.Load(192)) * 1.10000002384185791015625f), asfloat(AVFXData.Load(208)));
    float flut = vorFbm(param_1) - 0.5f;
    w *= ((1.0f + (asfloat(AVFXData.Load(176)) * (0.550000011920928955078125f + (1.5f * flut)))) + (0.14000000059604644775390625f * flut));
    float3 ctr = mul(float4(rad * asfloat(AVFXData.Load(48)), 1.0f), float4x4(float4(asfloat(AVFXData.Load4(608))), float4(asfloat(AVFXData.Load4(624))), float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))))).xyz;
    float4x4 _382 = float4x4(float4(asfloat(AVFXData.Load4(608))), float4(asfloat(AVFXData.Load4(624))), float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))));
    float3 tang = normalize(mul(normalize(((-asfloat(AVFXData.Load4(0)).xyz) * sin(ph)) + (asfloat(AVFXData.Load4(16)).xyz * cos(ph))), float3x3(_382[0].xyz, _382[1].xyz, _382[2].xyz)));
    float4x4 _437 = float4x4(float4(asfloat(AVFXData.Load4(608))), float4(asfloat(AVFXData.Load4(624))), float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))));
    float3 nrm = normalize(mul(asfloat(AVFXData.Load4(32)).xyz, float3x3(_437[0].xyz, _437[1].xyz, _437[2].xyz)));
    float3 across;
    if (asfloat(AVFXData.Load(272)) > 0.5f)
    {
        float3 view = normalize(asfloat(AVFXData.Load4(944)).xyz - ctr);
        across = normalize(cross(tang, view));
        float4x4 _500 = float4x4(float4(asfloat(AVFXData.Load4(608))), float4(asfloat(AVFXData.Load4(624))), float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))));
        float3 wr = normalize(mul(rad, float3x3(_500[0].xyz, _500[1].xyz, _500[2].xyz)));
        across *= sign(dot(across, wr));
    }
    else
    {
        float4x4 _546 = float4x4(float4(asfloat(AVFXData.Load4(608))), float4(asfloat(AVFXData.Load4(624))), float4(asfloat(AVFXData.Load4(640))), float4(asfloat(AVFXData.Load4(656))));
        across = normalize(mul(rad, float3x3(_546[0].xyz, _546[1].xyz, _546[2].xyz)));
    }
    float off = ((0.5f - asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 4u) * 16 + 0))) * w) + asfloat(AVFXData.Load(160));
    float3 p = ctr + (across * off);
    vWin = smoothstep(asfloat(AVFXData.Load(128)), asfloat(AVFXData.Load(128)) + 0.0350000001490116119384765625f, asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0))) * (1.0f - smoothstep(asfloat(AVFXData.Load(112)) - 0.01200000010430812835693359375f, asfloat(AVFXData.Load(112)), asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0))));
    vS = asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0));
    vQ = asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 4u) * 16 + 0));
    vTipD = clamp((asfloat(AVFXData.Load(112)) - asfloat(AVFXVertices.Load(((AVFXIndices.Load(gl_VertexIndex * 4 + 0) * 5u) + 3u) * 16 + 0))) / max(asfloat(AVFXData.Load(224)) * 0.75f, 0.001000000047497451305389404296875f), 0.0f, 1.0f);
    gl_Position = mul(float4(p, 1.0f), mul(float4x4(float4(asfloat(AVFXData.Load4(672))), float4(asfloat(AVFXData.Load4(688))), float4(asfloat(AVFXData.Load4(704))), float4(asfloat(AVFXData.Load4(720)))), float4x4(float4(asfloat(AVFXData.Load4(800))), float4(asfloat(AVFXData.Load4(816))), float4(asfloat(AVFXData.Load4(832))), float4(asfloat(AVFXData.Load4(848))))));
}

SPIRV_Cross_Output MainVS(SPIRV_Cross_Input stage_input)
{
    gl_VertexIndex = int(stage_input.gl_VertexIndex);
    vert_main();
    SPIRV_Cross_Output stage_output;
    stage_output.gl_Position = gl_Position;
    stage_output.vWin = vWin;
    stage_output.vS = vS;
    stage_output.vQ = vQ;
    stage_output.vTipD = vTipD;
    return stage_output;
}
