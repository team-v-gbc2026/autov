# Handoff — 2026-09-14 (technique cards, spikes, generalised vocabulary)

Read in this order: this file → `TOOLBOX_V2_STATUS.md` (last section) → `TOOLBOX_V2_MAPPING.md` §4–§17 →
`TECHNIQUES.md` → `research/2026-09-14-*.md`.

## Where things are

- Branch `feature/vfx-technique-cards` (35+ commits on top of `feature/vfx-toolbox-v2`, which is already in
  `main` via PR #28). Pushed. Working tree clean. Next step is a PR to `main` after `git merge origin/main`.
- Local clone `~/dev/v` (outside iCloud). Dataset symlink `~/dev/benchmark-verified-2026-09-13`
  (videos under `references/videos`, case → clip table in `scripts/run-benchmark.mjs`).
- Dev server: `.claude/launch.json` → `sh -c "cd frontend && AUTOV_LOCAL_MODE=1 VFX_FIXTURES_LOCAL_ONLY=1
  OPENAI_VFX_BUDGET_USD=60 npx next dev --hostname 127.0.0.1 --port 3032"` (3031 is held by the old iCloud
  worktree's server). Pages: `/dev/vfx-review` (exemplars + generated runs + reference clip + A/B),
  `/dev/vfx-v2` (gallery), `/dev/vfx-v2/spike-<key>` (smoke, heal, glitch, ice, shield, beam, portal,
  vortex, column, meteor, water, playful, slash; the fire spike is `/dev/vfx-v2/spike`).
- Generated evidence: `frontend/.autov-local/benchmarks/{v2-techniques-3,v2-techniques-4,v2-fast-dev}`.
- Tests: `npm test` (unit tests and the agent tests in one run); `npm run verify:webgpu` (renders all
  fifteen exemplars plus the workspace emitter on hardware WebGPU and fails on any GPU error or blank
  frame); `npx tsc --noEmit` clean.
- Fixtures are served from the `vfx-fixtures` bucket; after editing any `fixtures/v2/*/document.json` run
  `npm run upload:vfx-assets`.

## Rules that stay in force

- Commits only as `taiki_kawa <taiki@alche.studio>`; no AI attribution anywhere (code, docs, branches, PRs).
- Team-facing text in English; conversation with Taiki in Japanese.
- Budget: OpenAI project cap $80 (raised 2026-09-14). The local ledger caps at $60 by code
  (`SPEND_LIMIT_USD`, `budget.ts` rejects anything above) and stood at $56.7 after `v2-fast-dev`; to run
  more, archive `frontend/.autov-local/budget.json` and start a fresh ledger. Fast mode ≈ $2.3 per case
  (≈ 38k input tokens: vocabulary + technique brief + exemplar + schema), quality ≈ $4. Report to Taiki
  when the project remainder drops under $5.
- Holdout cases (fx04, fx05, fx09, fx16, fx17) need `--final-evaluation`; Taiki decides when.

## What was built (in order)

1. Research notes with sources for every benchmark reference (`research/`).
2. 24 technique cards (`src/lib/vfx-lab/techniques-v2.ts`) routed by family and prompt keywords into the
   candidate and restructure prompts (`techniqueBrief`).
3. 13 hand-built spikes (`frontend/dev-assets/vfx-v2/spike-*.html`, sources mirrored in
   `docs/vfx-lab/spike-*-reference.js.txt`), each iterated 8–14 rounds against same-phase reference frames.
4. Seven vocabulary ports (MAPPING §4–§17): blob/toon/outline/splash/height ramps; paths, ribbons,
   path-anchored emitters, wire bursts, jitter, twinkle, screen glitch; crystals, spherical hex lattice,
   band strips, sigil reveal, plane glow, ripples, layer-instance spawning; stripe panners, slabs, arcs,
   streak bursts, flat cel strips, lens flares, screen flash, collapse; frames with SDF rims, flow
   surfaces, reflections, swirl discs, orbit/path blob arrangements, path events, ground pools; sheet
   meshes, crescents with tonal stacks and erosion fronts, licks, symbol procedurals, sliver fans, squash,
   streaks/creases, camera-frame layers, backdrop; continuous colour fields (`ramp.space:"sprite"`,
   `ramp.blend`, `trail.ramp`, `toon.colorSource:"ramp"`), mesh-hero framing rules, timing-copy rule.
5. 15 exemplars (every benchmark family except the three lightning cases share `lightning-impact`).
6. `/dev/vfx-review`.

## What the live runs showed

The model uses the new vocabulary correctly (see STATUS). Remaining gaps: hero magnitude/framing, timing
windows that end before the prompt's peak, luminance washout on the beam, thin water tail. refine and
restructure never improved a review, so the improvement loop is the next thing to make useful:

- on `smallInFrame`: apply the exemplar camera AND scale the hero (blob rise/spread, crystals length,
  crescent radius) toward the exemplar's magnitudes, not only the knobs;
- on `washout`: cap authored emissive colours at 1.0 linear and lower bloom strength;
- copy exemplar timing windows in the repair step when a phase ends early.

Then re-run fx06, fx08, fx15 (validation) in fast mode and compare in `/dev/vfx-review`.

## Prompt for the next session

```
Read docs/vfx-lab/HANDOFF_2026-09-14.md, then TOOLBOX_V2_STATUS.md (last section). Repo ~/dev/v, branch
feature/vfx-technique-cards. Start with: merge origin/main, run tests, open the PR to main; then make the
improvement loop act on smallInFrame / washout / timing as listed in the handoff. Commits as taiki_kawa
only, no AI attribution. Fable orchestrates, Opus implements, Sonnet does research and chores. Show
results in /dev/vfx-review (dev server per .claude/launch.json, port 3032).
```
