// Generated by scripts/webgpu/port-shaders.mts. Static TSL; no runtime GLSL or eval.
import * as TSL from "three/tsl";

// Three.js Transpiler r186

export function particleVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const uTime = bindings.uTime;
const uStretch = bindings.uStretch;
const uAtlasTiles = bindings.uAtlasTiles;
const uMotionBlur = bindings.uMotionBlur;
const uFlipFps = bindings.uFlipFps;
const uRenderMode = bindings.uRenderMode;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uFlipMode = bindings.uFlipMode;
const uSize = bindings.uSize;
const uRot = bindings.uRot;
const uRotInit = bindings.uRotInit;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uEffectPathMode = bindings.uEffectPathMode;
const uEffectPathSpread = bindings.uEffectPathSpread;
const uEffectPathLength = bindings.uEffectPathLength;
const uEffectPathBaseLength = bindings.uEffectPathBaseLength;
const uEffectPathData = bindings.uEffectPathData;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, clamp, Break, If, mix, Loop, int, normalize, instance, fract, sqrt, cos, sin, tan, exp, add, mat2, smoothstep, mod } = TSL;

	const gl_Position = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

		return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	// One buffer keeps trail materials within WebGPU per-stage binding limits.

	const effectPathFrame = /*@__PURE__*/ Fn( ( [ d, pathPositionOut, pathNormalOut, pathTangentOut ] ) => {




		const x = clamp( d.div( uEffectPathLength ), 0., 1. ).mul( float( 64 ) ).toVar();
		const i = int( min( floor( x ), float( 63 ) ) ).toVar();
		const f = x.sub( float( i ) ).toVar();
		pathTangentOut.assign( normalize( mix( uEffectPathData.element( i.mul( 3 ).add( 2 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 2 ) ), f ) ) );
		pathNormalOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 1 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 1 ) ), f ) );
		pathNormalOut.assign( normalize( pathNormalOut.sub( pathTangentOut.mul( dot( pathNormalOut, pathTangentOut ) ) ) ) );
		pathPositionOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 0 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 0 ) ), f ).add( pathTangentOut.mul( d.sub( clamp( d, 0., uEffectPathLength ) ) ) ) );

	}, 'void' );

	const effectPathVector = /*@__PURE__*/ Fn( ( [ v, n, t ] ) => {

		return n.mul( v.x ).add( cross( t, n ).mul( v.y ) ).add( t.mul( v.z ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} );

		return result;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ).toVar();

		If( uShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = selfOrigin( s, e, e2 ).toVar();
		const dir = selfDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( selfSpeedI( u ) ) );
			scale.assign( selfSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
			scale.assign( exp( uDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );

		If( uEffectPathMode.greaterThan( 1 ), () => {

			const birth = select( uEffectPathSpread.equal( 1 ), e.w.mul( uEffectPathLength ), 0. ).toVar();
			const pp = property( 'vec3' ), nn = property( 'vec3' ), tt = property( 'vec3' );
			effectPathFrame( birth.add( select( uEffectPathMode.equal( 3 ), v0.mul( d ), 0. ) ), pp, nn, tt );
			pos.assign( pp.add( effectPathVector( vec3( origin.xy, 0. ), nn, tt ) ) );

			If( uEffectPathMode.equal( 2 ), () => {

				pos.addAssign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( d ) );

			} );

			vel.assign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( scale ) );

			If( uEffectPathMode.equal( 3 ), () => {

				const nextP = property( 'vec3' ), nextN = property( 'vec3' ), nextT = property( 'vec3' );
				effectPathFrame( birth.add( v0.mul( d ) ).add( .01 ), nextP, nextN, nextT );
				vel.assign( nextP.add( effectPathVector( vec3( origin.xy, 0. ), nextN, nextT ) ).sub( pos ).mul( 100. ).mul( v0 ).mul( scale ) );

			} );

		} );

		pos.addAssign( mul( .5, uGravity ).mul( a ).mul( a ).add( uWind.mul( a ) ) );
		vel.addAssign( uGravity.mul( a ).add( uWind ) );

		If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

		} );

		If( uHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uFloorY ).toVar();
			pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

		} );

	}, 'void' );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, age, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( age ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );

			// --- billboard ------------------------------------------------------------

			const size = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).toVar();
			const rot = mix( uRotInit.x, uRotInit.y, aExtra2.y ).add( mix( uRot.x, uRot.y, aExtra2.z ).mul( age ) ).toVar();
			const mv = property( 'vec4' );

			If( uRenderMode.equal( 2 ).or( uRenderMode.equal( 3 ) ), () => {

				// World-oriented quads: horizontal lies in XZ, vertical in XY.

				const c = cos( rot ).toVar(), s = sin( rot ).toVar();
				const q = mat2( c, s.negate(), s, c ).mul( position.xy ).mul( size ).toVar();
				const world = select( uRenderMode.equal( 2 ), vec3( q.x, 0., q.y ), vec3( q.x, q.y, 0. ) ).toVar();
				mv.assign( modelViewMatrix.mul( vec4( pos.add( world ), 1. ) ) );
				rot.assign( 0. );

			} ).Else( () => {

				mv.assign( modelViewMatrix.mul( vec4( pos, 1. ) ) );
				const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
				const sv = vec2( sv3.x, sv3.y ).toVar();
				const svl = length( sv ).toVar();
				const along = vec2( 0., 1. ).toVar(), across = vec2( 1., 0. ).toVar();
				const stretch = float( 1. ).toVar();

				If( uRenderMode.equal( 1 ).and( svl.greaterThan( 1e-4 ) ), () => {

					along.assign( sv.div( svl ) );

					// Winding: the across axis must be the clockwise perpendicular or the
					// quad is mirrored and back-face culled away.

					across.assign( vec2( along.y, along.x.negate() ) );

					// post.motionBlur is a multiplier on the velocity stretch, nothing else.

					rot.assign( 0. );
					stretch.assign( add( 1., uStretch.mul( add( 1., mul( 2., uMotionBlur ) ) ).mul( svl ) ) );

				} );

				const off = across.mul( position.x ).mul( size ).add( along.mul( position.y ).mul( size ).mul( stretch ) ).toVar();

				If( uMotionBlur.greaterThan( 0. ).and( svl.greaterThan( 1e-4 ) ), () => {

					// Non-stretched modes still elongate along screen velocity, never rotate.

					const vdir = sv.div( svl ).toVar();
					off.addAssign( vdir.mul( dot( off, vdir ) ).mul( uMotionBlur ).mul( min( svl, 8. ) ).mul( .25 ) );

				} );

				mv.xy.addAssign( off );

			} );

			vRot.assign( rot );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			const cols = float( max( uAtlasCols, 1 ) ).toVar(), rows = float( max( uAtlasRows, 1 ) ).toVar();
			const tiles = max( uAtlasTiles, 1. ).toVar();

			// uFlipMode: 0 = random atlas tile, 1 = flipbook over life, 2 = flipbook at fps.

			const ti = floor( aExtra.z.mul( tiles ).mul( .9999 ) ).toVar();

			If( uFlipMode.equal( 1 ), () => {

				ti.assign( floor( clamp( u, 0., .9999 ).mul( tiles ) ) );

			} ).ElseIf( uFlipMode.equal( 2 ), () => {

				ti.assign( floor( mod( max( age, 0. ).mul( uFlipFps ), tiles ) ) );

			} );

			vTile.assign( vec2( mod( ti, cols ).div( cols ), floor( ti.div( cols ) ).div( rows ) ) );

		} );

		return gl_Position;

	} );
	return main();

}
export const particleVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uStretch":{"type":"float","kind":"uniform"},"uAtlasTiles":{"type":"float","kind":"uniform"},"uMotionBlur":{"type":"float","kind":"uniform"},"uFlipFps":{"type":"float","kind":"uniform"},"uRenderMode":{"type":"int","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uFlipMode":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"uRot":{"type":"vec2","kind":"uniform"},"uRotInit":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uEffectPathMode":{"type":"int","kind":"uniform"},"uEffectPathSpread":{"type":"int","kind":"uniform"},"uEffectPathLength":{"type":"float","kind":"uniform"},"uEffectPathBaseLength":{"type":"float","kind":"uniform"},"uEffectPathData":{"type":"vec3","size":195,"kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"}};

// Three.js Transpiler r186

export function subParticleVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aPSeed = bindings.aPSeed;
const aPExtra = bindings.aPExtra;
const aPExtra2 = bindings.aPExtra2;
const uTime = bindings.uTime;
const uStretch = bindings.uStretch;
const uAtlasTiles = bindings.uAtlasTiles;
const uMotionBlur = bindings.uMotionBlur;
const uFlipFps = bindings.uFlipFps;
const uRenderMode = bindings.uRenderMode;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uFlipMode = bindings.uFlipMode;
const uSize = bindings.uSize;
const uRot = bindings.uRot;
const uRotInit = bindings.uRotInit;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uEffectPathMode = bindings.uEffectPathMode;
const uEffectPathSpread = bindings.uEffectPathSpread;
const uEffectPathLength = bindings.uEffectPathLength;
const uEffectPathBaseLength = bindings.uEffectPathBaseLength;
const uEffectPathData = bindings.uEffectPathData;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uParentPeriod = bindings.uParentPeriod;
const uParentSpawnWindow = bindings.uParentSpawnWindow;
const uParentSpawnDuration = bindings.uParentSpawnDuration;
const uParentShapeLength = bindings.uParentShapeLength;
const uParentShapeRadius = bindings.uParentShapeRadius;
const uParentShapeInner = bindings.uParentShapeInner;
const uParentShapeAngle = bindings.uParentShapeAngle;
const uParentDrag = bindings.uParentDrag;
const uParentCurl = bindings.uParentCurl;
const uParentCurlFreq = bindings.uParentCurlFreq;
const uParentCurlSpeed = bindings.uParentCurlSpeed;
const uParentFloorY = bindings.uParentFloorY;
const uParentFloorSoft = bindings.uParentFloorSoft;
const uParentAngle = bindings.uParentAngle;
const uParentVortexW = bindings.uParentVortexW;
const uParentVortexFalloff = bindings.uParentVortexFalloff;
const uParentSpawnMode = bindings.uParentSpawnMode;
const uParentShapeType = bindings.uParentShapeType;
const uParentVelMode = bindings.uParentVelMode;
const uParentHasFloor = bindings.uParentHasFloor;
const uParentSurfaceOnly = bindings.uParentSurfaceOnly;
const uParentSpeedN = bindings.uParentSpeedN;
const uParentBurstN = bindings.uParentBurstN;
const uParentAxis = bindings.uParentAxis;
const uParentDir = bindings.uParentDir;
const uParentGravity = bindings.uParentGravity;
const uParentWind = bindings.uParentWind;
const uParentBias = bindings.uParentBias;
const uParentShapeSize = bindings.uParentShapeSize;
const uParentVortexAxis = bindings.uParentVortexAxis;
const uParentLife = bindings.uParentLife;
const uParentSpeed = bindings.uParentSpeed;
const uParentSpeedKey = bindings.uParentSpeedKey;
const uParentBurstT = bindings.uParentBurstT;
const uParentBurstC = bindings.uParentBurstC;
const uParentTimeShift = bindings.uParentTimeShift;
const uInherit = bindings.uInherit;
const uPathT0 = bindings.uPathT0;
const uPathDt = bindings.uPathDt;
const uSubMode = bindings.uSubMode;
const uSubOffset = bindings.uSubOffset;
const uParentPath = bindings.uParentPath;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, clamp, Break, If, mix, Loop, int, normalize, instance, fract, sqrt, cos, sin, tan, exp, add, mat2, smoothstep, mod } = TSL;

	const gl_Position = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

		return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	// One buffer keeps trail materials within WebGPU per-stage binding limits.

	const effectPathFrame = /*@__PURE__*/ Fn( ( [ d, pathPositionOut, pathNormalOut, pathTangentOut ] ) => {




		const x = clamp( d.div( uEffectPathLength ), 0., 1. ).mul( float( 64 ) ).toVar();
		const i = int( min( floor( x ), float( 63 ) ) ).toVar();
		const f = x.sub( float( i ) ).toVar();
		pathTangentOut.assign( normalize( mix( uEffectPathData.element( i.mul( 3 ).add( 2 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 2 ) ), f ) ) );
		pathNormalOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 1 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 1 ) ), f ) );
		pathNormalOut.assign( normalize( pathNormalOut.sub( pathTangentOut.mul( dot( pathNormalOut, pathTangentOut ) ) ) ) );
		pathPositionOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 0 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 0 ) ), f ).add( pathTangentOut.mul( d.sub( clamp( d, 0., uEffectPathLength ) ) ) ) );

	}, 'void' );

	const effectPathVector = /*@__PURE__*/ Fn( ( [ v, n, t ] ) => {

		return n.mul( v.x ).add( cross( t, n ).mul( v.y ) ).add( t.mul( v.z ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} );

		return result;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ).toVar();

		If( uShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = selfOrigin( s, e, e2 ).toVar();
		const dir = selfDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( selfSpeedI( u ) ) );
			scale.assign( selfSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
			scale.assign( exp( uDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );

		If( uEffectPathMode.greaterThan( 1 ), () => {

			const birth = select( uEffectPathSpread.equal( 1 ), e.w.mul( uEffectPathLength ), 0. ).toVar();
			const pp = property( 'vec3' ), nn = property( 'vec3' ), tt = property( 'vec3' );
			effectPathFrame( birth.add( select( uEffectPathMode.equal( 3 ), v0.mul( d ), 0. ) ), pp, nn, tt );
			pos.assign( pp.add( effectPathVector( vec3( origin.xy, 0. ), nn, tt ) ) );

			If( uEffectPathMode.equal( 2 ), () => {

				pos.addAssign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( d ) );

			} );

			vel.assign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( scale ) );

			If( uEffectPathMode.equal( 3 ), () => {

				const nextP = property( 'vec3' ), nextN = property( 'vec3' ), nextT = property( 'vec3' );
				effectPathFrame( birth.add( v0.mul( d ) ).add( .01 ), nextP, nextN, nextT );
				vel.assign( nextP.add( effectPathVector( vec3( origin.xy, 0. ), nextN, nextT ) ).sub( pos ).mul( 100. ).mul( v0 ).mul( scale ) );

			} );

		} );

		pos.addAssign( mul( .5, uGravity ).mul( a ).mul( a ).add( uWind.mul( a ) ) );
		vel.addAssign( uGravity.mul( a ).add( uWind ) );

		If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

		} );

		If( uHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uFloorY ).toVar();
			pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

		} );

	}, 'void' );

	const parentLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uParentLife.x, uParentLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const parentBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uParentSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uParentSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uParentPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uParentPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uParentSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uParentSpawnMode.equal( 2 ), () => {

			const t0 = uParentBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uParentBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uParentBurstC.element( i ) ), () => {

					t0.assign( uParentBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uParentSpawnWindow ) ) );

		} );

		return result;

	} );

	const parentOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uParentSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uParentBias, 0., 1. ) ) );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uParentShapeLength.mul( e.w ) ).add( sph.mul( uParentShapeRadius ) ).toVar();

		If( uParentShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uParentShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 3 ), () => {

			const h = e.w.mul( uParentShapeLength ).toVar();
			const rr = uParentShapeRadius.add( h.mul( tan( min( uParentShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uParentShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 5 ), () => {

			const rr = mix( uParentShapeInner, uParentShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uParentShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uParentShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const parentDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uParentDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uParentAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uParentVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uParentVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uParentVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const parentSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uParentSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uParentSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uParentSpeedN ), () => {

					Break();

				} );

				const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uParentSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const parentSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uParentSpeedKey.element(0).x ).mul( uParentSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uParentSpeedN ), () => {

				Break();

			} );

			const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
			const va = uParentSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uParentSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uParentSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const parentTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = parentOrigin( s, e, e2 ).toVar();
		const dir = parentDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uParentSpeed.x, uParentSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uParentSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( parentSpeedI( u ) ) );
			scale.assign( parentSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uParentDrag.lessThan( .001 ), a, sub( 1., exp( uParentDrag.negate().mul( a ) ) ).div( uParentDrag ) ) );
			scale.assign( exp( uParentDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );
		pos.addAssign( mul( .5, uParentGravity ).mul( a ).mul( a ).add( uParentWind.mul( a ) ) );
		vel.addAssign( uParentGravity.mul( a ).add( uParentWind ) );

		If( abs( uParentVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uParentVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uParentVortexW.div( add( 1., uParentVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uParentCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uParentCurlFreq ).add( vec3( 0., t.negate().mul( uParentCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uParentCurl ).mul( 1.0 ).mul( a ) );

		} );

		If( uParentHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uParentFloorY ).toVar();
			pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

		} );

	}, 'void' );

	/** Parent layer transform delta at absolute time t, from an 8-sample table. */

	const parentPathAt = /*@__PURE__*/ Fn( ( [ t ] ) => {

		const x = clamp( t.sub( uPathT0 ).div( max( uPathDt, 1e-4 ) ), 0., float( 8 - 1 ) ).toVar();
		const i0 = floor( x ).toVar(), f = x.sub( i0 ).toVar();
		const a = vec3( 0. ).toVar(), b = vec3( 0. ).toVar();

		Loop( 8, ( { i } ) => {

			const fi = float( i ).toVar();
			a.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( i0 ) ), .5 ) ) );
			b.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( min( i0.add( 1. ), float( 8 - 1 ) ) ) ), .5 ) ) );

		} );

		return mix( a, b, f );

	} );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();

		// Sub-emitter: the child is born where and when the parent was at
		// parentBirth + offset, in the parent's own closed-form trajectory.

		const pLife = parentLife( aPSeed ).toVar();
		const r = fract( aSeed.x.mul( 13.37 ).add( aSeed.w.mul( 7.77 ) ).add( 0.113 ) ).toVar();
		const off = select( uSubMode.equal( 1 ), pLife, select( uSubMode.equal( 2 ), mix( uSubOffset.x, uSubOffset.y, r ), mix( uSubOffset.x, uSubOffset.y, fract( aSeed.x.mul( 3.0 ) ) ) ) ).toVar();
		off.assign( clamp( off, 0., pLife ) );
		const pNow = uTime.add( uParentTimeShift ).toVar();
		const pBirth = parentBirth( aPSeed, pNow.sub( off ) ).toVar();
		const ppos = property( 'vec3' ), pvel = property( 'vec3' );
		parentTraj( aPSeed, aPExtra, aPExtra2, off, pLife, pBirth.add( off ), ppos, pvel );
		birth.assign( pBirth.add( off ).sub( uParentTimeShift ) );
		subOrigin.assign( ppos.add( parentPathAt( pBirth.add( off ) ) ) );
		subVel.assign( pvel.mul( uInherit ) );

		If( pBirth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, age, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( age ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );

			// --- billboard ------------------------------------------------------------

			const size = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).toVar();
			const rot = mix( uRotInit.x, uRotInit.y, aExtra2.y ).add( mix( uRot.x, uRot.y, aExtra2.z ).mul( age ) ).toVar();
			const mv = property( 'vec4' );

			If( uRenderMode.equal( 2 ).or( uRenderMode.equal( 3 ) ), () => {

				// World-oriented quads: horizontal lies in XZ, vertical in XY.

				const c = cos( rot ).toVar(), s = sin( rot ).toVar();
				const q = mat2( c, s.negate(), s, c ).mul( position.xy ).mul( size ).toVar();
				const world = select( uRenderMode.equal( 2 ), vec3( q.x, 0., q.y ), vec3( q.x, q.y, 0. ) ).toVar();
				mv.assign( modelViewMatrix.mul( vec4( pos.add( world ), 1. ) ) );
				rot.assign( 0. );

			} ).Else( () => {

				mv.assign( modelViewMatrix.mul( vec4( pos, 1. ) ) );
				const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
				const sv = vec2( sv3.x, sv3.y ).toVar();
				const svl = length( sv ).toVar();
				const along = vec2( 0., 1. ).toVar(), across = vec2( 1., 0. ).toVar();
				const stretch = float( 1. ).toVar();

				If( uRenderMode.equal( 1 ).and( svl.greaterThan( 1e-4 ) ), () => {

					along.assign( sv.div( svl ) );

					// Winding: the across axis must be the clockwise perpendicular or the
					// quad is mirrored and back-face culled away.

					across.assign( vec2( along.y, along.x.negate() ) );

					// post.motionBlur is a multiplier on the velocity stretch, nothing else.

					rot.assign( 0. );
					stretch.assign( add( 1., uStretch.mul( add( 1., mul( 2., uMotionBlur ) ) ).mul( svl ) ) );

				} );

				const off = across.mul( position.x ).mul( size ).add( along.mul( position.y ).mul( size ).mul( stretch ) ).toVar();

				If( uMotionBlur.greaterThan( 0. ).and( svl.greaterThan( 1e-4 ) ), () => {

					// Non-stretched modes still elongate along screen velocity, never rotate.

					const vdir = sv.div( svl ).toVar();
					off.addAssign( vdir.mul( dot( off, vdir ) ).mul( uMotionBlur ).mul( min( svl, 8. ) ).mul( .25 ) );

				} );

				mv.xy.addAssign( off );

			} );

			vRot.assign( rot );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			const cols = float( max( uAtlasCols, 1 ) ).toVar(), rows = float( max( uAtlasRows, 1 ) ).toVar();
			const tiles = max( uAtlasTiles, 1. ).toVar();

			// uFlipMode: 0 = random atlas tile, 1 = flipbook over life, 2 = flipbook at fps.

			const ti = floor( aExtra.z.mul( tiles ).mul( .9999 ) ).toVar();

			If( uFlipMode.equal( 1 ), () => {

				ti.assign( floor( clamp( u, 0., .9999 ).mul( tiles ) ) );

			} ).ElseIf( uFlipMode.equal( 2 ), () => {

				ti.assign( floor( mod( max( age, 0. ).mul( uFlipFps ), tiles ) ) );

			} );

			vTile.assign( vec2( mod( ti, cols ).div( cols ), floor( ti.div( cols ) ).div( rows ) ) );

		} );

		return gl_Position;

	} );
	return main();

}
export const subParticleVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aPSeed":{"type":"vec4","kind":"attribute"},"aPExtra":{"type":"vec4","kind":"attribute"},"aPExtra2":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uStretch":{"type":"float","kind":"uniform"},"uAtlasTiles":{"type":"float","kind":"uniform"},"uMotionBlur":{"type":"float","kind":"uniform"},"uFlipFps":{"type":"float","kind":"uniform"},"uRenderMode":{"type":"int","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uFlipMode":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"uRot":{"type":"vec2","kind":"uniform"},"uRotInit":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uEffectPathMode":{"type":"int","kind":"uniform"},"uEffectPathSpread":{"type":"int","kind":"uniform"},"uEffectPathLength":{"type":"float","kind":"uniform"},"uEffectPathBaseLength":{"type":"float","kind":"uniform"},"uEffectPathData":{"type":"vec3","size":195,"kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentPeriod":{"type":"float","kind":"uniform"},"uParentSpawnWindow":{"type":"float","kind":"uniform"},"uParentSpawnDuration":{"type":"float","kind":"uniform"},"uParentShapeLength":{"type":"float","kind":"uniform"},"uParentShapeRadius":{"type":"float","kind":"uniform"},"uParentShapeInner":{"type":"float","kind":"uniform"},"uParentShapeAngle":{"type":"float","kind":"uniform"},"uParentDrag":{"type":"float","kind":"uniform"},"uParentCurl":{"type":"float","kind":"uniform"},"uParentCurlFreq":{"type":"float","kind":"uniform"},"uParentCurlSpeed":{"type":"float","kind":"uniform"},"uParentFloorY":{"type":"float","kind":"uniform"},"uParentFloorSoft":{"type":"float","kind":"uniform"},"uParentAngle":{"type":"float","kind":"uniform"},"uParentVortexW":{"type":"float","kind":"uniform"},"uParentVortexFalloff":{"type":"float","kind":"uniform"},"uParentSpawnMode":{"type":"int","kind":"uniform"},"uParentShapeType":{"type":"int","kind":"uniform"},"uParentVelMode":{"type":"int","kind":"uniform"},"uParentHasFloor":{"type":"int","kind":"uniform"},"uParentSurfaceOnly":{"type":"int","kind":"uniform"},"uParentSpeedN":{"type":"int","kind":"uniform"},"uParentBurstN":{"type":"int","kind":"uniform"},"uParentAxis":{"type":"vec3","kind":"uniform"},"uParentDir":{"type":"vec3","kind":"uniform"},"uParentGravity":{"type":"vec3","kind":"uniform"},"uParentWind":{"type":"vec3","kind":"uniform"},"uParentBias":{"type":"vec3","kind":"uniform"},"uParentShapeSize":{"type":"vec3","kind":"uniform"},"uParentVortexAxis":{"type":"vec3","kind":"uniform"},"uParentLife":{"type":"vec2","kind":"uniform"},"uParentSpeed":{"type":"vec2","kind":"uniform"},"uParentSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uParentBurstT":{"type":"float","size":8,"kind":"uniform"},"uParentBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentTimeShift":{"type":"float","kind":"uniform"},"uInherit":{"type":"float","kind":"uniform"},"uPathT0":{"type":"float","kind":"uniform"},"uPathDt":{"type":"float","kind":"uniform"},"uSubMode":{"type":"int","kind":"uniform"},"uSubOffset":{"type":"vec2","kind":"uniform"},"uParentPath":{"type":"vec3","size":8,"kind":"uniform"}};

// Three.js Transpiler r186

export function trailVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const uTime = bindings.uTime;
const uSegments = bindings.uSegments;
const uSpacing = bindings.uSpacing;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveG = bindings.uCurveG;
const uCurveGN = bindings.uCurveGN;
const uCurveGEase = bindings.uCurveGEase;
const uEffectPathMode = bindings.uEffectPathMode;
const uEffectPathSpread = bindings.uEffectPathSpread;
const uEffectPathLength = bindings.uEffectPathLength;
const uEffectPathBaseLength = bindings.uEffectPathBaseLength;
const uEffectPathData = bindings.uEffectPathData;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, clamp, Break, If, mix, Loop, int, normalize, instance, fract, sqrt, cos, sin, tan, exp, add, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

		return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveG = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveG.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveGN ), () => {

				Break();

			} );

			const a = uCurveG.element( i.sub( 1 ) ).x.toVar(), b = uCurveG.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveGEase ) );
			v.assign( mix( v, uCurveG.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	// One buffer keeps trail materials within WebGPU per-stage binding limits.

	const effectPathFrame = /*@__PURE__*/ Fn( ( [ d, pathPositionOut, pathNormalOut, pathTangentOut ] ) => {




		const x = clamp( d.div( uEffectPathLength ), 0., 1. ).mul( float( 64 ) ).toVar();
		const i = int( min( floor( x ), float( 63 ) ) ).toVar();
		const f = x.sub( float( i ) ).toVar();
		pathTangentOut.assign( normalize( mix( uEffectPathData.element( i.mul( 3 ).add( 2 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 2 ) ), f ) ) );
		pathNormalOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 1 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 1 ) ), f ) );
		pathNormalOut.assign( normalize( pathNormalOut.sub( pathTangentOut.mul( dot( pathNormalOut, pathTangentOut ) ) ) ) );
		pathPositionOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 0 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 0 ) ), f ).add( pathTangentOut.mul( d.sub( clamp( d, 0., uEffectPathLength ) ) ) ) );

	}, 'void' );

	const effectPathVector = /*@__PURE__*/ Fn( ( [ v, n, t ] ) => {

		return n.mul( v.x ).add( cross( t, n ).mul( v.y ) ).add( t.mul( v.z ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} );

		return result;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ).toVar();

		If( uShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = selfOrigin( s, e, e2 ).toVar();
		const dir = selfDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( selfSpeedI( u ) ) );
			scale.assign( selfSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
			scale.assign( exp( uDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );

		If( uEffectPathMode.greaterThan( 1 ), () => {

			const birth = select( uEffectPathSpread.equal( 1 ), e.w.mul( uEffectPathLength ), 0. ).toVar();
			const pp = property( 'vec3' ), nn = property( 'vec3' ), tt = property( 'vec3' );
			effectPathFrame( birth.add( select( uEffectPathMode.equal( 3 ), v0.mul( d ), 0. ) ), pp, nn, tt );
			pos.assign( pp.add( effectPathVector( vec3( origin.xy, 0. ), nn, tt ) ) );

			If( uEffectPathMode.equal( 2 ), () => {

				pos.addAssign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( d ) );

			} );

			vel.assign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( scale ) );

			If( uEffectPathMode.equal( 3 ), () => {

				const nextP = property( 'vec3' ), nextN = property( 'vec3' ), nextT = property( 'vec3' );
				effectPathFrame( birth.add( v0.mul( d ) ).add( .01 ), nextP, nextN, nextT );
				vel.assign( nextP.add( effectPathVector( vec3( origin.xy, 0. ), nextN, nextT ) ).sub( pos ).mul( 100. ).mul( v0 ).mul( scale ) );

			} );

		} );

		pos.addAssign( mul( .5, uGravity ).mul( a ).mul( a ).add( uWind.mul( a ) ) );
		vel.addAssign( uGravity.mul( a ).add( uWind ) );

		If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

		} );

		If( uHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uFloorY ).toVar();
			pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

		} );

	}, 'void' );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vUv.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const side = position.x.toVar();
			const k = position.y.toVar();
			const kn = clamp( k.div( max( uSegments.sub( 1. ), 1. ) ), 0., 1. ).toVar();
			vUv.assign( vec2( side.mul( .5 ).add( .5 ), kn ) );
			const ageK = max( age.sub( k.mul( uSpacing ) ), 0. ).toVar();
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, ageK, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( ageK ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();
			const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
			const sv = vec2( sv3.x, sv3.y ).toVar();
			const svl = length( sv ).toVar();
			const across = select( svl.greaterThan( 1e-4 ), vec2( sv.y, sv.x.negate() ).div( svl ), vec2( 1., 0. ) ).toVar();
			const width = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).mul( curveG( kn ) ).mul( .5 ).toVar();
			mv.xy.addAssign( across.mul( side ).mul( width ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
export const trailVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSegments":{"type":"float","kind":"uniform"},"uSpacing":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveG":{"type":"vec2","size":8,"kind":"uniform"},"uCurveGN":{"type":"int","kind":"uniform"},"uCurveGEase":{"type":"float","kind":"uniform"},"uEffectPathMode":{"type":"int","kind":"uniform"},"uEffectPathSpread":{"type":"int","kind":"uniform"},"uEffectPathLength":{"type":"float","kind":"uniform"},"uEffectPathBaseLength":{"type":"float","kind":"uniform"},"uEffectPathData":{"type":"vec3","size":195,"kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"}};

// Three.js Transpiler r186

export function subTrailVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aPSeed = bindings.aPSeed;
const aPExtra = bindings.aPExtra;
const aPExtra2 = bindings.aPExtra2;
const uTime = bindings.uTime;
const uSegments = bindings.uSegments;
const uSpacing = bindings.uSpacing;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveG = bindings.uCurveG;
const uCurveGN = bindings.uCurveGN;
const uCurveGEase = bindings.uCurveGEase;
const uEffectPathMode = bindings.uEffectPathMode;
const uEffectPathSpread = bindings.uEffectPathSpread;
const uEffectPathLength = bindings.uEffectPathLength;
const uEffectPathBaseLength = bindings.uEffectPathBaseLength;
const uEffectPathData = bindings.uEffectPathData;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uParentPeriod = bindings.uParentPeriod;
const uParentSpawnWindow = bindings.uParentSpawnWindow;
const uParentSpawnDuration = bindings.uParentSpawnDuration;
const uParentShapeLength = bindings.uParentShapeLength;
const uParentShapeRadius = bindings.uParentShapeRadius;
const uParentShapeInner = bindings.uParentShapeInner;
const uParentShapeAngle = bindings.uParentShapeAngle;
const uParentDrag = bindings.uParentDrag;
const uParentCurl = bindings.uParentCurl;
const uParentCurlFreq = bindings.uParentCurlFreq;
const uParentCurlSpeed = bindings.uParentCurlSpeed;
const uParentFloorY = bindings.uParentFloorY;
const uParentFloorSoft = bindings.uParentFloorSoft;
const uParentAngle = bindings.uParentAngle;
const uParentVortexW = bindings.uParentVortexW;
const uParentVortexFalloff = bindings.uParentVortexFalloff;
const uParentSpawnMode = bindings.uParentSpawnMode;
const uParentShapeType = bindings.uParentShapeType;
const uParentVelMode = bindings.uParentVelMode;
const uParentHasFloor = bindings.uParentHasFloor;
const uParentSurfaceOnly = bindings.uParentSurfaceOnly;
const uParentSpeedN = bindings.uParentSpeedN;
const uParentBurstN = bindings.uParentBurstN;
const uParentAxis = bindings.uParentAxis;
const uParentDir = bindings.uParentDir;
const uParentGravity = bindings.uParentGravity;
const uParentWind = bindings.uParentWind;
const uParentBias = bindings.uParentBias;
const uParentShapeSize = bindings.uParentShapeSize;
const uParentVortexAxis = bindings.uParentVortexAxis;
const uParentLife = bindings.uParentLife;
const uParentSpeed = bindings.uParentSpeed;
const uParentSpeedKey = bindings.uParentSpeedKey;
const uParentBurstT = bindings.uParentBurstT;
const uParentBurstC = bindings.uParentBurstC;
const uParentTimeShift = bindings.uParentTimeShift;
const uInherit = bindings.uInherit;
const uPathT0 = bindings.uPathT0;
const uPathDt = bindings.uPathDt;
const uSubMode = bindings.uSubMode;
const uSubOffset = bindings.uSubOffset;
const uParentPath = bindings.uParentPath;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, clamp, Break, If, mix, Loop, int, normalize, instance, fract, sqrt, cos, sin, tan, exp, add, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

		return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveG = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveG.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveGN ), () => {

				Break();

			} );

			const a = uCurveG.element( i.sub( 1 ) ).x.toVar(), b = uCurveG.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveGEase ) );
			v.assign( mix( v, uCurveG.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	// One buffer keeps trail materials within WebGPU per-stage binding limits.

	const effectPathFrame = /*@__PURE__*/ Fn( ( [ d, pathPositionOut, pathNormalOut, pathTangentOut ] ) => {




		const x = clamp( d.div( uEffectPathLength ), 0., 1. ).mul( float( 64 ) ).toVar();
		const i = int( min( floor( x ), float( 63 ) ) ).toVar();
		const f = x.sub( float( i ) ).toVar();
		pathTangentOut.assign( normalize( mix( uEffectPathData.element( i.mul( 3 ).add( 2 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 2 ) ), f ) ) );
		pathNormalOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 1 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 1 ) ), f ) );
		pathNormalOut.assign( normalize( pathNormalOut.sub( pathTangentOut.mul( dot( pathNormalOut, pathTangentOut ) ) ) ) );
		pathPositionOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 0 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 0 ) ), f ).add( pathTangentOut.mul( d.sub( clamp( d, 0., uEffectPathLength ) ) ) ) );

	}, 'void' );

	const effectPathVector = /*@__PURE__*/ Fn( ( [ v, n, t ] ) => {

		return n.mul( v.x ).add( cross( t, n ).mul( v.y ) ).add( t.mul( v.z ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} );

		return result;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ).toVar();

		If( uShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = selfOrigin( s, e, e2 ).toVar();
		const dir = selfDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( selfSpeedI( u ) ) );
			scale.assign( selfSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
			scale.assign( exp( uDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );

		If( uEffectPathMode.greaterThan( 1 ), () => {

			const birth = select( uEffectPathSpread.equal( 1 ), e.w.mul( uEffectPathLength ), 0. ).toVar();
			const pp = property( 'vec3' ), nn = property( 'vec3' ), tt = property( 'vec3' );
			effectPathFrame( birth.add( select( uEffectPathMode.equal( 3 ), v0.mul( d ), 0. ) ), pp, nn, tt );
			pos.assign( pp.add( effectPathVector( vec3( origin.xy, 0. ), nn, tt ) ) );

			If( uEffectPathMode.equal( 2 ), () => {

				pos.addAssign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( d ) );

			} );

			vel.assign( effectPathVector( dir, nn, tt ).mul( v0 ).mul( scale ) );

			If( uEffectPathMode.equal( 3 ), () => {

				const nextP = property( 'vec3' ), nextN = property( 'vec3' ), nextT = property( 'vec3' );
				effectPathFrame( birth.add( v0.mul( d ) ).add( .01 ), nextP, nextN, nextT );
				vel.assign( nextP.add( effectPathVector( vec3( origin.xy, 0. ), nextN, nextT ) ).sub( pos ).mul( 100. ).mul( v0 ).mul( scale ) );

			} );

		} );

		pos.addAssign( mul( .5, uGravity ).mul( a ).mul( a ).add( uWind.mul( a ) ) );
		vel.addAssign( uGravity.mul( a ).add( uWind ) );

		If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

		} );

		If( uHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uFloorY ).toVar();
			pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

		} );

	}, 'void' );

	const parentLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uParentLife.x, uParentLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const parentBirth = /*@__PURE__*/ Fn( ( [ s, t ] ) => {

		const instance = s.x.toVar(), birth = instance.mul( uParentSpawnWindow ).toVar();
		const result = birth.toVar();

		If( uParentSpawnMode.equal( 1 ), () => {

			const cycle = floor( t.sub( birth ).div( max( uParentPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uParentPeriod ) ).toVar();
			result.assign( select( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uParentSpawnDuration ) ), 1e9, absBirth ) );

		} ).ElseIf( uParentSpawnMode.equal( 2 ), () => {

			const t0 = uParentBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uParentBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uParentBurstC.element( i ) ), () => {

					t0.assign( uParentBurstT.element( i ) );
					Break();

				} );

			} );

			result.assign( t0.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uParentSpawnWindow ) ) );

		} );

		return result;

	} );

	const parentOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2 ] ) => {

		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uParentSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();
		sph.assign( mix( sph, abs( sph ), clamp( uParentBias, 0., 1. ) ) );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();
		const result = axis.mul( uParentShapeLength.mul( e.w ) ).add( sph.mul( uParentShapeRadius ) ).toVar();

		If( uParentShapeType.equal( 0 ), () => {

			result.assign( vec3( 0. ) );

		} ).ElseIf( uParentShapeType.equal( 1 ), () => {

			result.assign( sph.mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 2 ), () => {

			result.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 3 ), () => {

			const h = e.w.mul( uParentShapeLength ).toVar();
			const rr = uParentShapeRadius.add( h.mul( tan( min( uParentShapeAngle, 1.5 ) ) ) ).toVar();
			result.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uParentShapeType.equal( 4 ), () => {

			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 5 ), () => {

			const rr = mix( uParentShapeInner, uParentShapeRadius, safePow( e.z, .5 ) ).toVar();
			result.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uParentShapeType.equal( 6 ), () => {

			result.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uParentShapeSize ).mul( .5 ) );

		} );

		return result;

	} );

	const parentDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin ] ) => {

		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uParentDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();
		const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
		const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uParentAngle ).toVar();
		const result = safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ).toVar();

		If( uParentVelMode.equal( 0 ), () => {

			result.assign( safeDir( origin, radial ) );

		} ).ElseIf( uParentVelMode.equal( 1 ), () => {

			result.assign( base );

		} ).ElseIf( uParentVelMode.equal( 2 ), () => {

			result.assign( safeDir( cross( axis, radial ), base ) );

		} );

		return result;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const parentSpeedAt = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		const v = float( 1. ).toVar();

		If( uParentSpeedN.greaterThanEqual( 2 ), () => {

			u.assign( clamp( u, 0., 1. ) );
			v.assign( uParentSpeedKey.element(0).y );

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uParentSpeedN ), () => {

					Break();

				} );

				const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uParentSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

		} );

		return v;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const parentSpeedI = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uParentSpeedKey.element(0).x ).mul( uParentSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uParentSpeedN ), () => {

				Break();

			} );

			const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
			const va = uParentSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uParentSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uParentSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const parentTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, age, life, t, pos, vel ] ) => {



		const origin = parentOrigin( s, e, e2 ).toVar();
		const dir = parentDir( s, e, e2, origin ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const v0 = mix( uParentSpeed.x, uParentSpeed.y, e2.w ).toVar();
		const d = property( 'float' ), scale = property( 'float' );

		If( uParentSpeedN.greaterThan( 1 ), () => {

			// speedCurve replaces the drag integral: distance = v0 * life * I(u).

			d.assign( life.mul( parentSpeedI( u ) ) );
			scale.assign( parentSpeedAt( u ) );

		} ).Else( () => {

			d.assign( select( uParentDrag.lessThan( .001 ), a, sub( 1., exp( uParentDrag.negate().mul( a ) ) ).div( uParentDrag ) ) );
			scale.assign( exp( uParentDrag.negate().mul( a ) ) );

		} );

		pos.assign( origin.add( dir.mul( v0 ).mul( d ) ) );
		vel.assign( dir.mul( v0 ).mul( scale ) );
		pos.addAssign( mul( .5, uParentGravity ).mul( a ).mul( a ).add( uParentWind.mul( a ) ) );
		vel.addAssign( uParentGravity.mul( a ).add( uParentWind ) );

		If( abs( uParentVortexW ).greaterThan( 1e-5 ), () => {

			// Rotate the radial part of the displacement about the vortex axis by
			// omega*age, with omega falling off with distance from that axis.

			const ax = safeDir( uParentVortexAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( pos, ax ).toVar();
			const rad = pos.sub( ax.mul( h ) ).toVar();
			const r = length( rad ).toVar();

			If( r.greaterThan( 1e-5 ), () => {

				const w = uParentVortexW.div( add( 1., uParentVortexFalloff.mul( r ) ) ).toVar();
				const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
				pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
				vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

			} );

		} );

		If( uParentCurl.greaterThan( 0. ), () => {

			const c = curl3( pos.mul( uParentCurlFreq ).add( vec3( 0., t.negate().mul( uParentCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
			pos.addAssign( c.mul( uParentCurl ).mul( 1.0 ).mul( a ) );

		} );

		If( uParentHasFloor.equal( 1 ), () => {

			const dy = pos.y.sub( uParentFloorY ).toVar();
			pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

		} );

	}, 'void' );

	/** Parent layer transform delta at absolute time t, from an 8-sample table. */

	const parentPathAt = /*@__PURE__*/ Fn( ( [ t ] ) => {

		const x = clamp( t.sub( uPathT0 ).div( max( uPathDt, 1e-4 ) ), 0., float( 8 - 1 ) ).toVar();
		const i0 = floor( x ).toVar(), f = x.sub( i0 ).toVar();
		const a = vec3( 0. ).toVar(), b = vec3( 0. ).toVar();

		Loop( 8, ( { i } ) => {

			const fi = float( i ).toVar();
			a.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( i0 ) ), .5 ) ) );
			b.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( min( i0.add( 1. ), float( 8 - 1 ) ) ) ), .5 ) ) );

		} );

		return mix( a, b, f );

	} );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vUv.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();

		// Sub-emitter: the child is born where and when the parent was at
		// parentBirth + offset, in the parent's own closed-form trajectory.

		const pLife = parentLife( aPSeed ).toVar();
		const r = fract( aSeed.x.mul( 13.37 ).add( aSeed.w.mul( 7.77 ) ).add( 0.113 ) ).toVar();
		const off = select( uSubMode.equal( 1 ), pLife, select( uSubMode.equal( 2 ), mix( uSubOffset.x, uSubOffset.y, r ), mix( uSubOffset.x, uSubOffset.y, fract( aSeed.x.mul( 3.0 ) ) ) ) ).toVar();
		off.assign( clamp( off, 0., pLife ) );
		const pNow = uTime.add( uParentTimeShift ).toVar();
		const pBirth = parentBirth( aPSeed, pNow.sub( off ) ).toVar();
		const ppos = property( 'vec3' ), pvel = property( 'vec3' );
		parentTraj( aPSeed, aPExtra, aPExtra2, off, pLife, pBirth.add( off ), ppos, pvel );
		birth.assign( pBirth.add( off ).sub( uParentTimeShift ) );
		subOrigin.assign( ppos.add( parentPathAt( pBirth.add( off ) ) ) );
		subVel.assign( pvel.mul( uInherit ) );

		If( pBirth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const side = position.x.toVar();
			const k = position.y.toVar();
			const kn = clamp( k.div( max( uSegments.sub( 1. ), 1. ) ), 0., 1. ).toVar();
			vUv.assign( vec2( side.mul( .5 ).add( .5 ), kn ) );
			const ageK = max( age.sub( k.mul( uSpacing ) ), 0. ).toVar();
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, ageK, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( ageK ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();
			const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
			const sv = vec2( sv3.x, sv3.y ).toVar();
			const svl = length( sv ).toVar();
			const across = select( svl.greaterThan( 1e-4 ), vec2( sv.y, sv.x.negate() ).div( svl ), vec2( 1., 0. ) ).toVar();
			const width = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).mul( curveG( kn ) ).mul( .5 ).toVar();
			mv.xy.addAssign( across.mul( side ).mul( width ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
export const subTrailVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aPSeed":{"type":"vec4","kind":"attribute"},"aPExtra":{"type":"vec4","kind":"attribute"},"aPExtra2":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSegments":{"type":"float","kind":"uniform"},"uSpacing":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveG":{"type":"vec2","size":8,"kind":"uniform"},"uCurveGN":{"type":"int","kind":"uniform"},"uCurveGEase":{"type":"float","kind":"uniform"},"uEffectPathMode":{"type":"int","kind":"uniform"},"uEffectPathSpread":{"type":"int","kind":"uniform"},"uEffectPathLength":{"type":"float","kind":"uniform"},"uEffectPathBaseLength":{"type":"float","kind":"uniform"},"uEffectPathData":{"type":"vec3","size":195,"kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentPeriod":{"type":"float","kind":"uniform"},"uParentSpawnWindow":{"type":"float","kind":"uniform"},"uParentSpawnDuration":{"type":"float","kind":"uniform"},"uParentShapeLength":{"type":"float","kind":"uniform"},"uParentShapeRadius":{"type":"float","kind":"uniform"},"uParentShapeInner":{"type":"float","kind":"uniform"},"uParentShapeAngle":{"type":"float","kind":"uniform"},"uParentDrag":{"type":"float","kind":"uniform"},"uParentCurl":{"type":"float","kind":"uniform"},"uParentCurlFreq":{"type":"float","kind":"uniform"},"uParentCurlSpeed":{"type":"float","kind":"uniform"},"uParentFloorY":{"type":"float","kind":"uniform"},"uParentFloorSoft":{"type":"float","kind":"uniform"},"uParentAngle":{"type":"float","kind":"uniform"},"uParentVortexW":{"type":"float","kind":"uniform"},"uParentVortexFalloff":{"type":"float","kind":"uniform"},"uParentSpawnMode":{"type":"int","kind":"uniform"},"uParentShapeType":{"type":"int","kind":"uniform"},"uParentVelMode":{"type":"int","kind":"uniform"},"uParentHasFloor":{"type":"int","kind":"uniform"},"uParentSurfaceOnly":{"type":"int","kind":"uniform"},"uParentSpeedN":{"type":"int","kind":"uniform"},"uParentBurstN":{"type":"int","kind":"uniform"},"uParentAxis":{"type":"vec3","kind":"uniform"},"uParentDir":{"type":"vec3","kind":"uniform"},"uParentGravity":{"type":"vec3","kind":"uniform"},"uParentWind":{"type":"vec3","kind":"uniform"},"uParentBias":{"type":"vec3","kind":"uniform"},"uParentShapeSize":{"type":"vec3","kind":"uniform"},"uParentVortexAxis":{"type":"vec3","kind":"uniform"},"uParentLife":{"type":"vec2","kind":"uniform"},"uParentSpeed":{"type":"vec2","kind":"uniform"},"uParentSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uParentBurstT":{"type":"float","size":8,"kind":"uniform"},"uParentBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentTimeShift":{"type":"float","kind":"uniform"},"uInherit":{"type":"float","kind":"uniform"},"uPathT0":{"type":"float","kind":"uniform"},"uPathDt":{"type":"float","kind":"uniform"},"uSubMode":{"type":"int","kind":"uniform"},"uSubOffset":{"type":"vec2","kind":"uniform"},"uParentPath":{"type":"vec3","size":8,"kind":"uniform"}};

// Three.js Transpiler r186

export function particleFragment( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uMask = bindings.uMask;
const uNoise = bindings.uNoise;
const uHasMask = bindings.uHasMask;
const uHasNoise = bindings.uHasNoise;
const uUseErosion = bindings.uUseErosion;
const uBlendMode = bindings.uBlendMode;
const uProcedural = bindings.uProcedural;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uTime = bindings.uTime;
const uDistort = bindings.uDistort;
const uErodeSoft = bindings.uErodeSoft;
const uEdgeW = bindings.uEdgeW;
const uEdgeI = bindings.uEdgeI;
const uOpacity = bindings.uOpacity;
const uMaskRot = bindings.uMaskRot;
const uNoiseScale = bindings.uNoiseScale;
const uNoisePan = bindings.uNoisePan;
const uMaskScale = bindings.uMaskScale;
const uMaskPan = bindings.uMaskPan;
const uDistortPan = bindings.uDistortPan;
const uEdgeCol = bindings.uEdgeCol;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const uCurveC = bindings.uCurveC;
const uCurveCN = bindings.uCurveCN;
const uCurveCEase = bindings.uCurveCEase;
const tDepth = bindings.tDepth;
const uResolution = bindings.uResolution;
const uNear = bindings.uNear;
const uFar = bindings.uFar;
const uSoft = bindings.uSoft;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, smoothstep, mix, Loop, atan, div, mod, cos, sin, fract, int, add, Discard, mat2 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const curveC = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveC.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveCN ), () => {

				Break();

			} );

			const a = uCurveC.element( i.sub( 1 ) ).x.toVar(), b = uCurveC.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveCEase ) );
			v.assign( mix( v, uCurveC.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const linDepth = /*@__PURE__*/ Fn( ( [ z ] ) => {

		const zn = z.mul( 2. ).sub( 1. ).toVar();

		return mul( 2., uNear ).mul( uFar ).div( max( uFar.add( uNear ).sub( zn.mul( uFar.sub( uNear ) ) ), 1e-5 ) );

	} );

	const softDepth = /*@__PURE__*/ Fn( () => {

		const result = float( 1. ).toVar();

		If( uSoft.greaterThan( 0. ), () => {

			const sc = screenCoordinate.xy.div( max( uResolution, vec2( 1. ) ) ).toVar();
			const sd = linDepth( tDepth.sample( sc ).x ).toVar(), fd = linDepth( vClipPosition.z.div( vClipPosition.w ) ).toVar();
			result.assign( clamp( sd.sub( fd ).div( uSoft ), 0., 1. ) );

		} );

		return result;

	} );

	const pointedShape = /*@__PURE__*/ Fn( ( [ p, arms, outerRadius, innerRadius ] ) => {

		const angle = atan( p.y, p.x ).toVar(), halfAngle = div( 3.14159265, arms ).toVar();
		const q = abs( mod( angle.sub( 1.570796 ).add( halfAngle ), halfAngle.mul( 2. ) ).sub( halfAngle ) ).toVar();
		const outer = vec2( outerRadius, 0. ).toVar(), inner = innerRadius.mul( vec2( cos( halfAngle ), sin( halfAngle ) ) ).toVar();
		const ray = vec2( cos( q ), sin( q ) ).toVar(), edge = inner.sub( outer ).toVar();
		const boundary = outer.x.mul( inner.y ).div( ray.x.mul( edge.y ).sub( ray.y.mul( edge.x ) ) ).toVar();

		return sub( 1., smoothstep( boundary.sub( .008 ), boundary.add( .008 ), length( p ) ) );

	}, { p: 'vec2', arms: 'float', outerRadius: 'float', innerRadius: 'float', return: 'float' } );

	/** Hex lattice with bright cell walls; density scales with material.mask.uvScale. */

	const hexCells = /*@__PURE__*/ Fn( ( [ uv, cell ] ) => {

		const q = uv.mul( vec2( 24., 12. ) ).mul( max( cell, vec2( .02 ) ) ).toVar();
		const spacing = vec2( 1.73205, 3. ).toVar();
		const h1 = mod( q, spacing ).sub( spacing.mul( .5 ) ).toVar(), h2 = mod( q.sub( spacing.mul( .5 ) ), spacing ).sub( spacing.mul( .5 ) ).toVar();
		const h = select( dot( h1, h1 ).lessThan( dot( h2, h2 ) ), h1, h2 ).toVar();

		// Cell planes of the triangular lattice: neighbours at (sqrt(3),0) and (sqrt(3)/2,1.5).

		const edge = abs( max( abs( h.x ), abs( h.x ).mul( .5 ).add( abs( h.y ).mul( .866025 ) ) ).sub( .866025 ) ).toVar();

		return sub( 1., smoothstep( .035, .10, edge ) );

	}, { uv: 'vec2', cell: 'vec2', return: 'float' } );

	/** Reflect into one 30-degree wedge of the hexagonal point group. Ice Ih is
	 * hexagonal, and this fold is what makes a snowflake six-fold symmetric: every
	 * pattern evaluated after it is repeated around the axis twelve times. */

	const hexFold = /*@__PURE__*/ Fn( ( [ p_immutable ] ) => {

		const p = p_immutable.toVar();
		p.x.assign( abs( p.x ) );
		const mirrorA = vec2( - .8660254, .5 ).toVar(), mirrorB = vec2( .8660254, .5 ).toVar();
		p.subAssign( mul( 2., min( 0., dot( p, mirrorA ) ) ).mul( mirrorA ) );
		p.subAssign( mul( 2., min( 0., dot( p, mirrorB ) ) ).mul( mirrorB ) );

		return p;

	} );

	const iceHash = /*@__PURE__*/ Fn( ( [ c ] ) => {

		const h = fract( vec3( c.xyx ).mul( vec3( .1031, .1030, .0973 ) ) ).toVar();
		h.addAssign( dot( h, h.yzx.add( 33.33 ) ) );

		return fract( vec2( h.x.add( h.y ).mul( h.z ), h.x.add( h.z ).mul( h.y ) ) );

	} );

	/** Worley F2-F1: near zero on a grain boundary, large inside a grain, so the
	 * zero set is the facet web between crystal grains rather than a blob field. */

	const iceFacets = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const cellId = floor( p ).toVar(), offset = p.sub( cellId ).toVar();
		const f1 = float( 8. ).toVar(), f2 = float( 8. ).toVar();

		Loop( { start: int( - 1 ), end: 1, name: 'j', condition: '<=' }, ( { j } ) => {

			Loop( { start: int( - 1 ), end: 1, condition: '<=' }, ( { i } ) => {

				const o = vec2( float( i ), float( j ) ).toVar();
				const d = length( o.add( iceHash( cellId.add( o ) ) ).sub( offset ) ).toVar();

				// Branchless "keep the two smallest": max(f1,d) is d only once d clears f1.

				f2.assign( min( f2, max( f1, d ) ) );
				f1.assign( min( f1, d ) );

			} );

		} );

		// x: boundary web, y: distance to this grain's seed, which shades its face.


		return vec2( f2.sub( f1 ), f1 );

	} );

	/** The same fold-and-facet rule at three scales, each an irrational step apart
	 * so the levels never line up into visible tiling. Self-similar by
	 * construction: a grain of ice is grains of ice. */

	const iceCrystal = /*@__PURE__*/ Fn( ( [ uv, cell ] ) => {

		const p = uv.sub( .5 ).mul( max( cell, vec2( .02 ) ) ).mul( 9. ).toVar();
		const web = float( 0. ).toVar(), face = float( 0. ).toVar(), weight = float( 0. ).toVar(), scale = float( 1. ).toVar(), amp = float( 1. ).toVar();

		Loop( 3, () => {

			const q = hexFold( p.mul( scale ) ).toVar();
			const f = iceFacets( q.mul( 2.4 ) ).toVar();
			web.addAssign( amp.mul( sub( 1., smoothstep( .02, .20, f.x ) ) ) );
			face.addAssign( amp.mul( sub( 1., smoothstep( .28, .95, f.y ) ) ) );
			weight.addAssign( amp );
			scale.mulAssign( 2.718282 );
			amp.mulAssign( .55 );

		} );

		return vec2( web, face ).div( max( weight, 1e-4 ) );

	} );

	// uProcedural: 0 none (soft disc), 1 flame, 2 smoke, 3 solid, 4 hexagon,
	// 5 ice, 6 water, 7 water-streaks, 8 star, 9 sparkle, 10 portal, 11 energy-ribbon.

	const proceduralShape = /*@__PURE__*/ Fn( ( [ p, uv, n, t, fres, cell, dims, mode ] ) => {

		If( mode.equal( 3 ), () => {

			return 1.;

		} );

		const disc = smoothstep( .5, .1, length( p ) ).toVar();

		If( mode.equal( 1 ), () => {

			return clamp( disc.mul( add( .45, n.mul( 1.1 ) ) ), 0., 1. );

		} );

		If( mode.equal( 2 ), () => {

			return clamp( disc.mul( add( .6, n.mul( .8 ) ) ), 0., 1. );

		} );

		const q = p.mul( 2. ).toVar();

		If( mode.equal( 4 ), () => {

			return clamp( add( .10, mul( .90, hexCells( uv, cell ) ) ), 0., 1. );

		} );

		If( mode.equal( 5 ), () => {

			// Frozen body: a fresnel-lit interior behind a self-similar facet web.

			const crystal = iceCrystal( uv, cell ).toVar();

			return clamp( add( .10, mul( .40, fres ).mul( fres ) ).add( mul( .92, crystal.x ) ).add( mul( .16, crystal.y ) ), 0., 1. );

		} );

		If( mode.equal( 6 ).or( mode.equal( 7 ) ), () => {

			const bend = sin( q.y.mul( 2.8 ).sub( t.mul( 2.2 ) ) ).mul( .12 ).toVar();
			const waves = sin( q.x.add( bend ).mul( 9.5 ).add( q.y.mul( .7 ) ) ).toVar();
			const streak = smoothstep( .72, .92, waves ).mul( add( .65, mul( .35, sin( q.y.mul( 2. ).sub( t.mul( 2. ) ) ) ) ) ).toVar();

			return clamp( select( mode.equal( 7 ), streak, add( .88, mul( .12, streak ) ) ), 0., 1. );

		} );

		If( mode.equal( 8 ), () => {

			return pointedShape( q, 5., .78, .31 );

		} );

		If( mode.equal( 9 ), () => {

			return pointedShape( q, 4., .9, .09 );

		} );

		If( mode.equal( 10 ), () => {

			// A framed portal: a rim of geometry.thickness metres inside the card, misty fill.

			const edgeDistance = min( sub( 1., abs( q.x ) ).mul( max( dims.x, 1e-3 ) ), sub( 1., abs( q.y ) ).mul( max( dims.y, 1e-3 ) ).mul( .5 ) ).toVar();
			const border = sub( 1., smoothstep( max( dims.z, .001 ).mul( .65 ), max( dims.z, .001 ).mul( 1.15 ), edgeDistance ) ).toVar();

			return clamp( border.mul( .8 ).add( add( .08, mul( .22, n ) ).mul( sub( 1., border ) ) ), 0., 1. );

		} );

		If( mode.equal( 11 ), () => {

			// Continuous core with a narrow moving edge; no longitudinal holes.

			const edge = add( .48, mul( .035, sin( q.y.mul( 18. ).sub( t.mul( 10. ) ) ) ) ).toVar();
			const side = sub( 1., smoothstep( edge.sub( .025 ), edge.add( .025 ), abs( q.x ) ) ).toVar();
			const ends = sub( 1., smoothstep( .94, 1., abs( q.y ) ) ).toVar();

			return clamp( side.mul( ends ), 0., 1. );

		} );

		return disc;

	}, { p: 'vec2', uv: 'vec2', n: 'float', t: 'float', fres: 'float', cell: 'vec2', dims: 'vec3', mode: 'int', return: 'float' } );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		// material.mask.rotation turns the mask on every instance; the per-particle
		// random roll rides on top of it.

		const rot = vRot.add( uMaskRot ).toVar();
		const p = vUv.sub( .5 ).toVar();
		const c = cos( rot ).toVar(), s = sin( rot ).toVar();
		p.assign( mat2( c, s.negate(), s, c ).mul( p ) );
		const uvp = p.add( .5 ).toVar();
		const n = float( .5 ).toVar();

		// Plain discs and un-eroded masks do not consume noise. Avoid evaluating
		// three simplex octaves per fragment unless a feature actually uses them.

		const needsNoise = uUseErosion.equal( 1 ).or( abs( uDistort ).greaterThan( 0. ) ).or( uHasMask.equal( 0 ).and( uProcedural.equal( 1 ).or( uProcedural.equal( 2 ) ) ) ).toVar();

		If( needsNoise.and( uHasNoise.equal( 1 ) ), () => {

			const nuv = uvp.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( vSeed.xy.mul( 7. ) ).toVar();
			const n1 = uNoise.sample( nuv ).r.toVar();
			const n2 = uNoise.sample( nuv.mul( 1.7 ).add( vec2( .3, .1 ) ).sub( uNoisePan.mul( uTime ).mul( .6 ) ) ).r.toVar();
			n.assign( n1.mul( .65 ).add( n2.mul( .35 ) ) );

		} ).ElseIf( needsNoise, () => {

			n.assign( add( .5, mul( .5, fbm3( vec3( uvp.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ), vSeed.x.mul( 17. ) ) ) ) ) );

		} );

		// noise.distortionPan scrolls the field that drives the distortion (never the
		// mask lookup itself, which would slide the sprite out of its own UV range).

		const nd = n.toVar();
		const dp = uDistortPan.mul( uTime ).toVar();

		If( abs( uDistort ).greaterThan( 0. ).and( dot( dp, dp ).greaterThan( 0. ) ), () => {

			If( uHasNoise.equal( 1 ), () => {

				nd.assign( uNoise.sample( uvp.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( dp ).add( vSeed.xy.mul( 7. ) ) ).r );

			} ).Else( () => {

				nd.assign( add( .5, mul( .5, fbm3( vec3( uvp.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ).add( dp ), vSeed.x.mul( 17. ) ) ) ) ) );

			} );

		} );

		const duv = uvp.add( nd.sub( .5 ).mul( uDistort ).mul( add( .4, vU ) ) ).toVar();
		const inside = step( 0., duv.x ).mul( step( duv.x, 1. ) ).mul( step( 0., duv.y ) ).mul( step( duv.y, 1. ) ).toVar();
		const muv = duv.mul( uMaskScale ).add( uMaskPan ).toVar();
		const auv = vec2( muv.x.div( float( max( uAtlasCols, 1 ) ) ), muv.y.div( float( max( uAtlasRows, 1 ) ) ) ).add( vTile ).toVar();
		const shape = property( 'float' );

		If( uHasMask.equal( 1 ), () => {

			const m = uMask.sample( auv ).toVar();
			shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

		} ).Else( () => {

			shape.assign( proceduralShape( p, uvp, n, uTime, 0., uMaskScale, vec3( 1., 1., .1 ), uProcedural ) );

		} );

		shape.mulAssign( inside );
		const er = shape.toVar(), edge = float( 0. ).toVar();

		If( uUseErosion.equal( 1 ), () => {

			const th = curveC( vU ).toVar();
			const field = shape.mul( add( .35, n.mul( .9 ) ) ).toVar();
			er.assign( smoothstep( th, th.add( uErodeSoft ), field ) );
			edge.assign( smoothstep( th.sub( uEdgeW ), th.add( uErodeSoft.mul( .5 ) ), field ).sub( er ) );

		} );

		const col = rampColor( vU ).toVar();
		col.addAssign( uEdgeCol.mul( uEdgeI ).mul( edge ).mul( shape ) );
		const a = er.mul( vAlpha ).mul( uOpacity ).mul( softDepth() ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		// Alpha blending is set up premultiplied for every mode but "alpha".

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
export const particleFragmentBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uMask":{"type":"sampler2D","kind":"uniform"},"uNoise":{"type":"sampler2D","kind":"uniform"},"uHasMask":{"type":"int","kind":"uniform"},"uHasNoise":{"type":"int","kind":"uniform"},"uUseErosion":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uProcedural":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uDistort":{"type":"float","kind":"uniform"},"uErodeSoft":{"type":"float","kind":"uniform"},"uEdgeW":{"type":"float","kind":"uniform"},"uEdgeI":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uMaskRot":{"type":"float","kind":"uniform"},"uNoiseScale":{"type":"vec2","kind":"uniform"},"uNoisePan":{"type":"vec2","kind":"uniform"},"uMaskScale":{"type":"vec2","kind":"uniform"},"uMaskPan":{"type":"vec2","kind":"uniform"},"uDistortPan":{"type":"vec2","kind":"uniform"},"uEdgeCol":{"type":"vec3","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"},"uCurveC":{"type":"vec2","size":8,"kind":"uniform"},"uCurveCN":{"type":"int","kind":"uniform"},"uCurveCEase":{"type":"float","kind":"uniform"},"tDepth":{"type":"sampler2D","kind":"uniform"},"uResolution":{"type":"vec2","kind":"uniform"},"uNear":{"type":"float","kind":"uniform"},"uFar":{"type":"float","kind":"uniform"},"uSoft":{"type":"float","kind":"uniform"}};

// Three.js Transpiler r186

export function trailFragment( bindings ) {
const uTrail = bindings.uTrail;
const uHasTrail = bindings.uHasTrail;
const uBlendMode = bindings.uBlendMode;
const uOpacity = bindings.uOpacity;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, Discard, abs, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		const shape = property( 'float' );

		If( uHasTrail.equal( 1 ), () => {

			const m = uTrail.sample( vUv ).toVar();
			shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

		} ).Else( () => {

			shape.assign( smoothstep( 1., 0., abs( vUv.x.mul( 2. ).sub( 1. ) ) ) );

		} );

		const col = rampColor( vU ).toVar();
		const a = shape.mul( vAlpha ).mul( uOpacity ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
export const trailFragmentBindings = {"uTrail":{"type":"sampler2D","kind":"uniform"},"uHasTrail":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};

// Three.js Transpiler r186

export function surfaceVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uTime = bindings.uTime;
const uLength = bindings.uLength;
const uRadius = bindings.uRadius;
const uThickness = bindings.uThickness;
const uArc = bindings.uArc;
const uVertexAmp = bindings.uVertexAmp;
const uVertexFreq = bindings.uVertexFreq;
const uVertexSpeed = bindings.uVertexSpeed;
const uDisplaceShift = bindings.uDisplaceShift;
const uRoll = bindings.uRoll;
const uShell = bindings.uShell;
const uHasVertexNoise = bindings.uHasVertexNoise;
const uBillboard = bindings.uBillboard;
const uRibbon = bindings.uRibbon;
const uUseLocalZ = bindings.uUseLocalZ;
const uZRange = bindings.uZRange;
const uVertexBias = bindings.uVertexBias;
const vN = bindings.vN;
const vWp = bindings.vWp;
const vAlong = bindings.vAlong;
const vLobe = bindings.vLobe;
const vRing = bindings.vRing;
const vUv = bindings.vUv;
const uCurveF = bindings.uCurveF;
const uCurveFN = bindings.uCurveFN;
const uCurveFEase = bindings.uCurveFEase;
const uEffectPathMode = bindings.uEffectPathMode;
const uEffectPathSpread = bindings.uEffectPathSpread;
const uEffectPathLength = bindings.uEffectPathLength;
const uEffectPathBaseLength = bindings.uEffectPathBaseLength;
const uEffectPathData = bindings.uEffectPathData;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, mix, Loop, int, normalize, cross, sin, cos, smoothstep, sqrt, add, atan } = TSL;

	const gl_Position = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const curveF = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveF.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveFN ), () => {

				Break();

			} );

			const a = uCurveF.element( i.sub( 1 ) ).x.toVar(), b = uCurveF.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveFEase ) );
			v.assign( mix( v, uCurveF.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	// One buffer keeps trail materials within WebGPU per-stage binding limits.

	const effectPathFrame = /*@__PURE__*/ Fn( ( [ d, pathPositionOut, pathNormalOut, pathTangentOut ] ) => {




		const x = clamp( d.div( uEffectPathLength ), 0., 1. ).mul( float( 64 ) ).toVar();
		const i = int( min( floor( x ), float( 63 ) ) ).toVar();
		const f = x.sub( float( i ) ).toVar();
		pathTangentOut.assign( normalize( mix( uEffectPathData.element( i.mul( 3 ).add( 2 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 2 ) ), f ) ) );
		pathNormalOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 1 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 1 ) ), f ) );
		pathNormalOut.assign( normalize( pathNormalOut.sub( pathTangentOut.mul( dot( pathNormalOut, pathTangentOut ) ) ) ) );
		pathPositionOut.assign( mix( uEffectPathData.element( i.mul( 3 ).add( 0 ) ), uEffectPathData.element( i.add( 1 ).mul( 3 ).add( 0 ) ), f ).add( pathTangentOut.mul( d.sub( clamp( d, 0., uEffectPathLength ) ) ) ) );

	}, 'void' );

	const effectPathVector = /*@__PURE__*/ Fn( ( [ v, n, t ] ) => {

		return n.mul( v.x ).add( cross( t, n ).mul( v.y ) ).add( t.mul( v.z ) );

	} );

	const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

		return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

	} );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vLobe.assign( 0. );
		vRing.assign( uv.x );
		const worldPos = property( 'vec3' ), worldNormal = property( 'vec3' );

		If( uEffectPathMode.equal( 1 ).and( uShell.equal( 0 ) ), () => {

			const along = select( uRibbon.equal( 1 ), clamp( position.x, 0., 1. ), clamp( position.z, 0., 1. ) ).toVar();
			const pp = property( 'vec3' ), nn = property( 'vec3' ), tt = property( 'vec3' );
			effectPathFrame( along.mul( uEffectPathLength ).mul( uLength ).div( max( uEffectPathBaseLength, 1e-5 ) ), pp, nn, tt );
			const crossSection = position.xy.mul( uRadius ).toVar();

			If( uRibbon.equal( 1 ), () => {

				crossSection.assign( vec2( position.y.mul( uThickness ).mul( safePow( max( 0., sin( mul( 3.14159265, along ) ) ), .6 ) ), 0. ) );

			} );

			const local = pp.add( effectPathVector( vec3( crossSection, 0. ), nn, tt ) ).toVar();

			If( uHasVertexNoise.equal( 1 ), () => {

				local.addAssign( nn.mul( fbm3( vec3( crossSection.mul( uVertexFreq ), along.mul( uVertexFreq ).sub( uTime.mul( uVertexSpeed ) ) ) ) ).mul( uVertexAmp ).mul( curveF( along ) ) );

			} );

			worldPos.assign( modelMatrix.mul( vec4( local, 1. ) ).xyz );
			const localNormal = select( uRibbon.equal( 1 ), cross( tt, nn ), effectPathVector( normal, nn, tt ) ).toVar();
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( localNormal, 0. ) ).xyz, vec3( 0., 1., 0. ) ) );
			vAlong.assign( along );

		} ).ElseIf( uRibbon.equal( 1 ), () => {

			// A tapered arc sweep in the local XY plane (the swoosh of a slash):
			// geometry.radius is the arc radius, geometry.length the angle it sweeps
			// (uArc, radians) and geometry.thickness its half-width at the fattest
			// point. The strip carries (u along the arc, side) in the position attribute.

			const ua = clamp( position.x, 0., 1. ).toVar();
			const side = position.y.toVar();
			vAlong.assign( clamp( uv.y, 0., 1. ) );
			const a = ua.mul( uArc ).toVar();
			const taper = safePow( max( 0., sin( mul( 3.14159265, ua ) ) ), .6 ).toVar();
			const w = max( 1e-4, uThickness.mul( taper ) ).toVar();
			const rr = uRadius.add( side.mul( w ) ).toVar();
			const local = vec3( cos( a ).mul( rr ), sin( a ).mul( rr ), mul( .12, uRadius ).mul( sin( a.mul( 2. ) ) ) ).toVar();

			If( uHasVertexNoise.equal( 1 ), () => {

				const nz = fbm3( vec3( local.xy.mul( uVertexFreq ), a.sub( uTime.mul( uVertexSpeed ) ) ) ).toVar();
				local.addAssign( vec3( cos( a ), sin( a ), 0. ).mul( nz ).mul( uVertexAmp ).mul( uRadius ).mul( curveF( vAlong ) ) );

			} );

			worldPos.assign( modelMatrix.mul( vec4( local, 1. ) ).xyz );
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( 0., 0., 1., 0. ) ).xyz, vec3( 0., 0., 1. ) ) );

		} ).ElseIf( uShell.equal( 1 ), () => {

			// Unit sphere -> teardrop: along = (1-z)/2 on the local +Z axis (z=+1 is
			// the head). The tube frame is built from the WORLD axis so that the ring
			// "up" reference and the bias push direction live in the same space no
			// matter how the layer's transform decomposes into euler angles.

			const along = clamp( sub( 1., position.z ).mul( .5 ), 0., 1. ).toVar();
			vAlong.assign( along );

			// Squaring, not pow(): a negative base would be clamped by safePow and the
			// bulbous nose would flatten into a full-radius cylinder.

			const taper = along.mul( 2. ).sub( 1. ).toVar();

			// transform.scale reaches the analytic body too: the axis column of the
			// model matrix scales nose-to-tail, the lateral columns the body radius.

			const sAxis = length( modelMatrix.element(2).xyz ).toVar();
			const sRad = mul( .5, length( modelMatrix.element(0).xyz ).add( length( modelMatrix.element(1).xyz ) ) ).toVar();
			const radius = uRadius.mul( sRad ).toVar(), length_ = uLength.mul( sAxis ).toVar();

			// The tail is a fan of coincident vertices: closing it to a point over the
			// last 5% of 'along' replaces a torn cap with a smooth taper.

			const tailCap = smoothstep( 1., .95, along ).toVar();
			const r = radius.mul( mul( 1.05, sqrt( max( 0., sub( 1., taper.mul( taper ) ) ) ) ).mul( sub( 1., along.mul( .45 ) ) ).add( .06 ) ).mul( tailCap ).toVar();
			const len = length_.mul( select( along.lessThan( .5 ), along.mul( .9 ), add( .45, along.sub( .5 ).mul( 1.1 ) ) ) ).toVar();
			const head = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
			const axis = safeDir( modelMatrix.mul( vec4( 0., 0., 1., 0. ) ).xyz, vec3( 0., 0., 1. ) ).toVar();
			const t1 = orthoOf( axis ).toVar(), t2 = cross( axis, t1 ).toVar();

			If( uEffectPathMode.equal( 1 ), () => {

				const pathP = property( 'vec3' ), pathN = property( 'vec3' ), pathT = property( 'vec3' );
				effectPathFrame( len.div( max( uEffectPathBaseLength, 1e-5 ) ).mul( uEffectPathLength ), pathP, pathN, pathT );
				head.assign( modelMatrix.mul( vec4( pathP, 1. ) ).xyz );
				axis.assign( safeDir( modelMatrix.mul( vec4( pathT, 0. ) ).xyz, vec3( 0., 0., 1. ) ) );
				t1.assign( safeDir( modelMatrix.mul( vec4( pathN, 0. ) ).xyz, vec3( 1., 0., 0. ) ) );
				t2.assign( cross( axis, t1 ) );

			} );

			const ring = normalize( position.xy.add( vec2( 1e-5 ) ) ).toVar();
			vRing.assign( atan( ring.y, ring.x ).mul( .15915494 ).add( .5 ) );
			const radial = t1.mul( ring.x ).add( t2.mul( ring.y ) ).toVar();
			const base = head.add( axis.mul( select( uEffectPathMode.equal( 1 ), 0., len ) ) ).add( radial.mul( r ) ).toVar();
			const amp = select( uHasVertexNoise.equal( 1 ), uVertexAmp, 0. ).toVar();
			const nz = fbm3( vec3( ring.mul( uVertexFreq ).add( vec2( along.mul( 3.5 ), 0. ) ), along.mul( 4. ).sub( uTime.mul( uVertexSpeed ) ) ).add( vec3( 0., 0., along.mul( 3. ) ) ) ).toVar();
			const disp = nz.mul( amp ).mul( curveF( along ) ).mul( radius ).mul( 2.4 ).mul( tailCap ).toVar();

			// Low-frequency lobes on one side of the tube only: a few big licks lift
			// off the tail along the bias direction, so the body silhouette stays thin.

			const bias = safeDir( uVertexBias, vec3( 0., 1., 0. ) ).toVar();

			If( uEffectPathMode.equal( 1 ), () => {

				bias.assign( safeDir( t1.mul( uVertexBias.x ).add( t2.mul( uVertexBias.y ) ).add( axis.mul( uVertexBias.z ) ), t2 ) );

			} );

			const lobe = snoise( vec3( ring.mul( uVertexFreq.mul( .36 ) ).add( vec2( along.mul( 1.4 ), 0. ) ), along.mul( 2.2 ).sub( uTime.mul( uVertexSpeed.mul( .75 ) ) ) ) ).toVar();
			const up = smoothstep( .2, .95, ring.y ).mul( smoothstep( .35, .8, along ) ).toVar();
			vLobe.assign( max( 0., lobe ).mul( up ).mul( step( 1e-4, amp ) ) );
			const lick = vLobe.mul( radius ).mul( amp.mul( 6.2 ) ).mul( tailCap ).toVar();
			worldPos.assign( base.add( radial.mul( disp ) ).add( axis.mul( nz ).mul( .25 ).mul( along ) ).add( bias.mul( length_.mul( .236 ).mul( safePow( along, 2.3 ) ).mul( add( .6, mul( .4, nz ) ) ).add( lick ) ) ) );
			worldNormal.assign( safeDir( radial, vec3( 0., 1., 0. ) ) );

		} ).ElseIf( uBillboard.equal( 1 ), () => {

			// A sprite is a camera-facing square of half-size geometry.radius: the quad
			// is rebuilt on the view's right/up axes around the layer origin, so it
			// never edges out of the shot. transform.rotation.element(2) survives as a roll.

			vAlong.assign( clamp( uv.y, 0., 1. ) );
			const centre = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
			const right = vec3( viewMatrix.element(0)[ 0 ], viewMatrix.element(1)[ 0 ], viewMatrix.element(2)[ 0 ] ).toVar();
			const up = vec3( viewMatrix.element(0)[ 1 ], viewMatrix.element(1)[ 1 ], viewMatrix.element(2)[ 1 ] ).toVar();

			// The instance's own scale still shapes the quad (a streak stays a streak).

			const half2 = vec2( length( modelMatrix.element(0).xyz ), length( modelMatrix.element(1).xyz ) ).toVar();
			const c = cos( uRoll ).toVar(), s = sin( uRoll ).toVar();
			const q = vec2( position.x.mul( half2.x ), position.y.mul( half2.y ) ).toVar();
			q.assign( vec2( q.x.mul( c ).sub( q.y.mul( s ) ), q.x.mul( s ).add( q.y.mul( c ) ) ) );
			worldPos.assign( centre.add( right.mul( q.x ) ).add( up.mul( q.y ) ) );
			worldNormal.assign( safeDir( cross( right, up ), vec3( 0., 0., 1. ) ) );

		} ).Else( () => {

			// A mesh whose axis really runs along local +Z (a bar, a crystal) keys its
			// ramp and erosion on that axis; flat cards and lathed shapes keep uv.y.

			vAlong.assign( select( uUseLocalZ.equal( 1 ), clamp( position.z.sub( uZRange.x ).div( max( uZRange.y.sub( uZRange.x ), 1e-4 ) ), 0., 1. ), clamp( uv.y, 0., 1. ) ) );
			const pos = position.toVar();

			If( uHasVertexNoise.equal( 1 ), () => {

				const nz = fbm3( vec3( position.xy.mul( uVertexFreq ), position.z.mul( uVertexFreq ).sub( uTime.mul( uVertexSpeed ) ) ) ).toVar();
				const bias = safeDir( uVertexBias, normal ).toVar();
				pos.addAssign( mix( normal, bias, .5 ).mul( nz ).mul( uVertexAmp ).mul( curveF( vAlong ) ) );

			} );

			worldPos.assign( modelMatrix.mul( vec4( pos, 1. ) ).xyz );
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( normal, 0. ) ).xyz, vec3( 0., 1., 0. ) ) );

		} );

		vWp.assign( worldPos );
		vN.assign( worldNormal );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( worldPos, 1. ) ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
export const surfaceVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uTime":{"type":"float","kind":"uniform"},"uLength":{"type":"float","kind":"uniform"},"uRadius":{"type":"float","kind":"uniform"},"uThickness":{"type":"float","kind":"uniform"},"uArc":{"type":"float","kind":"uniform"},"uVertexAmp":{"type":"float","kind":"uniform"},"uVertexFreq":{"type":"float","kind":"uniform"},"uVertexSpeed":{"type":"float","kind":"uniform"},"uDisplaceShift":{"type":"float","kind":"uniform"},"uRoll":{"type":"float","kind":"uniform"},"uShell":{"type":"int","kind":"uniform"},"uHasVertexNoise":{"type":"int","kind":"uniform"},"uBillboard":{"type":"int","kind":"uniform"},"uRibbon":{"type":"int","kind":"uniform"},"uUseLocalZ":{"type":"int","kind":"uniform"},"uZRange":{"type":"vec2","kind":"uniform"},"uVertexBias":{"type":"vec3","kind":"uniform"},"vN":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vLobe":{"type":"float","kind":"varying"},"vRing":{"type":"float","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"uCurveF":{"type":"vec2","size":8,"kind":"uniform"},"uCurveFN":{"type":"int","kind":"uniform"},"uCurveFEase":{"type":"float","kind":"uniform"},"uEffectPathMode":{"type":"int","kind":"uniform"},"uEffectPathSpread":{"type":"int","kind":"uniform"},"uEffectPathLength":{"type":"float","kind":"uniform"},"uEffectPathBaseLength":{"type":"float","kind":"uniform"},"uEffectPathData":{"type":"vec3","size":195,"kind":"uniform"}};

// Three.js Transpiler r186

export function surfaceFragment( bindings ) {
const uTime = bindings.uTime;
const uOpacity = bindings.uOpacity;
const uErodeSoft = bindings.uErodeSoft;
const uEdgeW = bindings.uEdgeW;
const uEdgeI = bindings.uEdgeI;
const uProtect = bindings.uProtect;
const uRimBias = bindings.uRimBias;
const uDisplaceShift = bindings.uDisplaceShift;
const uFresnelPower = bindings.uFresnelPower;
const uFresnelStrength = bindings.uFresnelStrength;
const uDistort = bindings.uDistort;
const uRampKeyMode = bindings.uRampKeyMode;
const uLayerU = bindings.uLayerU;
const uMaskRot = bindings.uMaskRot;
const uRadius = bindings.uRadius;
const uLength = bindings.uLength;
const uThickness = bindings.uThickness;
const uShell = bindings.uShell;
const uHasMask = bindings.uHasMask;
const uHasNoise = bindings.uHasNoise;
const uUseErosion = bindings.uUseErosion;
const uBlendMode = bindings.uBlendMode;
const uProcedural = bindings.uProcedural;
const uHasFresnel = bindings.uHasFresnel;
const uBolt = bindings.uBolt;
const uNoiseScale = bindings.uNoiseScale;
const uNoisePan = bindings.uNoisePan;
const uMaskScale = bindings.uMaskScale;
const uMaskPan = bindings.uMaskPan;
const uDistortPan = bindings.uDistortPan;
const uEdgeCol = bindings.uEdgeCol;
const uCam = bindings.uCam;
const uMask = bindings.uMask;
const uNoise = bindings.uNoise;
const vN = bindings.vN;
const vWp = bindings.vWp;
const vAlong = bindings.vAlong;
const vLobe = bindings.vLobe;
const vRing = bindings.vRing;
const vUv = bindings.vUv;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const uCurveC = bindings.uCurveC;
const uCurveCN = bindings.uCurveCN;
const uCurveCEase = bindings.uCurveCEase;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, smoothstep, mix, Loop, atan, div, mod, cos, sin, fract, int, add, mat2, Discard } = TSL;

	const gl_FragColor = property( 'vec4' );

	const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec3', return: 'vec3' } );

	const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

	}, { x: 'vec4', return: 'vec4' } );

	const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

		return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

	}, { x: 'vec4', return: 'vec4' } );

	const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

		return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

	}, { r: 'vec4', return: 'vec4' } );

	const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

		const C = vec2( 1. / 6., 1. / 3. ).toVar();
		const D = vec4( 0., .5, 1., 2. ).toVar();
		const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
		const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
		const g = step( x0.yzx, x0.xyz ).toVar();
		const l = sub( 1., g ).toVar();
		const i1 = min( g.xyz, l.zxy ).toVar();
		const i2 = max( g.xyz, l.zxy ).toVar();
		const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
		const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
		const x3 = x0.sub( D.yyy ).toVar();
		i.assign( mod289v3( i ) );
		const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
		const n_ = float( .142857142857 ).toVar();
		const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
		const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
		const x_ = floor( j.mul( ns.z ) ).toVar();
		const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
		const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
		const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
		const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
		const b0 = vec4( x.xy, y.xy ).toVar();
		const b1 = vec4( x.zw, y.zw ).toVar();
		const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
		const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
		const sh = step( h, vec4( 0. ) ).negate().toVar();
		const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
		const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
		const p0 = vec3( a0.xy, h.x ).toVar();
		const p1 = vec3( a0.zw, h.y ).toVar();
		const p2 = vec3( a1.xy, h.z ).toVar();
		const p3 = vec3( a1.zw, h.w ).toVar();
		const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
		p0.mulAssign( norm.x );
		p1.mulAssign( norm.y );
		p2.mulAssign( norm.z );
		p3.mulAssign( norm.w );
		const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
		m.assign( m.mul( m ) );

		return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

	}, { v: 'vec3', return: 'float' } );

	const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

	}, { p: 'vec3', return: 'float' } );

	const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const e = float( .1 ).toVar();
		const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
		const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
		const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
		const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

		return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

	}, { p: 'vec3', return: 'vec3' } );

	/** normalize() with a defined result for the zero vector. */

	const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

		const l = length( v ).toVar();

		return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

	}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

	/** pow() that can never see a negative base. */

	const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

		return pow( max( base, 1e-5 ), e );

	}, { base: 'float', e: 'float', return: 'float' } );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const curveC = /*@__PURE__*/ Fn( ( [ u_immutable ] ) => {

		const u = u_immutable.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveC.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveCN ), () => {

				Break();

			} );

			const a = uCurveC.element( i.sub( 1 ) ).x.toVar(), b = uCurveC.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveCEase ) );
			v.assign( mix( v, uCurveC.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const pointedShape = /*@__PURE__*/ Fn( ( [ p, arms, outerRadius, innerRadius ] ) => {

		const angle = atan( p.y, p.x ).toVar(), halfAngle = div( 3.14159265, arms ).toVar();
		const q = abs( mod( angle.sub( 1.570796 ).add( halfAngle ), halfAngle.mul( 2. ) ).sub( halfAngle ) ).toVar();
		const outer = vec2( outerRadius, 0. ).toVar(), inner = innerRadius.mul( vec2( cos( halfAngle ), sin( halfAngle ) ) ).toVar();
		const ray = vec2( cos( q ), sin( q ) ).toVar(), edge = inner.sub( outer ).toVar();
		const boundary = outer.x.mul( inner.y ).div( ray.x.mul( edge.y ).sub( ray.y.mul( edge.x ) ) ).toVar();

		return sub( 1., smoothstep( boundary.sub( .008 ), boundary.add( .008 ), length( p ) ) );

	}, { p: 'vec2', arms: 'float', outerRadius: 'float', innerRadius: 'float', return: 'float' } );

	/** Hex lattice with bright cell walls; density scales with material.mask.uvScale. */

	const hexCells = /*@__PURE__*/ Fn( ( [ uv, cell ] ) => {

		const q = uv.mul( vec2( 24., 12. ) ).mul( max( cell, vec2( .02 ) ) ).toVar();
		const spacing = vec2( 1.73205, 3. ).toVar();
		const h1 = mod( q, spacing ).sub( spacing.mul( .5 ) ).toVar(), h2 = mod( q.sub( spacing.mul( .5 ) ), spacing ).sub( spacing.mul( .5 ) ).toVar();
		const h = select( dot( h1, h1 ).lessThan( dot( h2, h2 ) ), h1, h2 ).toVar();

		// Cell planes of the triangular lattice: neighbours at (sqrt(3),0) and (sqrt(3)/2,1.5).

		const edge = abs( max( abs( h.x ), abs( h.x ).mul( .5 ).add( abs( h.y ).mul( .866025 ) ) ).sub( .866025 ) ).toVar();

		return sub( 1., smoothstep( .035, .10, edge ) );

	}, { uv: 'vec2', cell: 'vec2', return: 'float' } );

	/** Reflect into one 30-degree wedge of the hexagonal point group. Ice Ih is
	 * hexagonal, and this fold is what makes a snowflake six-fold symmetric: every
	 * pattern evaluated after it is repeated around the axis twelve times. */

	const hexFold = /*@__PURE__*/ Fn( ( [ p_immutable ] ) => {

		const p = p_immutable.toVar();
		p.x.assign( abs( p.x ) );
		const mirrorA = vec2( - .8660254, .5 ).toVar(), mirrorB = vec2( .8660254, .5 ).toVar();
		p.subAssign( mul( 2., min( 0., dot( p, mirrorA ) ) ).mul( mirrorA ) );
		p.subAssign( mul( 2., min( 0., dot( p, mirrorB ) ) ).mul( mirrorB ) );

		return p;

	} );

	const iceHash = /*@__PURE__*/ Fn( ( [ c ] ) => {

		const h = fract( vec3( c.xyx ).mul( vec3( .1031, .1030, .0973 ) ) ).toVar();
		h.addAssign( dot( h, h.yzx.add( 33.33 ) ) );

		return fract( vec2( h.x.add( h.y ).mul( h.z ), h.x.add( h.z ).mul( h.y ) ) );

	} );

	/** Worley F2-F1: near zero on a grain boundary, large inside a grain, so the
	 * zero set is the facet web between crystal grains rather than a blob field. */

	const iceFacets = /*@__PURE__*/ Fn( ( [ p ] ) => {

		const cellId = floor( p ).toVar(), offset = p.sub( cellId ).toVar();
		const f1 = float( 8. ).toVar(), f2 = float( 8. ).toVar();

		Loop( { start: int( - 1 ), end: 1, name: 'j', condition: '<=' }, ( { j } ) => {

			Loop( { start: int( - 1 ), end: 1, condition: '<=' }, ( { i } ) => {

				const o = vec2( float( i ), float( j ) ).toVar();
				const d = length( o.add( iceHash( cellId.add( o ) ) ).sub( offset ) ).toVar();

				// Branchless "keep the two smallest": max(f1,d) is d only once d clears f1.

				f2.assign( min( f2, max( f1, d ) ) );
				f1.assign( min( f1, d ) );

			} );

		} );

		// x: boundary web, y: distance to this grain's seed, which shades its face.


		return vec2( f2.sub( f1 ), f1 );

	} );

	/** The same fold-and-facet rule at three scales, each an irrational step apart
	 * so the levels never line up into visible tiling. Self-similar by
	 * construction: a grain of ice is grains of ice. */

	const iceCrystal = /*@__PURE__*/ Fn( ( [ uv, cell ] ) => {

		const p = uv.sub( .5 ).mul( max( cell, vec2( .02 ) ) ).mul( 9. ).toVar();
		const web = float( 0. ).toVar(), face = float( 0. ).toVar(), weight = float( 0. ).toVar(), scale = float( 1. ).toVar(), amp = float( 1. ).toVar();

		Loop( 3, () => {

			const q = hexFold( p.mul( scale ) ).toVar();
			const f = iceFacets( q.mul( 2.4 ) ).toVar();
			web.addAssign( amp.mul( sub( 1., smoothstep( .02, .20, f.x ) ) ) );
			face.addAssign( amp.mul( sub( 1., smoothstep( .28, .95, f.y ) ) ) );
			weight.addAssign( amp );
			scale.mulAssign( 2.718282 );
			amp.mulAssign( .55 );

		} );

		return vec2( web, face ).div( max( weight, 1e-4 ) );

	} );

	// uProcedural: 0 none (soft disc), 1 flame, 2 smoke, 3 solid, 4 hexagon,
	// 5 ice, 6 water, 7 water-streaks, 8 star, 9 sparkle, 10 portal, 11 energy-ribbon.

	const proceduralShape = /*@__PURE__*/ Fn( ( [ p, uv, n, t, fres, cell, dims, mode ] ) => {

		If( mode.equal( 3 ), () => {

			return 1.;

		} );

		const disc = smoothstep( .5, .1, length( p ) ).toVar();

		If( mode.equal( 1 ), () => {

			return clamp( disc.mul( add( .45, n.mul( 1.1 ) ) ), 0., 1. );

		} );

		If( mode.equal( 2 ), () => {

			return clamp( disc.mul( add( .6, n.mul( .8 ) ) ), 0., 1. );

		} );

		const q = p.mul( 2. ).toVar();

		If( mode.equal( 4 ), () => {

			return clamp( add( .10, mul( .90, hexCells( uv, cell ) ) ), 0., 1. );

		} );

		If( mode.equal( 5 ), () => {

			// Frozen body: a fresnel-lit interior behind a self-similar facet web.

			const crystal = iceCrystal( uv, cell ).toVar();

			return clamp( add( .10, mul( .40, fres ).mul( fres ) ).add( mul( .92, crystal.x ) ).add( mul( .16, crystal.y ) ), 0., 1. );

		} );

		If( mode.equal( 6 ).or( mode.equal( 7 ) ), () => {

			const bend = sin( q.y.mul( 2.8 ).sub( t.mul( 2.2 ) ) ).mul( .12 ).toVar();
			const waves = sin( q.x.add( bend ).mul( 9.5 ).add( q.y.mul( .7 ) ) ).toVar();
			const streak = smoothstep( .72, .92, waves ).mul( add( .65, mul( .35, sin( q.y.mul( 2. ).sub( t.mul( 2. ) ) ) ) ) ).toVar();

			return clamp( select( mode.equal( 7 ), streak, add( .88, mul( .12, streak ) ) ), 0., 1. );

		} );

		If( mode.equal( 8 ), () => {

			return pointedShape( q, 5., .78, .31 );

		} );

		If( mode.equal( 9 ), () => {

			return pointedShape( q, 4., .9, .09 );

		} );

		If( mode.equal( 10 ), () => {

			// A framed portal: a rim of geometry.thickness metres inside the card, misty fill.

			const edgeDistance = min( sub( 1., abs( q.x ) ).mul( max( dims.x, 1e-3 ) ), sub( 1., abs( q.y ) ).mul( max( dims.y, 1e-3 ) ).mul( .5 ) ).toVar();
			const border = sub( 1., smoothstep( max( dims.z, .001 ).mul( .65 ), max( dims.z, .001 ).mul( 1.15 ), edgeDistance ) ).toVar();

			return clamp( border.mul( .8 ).add( add( .08, mul( .22, n ) ).mul( sub( 1., border ) ) ), 0., 1. );

		} );

		If( mode.equal( 11 ), () => {

			// Continuous core with a narrow moving edge; no longitudinal holes.

			const edge = add( .48, mul( .035, sin( q.y.mul( 18. ).sub( t.mul( 10. ) ) ) ) ).toVar();
			const side = sub( 1., smoothstep( edge.sub( .025 ), edge.add( .025 ), abs( q.x ) ) ).toVar();
			const ends = sub( 1., smoothstep( .94, 1., abs( q.y ) ) ).toVar();

			return clamp( side.mul( ends ), 0., 1. );

		} );

		return disc;

	}, { p: 'vec2', uv: 'vec2', n: 'float', t: 'float', fres: 'float', cell: 'vec2', dims: 'vec3', mode: 'int', return: 'float' } );

	const main = /*@__PURE__*/ Fn( () => {

		const V = safeDir( uCam.sub( vWp ), vec3( 0., 0., 1. ) ).toVar();
		const fres = sub( 1., abs( dot( vN, V ) ) ).toVar();
		const n = float( .5 ).toVar();

		If( uShell.equal( 1 ), () => {

			n.assign( add( .5, mul( .5, mul( .7, snoise( vec3( vUv.x.mul( 3.0 ), vAlong.mul( 3.2 ).sub( uTime.mul( 2.0 ) ), .7 ) ) ).add( mul( .3, snoise( vec3( vUv.x.mul( 6.0 ), vAlong.mul( 6.0 ).sub( uTime.mul( 3.1 ) ), 2.3 ) ) ) ) ) ) );

			If( uHasNoise.equal( 1 ), () => {

				const ntex = uNoise.sample( vec2( vUv.x.mul( uNoiseScale.x ), vAlong.mul( uNoiseScale.y ).add( uNoisePan.y.mul( uTime ) ) ) ).r.toVar();
				n.assign( n.mul( .75 ).add( ntex.mul( .25 ) ) );

			} );

		} ).ElseIf( uHasNoise.equal( 1 ), () => {

			n.assign( uNoise.sample( vUv.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ) ).r );

		} ).Else( () => {

			n.assign( add( .5, mul( .5, fbm3( vec3( vUv.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ), 3.1 ) ) ) ) );

		} );

		const shape = float( 1. ).toVar();
		const dims = vec3( uRadius, uLength, uThickness ).toVar();

		If( uBolt.equal( 1 ), () => {

			// A bolt is a tube: its normals face the camera along the centreline and
			// graze it at the silhouette, which is exactly the falloff a hot core with
			// a soft edge wants. Without it the tube reads as a flat blown-out slab.

			shape.assign( safePow( abs( dot( vN, V ) ), .65 ) );

		} ).ElseIf( uShell.equal( 1 ), () => {

			// The analytic body has no mask, but a procedural *pattern* still applies:
			// its surface coordinate is (angle around the tube, distance along it).
			// Modes below 4 are billboard silhouettes (a soft disc, a flame, a puff)
			// and describe a sprite's outline, which a closed body already has.

			If( uProcedural.greaterThanEqual( 4 ), () => {

				shape.assign( proceduralShape( vec2( vRing, vAlong ).sub( .5 ), vec2( vRing, vAlong ), n, uTime, fres, uMaskScale, dims, uProcedural ) );

			} );

		} ).Else( () => {

			// noise.distortionPan scrolls the field that drives the distortion.

			const nd = n.toVar();
			const dp = uDistortPan.mul( uTime ).toVar();

			If( dot( dp, dp ).greaterThan( 0. ), () => {

				nd.assign( select( uHasNoise.equal( 1 ), uNoise.sample( vUv.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( dp ) ).r, add( .5, mul( .5, fbm3( vec3( vUv.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ).add( dp ), 3.1 ) ) ) ) ) );

			} );

			const duv = vUv.add( nd.sub( .5 ).mul( uDistort ) ).toVar();
			const mc = cos( uMaskRot ).toVar(), ms = sin( uMaskRot ).toVar();
			const muv = mat2( mc, ms.negate(), ms, mc ).mul( duv.sub( .5 ) ).add( .5 ).mul( uMaskScale ).add( uMaskPan ).toVar();

			If( uHasMask.equal( 1 ), () => {

				const m = uMask.sample( muv ).toVar();
				shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

			} ).Else( () => {

				shape.assign( proceduralShape( vUv.sub( .5 ), vUv, n, uTime, fres, uMaskScale, dims, uProcedural ) );

			} );

		} );

		const key = property( 'float' );

		If( uRampKeyMode.greaterThan( 1.5 ), () => {

			key.assign( clamp( vAlong.mul( 1.08 ).add( n.sub( .5 ).mul( .35 ).mul( smoothstep( .15, .7, vAlong ) ) ), 0., 1. ) );

		} ).ElseIf( uRampKeyMode.greaterThan( 0.5 ), () => {

			key.assign( clamp( uLayerU, 0., 1. ) );

		} ).Else( () => {

			key.assign( clamp( vAlong, 0., 1. ) );

		} );

		If( uHasFresnel.equal( 1 ), () => {

			key.addAssign( safePow( fres, uFresnelPower ).mul( uFresnelStrength ).mul( smoothstep( 0., .4, vAlong ) ) );

		} );

		key.addAssign( uDisplaceShift.mul( vLobe ) );
		key.assign( clamp( key, 0., 1. ) );
		const alpha = uOpacity.toVar();
		const rim = float( 0. ).toVar();

		If( uUseErosion.equal( 1 ), () => {

			// The field stays mostly low frequency so a tail carves into a few long
			// tongues instead of confetti.

			const low = add( .5, mul( .5, snoise( vec3( vUv.x.mul( 1.15 ), vAlong.mul( 1.25 ).sub( uTime.mul( 1.5 ) ), 4.1 ) ) ) ).toVar();
			const field = select( uShell.equal( 1 ), mul( .25, low ).add( mul( .75, n ) ).mul( .9 ).add( .2 ), shape.mul( add( .35, n.mul( .9 ) ) ) ).toVar();
			const th = max( 0., curveC( vAlong ).sub( vLobe.mul( uProtect ) ).add( fres.mul( uRimBias ).mul( .5 ) ) ).toVar();
			const er = smoothstep( th, th.add( uErodeSoft ), field ).toVar();

			If( er.lessThan( .01 ), () => {

				Discard();

			} );

			rim.assign( smoothstep( th.sub( uEdgeW ), th.add( uErodeSoft.mul( .5 ) ), field ).sub( er ) );
			alpha.mulAssign( er );

		} );

		// shape is 1 on an analytic shell with procedural "none", so this is a no-op
		// there and the procedural pattern applies everywhere else.

		alpha.mulAssign( shape );
		const col = rampColor( key ).toVar();

		If( uShell.equal( 1 ), () => {

			col.mulAssign( mix( add( 1., n.sub( .5 ).mul( .18 ) ), 1., smoothstep( .08, .45, vAlong ) ) );
			alpha.mulAssign( sub( 1., smoothstep( .5, 1., vAlong ).mul( .5 ) ) );

		} );

		col.addAssign( uEdgeCol.mul( uEdgeI ).mul( rim ).mul( sub( 1., smoothstep( .5, .95, vAlong ) ) ) );

		If( alpha.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, alpha ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( alpha ), alpha ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
export const surfaceFragmentBindings = {"uTime":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uErodeSoft":{"type":"float","kind":"uniform"},"uEdgeW":{"type":"float","kind":"uniform"},"uEdgeI":{"type":"float","kind":"uniform"},"uProtect":{"type":"float","kind":"uniform"},"uRimBias":{"type":"float","kind":"uniform"},"uDisplaceShift":{"type":"float","kind":"uniform"},"uFresnelPower":{"type":"float","kind":"uniform"},"uFresnelStrength":{"type":"float","kind":"uniform"},"uDistort":{"type":"float","kind":"uniform"},"uRampKeyMode":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uMaskRot":{"type":"float","kind":"uniform"},"uRadius":{"type":"float","kind":"uniform"},"uLength":{"type":"float","kind":"uniform"},"uThickness":{"type":"float","kind":"uniform"},"uShell":{"type":"int","kind":"uniform"},"uHasMask":{"type":"int","kind":"uniform"},"uHasNoise":{"type":"int","kind":"uniform"},"uUseErosion":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uProcedural":{"type":"int","kind":"uniform"},"uHasFresnel":{"type":"int","kind":"uniform"},"uBolt":{"type":"int","kind":"uniform"},"uNoiseScale":{"type":"vec2","kind":"uniform"},"uNoisePan":{"type":"vec2","kind":"uniform"},"uMaskScale":{"type":"vec2","kind":"uniform"},"uMaskPan":{"type":"vec2","kind":"uniform"},"uDistortPan":{"type":"vec2","kind":"uniform"},"uEdgeCol":{"type":"vec3","kind":"uniform"},"uCam":{"type":"vec3","kind":"uniform"},"uMask":{"type":"sampler2D","kind":"uniform"},"uNoise":{"type":"sampler2D","kind":"uniform"},"vN":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vLobe":{"type":"float","kind":"varying"},"vRing":{"type":"float","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"},"uCurveC":{"type":"vec2","size":8,"kind":"uniform"},"uCurveCN":{"type":"int","kind":"uniform"},"uCurveCEase":{"type":"float","kind":"uniform"}};
