// Generated by scripts/webgpu/port-shaders.mts. Static TSL; no runtime GLSL or eval.
import * as TSL from "three/tsl";

// Built once, so every material of every kind names them identically.
const { Break, Continue, Discard, Fn, If, Loop, abs, acos, add, asin, atan, cameraPosition, cameraProjectionMatrix: projectionMatrix, cameraViewMatrix: viewMatrix, clamp, cos, cross, distance, div, dot, exp, float, floor, fract, frontFacing: gl_FrontFacing, fwidth, int, length, log, mat2, mat3, max, min, mix, mod, modelViewMatrix, modelWorldMatrix: modelMatrix, mul, normalLocal: normal, normalize, positionLocal: position, pow, property, screenCoordinate, select, shadow, sign, sin, smoothstep, sqrt, step, sub, tan, vec2, vec3, vec4 } = TSL;

const mod289v3 = /*@__PURE__*/ Fn( ( [ x ] ) => {

	return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

}, { x: 'vec3', return: 'vec3' } );

const mod289v4 = /*@__PURE__*/ Fn( ( [ x ] ) => {

	return x.sub( floor( x.mul( 1. / 289. ) ).mul( 289. ) );

}, { x: 'vec4', return: 'vec4' } );

const permute289 = /*@__PURE__*/ Fn( ( [ x ] ) => {

	return mod289v4( x.mul( 34. ).add( 1. ).mul( x ) );

}, { x: 'vec4', return: 'vec4' } );

const taylorInvSqrtV = /*@__PURE__*/ Fn( ( [ r ] ) => {

	return sub( 1.79284291400159, mul( 0.85373472095314, r ) );

}, { r: 'vec4', return: 'vec4' } );

const snoise = /*@__PURE__*/ Fn( ( [ v ] ) => {

	const C = vec2( 1. / 6., 1. / 3. ).toVar();
	const D = vec4( 0., .5, 1., 2. ).toVar();
	const i = floor( v.add( dot( v, C.yyy ) ) ).toVar();
	const x0 = v.sub( i ).add( dot( i, C.xxx ) ).toVar();
	const g = step( x0.yzx, x0.xyz ).toVar();
	const l = sub( 1., g ).toVar();
	const i1 = min( g.xyz, l.zxy ).toVar();
	const i2 = max( g.xyz, l.zxy ).toVar();
	const x1 = x0.sub( i1 ).add( C.xxx ).toVar();
	const x2 = x0.sub( i2 ).add( C.yyy ).toVar();
	const x3 = x0.sub( D.yyy ).toVar();
	i.assign( mod289v3( i ) );
	const p = permute289( permute289( permute289( i.z.add( vec4( 0., i1.z, i2.z, 1. ) ) ).add( i.y ).add( vec4( 0., i1.y, i2.y, 1. ) ) ).add( i.x ).add( vec4( 0., i1.x, i2.x, 1. ) ) ).toVar();
	const n_ = float( .142857142857 ).toVar();
	const ns = n_.mul( D.wyz ).sub( D.xzx ).toVar();
	const j = p.sub( mul( 49., floor( p.mul( ns.z ).mul( ns.z ) ) ) ).toVar();
	const x_ = floor( j.mul( ns.z ) ).toVar();
	const y_ = floor( j.sub( mul( 7., x_ ) ) ).toVar();
	const x = x_.mul( ns.x ).add( ns.yyyy ).toVar();
	const y = y_.mul( ns.x ).add( ns.yyyy ).toVar();
	const h = sub( 1., abs( x ) ).sub( abs( y ) ).toVar();
	const b0 = vec4( x.xy, y.xy ).toVar();
	const b1 = vec4( x.zw, y.zw ).toVar();
	const s0 = floor( b0 ).mul( 2. ).add( 1. ).toVar();
	const s1 = floor( b1 ).mul( 2. ).add( 1. ).toVar();
	const sh = step( h, vec4( 0. ) ).negate().toVar();
	const a0 = b0.xzyw.add( s0.xzyw.mul( sh.xxyy ) ).toVar();
	const a1 = b1.xzyw.add( s1.xzyw.mul( sh.zzww ) ).toVar();
	const p0 = vec3( a0.xy, h.x ).toVar();
	const p1 = vec3( a0.zw, h.y ).toVar();
	const p2 = vec3( a1.xy, h.z ).toVar();
	const p3 = vec3( a1.zw, h.w ).toVar();
	const norm = taylorInvSqrtV( vec4( dot( p0, p0 ), dot( p1, p1 ), dot( p2, p2 ), dot( p3, p3 ) ) ).toVar();
	p0.mulAssign( norm.x );
	p1.mulAssign( norm.y );
	p2.mulAssign( norm.z );
	p3.mulAssign( norm.w );
	const m = max( sub( .6, vec4( dot( x0, x0 ), dot( x1, x1 ), dot( x2, x2 ), dot( x3, x3 ) ) ), 0. ).toVar();
	m.assign( m.mul( m ) );

	return mul( 42., dot( m.mul( m ), vec4( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 ), dot( p3, x3 ) ) ) );

}, { v: 'vec3', return: 'float' } );

const fbm3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	return mul( .5, snoise( p ) ).add( mul( .25, snoise( p.mul( 2.02 ).add( 7. ) ) ) ).add( mul( .125, snoise( p.mul( 4.05 ).add( 3. ) ) ) ).add( mul( .0625, snoise( p.mul( 8.1 ).add( 1. ) ) ) );

}, { p: 'vec3', return: 'float' } );

const curl3 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const e = float( .1 ).toVar();
	const dx = vec3( e, 0., 0. ).toVar(), dy = vec3( 0., e, 0. ).toVar(), dz = vec3( 0., 0., e ).toVar();
	const dady = snoise( p.add( dy ) ).sub( snoise( p.sub( dy ) ) ).toVar(), dadz = snoise( p.add( dz ) ).sub( snoise( p.sub( dz ) ) ).toVar();
	const dbdx = snoise( p.add( dx ).add( 31.4 ) ).sub( snoise( p.sub( dx ).add( 31.4 ) ) ).toVar(), dbdz = snoise( p.add( dz ).add( 31.4 ) ).sub( snoise( p.sub( dz ).add( 31.4 ) ) ).toVar();
	const dcdx = snoise( p.add( dx ).sub( 17.2 ) ).sub( snoise( p.sub( dx ).sub( 17.2 ) ) ).toVar(), dcdy = snoise( p.add( dy ).sub( 17.2 ) ).sub( snoise( p.sub( dy ).sub( 17.2 ) ) ).toVar();

	return vec3( dcdy.sub( dbdz ), dadz.sub( dcdx ), dbdx.sub( dady ) ).div( mul( 2., e ) );

}, { p: 'vec3', return: 'vec3' } );

const safeDir = /*@__PURE__*/ Fn( ( [ v, fallback ] ) => {

	const l = length( v ).toVar();

	return select( l.greaterThan( 1e-5 ), v.div( l ), fallback );

}, { v: 'vec3', fallback: 'vec3', return: 'vec3' } );

const safePow = /*@__PURE__*/ Fn( ( [ base, e ] ) => {

	return pow( max( base, 1e-5 ), e );

}, { base: 'float', e: 'float', return: 'float' } );

const orthoOf = /*@__PURE__*/ Fn( ( [ a ] ) => {

	return safeDir( select( abs( a.y ).lessThan( .9 ), cross( a, vec3( 0, 1, 0 ) ), cross( a, vec3( 1, 0, 0 ) ) ), vec3( 1, 0, 0 ) );

}, { a: 'vec3', return: 'vec3' } );

const stripHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const slvHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const procHash21 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const q = fract( vec3( p.xyx ).mul( 0.1031 ) ).toVar();
	q.addAssign( dot( q, q.yzx.add( 33.33 ) ) );

	return fract( q.x.add( q.y ).mul( q.z ) );

}, { p: 'vec2', return: 'float' } );

const procNoise = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const i = floor( p ).toVar(), f = fract( p ).toVar();
	const a = procHash21( i ).toVar(), b = procHash21( i.add( vec2( 1., 0. ) ) ).toVar();
	const c = procHash21( i.add( vec2( 0., 1. ) ) ).toVar(), d = procHash21( i.add( vec2( 1., 1. ) ) ).toVar();
	const u = f.mul( f ).mul( sub( 3., mul( 2., f ) ) ).toVar();

	return mix( mix( a, b, u.x ), mix( c, d, u.x ), u.y );

}, { p: 'vec2', return: 'float' } );

const pointedShape = /*@__PURE__*/ Fn( ( [ p, arms, outerRadius, innerRadius ] ) => {

	const angle = atan( p.y, p.x ).toVar(), halfAngle = div( 3.14159265, arms ).toVar();
	const q = abs( mod( angle.sub( 1.570796 ).add( halfAngle ), halfAngle.mul( 2. ) ).sub( halfAngle ) ).toVar();
	const outer = vec2( outerRadius, 0. ).toVar(), inner = innerRadius.mul( vec2( cos( halfAngle ), sin( halfAngle ) ) ).toVar();
	const ray = vec2( cos( q ), sin( q ) ).toVar(), edge = inner.sub( outer ).toVar();
	const boundary = outer.x.mul( inner.y ).div( ray.x.mul( edge.y ).sub( ray.y.mul( edge.x ) ) ).toVar();

	return sub( 1., smoothstep( boundary.sub( .008 ), boundary.add( .008 ), length( p ) ) );

}, { p: 'vec2', arms: 'float', outerRadius: 'float', innerRadius: 'float', return: 'float' } );

const hexCells = /*@__PURE__*/ Fn( ( [ uv, cell ] ) => {

	const q = uv.mul( vec2( 24., 12. ) ).mul( max( cell, vec2( .02 ) ) ).toVar();
	const spacing = vec2( 1.73205, 3. ).toVar();
	const h1 = mod( q, spacing ).sub( spacing.mul( .5 ) ).toVar(), h2 = mod( q.sub( spacing.mul( .5 ) ), spacing ).sub( spacing.mul( .5 ) ).toVar();
	const h = select( dot( h1, h1 ).lessThan( dot( h2, h2 ) ), h1, h2 ).toVar();

	// Cell planes of the triangular lattice: neighbours at (sqrt(3),0) and (sqrt(3)/2,1.5).

	const edge = abs( max( abs( h.x ), abs( h.x ).mul( .5 ).add( abs( h.y ).mul( .866025 ) ) ).sub( .866025 ) ).toVar();

	return sub( 1., smoothstep( .035, .10, edge ) );

}, { uv: 'vec2', cell: 'vec2', return: 'float' } );

const stripeHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const symC = /*@__PURE__*/ Fn( ( [ p, r ] ) => {

	return length( p ).sub( r );

}, { p: 'vec2', r: 'float', return: 'float' } );

const symE = /*@__PURE__*/ Fn( ( [ p, r ] ) => {

	return length( p.div( r ) ).sub( 1. ).mul( min( r.x, r.y ) );

}, { p: 'vec2', r: 'vec2', return: 'float' } );

const symSeg = /*@__PURE__*/ Fn( ( [ p, a, b ] ) => {

	const pa = p.sub( a ).toVar(), ba = b.sub( a ).toVar();
	const h = clamp( dot( pa, ba ).div( dot( ba, ba ) ), 0., 1. ).toVar();

	return length( pa.sub( ba.mul( h ) ) );

}, { p: 'vec2', a: 'vec2', b: 'vec2', return: 'float' } );

const symStar = /*@__PURE__*/ Fn( ( [ p_arg, r, rf, points ] ) => {

	const p = p_arg.toVar();
	const m = max( points, 3. ).toVar();
	const an = div( 3.141592653589793, m ).toVar(), en = div( 3.141592653589793, mix( 3., m, clamp( rf, 0., 1. ) ) ).toVar();
	const acs = vec2( cos( an ), sin( an ) ).toVar(), ecs = vec2( cos( en ), sin( en ) ).toVar();
	const bn = mod( atan( p.x, p.y ), mul( 2., an ) ).sub( an ).toVar();
	p.assign( length( p ).mul( vec2( cos( bn ), abs( sin( bn ) ) ) ) );
	p.subAssign( r.mul( acs ) );
	p.addAssign( ecs.mul( clamp( dot( p, ecs ).negate(), 0., r.mul( acs.y ).div( ecs.y ) ) ) );

	return length( p ).mul( sign( p.x ) );

}, { p_arg: 'vec2', r: 'float', rf: 'float', points: 'float', return: 'float' } );

const symHeart = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.x.assign( abs( p.x ) );

	If( p.y.add( p.x ).greaterThan( 1. ), () => {

		return sqrt( dot( p.sub( vec2( .25, .75 ) ), p.sub( vec2( .25, .75 ) ) ) ).sub( sqrt( 2. ).div( 4. ) );

	} );

	const a = p.sub( vec2( 0., 1. ) ).toVar(), b = p.sub( mul( .5, max( p.x.add( p.y ), 0. ) ) ).toVar();

	return sqrt( min( dot( a, a ), dot( b, b ) ) ).mul( sign( p.x.sub( p.y ) ) );

}, { p_arg: 'vec2', return: 'float' } );

const sdRoundRect = /*@__PURE__*/ Fn( ( [ p, h, r ] ) => {

	const d = abs( p ).sub( h ).add( r ).toVar();

	return length( max( d, 0. ) ).add( min( max( d.x, d.y ), 0. ) ).sub( r );

}, { p: 'vec2', h: 'vec2', r: 'float', return: 'float' } );

const framePerimeter = /*@__PURE__*/ Fn( ( [ p, h ] ) => {

	const a = vec2( abs( p.x ), p.y ).toVar();
	const c = clamp( a, vec2( 0., h.y.negate() ), h ).toVar();
	const db = c.y.add( h.y ).toVar(), dt = h.y.sub( c.y ).toVar(), dr = h.x.sub( c.x ).toVar();
	const m = min( dr, min( db, dt ) ).toVar();
	const sArc = select( m.equal( db ), c.x, select( m.equal( dr ), h.x.add( c.y ).add( h.y ), h.x.add( mul( 2., h.y ) ).add( h.x ).sub( c.x ) ) ).toVar();

	return sArc.div( max( mul( 2., h.x ).add( mul( 2., h.y ) ), 1e-4 ) );

}, { p: 'vec2', h: 'vec2', return: 'float' } );

const flowNoise = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const i = floor( p ).toVar(), f = fract( p ).toVar();
	f.assign( f.mul( f ).mul( sub( 3., mul( 2., f ) ) ) );
	const a = procHash21( i ).toVar(), b = procHash21( i.add( vec2( 1., 0. ) ) ).toVar();
	const c = procHash21( i.add( vec2( 0., 1. ) ) ).toVar(), d = procHash21( i.add( vec2( 1., 1. ) ) ).toVar();

	return mix( mix( a, b, f.x ), mix( c, d, f.x ), f.y );

}, { p: 'vec2', return: 'float' } );

const swirlFbm2 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	return flowNoise( p ).add( mul( .5, flowNoise( p.mul( 2.07 ).add( 19.3 ) ) ) ).div( 1.5 );

}, { p: 'vec2', return: 'float' } );

const swirlFbm3 = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	const acc = float( 0. ).toVar(), a = float( .5 ).toVar();

	Loop( 3, () => {

		acc.addAssign( a.mul( flowNoise( p ) ) );
		p.assign( mat2( .8, .6, - .6, .8 ).mul( p ).mul( 2.03 ).add( vec2( 11.3, 7.1 ) ) );
		a.mulAssign( .5 );

	} );

	return acc.div( .875 );

}, { p_arg: 'vec2', return: 'float' } );

const latHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const safeDirLocal = /*@__PURE__*/ Fn( ( [ v ] ) => {

	const l = length( v ).toVar();

	return select( l.greaterThan( 1e-5 ), v.div( l ), vec3( 0., 1., 0. ) );

}, { v: 'vec3', return: 'vec3' } );

const ribbonHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const burstHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const arcHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const stHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const vorHash11 = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

const vorHash21 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	return vorHash11( dot( p, vec2( 127.1, 311.7 ) ) );

}, { p: 'vec2', return: 'float' } );

const vorNoise = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const i = floor( p ).toVar(), f = fract( p ).toVar();
	const a = vorHash21( i ).toVar(), b = vorHash21( i.add( vec2( 1., 0. ) ) ).toVar();
	const c = vorHash21( i.add( vec2( 0., 1. ) ) ).toVar(), d = vorHash21( i.add( vec2( 1., 1. ) ) ).toVar();
	const u = f.mul( f ).mul( sub( 3., mul( 2., f ) ) ).toVar();

	return mix( mix( a, b, u.x ), mix( c, d, u.x ), u.y );

}, { p: 'vec2', return: 'float' } );

const vorFbm = /*@__PURE__*/ Fn( ( [ p ] ) => {

	return mul( .55, vorNoise( p ) ).add( mul( .28, vorNoise( p.mul( 2.07 ).add( 11.3 ) ) ) ).add( mul( .17, vorNoise( p.mul( 4.11 ).add( 5.7 ) ) ) );

}, { p: 'vec2', return: 'float' } );

const voronoi2 = /*@__PURE__*/ Fn( ( [ p ] ) => {

	const ip = floor( p ).toVar(), fp = fract( p ).toVar();
	const d1 = float( 8. ).toVar(), d2 = float( 8. ).toVar(), id = float( 0. ).toVar();

	Loop( { start: int( - 1 ), end: 1, name: 'y', condition: '<=' }, ( { y } ) => {

		Loop( { start: int( - 1 ), end: 1, name: 'x', condition: '<=' }, ( { x } ) => {

			const g = vec2( float( x ), float( y ) ).toVar();
			const o = vec2( vorHash21( ip.add( g ) ), vorHash21( ip.add( g ).add( vec2( 37.7, 11.3 ) ) ) ).toVar();
			const r = g.add( o ).sub( fp ).toVar();
			const d = dot( r, r ).toVar();

			If( d.lessThan( d1 ), () => {

				d2.assign( d1 );
				d1.assign( d );
				id.assign( vorHash21( ip.add( g ).add( vec2( 7.1, 3.9 ) ) ) );

			} ).ElseIf( d.lessThan( d2 ), () => {

				d2.assign( d );

			} );

		} );

	} );

	return vec2( id, sqrt( d2 ).sub( sqrt( d1 ) ) );

}, { p: 'vec2', return: 'vec2' } );

const lickHash = /*@__PURE__*/ Fn( ( [ p_arg ] ) => {

	const p = p_arg.toVar();
	p.assign( fract( p.mul( .1031 ) ) );
	p.mulAssign( p.add( 33.33 ) );
	p.mulAssign( p.add( p ) );

	return fract( p );

}, { p_arg: 'float', return: 'float' } );

// Three.js Transpiler r186

export function particleVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const uTime = bindings.uTime;
const uStretch = bindings.uStretch;
const uAtlasTiles = bindings.uAtlasTiles;
const uMotionBlur = bindings.uMotionBlur;
const uFlipFps = bindings.uFlipFps;
const uSpan = bindings.uSpan;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uRenderMode = bindings.uRenderMode;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uFlipMode = bindings.uFlipMode;
const uAnchorHead = bindings.uAnchorHead;
const uSize = bindings.uSize;
const uRot = bindings.uRot;
const uRotInit = bindings.uRotInit;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, mat2, smoothstep, mod } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime, aEvent, aIndex ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, age, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( age ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );

			// --- billboard ------------------------------------------------------------

			const size = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).toVar();
			const rot = mix( uRotInit.x, uRotInit.y, aExtra2.y ).add( mix( uRot.x, uRot.y, aExtra2.z ).mul( age ) ).toVar();

			// pathAligned reuses the velocity-stretch machinery with the PATH's heading
			// instead of the particle's velocity: a path-anchored dash holds still, so it
			// has no velocity of its own to align to.

			const heading = select( uRenderMode.equal( 4 ).and( uShapeType.equal( 8 ) ), pathTangentE( aIndex ), vel ).toVar();
			const mv = property( 'vec4' );

			If( uRenderMode.equal( 2 ).or( uRenderMode.equal( 3 ) ), () => {

				// World-oriented quads: horizontal lies in XZ, vertical in XY.

				const c = cos( rot ).toVar(), s = sin( rot ).toVar();
				const q = mat2( c, s.negate(), s, c ).mul( position.xy ).mul( size ).toVar();
				const world = select( uRenderMode.equal( 2 ), vec3( q.x, 0., q.y ), vec3( q.x, q.y, 0. ) ).toVar();
				mv.assign( modelViewMatrix.mul( vec4( pos.add( world ), 1. ) ) );
				rot.assign( 0. );

			} ).Else( () => {

				mv.assign( modelViewMatrix.mul( vec4( pos, 1. ) ) );
				const sv3 = modelViewMatrix.mul( vec4( heading, 0. ) ).xyz.toVar();
				const sv = vec2( sv3.x, sv3.y ).toVar();
				const svl = length( sv ).toVar();
				const along = vec2( 0., 1. ).toVar(), across = vec2( 1., 0. ).toVar();
				const stretch = float( 1. ).toVar();

				If( uRenderMode.equal( 1 ).or( uRenderMode.equal( 4 ) ).and( svl.greaterThan( 1e-4 ) ), () => {

					along.assign( sv.div( svl ) );

					// Winding: the across axis must be the clockwise perpendicular or the
					// quad is mirrored and back-face culled away.

					across.assign( vec2( along.y, along.x.negate() ) );

					// post.motionBlur is a multiplier on the velocity stretch, nothing else.
					// A pathAligned quad is stretched by uStretch alone: its heading is a
					// unit tangent, so there is no speed to scale by.

					rot.assign( 0. );
					stretch.assign( select( uRenderMode.equal( 4 ), add( 1., uStretch ), add( 1., uStretch.mul( add( 1., mul( 2., uMotionBlur ) ) ).mul( svl ) ) ) );

				} );

				// render.anchor "head": the LEADING point of the stretched card sits on the
				// instance and the streak trails behind it, which is what a speed-line cap
				// needs — the tip is the projectile, the streak is where it has been.

				const anchorShift = select( uAnchorHead.equal( 1 ), TSL.float(-.5).mul( size ).mul( stretch ), 0. ).toVar();
				const off = across.mul( position.x ).mul( size ).add( along.mul( position.y.mul( size ).mul( stretch ).add( anchorShift ) ) ).toVar();

				If( uMotionBlur.greaterThan( 0. ).and( svl.greaterThan( 1e-4 ) ), () => {

					// Non-stretched modes still elongate along screen velocity, never rotate.

					const vdir = sv.div( svl ).toVar();
					off.addAssign( vdir.mul( dot( off, vdir ) ).mul( uMotionBlur ).mul( min( svl, 8. ) ).mul( .25 ) );

				} );

				mv.xy.addAssign( off );

			} );

			vRot.assign( rot );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			// render.twinkle: a phase hashed off the instance seed, so no two particles
			// blink together. depth 0 leaves the alpha untouched.

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

			const cols = float( max( uAtlasCols, 1 ) ).toVar(), rows = float( max( uAtlasRows, 1 ) ).toVar();
			const tiles = max( uAtlasTiles, 1. ).toVar();

			// uFlipMode: 0 = random atlas tile, 1 = flipbook over life, 2 = flipbook at fps.

			const ti = floor( aExtra.z.mul( tiles ).mul( .9999 ) ).toVar();

			If( uFlipMode.equal( 1 ), () => {

				ti.assign( floor( clamp( u, 0., .9999 ).mul( tiles ) ) );

			} ).ElseIf( uFlipMode.equal( 2 ), () => {

				ti.assign( floor( mod( max( age, 0. ).mul( uFlipFps ), tiles ) ) );

			} );

			vTile.assign( vec2( mod( ti, cols ).div( cols ), floor( ti.div( cols ) ).div( rows ) ) );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function subParticleVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aPSeed = bindings.aPSeed;
const aPExtra = bindings.aPExtra;
const aPExtra2 = bindings.aPExtra2;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const uTime = bindings.uTime;
const uStretch = bindings.uStretch;
const uAtlasTiles = bindings.uAtlasTiles;
const uMotionBlur = bindings.uMotionBlur;
const uFlipFps = bindings.uFlipFps;
const uSpan = bindings.uSpan;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uRenderMode = bindings.uRenderMode;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uFlipMode = bindings.uFlipMode;
const uAnchorHead = bindings.uAnchorHead;
const uSize = bindings.uSize;
const uRot = bindings.uRot;
const uRotInit = bindings.uRotInit;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const uParentPeriod = bindings.uParentPeriod;
const uParentSpawnWindow = bindings.uParentSpawnWindow;
const uParentSpawnDuration = bindings.uParentSpawnDuration;
const uParentShapeLength = bindings.uParentShapeLength;
const uParentShapeRadius = bindings.uParentShapeRadius;
const uParentShapeInner = bindings.uParentShapeInner;
const uParentShapeAngle = bindings.uParentShapeAngle;
const uParentDrag = bindings.uParentDrag;
const uParentCurl = bindings.uParentCurl;
const uParentCurlFreq = bindings.uParentCurlFreq;
const uParentCurlSpeed = bindings.uParentCurlSpeed;
const uParentFloorY = bindings.uParentFloorY;
const uParentFloorSoft = bindings.uParentFloorSoft;
const uParentAngle = bindings.uParentAngle;
const uParentPlanarDrag = bindings.uParentPlanarDrag;
const uParentVortexW = bindings.uParentVortexW;
const uParentVortexFalloff = bindings.uParentVortexFalloff;
const uParentSpawnMode = bindings.uParentSpawnMode;
const uParentShapeType = bindings.uParentShapeType;
const uParentVelMode = bindings.uParentVelMode;
const uParentHasFloor = bindings.uParentHasFloor;
const uParentSurfaceOnly = bindings.uParentSurfaceOnly;
const uParentSpeedN = bindings.uParentSpeedN;
const uParentBurstN = bindings.uParentBurstN;
const uParentAxis = bindings.uParentAxis;
const uParentDir = bindings.uParentDir;
const uParentGravity = bindings.uParentGravity;
const uParentWind = bindings.uParentWind;
const uParentBias = bindings.uParentBias;
const uParentShapeSize = bindings.uParentShapeSize;
const uParentVortexAxis = bindings.uParentVortexAxis;
const uParentLife = bindings.uParentLife;
const uParentSpeed = bindings.uParentSpeed;
const uParentSpeedKey = bindings.uParentSpeedKey;
const uParentBurstT = bindings.uParentBurstT;
const uParentBurstC = bindings.uParentBurstC;
const uParentInterior = bindings.uParentInterior;
const uParentAngleJitter = bindings.uParentAngleJitter;
const uParentAngleBias = bindings.uParentAngleBias;
const uParentTimeShift = bindings.uParentTimeShift;
const uInherit = bindings.uInherit;
const uPathT0 = bindings.uPathT0;
const uPathDt = bindings.uPathDt;
const uSubMode = bindings.uSubMode;
const uSubOffset = bindings.uSubOffset;
const uParentPath = bindings.uParentPath;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, mat2, smoothstep, mod } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	const parentLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uParentLife.x, uParentLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const parentBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uParentSpawnWindow ).toVar();

		If( uParentSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uParentPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uParentPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uParentSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uParentSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uParentBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uParentBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uParentBurstC.element( i ) ), () => {

					t0.assign( uParentBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uParentSpawnWindow ) ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const parentOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uParentSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uParentBias, 0., 1. ) ) );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uParentShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uParentShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 3 ), () => {

			const h = e.w.mul( uParentShapeLength ).toVar();
			const rr = uParentShapeRadius.add( h.mul( tan( min( uParentShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uParentShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 5 ), () => {

			const rr = mix( uParentShapeInner, uParentShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uParentShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uParentShapeSize ).mul( .5 ) );

		} ).ElseIf( uParentShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uParentShapeRadius, uParentShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uParentInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uParentShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uParentShapeInner ) ) );

		} ).ElseIf( uParentShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uParentAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uParentAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uParentShapeInner, uParentShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uParentShapeLength.mul( e.w ) ).add( sph.mul( uParentShapeRadius ) ) );

		} );

		return portResult;

	} );

	const parentDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uParentDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		If( uParentVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uParentVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uParentVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uParentAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const parentSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uParentSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uParentSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uParentSpeedN ), () => {

					Break();

				} );

				const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uParentSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const parentSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uParentSpeedKey.element(0).x ).mul( uParentSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uParentSpeedN ), () => {

				Break();

			} );

			const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
			const va = uParentSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uParentSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uParentSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const parentTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = parentOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = parentDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uParentVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uParentAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uParentSpeed.y.mul( safePow( r.div( max( uParentShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uParentSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uParentHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uParentFloorY ).toVar();
				pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uParentSpeed.x, uParentSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uParentSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( parentSpeedI( u ) ) );
				scale.assign( parentSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uParentDrag.lessThan( .001 ), a, sub( 1., exp( uParentDrag.negate().mul( a ) ) ).div( uParentDrag ) ) );
				scale.assign( exp( uParentDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uParentGravity ).mul( a ).mul( a ) ).add( uParentWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uParentGravity.mul( a ) ).add( uParentWind ) );

			If( uParentPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uParentPlanarDrag.negate().mul( a ) ) ).div( uParentPlanarDrag ).toVar();
				const sp = exp( uParentPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uParentWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uParentWind ).xz );

			} );

			If( abs( uParentVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uParentVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uParentVortexW.div( add( 1., uParentVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uParentCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uParentCurlFreq ).add( vec3( 0., t.negate().mul( uParentCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uParentCurl ).mul( 1.0 ).mul( a ) );

			} );

			If( uParentHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uParentFloorY ).toVar();
				pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	/** Parent layer transform delta at absolute time t, from an 8-sample table. */

	const parentPathAt = /*@__PURE__*/ Fn( ( [ t ] ) => {

		const x = clamp( t.sub( uPathT0 ).div( max( uPathDt, 1e-4 ) ), 0., float( 8 - 1 ) ).toVar();
		const i0 = floor( x ).toVar(), f = x.sub( i0 ).toVar();
		const a = vec3( 0. ).toVar(), b = vec3( 0. ).toVar();

		Loop( 8, ( { i } ) => {

			const fi = float( i ).toVar();
			a.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( i0 ) ), .5 ) ) );
			b.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( min( i0.add( 1. ), float( 8 - 1 ) ) ) ), .5 ) ) );

		} );

		return mix( a, b, f );

	} );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();

		// Sub-emitter: the child is born where and when the parent was at
		// parentBirth + offset, in the parent's own closed-form trajectory.

		const pLife = parentLife( aPSeed ).toVar();
		const r = fract( aSeed.x.mul( 13.37 ).add( aSeed.w.mul( 7.77 ) ).add( 0.113 ) ).toVar();
		const off = select( uSubMode.equal( 1 ), pLife, select( uSubMode.equal( 2 ), mix( uSubOffset.x, uSubOffset.y, r ), mix( uSubOffset.x, uSubOffset.y, fract( aSeed.x.mul( 3.0 ) ) ) ) ).toVar();
		off.assign( clamp( off, 0., pLife ) );
		const pNow = uTime.add( uParentTimeShift ).toVar();
		const pBirth = parentBirth( aPSeed, pNow.sub( off ), aEvent, aIndex ).toVar();
		const ppos = property( 'vec3' ), pvel = property( 'vec3' );
		parentTraj( aPSeed, aPExtra, aPExtra2, aSrcPos, aSrcDir, aEvent, aIndex, off, pLife, pBirth.add( off ), ppos, pvel );
		birth.assign( pBirth.add( off ).sub( uParentTimeShift ) );
		subOrigin.assign( ppos.add( parentPathAt( pBirth.add( off ) ) ) );
		subVel.assign( pvel.mul( uInherit ) );

		If( pBirth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, age, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( age ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );

			// --- billboard ------------------------------------------------------------

			const size = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).toVar();
			const rot = mix( uRotInit.x, uRotInit.y, aExtra2.y ).add( mix( uRot.x, uRot.y, aExtra2.z ).mul( age ) ).toVar();

			// pathAligned reuses the velocity-stretch machinery with the PATH's heading
			// instead of the particle's velocity: a path-anchored dash holds still, so it
			// has no velocity of its own to align to.

			const heading = select( uRenderMode.equal( 4 ).and( uShapeType.equal( 8 ) ), pathTangentE( aIndex ), vel ).toVar();
			const mv = property( 'vec4' );

			If( uRenderMode.equal( 2 ).or( uRenderMode.equal( 3 ) ), () => {

				// World-oriented quads: horizontal lies in XZ, vertical in XY.

				const c = cos( rot ).toVar(), s = sin( rot ).toVar();
				const q = mat2( c, s.negate(), s, c ).mul( position.xy ).mul( size ).toVar();
				const world = select( uRenderMode.equal( 2 ), vec3( q.x, 0., q.y ), vec3( q.x, q.y, 0. ) ).toVar();
				mv.assign( modelViewMatrix.mul( vec4( pos.add( world ), 1. ) ) );
				rot.assign( 0. );

			} ).Else( () => {

				mv.assign( modelViewMatrix.mul( vec4( pos, 1. ) ) );
				const sv3 = modelViewMatrix.mul( vec4( heading, 0. ) ).xyz.toVar();
				const sv = vec2( sv3.x, sv3.y ).toVar();
				const svl = length( sv ).toVar();
				const along = vec2( 0., 1. ).toVar(), across = vec2( 1., 0. ).toVar();
				const stretch = float( 1. ).toVar();

				If( uRenderMode.equal( 1 ).or( uRenderMode.equal( 4 ) ).and( svl.greaterThan( 1e-4 ) ), () => {

					along.assign( sv.div( svl ) );

					// Winding: the across axis must be the clockwise perpendicular or the
					// quad is mirrored and back-face culled away.

					across.assign( vec2( along.y, along.x.negate() ) );

					// post.motionBlur is a multiplier on the velocity stretch, nothing else.
					// A pathAligned quad is stretched by uStretch alone: its heading is a
					// unit tangent, so there is no speed to scale by.

					rot.assign( 0. );
					stretch.assign( select( uRenderMode.equal( 4 ), add( 1., uStretch ), add( 1., uStretch.mul( add( 1., mul( 2., uMotionBlur ) ) ).mul( svl ) ) ) );

				} );

				// render.anchor "head": the LEADING point of the stretched card sits on the
				// instance and the streak trails behind it, which is what a speed-line cap
				// needs — the tip is the projectile, the streak is where it has been.

				const anchorShift = select( uAnchorHead.equal( 1 ), TSL.float(-.5).mul( size ).mul( stretch ), 0. ).toVar();
				const off = across.mul( position.x ).mul( size ).add( along.mul( position.y.mul( size ).mul( stretch ).add( anchorShift ) ) ).toVar();

				If( uMotionBlur.greaterThan( 0. ).and( svl.greaterThan( 1e-4 ) ), () => {

					// Non-stretched modes still elongate along screen velocity, never rotate.

					const vdir = sv.div( svl ).toVar();
					off.addAssign( vdir.mul( dot( off, vdir ) ).mul( uMotionBlur ).mul( min( svl, 8. ) ).mul( .25 ) );

				} );

				mv.xy.addAssign( off );

			} );

			vRot.assign( rot );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			// render.twinkle: a phase hashed off the instance seed, so no two particles
			// blink together. depth 0 leaves the alpha untouched.

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

			const cols = float( max( uAtlasCols, 1 ) ).toVar(), rows = float( max( uAtlasRows, 1 ) ).toVar();
			const tiles = max( uAtlasTiles, 1. ).toVar();

			// uFlipMode: 0 = random atlas tile, 1 = flipbook over life, 2 = flipbook at fps.

			const ti = floor( aExtra.z.mul( tiles ).mul( .9999 ) ).toVar();

			If( uFlipMode.equal( 1 ), () => {

				ti.assign( floor( clamp( u, 0., .9999 ).mul( tiles ) ) );

			} ).ElseIf( uFlipMode.equal( 2 ), () => {

				ti.assign( floor( mod( max( age, 0. ).mul( uFlipFps ), tiles ) ) );

			} );

			vTile.assign( vec2( mod( ti, cols ).div( cols ), floor( ti.div( cols ) ).div( rows ) ) );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function trailVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const uTime = bindings.uTime;
const uSegments = bindings.uSegments;
const uSpacing = bindings.uSpacing;
const uSpan = bindings.uSpan;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveG = bindings.uCurveG;
const uCurveGN = bindings.uCurveGN;
const uCurveGEase = bindings.uCurveGEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveG = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveG.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveGN ), () => {

				Break();

			} );

			const a = uCurveG.element( i.sub( 1 ) ).x.toVar(), b = uCurveG.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveGEase ) );
			v.assign( mix( v, uCurveG.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vUv.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime, aEvent, aIndex ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const side = position.x.toVar();
			const k = position.y.toVar();
			const kn = clamp( k.div( max( uSegments.sub( 1. ), 1. ) ), 0., 1. ).toVar();
			vUv.assign( vec2( side.mul( .5 ).add( .5 ), kn ) );
			const ageK = max( age.sub( k.mul( uSpacing ) ), 0. ).toVar();
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, ageK, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( ageK ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();
			const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
			const sv = vec2( sv3.x, sv3.y ).toVar();
			const svl = length( sv ).toVar();
			const across = select( svl.greaterThan( 1e-4 ), vec2( sv.y, sv.x.negate() ).div( svl ), vec2( 1., 0. ) ).toVar();
			const width = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).mul( curveG( kn ) ).mul( .5 ).toVar();
			mv.xy.addAssign( across.mul( side ).mul( width ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function subTrailVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aPSeed = bindings.aPSeed;
const aPExtra = bindings.aPExtra;
const aPExtra2 = bindings.aPExtra2;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const uTime = bindings.uTime;
const uSegments = bindings.uSegments;
const uSpacing = bindings.uSpacing;
const uSpan = bindings.uSpan;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveG = bindings.uCurveG;
const uCurveGN = bindings.uCurveGN;
const uCurveGEase = bindings.uCurveGEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const uParentPeriod = bindings.uParentPeriod;
const uParentSpawnWindow = bindings.uParentSpawnWindow;
const uParentSpawnDuration = bindings.uParentSpawnDuration;
const uParentShapeLength = bindings.uParentShapeLength;
const uParentShapeRadius = bindings.uParentShapeRadius;
const uParentShapeInner = bindings.uParentShapeInner;
const uParentShapeAngle = bindings.uParentShapeAngle;
const uParentDrag = bindings.uParentDrag;
const uParentCurl = bindings.uParentCurl;
const uParentCurlFreq = bindings.uParentCurlFreq;
const uParentCurlSpeed = bindings.uParentCurlSpeed;
const uParentFloorY = bindings.uParentFloorY;
const uParentFloorSoft = bindings.uParentFloorSoft;
const uParentAngle = bindings.uParentAngle;
const uParentPlanarDrag = bindings.uParentPlanarDrag;
const uParentVortexW = bindings.uParentVortexW;
const uParentVortexFalloff = bindings.uParentVortexFalloff;
const uParentSpawnMode = bindings.uParentSpawnMode;
const uParentShapeType = bindings.uParentShapeType;
const uParentVelMode = bindings.uParentVelMode;
const uParentHasFloor = bindings.uParentHasFloor;
const uParentSurfaceOnly = bindings.uParentSurfaceOnly;
const uParentSpeedN = bindings.uParentSpeedN;
const uParentBurstN = bindings.uParentBurstN;
const uParentAxis = bindings.uParentAxis;
const uParentDir = bindings.uParentDir;
const uParentGravity = bindings.uParentGravity;
const uParentWind = bindings.uParentWind;
const uParentBias = bindings.uParentBias;
const uParentShapeSize = bindings.uParentShapeSize;
const uParentVortexAxis = bindings.uParentVortexAxis;
const uParentLife = bindings.uParentLife;
const uParentSpeed = bindings.uParentSpeed;
const uParentSpeedKey = bindings.uParentSpeedKey;
const uParentBurstT = bindings.uParentBurstT;
const uParentBurstC = bindings.uParentBurstC;
const uParentInterior = bindings.uParentInterior;
const uParentAngleJitter = bindings.uParentAngleJitter;
const uParentAngleBias = bindings.uParentAngleBias;
const uParentTimeShift = bindings.uParentTimeShift;
const uInherit = bindings.uInherit;
const uPathT0 = bindings.uPathT0;
const uPathDt = bindings.uPathDt;
const uSubMode = bindings.uSubMode;
const uSubOffset = bindings.uSubOffset;
const uParentPath = bindings.uParentPath;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveG = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveG.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveGN ), () => {

				Break();

			} );

			const a = uCurveG.element( i.sub( 1 ) ).x.toVar(), b = uCurveG.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveGEase ) );
			v.assign( mix( v, uCurveG.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	const parentLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uParentLife.x, uParentLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const parentBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uParentSpawnWindow ).toVar();

		If( uParentSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uParentPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uParentPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uParentSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uParentSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uParentBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uParentBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uParentBurstC.element( i ) ), () => {

					t0.assign( uParentBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uParentSpawnWindow ) ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const parentOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uParentSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uParentBias, 0., 1. ) ) );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uParentShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uParentShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 3 ), () => {

			const h = e.w.mul( uParentShapeLength ).toVar();
			const rr = uParentShapeRadius.add( h.mul( tan( min( uParentShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uParentShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 5 ), () => {

			const rr = mix( uParentShapeInner, uParentShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uParentShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uParentShapeSize ).mul( .5 ) );

		} ).ElseIf( uParentShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uParentShapeRadius, uParentShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uParentInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uParentShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uParentShapeInner ) ) );

		} ).ElseIf( uParentShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uParentAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uParentAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uParentShapeRadius ) );

		} ).ElseIf( uParentShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uParentShapeInner, uParentShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uParentShapeLength.mul( e.w ) ).add( sph.mul( uParentShapeRadius ) ) );

		} );

		return portResult;

	} );

	const parentDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uParentAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uParentDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		If( uParentVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uParentVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uParentVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uParentAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const parentSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uParentSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uParentSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uParentSpeedN ), () => {

					Break();

				} );

				const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uParentSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const parentSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uParentSpeedKey.element(0).x ).mul( uParentSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uParentSpeedN ), () => {

				Break();

			} );

			const a = uParentSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uParentSpeedKey.element( i ).x.toVar();
			const va = uParentSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uParentSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uParentSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const parentTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = parentOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = parentDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uParentVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uParentAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uParentSpeed.y.mul( safePow( r.div( max( uParentShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uParentSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uParentHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uParentFloorY ).toVar();
				pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uParentSpeed.x, uParentSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uParentSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( parentSpeedI( u ) ) );
				scale.assign( parentSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uParentDrag.lessThan( .001 ), a, sub( 1., exp( uParentDrag.negate().mul( a ) ) ).div( uParentDrag ) ) );
				scale.assign( exp( uParentDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uParentGravity ).mul( a ).mul( a ) ).add( uParentWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uParentGravity.mul( a ) ).add( uParentWind ) );

			If( uParentPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uParentPlanarDrag.negate().mul( a ) ) ).div( uParentPlanarDrag ).toVar();
				const sp = exp( uParentPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uParentWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uParentWind ).xz );

			} );

			If( abs( uParentVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uParentVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uParentVortexW.div( add( 1., uParentVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uParentCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uParentCurlFreq ).add( vec3( 0., t.negate().mul( uParentCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uParentCurl ).mul( 1.0 ).mul( a ) );

			} );

			If( uParentHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uParentFloorY ).toVar();
				pos.y.assign( uParentFloorY.add( max( dy, dy.mul( uParentFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );

	/** Parent layer transform delta at absolute time t, from an 8-sample table. */

	const parentPathAt = /*@__PURE__*/ Fn( ( [ t ] ) => {

		const x = clamp( t.sub( uPathT0 ).div( max( uPathDt, 1e-4 ) ), 0., float( 8 - 1 ) ).toVar();
		const i0 = floor( x ).toVar(), f = x.sub( i0 ).toVar();
		const a = vec3( 0. ).toVar(), b = vec3( 0. ).toVar();

		Loop( 8, ( { i } ) => {

			const fi = float( i ).toVar();
			a.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( i0 ) ), .5 ) ) );
			b.addAssign( uParentPath.element( i ).mul( step( abs( fi.sub( min( i0.add( 1. ), float( 8 - 1 ) ) ) ), .5 ) ) );

		} );

		return mix( a, b, f );

	} );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vUv.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aExtra2.xyz );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();

		// Sub-emitter: the child is born where and when the parent was at
		// parentBirth + offset, in the parent's own closed-form trajectory.

		const pLife = parentLife( aPSeed ).toVar();
		const r = fract( aSeed.x.mul( 13.37 ).add( aSeed.w.mul( 7.77 ) ).add( 0.113 ) ).toVar();
		const off = select( uSubMode.equal( 1 ), pLife, select( uSubMode.equal( 2 ), mix( uSubOffset.x, uSubOffset.y, r ), mix( uSubOffset.x, uSubOffset.y, fract( aSeed.x.mul( 3.0 ) ) ) ) ).toVar();
		off.assign( clamp( off, 0., pLife ) );
		const pNow = uTime.add( uParentTimeShift ).toVar();
		const pBirth = parentBirth( aPSeed, pNow.sub( off ), aEvent, aIndex ).toVar();
		const ppos = property( 'vec3' ), pvel = property( 'vec3' );
		parentTraj( aPSeed, aPExtra, aPExtra2, aSrcPos, aSrcDir, aEvent, aIndex, off, pLife, pBirth.add( off ), ppos, pvel );
		birth.assign( pBirth.add( off ).sub( uParentTimeShift ) );
		subOrigin.assign( ppos.add( parentPathAt( pBirth.add( off ) ) ) );
		subVel.assign( pvel.mul( uInherit ) );

		If( pBirth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const side = position.x.toVar();
			const k = position.y.toVar();
			const kn = clamp( k.div( max( uSegments.sub( 1. ), 1. ) ), 0., 1. ).toVar();
			vUv.assign( vec2( side.mul( .5 ).add( .5 ), kn ) );
			const ageK = max( age.sub( k.mul( uSpacing ) ), 0. ).toVar();
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, ageK, life, uTime, pos, vel );
			pos.addAssign( subOrigin.add( subVel.mul( ageK ) ) );
			vel.addAssign( subVel );
			vWp.assign( pos );
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();
			const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
			const sv = vec2( sv3.x, sv3.y ).toVar();
			const svl = length( sv ).toVar();
			const across = select( svl.greaterThan( 1e-4 ), vec2( sv.y, sv.x.negate() ).div( svl ), vec2( 1., 0. ) ).toVar();
			const width = mix( uSize.x, uSize.y, aExtra2.x ).mul( curveA( u ) ).mul( curveG( kn ) ).mul( .5 ).toVar();
			mv.xy.addAssign( across.mul( side ).mul( width ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function stripVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const uTime = bindings.uTime;
const uSpan = bindings.uSpan;
const uStripStep = bindings.uStripStep;
const uStripWave = bindings.uStripWave;
const uPalettes = bindings.uPalettes;
const uStripCount = bindings.uStripCount;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uStripLen = bindings.uStripLen;
const uStripWide = bindings.uStripWide;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const vPalette = bindings.vPalette;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, mod, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );


	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );
		vPalette.assign( 0. );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		vSeed.assign( aExtra2.xyz );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime, aEvent, aIndex ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, age, life, uTime, pos, vel );
			vWp.assign( pos );

			// The flipbook step: everything about the lick's SHAPE is hashed off k, so it
			// holds for 1/stepRate seconds and then jumps to a new one.

			const k = floor( uTime.mul( uStripStep ) ).add( aSeed.x.mul( 97.3 ) ).add( aIndex.mul( 13.7 ) ).toVar();
			const L = mix( uStripLen.x, uStripLen.y, stripHash( k.mul( 1.31 ) ) ).toVar();
			const W = mix( uStripWide.x, uStripWide.y, stripHash( k.mul( 3.77 ).add( 2.1 ) ) ).toVar();
			const wob = stripHash( k.mul( 5.51 ).add( 7.7 ) ).toVar();
			const yc = stripHash( k.mul( 9.13 ).add( 3.3 ) ).mul( uShapeRadius ).toVar();
			const side = select( aSeed.y.greaterThan( .5 ), 1., - 1. ).toVar();
			const s = position.x.toVar(), v = position.y.toVar();

			// The lick tapers at both ends: fat a little way in, sharp at the tip.

			const w = W.mul( .5 ).mul( safePow( s.add( .05 ), .30 ) ).mul( safePow( max( sub( 1., s ), 0. ), .85 ) ).mul( curveA( u ) ).toVar();
			const cy = uStripWave.mul( .16 ).mul( sin( s.mul( 3.1 ).add( wob.mul( 6.2831853 ) ) ) ).mul( s ).toVar();
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();
			const axisV = modelViewMatrix.mul( vec4( safeDir( uAxis, vec3( 0., 1., 0. ) ), 0. ) ).xyz.toVar();
			const along = select( length( axisV.xy ).greaterThan( 1e-5 ), normalize( axisV.xy ), vec2( 1., 0. ) ).toVar();
			const across = vec2( along.y.negate(), along.x ).toVar();
			mv.xy.addAssign( along.mul( s.mul( L ) ).add( across.mul( side.mul( yc.add( cy ) ).add( v.mul( w ) ) ) ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vUv.assign( vec2( s, v.mul( .5 ).add( .5 ) ) );

			// Parity on the INSTANCE INDEX, not on a hash: the draw walks the instances
			// in order, so the odd (light) licks always land over the even (dark) ones.
			// That is why a flatStrip layer wants render.sortMode "none".

			const idx = floor( aIndex.mul( max( uStripCount.sub( 1. ), 1. ) ).add( .5 ) ).toVar();
			vPalette.assign( select( uPalettes.greaterThan( 1.5 ), mod( idx, 2. ), 0. ) );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function sliverVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aExtra = bindings.aExtra;
const aExtra2 = bindings.aExtra2;
const aIndex = bindings.aIndex;
const aSrcPos = bindings.aSrcPos;
const aSrcDir = bindings.aSrcDir;
const aEvent = bindings.aEvent;
const aSub = bindings.aSub;
const uTime = bindings.uTime;
const uSpan = bindings.uSpan;
const uSliverCurve = bindings.uSliverCurve;
const uSliverTaper = bindings.uSliverTaper;
const uSliverJag = bindings.uSliverJag;
const uSecondary = bindings.uSecondary;
const uRetractOn = bindings.uRetractOn;
const uRetractStart = bindings.uRetractStart;
const uRetractEnd = bindings.uRetractEnd;
const uRetractTip = bindings.uRetractTip;
const uSecLength = bindings.uSecLength;
const uTwinkleFreq = bindings.uTwinkleFreq;
const uTwinkleDepth = bindings.uTwinkleDepth;
const uHasAlphaSpawn = bindings.uHasAlphaSpawn;
const uSliverLen = bindings.uSliverLen;
const uSliverWide = bindings.uSliverWide;
const uSecAlong = bindings.uSecAlong;
const uSize = bindings.uSize;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uEPathType = bindings.uEPathType;
const uEPathA = bindings.uEPathA;
const uEPathB = bindings.uEPathB;
const uEPathC = bindings.uEPathC;
const uEPathD = bindings.uEPathD;
const uEPathW = bindings.uEPathW;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const uCurveB = bindings.uCurveB;
const uCurveBN = bindings.uCurveBN;
const uCurveBEase = bindings.uCurveBEase;
const uCurveD = bindings.uCurveD;
const uCurveDN = bindings.uCurveDN;
const uCurveDEase = bindings.uCurveDEase;
const uCurveE = bindings.uCurveE;
const uCurveEN = bindings.uCurveEN;
const uCurveEEase = bindings.uCurveEEase;
const uCurveH = bindings.uCurveH;
const uCurveHN = bindings.uCurveHN;
const uCurveHEase = bindings.uCurveHEase;
const uCurveT = bindings.uCurveT;
const uCurveTN = bindings.uCurveTN;
const uCurveTEase = bindings.uCurveTEase;
const uPeriod = bindings.uPeriod;
const uSpawnWindow = bindings.uSpawnWindow;
const uSpawnDuration = bindings.uSpawnDuration;
const uShapeLength = bindings.uShapeLength;
const uShapeRadius = bindings.uShapeRadius;
const uShapeInner = bindings.uShapeInner;
const uShapeAngle = bindings.uShapeAngle;
const uDrag = bindings.uDrag;
const uCurl = bindings.uCurl;
const uCurlFreq = bindings.uCurlFreq;
const uCurlSpeed = bindings.uCurlSpeed;
const uFloorY = bindings.uFloorY;
const uFloorSoft = bindings.uFloorSoft;
const uAngle = bindings.uAngle;
const uPlanarDrag = bindings.uPlanarDrag;
const uVortexW = bindings.uVortexW;
const uVortexFalloff = bindings.uVortexFalloff;
const uSpawnMode = bindings.uSpawnMode;
const uShapeType = bindings.uShapeType;
const uVelMode = bindings.uVelMode;
const uHasFloor = bindings.uHasFloor;
const uSurfaceOnly = bindings.uSurfaceOnly;
const uSpeedN = bindings.uSpeedN;
const uBurstN = bindings.uBurstN;
const uAxis = bindings.uAxis;
const uDir = bindings.uDir;
const uGravity = bindings.uGravity;
const uWind = bindings.uWind;
const uBias = bindings.uBias;
const uShapeSize = bindings.uShapeSize;
const uVortexAxis = bindings.uVortexAxis;
const uLife = bindings.uLife;
const uSpeed = bindings.uSpeed;
const uSpeedKey = bindings.uSpeedKey;
const uBurstT = bindings.uBurstT;
const uBurstC = bindings.uBurstC;
const uInterior = bindings.uInterior;
const uAngleJitter = bindings.uAngleJitter;
const uAngleBias = bindings.uAngleBias;
const uFrontC = bindings.uFrontC;
const uFrontEx = bindings.uFrontEx;
const uFrontEy = bindings.uFrontEy;
const uFrontN = bindings.uFrontN;
const uFrontR = bindings.uFrontR;
const uFrontPh0 = bindings.uFrontPh0;
const uFrontSweep = bindings.uFrontSweep;
const uFrontSpan = bindings.uFrontSpan;
const uFrontStart = bindings.uFrontStart;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, cross, sin, cos, If, normalize, clamp, Break, mix, Loop, asin, fract, sqrt, tan, add, exp, smoothstep } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	const pathPointE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uEPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uEPathA ).add( mul( 2., m ).mul( u ).mul( uEPathB ) ).add( u.mul( u ).mul( uEPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uEPathD.z ).mul( u ).add( uEPathD.w ).toVar();
			const r = uEPathD.x.add( uEPathW.x.mul( sin( uEPathW.y.mul( th ) ) ) ).toVar();
			const y = uEPathA.y.add( uEPathD.y.mul( u ) ).add( uEPathW.x.mul( .5 ).mul( sin( uEPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uEPathA.x.add( cos( th ).mul( r ) ), y, uEPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointE( u.add( 1e-3 ) ).sub( pathPointE( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentE( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpE = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideE( u ), pathTangentE( u ) ) );

	} );

	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveB = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveB.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveBN ), () => {

				Break();

			} );

			const a = uCurveB.element( i.sub( 1 ) ).x.toVar(), b = uCurveB.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveBEase ) );
			v.assign( mix( v, uCurveB.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveD = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveD.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveDN ), () => {

				Break();

			} );

			const a = uCurveD.element( i.sub( 1 ) ).x.toVar(), b = uCurveD.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveDEase ) );
			v.assign( mix( v, uCurveD.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveE = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveE.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveEN ), () => {

				Break();

			} );

			const a = uCurveE.element( i.sub( 1 ) ).x.toVar(), b = uCurveE.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveEEase ) );
			v.assign( mix( v, uCurveE.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveH = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveH.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).x.toVar(), b = uCurveH.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveHEase ) );
			v.assign( mix( v, uCurveH.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveHInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveH.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveHN ), () => {

				Break();

			} );

			const a = uCurveH.element( i.sub( 1 ) ).toVar(), b = uCurveH.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveHN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveHEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	const curveT = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveT.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).x.toVar(), b = uCurveT.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveTEase ) );
			v.assign( mix( v, uCurveT.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const curveTInverse = /*@__PURE__*/ Fn( ( [ y ] ) => {

		const portResult = property( 'float' );
		const portDone = TSL.bool(false).toVar();
		const last = uCurveT.element(0).x.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveTN ), () => {

				Break();

			} );

			const a = uCurveT.element( i.sub( 1 ) ).toVar(), b = uCurveT.element( i ).toVar();
			last.assign( b.x );

			If( y.lessThanEqual( b.y ).or( i.equal( uCurveTN.sub( 1 ) ) ), () => {

				const s = clamp( y.sub( a.y ).div( max( b.y.sub( a.y ), 1e-5 ) ), 0., 1. ).toVar();
				const f = mix( s, sub( .5, sin( asin( clamp( sub( 1., mul( 2., s ) ), - 1., 1. ) ).div( 3. ) ) ), uCurveTEase ).toVar();
				portResult.assign( a.x.add( b.x.sub( a.x ).mul( f ) ) );
				portDone.assign( true );
				Break();

			} );

		} );

		If( portDone.equal( false ), () => {

			portResult.assign( last );

		} );

		return portResult;

	} );

	/* The arc point of the source crescent at parameter s, in THIS layer's space. */

	const frontPoint = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return uFrontC.add( uFrontEx.mul( cos( ph ) ).add( uFrontEy.mul( sin( ph ) ) ).mul( uFrontR ) );

	} );

	const frontTangent = /*@__PURE__*/ Fn( ( [ sArc ] ) => {

		const ph = uFrontPh0.add( sArc.mul( uFrontSweep ) ).toVar();

		return safeDir( uFrontEx.negate().mul( sin( ph ) ).add( uFrontEy.mul( cos( ph ) ) ), vec3( 1., 0., 0. ) );

	} );

	const selfLife = /*@__PURE__*/ Fn( ( [ s ] ) => {

		return mix( uLife.x, uLife.y, s.y );

	} );

	/** Birth time in layer-local seconds, or 1e9 when the instance never spawns. */

	const selfBirth = /*@__PURE__*/ Fn( ( [ s, t, ev, idx ] ) => {

		const portResult = property( 'float' );
		const instance = s.x.toVar();
		const birth = instance.mul( uSpawnWindow ).toVar();

		If( uSpawnMode.equal( 1 ), () => {

			// continuous: the emitter loops with period = count / rate.

			const cycle = floor( t.sub( birth ).div( max( uPeriod, 1e-4 ) ) ).toVar();
			const absBirth = birth.add( cycle.mul( uPeriod ) ).toVar();

			If( absBirth.lessThan( 0. ).or( absBirth.greaterThan( uSpawnDuration ) ), () => {

				portResult.assign( 1e9 );

			} ).Else( () => {

				portResult.assign( absBirth );

			} );

		} ).ElseIf( uSpawnMode.equal( 2 ), () => {

			// bursts: pick the burst whose cumulative share covers this instance.

			const t0 = uBurstT.element(0).toVar();

			Loop( 8, ( { i } ) => {

				If( i.greaterThanEqual( uBurstN ), () => {

					Break();

				} );

				If( instance.lessThanEqual( uBurstC.element( i ) ), () => {

					t0.assign( uBurstT.element( i ) );
					Break();

				} );

			} );

			portResult.assign( t0.add( fract( instance.mul( 7.13 ).add( 0.37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			// event: the instance is born when its own path's head reaches the END of
			// that path. The moment and the origin are one instance attribute, computed
			// on the CPU from whichever layer drives the path (see events-v2.ts), so a
			// single layer can carry the debris of every impact in the document.

			portResult.assign( ev.w.add( fract( instance.mul( 7.13 ).add( .37 ) ).mul( uSpawnWindow ) ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			// frontAnchored: the instance owns a hashed parameter along the source
			// crescent's arc and is born the moment that crescent's TAIL curve reaches
			// it. The curve is inverted in closed form, so the embers appear in the
			// order the blade tears with no time table anywhere.

			portResult.assign( curveTInverse( instance ).mul( uFrontSpan ).add( uFrontStart ) );

		} ).ElseIf( uSpawnMode.equal( 3 ), () => {

			// pathAnchored: instance i owns u = i/(count-1) and is born the moment the
			// head curve passes it. The head curve's domain is the layer's own 0..1
			// progress, so the inverse is scaled back into layer seconds.

			portResult.assign( curveHInverse( idx ).mul( uSpan ) );

		} ).Else( () => {

			portResult.assign( birth );

		} );

		return portResult;

	} );

	const selfOrigin = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, ev, idx ] ) => {

		const portResult = property( 'vec3' );
		const ca = e.x.mul( 6.2831853 ).toVar(), cz = e.y.mul( 2. ).sub( 1. ).toVar(), cr = sqrt( max( 0., sub( 1., cz.mul( cz ) ) ) ).toVar();
		const unit = vec3( cr.mul( cos( ca ) ), cz, cr.mul( sin( ca ) ) ).toVar();
		const fill = select( uSurfaceOnly.equal( 1 ), 1., safePow( e.z, .5 ) ).toVar();
		const sph = unit.mul( fill ).toVar();

		// bias mirrors samples toward the positive axis (0 = symmetric, 1 = fully biased).

		sph.assign( mix( sph, abs( sph ), clamp( uBias, 0., 1. ) ) );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const a1 = orthoOf( axis ).toVar(), a2 = cross( axis, a1 ).toVar();

		If( uShapeType.equal( 0 ), () => {

			portResult.assign( vec3( 0. ) );

		} ).ElseIf( uShapeType.equal( 1 ), () => {

			portResult.assign( sph.mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 2 ), () => {

			portResult.assign( vec3( sph.x, abs( sph.y ), sph.z ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 3 ), () => {

			const h = e.w.mul( uShapeLength ).toVar();
			const rr = uShapeRadius.add( h.mul( tan( min( uShapeAngle, 1.5 ) ) ) ).toVar();
			portResult.assign( axis.mul( h ).add( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ).mul( safePow( e.z, .5 ) ) ) );

		} ).ElseIf( uShapeType.equal( 4 ), () => {

			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 5 ), () => {

			const rr = mix( uShapeInner, uShapeRadius, safePow( e.z, .5 ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uShapeType.equal( 6 ), () => {

			portResult.assign( vec3( e.x.mul( 2. ).sub( 1. ), e.y.mul( 2. ).sub( 1. ), e.z.mul( 2. ).sub( 1. ) ).mul( uShapeSize ).mul( .5 ) );

		} ).ElseIf( uShapeType.equal( 11 ), () => {

			// frame: the PERIMETER of a rectangle of half-extent (radius, length/2) in
			// the plane across shape.axis, with shape.interiorFraction of the
			// population scattered inside it instead. A rim spray, not a surface one.

			const H = vec2( uShapeRadius, uShapeLength.mul( .5 ) ).toVar();
			const pt = property( 'vec2' );
			const nrm = property( 'vec2' );

			If( e.w.lessThan( uInterior ), () => {

				pt.assign( vec2( e.x.mul( 2. ).sub( 1. ).mul( H.x ).mul( .88 ), e.y.mul( 2. ).sub( 1. ).mul( H.y ).mul( .90 ) ) );
				nrm.assign( normalize( pt.add( vec2( 1e-4 ) ) ) );

			} ).Else( () => {

				const per = mul( 2., H.x ).add( mul( 2., H.y ) ).toVar();
				const sgn = select( e.z.lessThan( .5 ), - 1., 1. ).toVar();
				const dd = e.x.mul( per ).toVar();

				If( dd.lessThan( H.x ), () => {

					pt.assign( vec2( dd, H.y.negate() ) );
					nrm.assign( vec2( 0., - 1. ) );

				} ).ElseIf( dd.lessThan( H.x.add( mul( 2., H.y ) ) ), () => {

					pt.assign( vec2( H.x, H.y.negate().add( dd.sub( H.x ) ) ) );
					nrm.assign( vec2( 1., 0. ) );

				} ).Else( () => {

					pt.assign( vec2( H.x.sub( dd.sub( H.x ).sub( mul( 2., H.y ) ) ), H.y ) );
					nrm.assign( vec2( 0., 1. ) );

				} );

				pt.x.mulAssign( sgn );
				nrm.x.mulAssign( sgn );
				pt.addAssign( nrm.mul( e.y.sub( .3 ) ).mul( uShapeInner ) );

			} );

			portResult.assign( a1.mul( pt.x ).add( a2.mul( pt.y ) ).add( axis.mul( e.z.sub( .5 ) ).mul( uShapeInner ) ) );

		} ).ElseIf( uShapeType.equal( 13 ), () => {

			// radialFan: an evenly spaced fan in the plane across shape.axis. The even
			// slot is what keeps a burst from clumping, and angleBias both compresses
			// the fan (a full even ring reads as a clock face) and leans it.

			const bias = clamp( uAngleBias, - 1., 1. ).toVar();
			const ang = idx.mul( 6.2831853 ).mul( sub( 1., mul( .25, abs( bias ) ) ) ).add( bias.mul( 1.5707963 ) ).add( e.x.sub( .5 ).mul( uAngleJitter ) ).toVar();
			portResult.assign( a1.mul( cos( ang ) ).add( a2.mul( sin( ang ) ) ).mul( uShapeRadius ) );

		} ).ElseIf( uShapeType.equal( 12 ), () => {

			// orbit: a ring BAND between shape.innerRadius and shape.radius in the
			// plane across shape.axis, hashed on a sqrt so the lane population is even
			// in area rather than in radius.

			const rr = mix( uShapeInner, uShapeRadius, sqrt( e.z ) ).toVar();
			portResult.assign( a1.mul( cos( ca ) ).add( a2.mul( sin( ca ) ) ).mul( rr ) );

		} ).ElseIf( uSpawnMode.equal( 5 ), () => {

			portResult.assign( frontPoint( s.x ).add( uFrontN.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ).add( sph.mul( uShapeRadius ).mul( .4 ) ) );

		} ).ElseIf( uShapeType.equal( 8 ), () => {

			// path: the instance sits ON the document path at its own u, scattered
			// across the path frame by shape.radius so the row reads as a dotted band
			// rather than a string of beads. Paths are in DOCUMENT space.

			const tg = pathTangentE( idx ).toVar(), sd = pathSideE( idx ).toVar(), upn = pathUpE( idx ).toVar();
			portResult.assign( pathPointE( idx ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 1.5 ).mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 10 ), () => {

			// pathLine: the instance is SCATTERED along the path at its own hashed u,
			// not at i/(count-1), and spread across the path frame by shape.radius —
			// residue lying along a line rather than an ordered row of beads.

			const pu = fract( s.z.mul( 7.31 ).add( s.w.mul( 3.17 ) ) ).toVar();
			const tg = pathTangentE( pu ).toVar(), sd = pathSideE( pu ).toVar(), upn = pathUpE( pu ).toVar();
			portResult.assign( pathPointE( pu ).add( sd.mul( e.x.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( upn.mul( e.y.sub( .5 ) ).mul( 2. ).mul( uShapeRadius ) ).add( tg.mul( e.z.sub( .5 ) ).mul( uShapeRadius ) ) );

		} ).ElseIf( uSpawnMode.equal( 4 ), () => {

			portResult.assign( ev.xyz.add( sph.mul( uShapeRadius ) ) );

		} ).ElseIf( uShapeType.equal( 9 ), () => {

			// layerInstances: the site was generated from the SOURCE layer's own hash
			// (crystals-v2 / blob-v2) and rides in as an instance attribute, so it is
			// closed form and independent of how either layer is drawn.

			portResult.assign( srcPos.add( sph.mul( uShapeRadius ) ) );

		} ).Else( () => {

			portResult.assign( axis.mul( uShapeLength.mul( e.w ) ).add( sph.mul( uShapeRadius ) ) );

		} );

		return portResult;

	} );

	const selfDir = /*@__PURE__*/ Fn( ( [ s, e, e2, origin, srcDir ] ) => {

		const portResult = property( 'vec3' );
		const axis = safeDir( uAxis, vec3( 0, 1, 0 ) ).toVar();
		const base = safeDir( uDir, vec3( 0, 1, 0 ) ).toVar();
		const radial = safeDir( origin, axis ).toVar();

		// A layer-instance spawn is thrown along the instance's OWN axis, which is
		// what makes a shatter follow the spikes it broke off rather than the
		// cluster's centre. Every other velocity mode behaves as usual.

		If( uShapeType.equal( 9 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( srcDir, base ) );

		} ).ElseIf( uSpawnMode.equal( 5 ).and( uVelMode.equal( 0 ) ), () => {

			portResult.assign( safeDir( frontTangent( s.x ).negate(), base ) );

		} ).ElseIf( uVelMode.equal( 0 ), () => {

			portResult.assign( safeDir( origin, radial ) );

		} ).ElseIf( uVelMode.equal( 1 ), () => {

			portResult.assign( base );

		} ).ElseIf( uVelMode.equal( 2 ), () => {

			portResult.assign( safeDir( cross( axis, radial ), base ) );

		} ).Else( () => {

			const t1 = orthoOf( base ).toVar(), t2 = cross( base, t1 ).toVar();
			const ph = s.z.mul( 6.2831853 ).toVar(), cone = s.w.mul( uAngle ).toVar();
			portResult.assign( safeDir( base.add( t1.mul( cos( ph ) ).add( t2.mul( sin( ph ) ) ).mul( cone ) ), base ) );

		} );

		return portResult;

	} );

	/** Speed multiplier at normalized life u (1 when the layer has no speedCurve). */

	const selfSpeedAt = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const portResult = property( 'float' );
		const u = u_arg.toVar();

		If( uSpeedN.lessThan( 2 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			u.assign( clamp( u, 0., 1. ) );
			const v = uSpeedKey.element(0).y.toVar();

			Loop( { start: 1, end: 8 }, ( { i } ) => {

				If( i.greaterThanEqual( uSpeedN ), () => {

					Break();

				} );

				const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
				const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
				v.assign( mix( v, uSpeedKey.element( i ).y, step( a, u ).mul( f ) ) );

			} );

			portResult.assign( v );

		} );

		return portResult;

	} );

	/**
	 * Integral of the piecewise-linear speed table from 0 to u. Each trapezoid is
	 * closed form, so distance travelled stays a pure function of time.
	 */

	const selfSpeedI = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const acc = min( u, uSpeedKey.element(0).x ).mul( uSpeedKey.element(0).y ).toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uSpeedN ), () => {

				Break();

			} );

			const a = uSpeedKey.element( i.sub( 1 ) ).x.toVar(), b = uSpeedKey.element( i ).x.toVar();
			const va = uSpeedKey.element( i.sub( 1 ) ).y.toVar(), vb = uSpeedKey.element( i ).y.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			acc.addAssign( b.sub( a ).mul( f ).mul( va.add( vb.sub( va ).mul( f ).mul( .5 ) ) ) );

			If( i.equal( uSpeedN.sub( 1 ) ), () => {

				acc.addAssign( max( u.sub( b ), 0. ).mul( vb ) );

			} );

		} );

		return acc;

	} );

	const selfTraj = /*@__PURE__*/ Fn( ( [ s, e, e2, srcPos, srcDir, ev, idx, age, life, t, pos, vel ] ) => {

		
		
		const origin = selfOrigin( s, e, e2, srcPos, ev, idx ).toVar();
		const dir = selfDir( s, e, e2, origin, srcDir ).toVar();
		const a = max( age, 0. ).toVar();
		const u = clamp( a.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( uVelMode.equal( 4 ), () => {

			// alongPath: the instance does not fly, it RUNS. One shared head envelope
			// (velocity.speedCurve, sampled on the LAYER's own 0..1 progress) drives
			// every instance, each lagging behind it by its own hashed offset out of
			// the velocity.speed band, so the run reads as a wave travelling down the
			// line rather than as a shower. shape.radius still scatters it.

			const lu = clamp( t.div( max( uSpan, 1e-4 ) ), 0., 1. ).toVar();
			const lag = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const ru = clamp( selfSpeedAt( lu ).sub( lag ), 0., 1. ).toVar();
			pos.assign( pathPointE( ru ).add( origin ) );
			vel.assign( pathTangentE( ru ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).ElseIf( uVelMode.equal( 5 ), () => {

			// orbit: the instance CIRCLES the emitter axis at its own spawn radius.
			// Angular speed is velocity.speed.element(1) * (r/radius)^-0.5, so the inner lane
			// laps the outer one, plus a small hashed out-of-plane bob. Closed form in
			// age, so a seek lands on exactly the phase playback would have drawn.

			const ax = safeDir( uAxis, vec3( 0., 1., 0. ) ).toVar();
			const h = dot( origin, ax ).toVar();
			const rad = origin.sub( ax.mul( h ) ).toVar();
			const r = max( length( rad ), 1e-4 ).toVar();
			const w = uSpeed.y.mul( safePow( r.div( max( uShapeRadius, 1e-3 ) ), - .5 ) ).toVar();
			const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
			const turned = rad.mul( c ).add( cross( ax, rad ).mul( sn ) ).toVar();
			const bob = e2.w.sub( .5 ).mul( uSpeed.x ).mul( sin( a.mul( add( .6, mul( 1.2, e2.x ) ) ).add( e.x.mul( 6.2831853 ) ) ) ).toVar();
			pos.assign( turned.add( ax.mul( h.add( bob ) ) ) );
			vel.assign( cross( ax, turned ).mul( w ) );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} ).Else( () => {

			const v0 = mix( uSpeed.x, uSpeed.y, e2.w ).toVar();
			const d = property( 'float' ), scale = property( 'float' );

			If( uSpeedN.greaterThan( 1 ), () => {

				// speedCurve replaces the drag integral: distance = v0 * life * I(u).

				d.assign( life.mul( selfSpeedI( u ) ) );
				scale.assign( selfSpeedAt( u ) );

			} ).Else( () => {

				d.assign( select( uDrag.lessThan( .001 ), a, sub( 1., exp( uDrag.negate().mul( a ) ) ).div( uDrag ) ) );
				scale.assign( exp( uDrag.negate().mul( a ) ) );

			} );

			pos.assign( origin.add( dir.mul( v0 ).mul( d ) ).add( mul( .5, uGravity ).mul( a ).mul( a ) ).add( uWind.mul( a ) ) );
			vel.assign( dir.mul( v0 ).mul( scale ).add( uGravity.mul( a ) ).add( uWind ) );

			If( uPlanarDrag.greaterThan( 0. ), () => {

				// forces.planarDrag: the HORIZONTAL travel uses its own drag integral while
				// the vertical stays ballistic, so a burst spreads, stops spreading and
				// then settles into a drifting disc.

				const dp = sub( 1., exp( uPlanarDrag.negate().mul( a ) ) ).div( uPlanarDrag ).toVar();
				const sp = exp( uPlanarDrag.negate().mul( a ) ).toVar();
				const flat_ = origin.add( dir.mul( v0 ).mul( dp ) ).add( uWind.mul( a ) ).toVar();
				pos.xz.assign( flat_.xz );
				vel.xz.assign( dir.mul( v0 ).mul( sp ).add( uWind ).xz );

			} );

			If( abs( uVortexW ).greaterThan( 1e-5 ), () => {

				// Rotate the radial part of the displacement about the vortex axis by
				// omega*age, with omega falling off with distance from that axis.

				const ax = safeDir( uVortexAxis, vec3( 0., 1., 0. ) ).toVar();
				const h = dot( pos, ax ).toVar();
				const rad = pos.sub( ax.mul( h ) ).toVar();
				const r = length( rad ).toVar();

				If( r.greaterThan( 1e-5 ), () => {

					const w = uVortexW.div( add( 1., uVortexFalloff.mul( r ) ) ).toVar();
					const ang = w.mul( a ).toVar(), c = cos( ang ).toVar(), sn = sin( ang ).toVar();
					pos.assign( ax.mul( h ).add( rad.mul( c ) ).add( cross( ax, rad ).mul( sn ) ) );
					vel.assign( vel.mul( c ).add( cross( ax, vel ).mul( sn ) ) );

				} );

			} );

			If( uCurl.greaterThan( 0. ), () => {

				const c = curl3( pos.mul( uCurlFreq ).add( vec3( 0., t.negate().mul( uCurlSpeed ), 0. ) ).add( e2.xyz.mul( 3. ) ) ).toVar();
				pos.addAssign( c.mul( uCurl ).mul( curveE( u ) ).mul( a ) );

			} );

			If( uHasFloor.equal( 1 ), () => {

				const dy = pos.y.sub( uFloorY ).toVar();
				pos.y.assign( uFloorY.add( max( dy, dy.mul( uFloorSoft ) ) ) );

			} );

		} );

	}, 'void' );


	/* One notched edge: 2-4 teeth of a hashed depth, exactly the splash sliver's. */

	const slvEdge = /*@__PURE__*/ Fn( ( [ t, base, h0, h1, h2 ] ) => {

		const teeth = add( 2., floor( h0.mul( 3. ) ) ).toVar();
		const depth = uSliverJag.mul( add( .34, mul( .30, h1 ) ) ).toVar();

		return base.mul( sub( 1., depth.mul( fract( t.mul( teeth ).add( h2 ) ) ) ) );

	} );

	const kill = /*@__PURE__*/ Fn( () => {

		gl_Position.assign( vec4( 2., 2., 2., 1. ) );
		vClipPosition.assign( gl_Position );
		vAlpha.assign( 0. );
		vU.assign( 0. );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		vWp.assign( vec3( 0. ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aExtra2.xyz );
		vRot.assign( 0. );
		vTile.assign( vec2( 0. ) );
		const life = selfLife( aSeed ).toVar();
		const birth = property( 'float' );
		const subOrigin = vec3( 0. ).toVar();
		const subVel = vec3( 0. ).toVar();
		const dead = TSL.bool(false).toVar();
		birth.assign( selfBirth( aSeed, uTime, aEvent, aIndex ) );

		If( birth.greaterThan( 1e8 ), () => {

			dead.assign( true );

		} );

		const age = uTime.sub( birth ).toVar();

		If( age.lessThan( 0. ).or( age.greaterThanEqual( life ) ), () => {

			dead.assign( true );

		} );

		const u = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();

		If( dead, () => {

			kill();

		} ).Else( () => {

			vU.assign( u );
			const pos = property( 'vec3' ), vel = property( 'vec3' );
			selfTraj( aSeed, aExtra, aExtra2, aSrcPos, aSrcDir, aEvent, aIndex, age, life, uTime, pos, vel );
			vWp.assign( pos );
			const hs = aSeed.z.mul( 7.3 ).add( aSub.mul( 2.17 ) ).toVar();
			const L = mix( uSliverLen.x, uSliverLen.y, slvHash( hs.mul( 1.7 ).add( .9 ) ) ).mul( curveA( u ) ).toVar();
			const W = mix( uSliverWide.x, uSliverWide.y, slvHash( hs.mul( 3.1 ).add( 5.5 ) ) ).toVar();

			// render.retract: the inner end travels outward while the length collapses.

			const root = float( 0. ).toVar();

			If( uRetractOn.greaterThan( .5 ), () => {

				const eat = smoothstep( uRetractStart, uRetractEnd, u ).toVar();

				If( uRetractTip.greaterThan( .5 ), () => {

					L.mulAssign( sub( 1., eat ) );

				} ).Else( () => {

					root.assign( L.mul( eat ).mul( .9 ) );
					L.mulAssign( sub( 1., eat ) );

				} );

			} );

			const s = clamp( position.x, 0., 1. ).toVar(), v = position.y.toVar();
			const mv = modelViewMatrix.mul( vec4( pos, 1. ) ).toVar();

			// The needle lies along the SCREEN projection of its own heading, so a fan
			// read from a three-quarter camera still fans across the frame.

			const sv3 = modelViewMatrix.mul( vec4( vel, 0. ) ).xyz.toVar();
			const along = select( length( sv3.xy ).greaterThan( 1e-5 ), normalize( sv3.xy ), vec2( 0., 1. ) ).toVar();
			const across = vec2( along.y.negate(), along.x ).toVar();

			If( uSecondary.greaterThan( .5 ), () => {

				// A short bit riding its parent needle, at a hashed point along it.

				const at = mix( uSecAlong.x, uSecAlong.y, slvHash( aSub.mul( 5.3 ).add( aSeed.w.mul( 9.1 ) ) ) ).toVar();
				root.addAssign( L.mul( at ) );
				L.mulAssign( uSecLength );

			} );

			const base = W.mul( .5 ).mul( safePow( max( sub( 1., s ), 0. ), uSliverTaper ) ).mul( safePow( smoothstep( 0., .22, s ), .6 ) ).toVar();
			const left = slvEdge( s, base, slvHash( hs.mul( 2.3 ) ), slvHash( hs.mul( 4.7 ).add( 1.1 ) ), slvHash( hs.mul( 6.1 ).add( 3.3 ) ) ).toVar();
			const right = slvEdge( s, base, slvHash( hs.mul( 8.9 ).add( 2.7 ) ), slvHash( hs.mul( 11.3 ).add( 4.9 ) ), slvHash( hs.mul( 13.7 ).add( 6.1 ) ) ).toVar();
			const w = select( v.lessThan( 0. ), left, right ).toVar();

			// Two or three hashed bends, so no two needles are the same straight line.

			const bends = add( 2., floor( slvHash( hs.mul( 17.3 ).add( 8.3 ) ).mul( 2. ) ) ).toVar();
			const bend = uSliverCurve.mul( L ).mul( .12 ).mul( sin( s.mul( 3.14159265 ).mul( bends ).add( slvHash( hs.mul( 19.1 ) ).mul( 6.2831853 ) ) ) ).mul( s ).mul( select( slvHash( hs.mul( 23.9 ) ).lessThan( .5 ), - 1., 1. ) ).toVar();
			mv.xy.addAssign( along.mul( root.add( s.mul( L ) ) ).add( across.mul( v.mul( w ).add( bend ) ) ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );
			vUv.assign( vec2( v.mul( .5 ).add( .5 ), s ) );
			vAlpha.assign( curveB( u ).mul( smoothstep( 0., .03, age ) ) );

			If( uHasAlphaSpawn.equal( 1 ), () => {

				vAlpha.mulAssign( curveD( aExtra.w ) );

			} );

			If( uTwinkleDepth.greaterThan( 0. ), () => {

				vAlpha.mulAssign( mix( 1., safePow( abs( sin( uTime.mul( uTwinkleFreq ).add( aExtra2.y.mul( 19.7 ) ) ) ), 1.5 ), uTwinkleDepth ) );

			} );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function particleFragment( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uMask = bindings.uMask;
const uNoise = bindings.uNoise;
const uHasMask = bindings.uHasMask;
const uHasNoise = bindings.uHasNoise;
const uUseErosion = bindings.uUseErosion;
const uBlendMode = bindings.uBlendMode;
const uProcedural = bindings.uProcedural;
const uAtlasCols = bindings.uAtlasCols;
const uAtlasRows = bindings.uAtlasRows;
const uTime = bindings.uTime;
const uDistort = bindings.uDistort;
const uErodeSoft = bindings.uErodeSoft;
const uEdgeW = bindings.uEdgeW;
const uEdgeI = bindings.uEdgeI;
const uOpacity = bindings.uOpacity;
const uMaskRot = bindings.uMaskRot;
const uRampKeyMode = bindings.uRampKeyMode;
const uGroundY = bindings.uGroundY;
const uHeightSpan = bindings.uHeightSpan;
const uFlicker = bindings.uFlicker;
const uRampBlendMode = bindings.uRampBlendMode;
const uRampBlendWeight = bindings.uRampBlendWeight;
const uNoiseScale = bindings.uNoiseScale;
const uNoisePan = bindings.uNoisePan;
const uMaskScale = bindings.uMaskScale;
const uMaskPan = bindings.uMaskPan;
const uDistortPan = bindings.uDistortPan;
const uEdgeCol = bindings.uEdgeCol;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const uCurveC = bindings.uCurveC;
const uCurveCN = bindings.uCurveCN;
const uCurveCEase = bindings.uCurveCEase;
const tDepth = bindings.tDepth;
const uResolution = bindings.uResolution;
const uNear = bindings.uNear;
const uFar = bindings.uFar;
const uSoft = bindings.uSoft;
const uProcParams = bindings.uProcParams;
const uStripeA = bindings.uStripeA;
const uStripeB = bindings.uStripeB;
const uStripeN = bindings.uStripeN;
const uSymFill = bindings.uSymFill;
const uSymOutline = bindings.uSymOutline;
const uSymHigh = bindings.uSymHigh;
const uSymInk = bindings.uSymInk;
const uSymHot = bindings.uSymHot;
const uSymHotI = bindings.uSymHotI;
const uSymHotA = bindings.uSymHotA;
const uScreenPitch = bindings.uScreenPitch;
const uScreenOn = bindings.uScreenOn;
const uScreenWorld = bindings.uScreenWorld;
const uScreenCol = bindings.uScreenCol;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, smoothstep, mix, Loop, fract, atan, div, mod, cos, sin, add, exp, sign, sqrt, fwidth, Discard, mat2 } = TSL;

	const gl_FragColor = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const curveC = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveC.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveCN ), () => {

				Break();

			} );

			const a = uCurveC.element( i.sub( 1 ) ).x.toVar(), b = uCurveC.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveCEase ) );
			v.assign( mix( v, uCurveC.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	const linDepth = /*@__PURE__*/ Fn( ( [ z ] ) => {

		const zn = z.mul( 2. ).sub( 1. ).toVar();

		return mul( 2., uNear ).mul( uFar ).div( max( uFar.add( uNear ).sub( zn.mul( uFar.sub( uNear ) ) ), 1e-5 ) );

	} );

	const softDepth = /*@__PURE__*/ Fn( () => {

		const portResult = property( 'float' );

		If( uSoft.lessThanEqual( 0. ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			const sc = screenCoordinate.xy.div( max( uResolution, vec2( 1. ) ) ).toVar();
			const sd = linDepth( tDepth.sample( sc ).x ).toVar();
			const fd = linDepth( vClipPosition.z.div( vClipPosition.w ) ).toVar();
			portResult.assign( clamp( sd.sub( fd ).div( uSoft ), 0., 1. ) );

		} );

		return portResult;

	} );

	/** Value noise on a hashed integer lattice; the swirl/fill patterns' grain. */




	/** Hex lattice with bright cell walls; density scales with material.mask.uvScale. */


	// uProcedural: 0 none (soft disc), 1 flame, 2 smoke, 3 solid, 4 hexagon,
	// 5 ice, 6 water, 7 water-streaks, 8 star, 9 sparkle, 10 portal, 11 energy-ribbon,
	// 12 star4, 13 softRadial, 14 swirlRing, 15 ringFill, 16 sigil, 17 lensFlare,
	// 18 radialRays, 19 swirlDisc (a SURFACE pattern with its own branch in
	// surfaceFragmentV2, see glslSwirl), 20 teardropStreak.
	// Modes 0-3, 12-18 and 20 are BILLBOARD SILHOUETTES: they describe a sprite's whole
	// outline, so a closed body (the analytic shell) ignores them. 4-11 are surface
	// patterns and apply everywhere; surfaceFragmentV2 gates on exactly that.
	//
	// 17 and 18 also supply their own RAMP KEY (proceduralKey below), radially, so
	// the hot core and the outer halo are two stops of one ramp rather than two
	// layers. Every other mode leaves the key alone.

	const proceduralShape = /*@__PURE__*/ Fn( ( [ p, uv, n_arg, t, fres, cell, dims, mode ] ) => {

		const portResult = property( 'float' );
		const n = n_arg.toVar();

		If( mode.equal( 3 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			const disc = smoothstep( .5, .1, length( p ) ).toVar();

			If( mode.equal( 1 ), () => {

				portResult.assign( clamp( disc.mul( add( .45, n.mul( 1.1 ) ) ), 0., 1. ) );

			} ).ElseIf( mode.equal( 2 ), () => {

				portResult.assign( clamp( disc.mul( add( .6, n.mul( .8 ) ) ), 0., 1. ) );

			} ).Else( () => {

				const q = p.mul( 2. ).toVar();

				If( mode.equal( 4 ), () => {

					portResult.assign( clamp( add( .10, mul( .90, hexCells( uv, cell ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 5 ), () => {

					// Frozen body: a fresnel-lit interior crossed by thin veins.

					const field = add( .5, mul( .5, snoise( vec3( uv.mul( vec2( 12., 6. ) ).mul( max( cell, vec2( .02 ) ) ), 1. ) ) ) ).toVar();
					const veins = sub( 1., smoothstep( .012, .03, abs( field.sub( .5 ) ) ) ).toVar();
					portResult.assign( clamp( add( .12, mul( .55, fres ).mul( fres ) ).add( mul( .80, veins ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 6 ).or( mode.equal( 7 ) ), () => {

					const bend = sin( q.y.mul( 2.8 ).sub( t.mul( 2.2 ) ) ).mul( .12 ).toVar();
					const waves = sin( q.x.add( bend ).mul( 9.5 ).add( q.y.mul( .7 ) ) ).toVar();
					const streak = smoothstep( .72, .92, waves ).mul( add( .65, mul( .35, sin( q.y.mul( 2. ).sub( t.mul( 2. ) ) ) ) ) ).toVar();
					portResult.assign( clamp( select( mode.equal( 7 ), streak, add( .88, mul( .12, streak ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 8 ), () => {

					portResult.assign( pointedShape( q, 5., .78, .31 ) );

				} ).ElseIf( mode.equal( 9 ), () => {

					portResult.assign( pointedShape( q, 4., .9, .09 ) );

				} ).ElseIf( mode.equal( 10 ), () => {

					// A framed portal: a rim of geometry.thickness metres inside the card, misty fill.

					const edgeDistance = min( sub( 1., abs( q.x ) ).mul( max( dims.x, 1e-3 ) ), sub( 1., abs( q.y ) ).mul( max( dims.y, 1e-3 ) ).mul( .5 ) ).toVar();
					const border = sub( 1., smoothstep( max( dims.z, .001 ).mul( .65 ), max( dims.z, .001 ).mul( 1.15 ), edgeDistance ) ).toVar();
					portResult.assign( clamp( border.mul( .8 ).add( add( .08, mul( .22, n ) ).mul( sub( 1., border ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 12 ), () => {

					// Four thin spikes plus a hot core and a halo, rolled slightly off-axis:
					// the anticipation glint. Same shape the spike drew by hand.

					const c = cos( .38 ).toVar(), sn = sin( .38 ).toVar();
					const g = vec2( q.x.mul( c ).sub( q.y.mul( sn ) ), q.x.mul( sn ).add( q.y.mul( c ) ) ).toVar();
					const r = length( g ).toVar();
					const core = safePow( max( 0., sub( 1., r ) ), 6. ).toVar();
					const ax = safePow( max( 0., sub( 1., abs( g.x ) ) ), 2. ).mul( safePow( max( 0., sub( 1., abs( g.y ).mul( 7. ) ) ), 2.5 ) ).toVar();
					const ay = safePow( max( 0., sub( 1., abs( g.y ) ) ), 2. ).mul( safePow( max( 0., sub( 1., abs( g.x ).mul( 7. ) ) ), 2.5 ) ).toVar();
					portResult.assign( clamp( core.mul( 1.2 ).add( ax ).add( ay ), 0., 2. ) );

				} ).ElseIf( mode.equal( 13 ), () => {

					portResult.assign( safePow( max( 0., sub( 1., length( q ) ) ), 2.6 ) );

				} ).ElseIf( mode.equal( 14 ), () => {

					// swirlRing: three thin strands on the same rim, each at its own offset and
					// rotating at its own multiple of uProcParams.w, plus a few short arcs
					// detached just outside the rim. uProcParams = (rim radius 0..1 of the
					// card, strand half-width, wobble amplitude, rotation rate rad/s).

					const R = max( uProcParams.x, .02 ).toVar(), hw = max( uProcParams.y, .002 ).toVar(), amp = uProcParams.z.toVar();
					const d = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const m = float( 0. ).toVar();

					Loop( 3, ( { i } ) => {

						const fi = float( i ).toVar();
						const spin = uProcParams.w.mul( t ).mul( add( 1., fi.mul( .35 ) ) ).add( fi.mul( 2.09 ) ).toVar();
						const wob = amp.mul( sin( ang.mul( add( 4., fi ) ).add( spin ) ).mul( .6 ).add( sin( ang.mul( add( 7., fi.mul( 2. ) ) ).sub( spin.mul( .7 ) ) ).mul( .4 ) ) ).toVar();
						const r = R.mul( add( 1., fi.mul( .035 ) ).sub( .035 ) ).add( wob ).toVar();

						// Squared, not safePow: safePow clamps its base to a positive epsilon,
						// which would make the whole disc inside the rim read as the rim.

						const g = d.sub( r ).div( hw ).toVar();
						m.addAssign( exp( g.negate().mul( g ) ).mul( sub( 1., fi.mul( .22 ) ) ) );

					} );

					// Detached arcs: a coarse angular comb just outside the rim, gated so only
					// a few sectors are lit at a time. They ride on the WOBBLE: a rim with no
					// wobble is a plain Gaussian ring (a shield's floor pool), and only a
					// living, breathing rim throws arcs off itself.

					const sector = floor( ang.add( 3.14159265 ).div( 6.2831853 ).mul( 9. ).add( uProcParams.w.mul( t ).mul( .5 ) ) ).toVar();
					const ga = d.sub( R.mul( 1.16 ) ).div( hw.mul( 1.4 ) ).toVar();
					const arc = step( .62, procHash21( vec2( sector, 3.7 ) ) ).mul( exp( ga.negate().mul( ga ) ) ).mul( safePow( abs( sin( ang.mul( 9. ).add( uProcParams.w.mul( t ).mul( .5 ) ) ) ), 6. ) ).mul( smoothstep( 0., .004, amp ) ).toVar();
					portResult.assign( clamp( m.add( arc.mul( .9 ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 15 ), () => {

					// ringFill: a soft radial fill with a slow pulse and a value-noise grain.
					// uProcParams = (fill radius 0..1, pulse rate rad/s, noise amount 0..1,
					// edge softness 0..1).

					const R = max( uProcParams.x, .02 ).toVar(), soft = max( uProcParams.w, .02 ).toVar();
					const d = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const pulse = add( .72, mul( .28, sin( uProcParams.y.mul( t ) ) ) ).toVar();
					const fill = smoothstep( R.add( soft ), R.mul( sub( 1., soft ) ), d ).mul( pulse ).toVar();
					fill.mulAssign( mix( 1., add( .55, mul( .45, procNoise( vec2( ang.mul( 2.4 ), d.mul( 5. ).sub( t.mul( .7 ) ) ) ) ) ), clamp( uProcParams.z, 0., 1. ) ) );
					portResult.assign( clamp( fill, 0., 1. ) );

				} ).ElseIf( mode.equal( 16 ), () => {

					// sigil: a cast circle drawn entirely in polar coordinates.
					// uProcParams = (ring count 1-6, rune cells, radial spokes, gold rim 0..1).

					const r = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const ta = ang.div( 6.2831853 ).add( .5 ).toVar();
					const rings = float( 0. ).toVar();
					const nr = max( uProcParams.x, 1. ).toVar();

					Loop( 6, ( { i } ) => {

						If( float( i ).greaterThanEqual( nr ), () => {

							Break();

						} );

						// Rings march inward in pairs: a bright one and a hairline beside it.

						const rr = sub( .985, float( i ).mul( .215 ) ).toVar();
						rings.addAssign( sub( 1., smoothstep( 0., .0075, abs( r.sub( rr ) ) ) ).mul( 1.15 ) );
						rings.addAssign( sub( 1., smoothstep( 0., .0045, abs( r.sub( rr ).add( .03 ) ) ) ).mul( .55 ) );

					} );

					// Rune band: each cell draws up to three ticks at hashed radii and lengths.

					const cells = max( uProcParams.y, 1. ).toVar();
					const ci = floor( ta.mul( cells ) ).toVar(), cf = fract( ta.mul( cells ) ).toVar();
					const glyph = float( 0. ).toVar();

					Loop( { start: 0, end: 3, name: 'k' }, ( { k } ) => {

						const hk = procHash21( vec2( ci.mul( 3.7 ).add( float( k ).mul( 11.3 ) ), 1. ) ).toVar();
						const rr = add( .805, mul( .135, procHash21( vec2( ci.mul( 5.1 ).add( float( k ).mul( 7.9 ) ), 2. ) ) ) ).toVar();
						const ln = add( .16, mul( .28, procHash21( vec2( ci.mul( 2.3 ).add( float( k ).mul( 19.1 ) ), 3. ) ) ) ).toVar();
						const th = add( .0055, mul( .0055, procHash21( vec2( ci.mul( 9.7 ).add( float( k ).mul( 3.1 ) ), 4. ) ) ) ).toVar();
						glyph.addAssign( step( .30, hk ).mul( sub( 1., smoothstep( 0., th, abs( r.sub( rr ) ) ) ) ).mul( smoothstep( ln, ln.mul( .55 ), abs( cf.sub( .5 ) ) ) ) );

					} );

					const spokes = max( uProcParams.z, 1. ).toVar();
					const spoke = step( .5, sub( 1., abs( fract( ta.mul( spokes ) ).sub( .5 ) ).mul( 9. ) ) ).mul( smoothstep( 0., .02, r.sub( .795 ) ) ).mul( smoothstep( .955, .94, r ) ).toVar();
					glyph.addAssign( max( spoke, 0. ).mul( .7 ) );

					// A second, finer tick row just inside the band keeps the middle busy.

					const ci2 = floor( ta.mul( cells ).mul( .62 ) ).toVar(), cf2 = fract( ta.mul( cells ).mul( .62 ) ).toVar();
					const r2 = add( .660, mul( .070, procHash21( vec2( ci2.mul( 8.3 ), 5. ) ) ) ).toVar();
					glyph.addAssign( step( .42, procHash21( vec2( ci2.mul( 2.7 ).add( 5.5 ), 6. ) ) ).mul( sub( 1., smoothstep( 0., .006, abs( r.sub( r2 ) ) ) ) ).mul( smoothstep( .30, .16, abs( cf2.sub( .5 ) ) ) ).mul( .8 ) );

					// Two panning noise layers inside the circle: the mist the sigil sits in.

					const m1 = procNoise( vec2( ta.mul( 7. ).add( t.mul( .11 ) ), r.mul( 3.2 ).sub( t.mul( .18 ) ) ) ).toVar();
					const m2 = procNoise( vec2( ta.mul( 4. ).sub( t.mul( .07 ) ), r.mul( 5.4 ).add( t.mul( .09 ) ) ) ).toVar();
					const mist = safePow( clamp( m1.mul( .72 ).add( m2.mul( .60 ) ).sub( .26 ), 0., 1. ), 1.5 ).mul( smoothstep( .80, .10, r ) ).add( mul( .34, smoothstep( .84, .10, r ) ) ).toVar();
					const gold = sub( 1., smoothstep( 0., .016, abs( r.sub( 1.030 ) ) ) ).mul( clamp( uProcParams.w, 0., 1. ) ).toVar();
					portResult.assign( clamp( rings.mul( 1.25 ).add( glyph.mul( 1.0 ) ).add( mist.mul( .72 ) ).add( gold ), 0., 1.6 ) );

				} ).ElseIf( mode.equal( 17 ), () => {

					// lensFlare: a core, two ghosts, the horizontal and vertical lens streaks
					// and four soft spikes, all on one card. uProcParams = (core tightness,
					// anisotropy >1 narrows it horizontally into a blade, spike count, halo
					// falloff). The radial cutoff at the end is what keeps the card's own
					// rectangle from ever showing.

					const tight = max( uProcParams.x, 1. ).toVar();
					const aniso = max( uProcParams.y, .05 ).toVar();
					const spikes = max( uProcParams.z, 1. ).toVar();
					const fall = max( uProcParams.w, .05 ).toVar();
					const qa = vec2( q.x.mul( aniso ), q.y ).toVar();
					const r = length( qa ).toVar(), rr = length( q ).toVar();
					const core = exp( r.negate().mul( r ).mul( tight ) ).toVar();
					const mid = exp( r.negate().mul( r ).mul( tight ).mul( .17 ) ).mul( .44 ).toVar();
					const wide = exp( r.negate().mul( r ).mul( tight ).mul( .06 ) ).mul( .12 ).toVar();
					const hstr = exp( qa.y.negate().mul( qa.y ).mul( 230. ) ).mul( exp( abs( qa.x ).negate().mul( 2.4 ) ) ).mul( .44 ).toVar();
					const vstr = exp( qa.x.negate().mul( qa.x ).mul( 330. ) ).mul( exp( abs( qa.y ).negate().mul( 1.7 ) ) ).mul( .38 ).toVar();
					const ang = atan( q.y, q.x ).toVar();
					const spk = safePow( max( 0., cos( ang.mul( spikes ).add( .4 ) ) ), 16. ).mul( exp( rr.negate().mul( fall ) ) ).mul( .16 ).toVar();
					const flick = add( .90, mul( .14, procNoise( vec2( ang.mul( 2.3 ), t.mul( 3.1 ) ) ) ) ).toVar();
					const a = core.mul( 1.35 ).add( mid ).add( wide ).add( hstr ).add( vstr ).add( spk ).mul( flick ).toVar();
					portResult.assign( clamp( a.mul( sub( 1., smoothstep( 0.62, 1., rr ) ) ), 0., 4. ) );

				} ).ElseIf( mode.equal( 18 ), () => {

					// radialRays: a fan of hashed rays turning at uProcParams.z rad/s.
					// uProcParams = (ray count, length jitter 0..1, rotation rate, sharpness).

					const n = max( uProcParams.x, 1. ).toVar();
					const jit = clamp( uProcParams.y, 0., 1. ).toVar();
					const sharp = clamp( uProcParams.w, 0., 1. ).toVar();
					const r = length( q ).toVar(), ang = atan( q.y, q.x ).add( uProcParams.z.mul( t ) ).toVar();
					const ta = ang.div( 6.2831853 ).add( .5 ).mul( n ).toVar();
					const idx = floor( ta ).toVar();
					const len = mix( sub( 1., jit ), 1., procHash21( vec2( idx, 3.1 ) ) ).toVar();
					const f = abs( fract( ta ).sub( .5 ) ).mul( 2. ).toVar();
					const ray = safePow( max( 0., sub( 1., f ) ), mix( 2., 24., sharp ) ).toVar();
					const fade = sub( 1., smoothstep( 0., max( len, .02 ), r ) ).toVar();
					portResult.assign( clamp( ray.mul( fade ).mul( fade ).mul( sub( 1., smoothstep( 0.899, 1., r ) ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 20 ), () => {

					// teardropStreak: the speed-line cap of a velocity-stretched sprite. The
					// quad's LONG axis is uv.y (that is the axis render.stretch elongates), so
					// u runs 0 at the tail to 1 at the leading point; with render.anchor "head"
					// the bright tip sits exactly on the instance and the streak trails behind.
					// uProcParams = (dash frequency, dash scroll, core tightness, halo reach).

					const u = uv.y.toVar(), v = uv.x.sub( .5 ).mul( 2. ).toVar();
					const w = mix( .30, 1., smoothstep( 0., .62, u ) ).mul( sub( 1., smoothstep( .86, 1., u ) ) ).toVar();
					const dd = abs( v ).div( max( w, 1e-3 ) ).toVar();
					const body = safePow( max( 0., sub( 1., dd ) ), 2.2 ).toVar();
					const core = safePow( max( 0., sub( 1., dd.mul( max( uProcParams.z, 1. ) ) ) ), 5. ).mul( smoothstep( .20, .80, u ) ).toVar();
					const halo = safePow( max( 0., sub( 1., length( vec2( u.sub( .80 ).mul( 2.1 ), v.mul( 1.05 ) ) ) ) ), max( uProcParams.w, .5 ) ).toVar();
					const dash = step( .62, fract( u.mul( max( uProcParams.x, 0. ) ).sub( t.mul( uProcParams.y ) ) ) ).mul( safePow( max( 0., sub( 1., dd.mul( 1.4 ) ) ), 3. ) ).mul( smoothstep( 0., .35, u ) ).mul( sub( 1., smoothstep( .55, .85, u ) ) ).toVar();
					portResult.assign( clamp( body.mul( .8 ).add( core ).add( halo.mul( 1.1 ) ).add( dash.mul( .7 ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 11 ), () => {

					// Continuous core with a narrow moving edge; no longitudinal holes.

					const edge = add( .48, mul( .035, sin( q.y.mul( 18. ).sub( t.mul( 10. ) ) ) ) ).toVar();
					const side = sub( 1., smoothstep( edge.sub( .025 ), edge.add( .025 ), abs( q.x ) ) ).toVar();
					const ends = sub( 1., smoothstep( .94, 1., abs( q.y ) ) ).toVar();
					portResult.assign( clamp( side.mul( ends ), 0., 1. ) );

				} ).Else( () => {

					portResult.assign( disc );

				} );

			} );

		} );

		return portResult;

	} );

	/**
	 * The ramp key the two radial patterns supply for themselves: 0 at the hot
	 * centre, 1 at the outer halo. Returns -1 for every other mode, which the
	 * fragment reads as "leave the key alone".
	 */

	const proceduralKey = /*@__PURE__*/ Fn( ( [ p, t, mode ] ) => {

		const portResult = property( 'float' );
		const q = p.mul( 2. ).toVar();

		If( mode.equal( 17 ), () => {

			const tight = max( uProcParams.x, 1. ).toVar(), aniso = max( uProcParams.y, .05 ).toVar();
			const qa = vec2( q.x.mul( aniso ), q.y ).toVar();
			const r = length( qa ).toVar();
			const core = exp( r.negate().mul( r ).mul( tight ) ).toVar();
			const hstr = exp( qa.y.negate().mul( qa.y ).mul( 230. ) ).mul( exp( abs( qa.x ).negate().mul( 2.4 ) ) ).mul( .44 ).toVar();
			const vstr = exp( qa.x.negate().mul( qa.x ).mul( 330. ) ).mul( exp( abs( qa.y ).negate().mul( 1.7 ) ) ).mul( .38 ).toVar();
			portResult.assign( clamp( sub( 1., clamp( core.mul( 2.2 ).add( hstr.add( vstr ).mul( 1.1 ) ), 0., 1. ) ), 0., 1. ) );

		} ).ElseIf( mode.equal( 18 ), () => {

			portResult.assign( clamp( length( q ), 0., 1. ) );

		} ).Else( () => {

			portResult.assign( - 1. );

		} );

		return portResult;

	} );

	/**
	 * material.stripes: up to three sets of hard panning bands keyed on the ALONG
	 * coordinate in world metres. The phase field is how far each circumferential RING is
	 * offset from its neighbours: 0 runs the bands straight round the body (a
	 * machine segment ladder) and 1 breaks them into independent filaments. The
	 * largest contrast in the list is the mix weight, so a core at .2 keeps a solid
	 * body and a sheath at 1 is nothing but bands.
	 */


	const stripeTerm = /*@__PURE__*/ Fn( ( [ along, ring, t ] ) => {

		const portResult = property( 'float' );

		If( uStripeN.lessThan( 1 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			const acc = float( 0. ).toVar(), weight = float( 0. ).toVar();
			const band = floor( ring.mul( 15. ) ).toVar();

			Loop( 3, ( { i } ) => {

				If( i.greaterThanEqual( uStripeN ), () => {

					Break();

				} );

				const s = uStripeA.element( i ).toVar();
				const e = mix( .14, .008, clamp( s.w, 0., 1. ) ).toVar();

				// s.z is the RING PHASE: 0 runs the bands straight round the body (a
				// segment ladder), 1 gives every ring its own offset (filaments).

				const o = s.z.mul( stripeHash( band.mul( 3.17 ).add( 1.7 ).add( float( i ).mul( 7.3 ) ) ) ).toVar();
				const f = fract( along.mul( s.x ).sub( t.mul( s.y ) ).add( o ) ).toVar();
				acc.addAssign( smoothstep( sub( .70, e ), add( .70, e ), f ) );
				weight.assign( max( weight, uStripeB.element( i ).x ) );

			} );

			portResult.assign( mix( 1., add( .08, acc.mul( .9 ) ), clamp( weight, 0., 1. ) ) );

		} );

		return portResult;

	} );




	/* iq's star SDF: an n-pointed star whose inner radius is rf of the outer. */


	/* iq's heart SDF, cusp at the origin and lobes at y = 1. */


	/* Coverage of the four parts of a symbol: the fill, the ink outline around it,
	   the ink marks inside it, the lighter accent and the hot inner copy. */

	const symbolShape = /*@__PURE__*/ Fn( ( [ p, sd, mode, body, ring, ink, high, hot ] ) => {

		
		
		
		
		
		const aa = fwidth( p.x ).mul( 1.4 ).add( .004 ).toVar();
		const ow = max( uProcParams.x, .0 ).toVar();
		const d = float( 1e3 ).toVar(), dInk = float( 1e3 ).toVar(), dHot = float( 1e3 ).toVar(), dHigh = float( 1e3 ).toVar();

		If( mode.equal( 21 ), () => {

			d.assign( symStar( p, .70, clamp( uProcParams.y, .1, .9 ), max( uProcParams.x, 3. ) ) );

			// The hot inner copy is the SAME star at a fraction of the radius: one
			// shape, so the flash can cut without the outline moving.

			dHot.assign( symStar( p, mul( .70, clamp( uProcParams.w, .05, 1. ) ), clamp( uProcParams.y, .1, .9 ), max( uProcParams.x, 3. ) ) );
			ow.assign( max( uProcParams.z, .0 ) );

		} ).ElseIf( mode.equal( 22 ), () => {

			// A head and two ears, an expression hashed out of the instance seed, and
			// a lighter muzzle and ear inners so the disc reads as a ball.

			const ear = clamp( uProcParams.z, .05, .6 ).toVar();
			d.assign( symC( p, .58 ) );
			d.assign( min( d, symC( p.sub( vec2( - .44, .46 ) ), ear ) ) );
			d.assign( min( d, symC( p.sub( vec2( .44, .46 ) ), ear ) ) );
			const expr = floor( fract( sd.mul( 7.31 ) ).mul( max( uProcParams.x, 1. ) ) ).toVar();
			const el = p.sub( vec2( - .235, .115 ) ).toVar(), er = p.sub( vec2( .235, .115 ) ).toVar();
			const eye = property( 'float' );

			If( expr.greaterThan( .5 ).and( expr.lessThan( 1.5 ) ), () => {

				eye.assign( min( abs( symC( el, .135 ) ).sub( .042 ), abs( symC( er, .135 ) ).sub( .042 ) ) );
				eye.assign( max( eye, p.y.sub( .085 ).negate() ) );

			} ).Else( () => {

				eye.assign( min( symE( el, vec2( .088, .115 ) ), symE( er, vec2( .088, .115 ) ) ) );

			} );

			const mouth = select( expr.greaterThan( 1.5 ), symE( p.sub( vec2( 0., - .185 ) ), vec2( .115, .095 ) ), max( abs( symC( p.sub( vec2( 0., .12 ) ), .30 ) ).sub( .038 ), p.y.negate().sub( .055 ).negate() ) ).toVar();
			dInk.assign( min( eye, mouth ) );
			const inner = min( symC( p.sub( vec2( - .44, .47 ) ), ear.mul( .49 ) ), symC( p.sub( vec2( .44, .47 ) ), ear.mul( .49 ) ) ).toVar();
			dHigh.assign( min( inner, symE( p.sub( vec2( 0., - .115 ) ), vec2( .235, .175 ).mul( clamp( uProcParams.w, .1, 2. ) ) ) ) );
			ow.assign( max( uProcParams.y, .0 ) );

		} ).ElseIf( mode.equal( 23 ), () => {

			d.assign( symHeart( p.add( vec2( 0., .78 ) ).div( 1.55 ) ).mul( 1.55 ) );

		} ).ElseIf( mode.equal( 24 ), () => {

			// A comma crescent: a disc with an offset bite taken out of it.

			const bite = clamp( uProcParams.y, 0., 1. ).toVar();
			d.assign( max( symC( p.sub( vec2( - .06, .02 ) ), .56 ), symC( p.sub( vec2( add( .26, bite.mul( .2 ) ), .24 ) ), .50 ).negate() ) );

		} ).ElseIf( mode.equal( 25 ), () => {

			// A union of hashed discs: a flat cel puff, one shape and no gradient.

			const lobes = max( uProcParams.x, 1. ).toVar();
			const rr = clamp( uProcParams.y, .05, .6 ).toVar();

			Loop( 4, ( { i } ) => {

				If( float( i ).greaterThanEqual( lobes ), () => {

					Break();

				} );

				const f = float( i ).add( sd.mul( 13. ) ).toVar();
				const o = vec2( procHash21( vec2( f, 1. ) ).sub( .5 ), procHash21( vec2( f.add( 3.1 ), 2. ) ).sub( .5 ) ).mul( .72 ).toVar();
				d.assign( min( d, length( p.sub( o ) ).sub( rr.add( mul( .16, procHash21( vec2( f.add( 7.3 ), 3. ) ) ) ) ) ) );

			} );

			// A lighter cap on the upper edge, which is what makes a puff a billow.

			dHigh.assign( d.add( .16 ).sub( mul( .30, p.y.mul( .5 ).add( .5 ) ) ) );
			ow.assign( .0 );

		} ).Else( () => {

			// A three-segment polyline: the little Z bolt a comic flicks past the rim.

			const w = max( uProcParams.x, .02 ).mul( sub( 1., clamp( uProcParams.y, 0., .9 ).mul( abs( p.y ) ) ) ).toVar();
			const seg = symSeg( p, vec2( - .30, .86 ), vec2( .16, .16 ) ).toVar();
			seg.assign( min( seg, symSeg( p, vec2( .16, .16 ), vec2( - .16, - .02 ) ) ) );
			seg.assign( min( seg, symSeg( p, vec2( - .16, - .02 ), vec2( .30, - .86 ) ) ) );
			d.assign( seg.sub( w ) );
			ow.assign( .0 );

		} );

		body.assign( smoothstep( aa, aa.negate(), d ) );
		ring.assign( select( ow.greaterThan( 0. ), max( smoothstep( aa, aa.negate(), d.sub( ow ) ).sub( body ), 0. ), 0. ) );
		ink.assign( smoothstep( aa, aa.negate(), dInk ).mul( body ) );
		high.assign( smoothstep( aa, aa.negate(), dHigh ).mul( body ) );
		hot.assign( smoothstep( aa, aa.negate(), dHot ).mul( body ) );

	}, 'void' );

	/** The halftone lattice, in world metres or in the card's own UV. */

	const screentoneAt = /*@__PURE__*/ Fn( ( [ uvp, wp ] ) => {

		const portResult = property( 'float' );

		If( uScreenOn.lessThan( .5 ), () => {

			portResult.assign( 0. );

		} ).Else( () => {

			const q = select( uScreenWorld.greaterThan( .5 ), wp.xy, uvp ).toVar();
			const g = fract( q.div( max( uScreenPitch, 1e-3 ) ) ).sub( .5 ).toVar();
			portResult.assign( smoothstep( .36, .24, length( g ) ) );

		} );

		return portResult;

	} );

	/**
	 * The ramp key for one of the five spaces a particle understands. 5 is
	 * "sprite": the card's own V, 0 at the bottom (the tail of a stretched sprite)
	 * and 1 at the top (its head). 4 is "radial" on the card, 3 world height, and
	 * everything else is the particle's normalized age.
	 */

	const particleRampKey = /*@__PURE__*/ Fn( ( [ mode ] ) => {

		const portResult = property( 'float' );

		If( mode.greaterThan( 4.5 ), () => {

			portResult.assign( clamp( vUv.y, 0., 1. ) );

		} ).ElseIf( mode.greaterThan( 3.5 ), () => {

			portResult.assign( clamp( length( vUv.sub( .5 ).mul( 2. ) ), 0., 1. ) );

		} ).ElseIf( mode.greaterThan( 2.5 ), () => {

			portResult.assign( clamp( vWp.y.sub( uGroundY ).div( max( uHeightSpan, 1e-3 ) ), 0., 1. ) );

		} ).Else( () => {

			portResult.assign( clamp( vU, 0., 1. ) );

		} );

		return portResult;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		If( uProcedural.greaterThanEqual( 21 ), () => {

			// The same drawn symbol a sprite draws, on an instanced billboard: the
			// expression (and the cloud lobe layout) is hashed off the instance seed,
			// so a population of six faces is six faces and not one repeated.

			const sp = vUv.sub( .5 ).mul( 2. ).toVar();
			const body = property( 'float' ), ring = property( 'float' ), ink = property( 'float' ), high = property( 'float' ), hot = property( 'float' );
			symbolShape( sp, vSeed.x, uProcedural, body, ring, ink, high, hot );
			const a = body.add( ring ).mul( vAlpha ).mul( uOpacity ).mul( uFlicker ).mul( softDepth() ).toVar();

			If( body.add( ring ).lessThan( .004 ).or( a.lessThan( .003 ) ), () => {

				Discard();

			} );

			const tone = screentoneAt( vUv, vWp ).mul( body ).toVar();
			const col = mix( uSymFill, uScreenCol, tone.mul( .55 ) ).toVar();
			col.assign( mix( col, uSymHigh, high.mul( .85 ) ) );
			col.assign( mix( col, uSymHot, hot.mul( uSymHotA ) ) );
			col.assign( mix( col, uSymOutline, ring ) );
			col.assign( mix( col, uSymInk, ink ) );
			col.mulAssign( add( 1., hot.mul( uSymHotA ).mul( uSymHotI ) ) );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, clamp( a, 0., 1. ) ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), clamp( a, 0., 1. ) ) );

			} );

		} ).Else( () => {

			// material.mask.rotation turns the mask on every instance; the per-particle
			// random roll rides on top of it.

			const rot = vRot.add( uMaskRot ).toVar();
			const p = vUv.sub( .5 ).toVar();
			const c = cos( rot ).toVar(), s = sin( rot ).toVar();
			p.assign( mat2( c, s.negate(), s, c ).mul( p ) );
			const uvp = p.add( .5 ).toVar();
			const n = float( .5 ).toVar();

			// Plain discs and un-eroded masks do not consume noise. Avoid evaluating
			// three simplex octaves per fragment unless a feature actually uses them.

			const needsNoise = uUseErosion.equal( 1 ).or( abs( uDistort ).greaterThan( 0. ) ).or( uHasMask.equal( 0 ).and( uProcedural.equal( 1 ).or( uProcedural.equal( 2 ) ) ) ).toVar();

			If( needsNoise.and( uHasNoise.equal( 1 ) ), () => {

				const nuv = uvp.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( vSeed.xy.mul( 7. ) ).toVar();
				const n1 = uNoise.sample( nuv ).r.toVar();
				const n2 = uNoise.sample( nuv.mul( 1.7 ).add( vec2( .3, .1 ) ).sub( uNoisePan.mul( uTime ).mul( .6 ) ) ).r.toVar();
				n.assign( n1.mul( .65 ).add( n2.mul( .35 ) ) );

			} ).ElseIf( needsNoise, () => {

				n.assign( add( .5, mul( .5, fbm3( vec3( uvp.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ), vSeed.x.mul( 17. ) ) ) ) ) );

			} );

			// noise.distortionPan scrolls the field that drives the distortion (never the
			// mask lookup itself, which would slide the sprite out of its own UV range).

			const nd = n.toVar();
			const dp = uDistortPan.mul( uTime ).toVar();

			If( abs( uDistort ).greaterThan( 0. ).and( dot( dp, dp ).greaterThan( 0. ) ), () => {

				If( uHasNoise.equal( 1 ), () => {

					nd.assign( uNoise.sample( uvp.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( dp ).add( vSeed.xy.mul( 7. ) ) ).r );

				} ).Else( () => {

					nd.assign( add( .5, mul( .5, fbm3( vec3( uvp.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ).add( dp ), vSeed.x.mul( 17. ) ) ) ) ) );

				} );

			} );

			const duv = uvp.add( nd.sub( .5 ).mul( uDistort ).mul( add( .4, vU ) ) ).toVar();
			const inside = step( 0., duv.x ).mul( step( duv.x, 1. ) ).mul( step( 0., duv.y ) ).mul( step( duv.y, 1. ) ).toVar();
			const muv = duv.mul( uMaskScale ).add( uMaskPan ).toVar();
			const auv = vec2( muv.x.div( float( max( uAtlasCols, 1 ) ) ), muv.y.div( float( max( uAtlasRows, 1 ) ) ) ).add( vTile ).toVar();
			const shape = property( 'float' );

			If( uHasMask.equal( 1 ), () => {

				const m = uMask.sample( auv ).toVar();
				shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

			} ).Else( () => {

				shape.assign( proceduralShape( p, uvp, n, uTime, 0., uMaskScale, vec3( 1., 1., .1 ), uProcedural ) );

			} );

			shape.mulAssign( inside );
			const er = shape.toVar(), edge = float( 0. ).toVar();

			If( uUseErosion.equal( 1 ), () => {

				const th = curveC( vU ).toVar();
				const field = shape.mul( add( .35, n.mul( .9 ) ) ).toVar();
				er.assign( smoothstep( th, th.add( uErodeSoft ), field ) );
				edge.assign( smoothstep( th.sub( uEdgeW ), th.add( uErodeSoft.mul( .5 ) ), field ).sub( er ) );

			} );

			// Particles key their ramp on life (mode 0/1/2), on world height (3), on the
			// distance out from the card's own centre (4) or along the card itself (5) —
			// "sprite" space, where the quad's +Y is the head of a velocity-stretched
			// streak, so ONE particle carries the whole gradient. material.ramp.blend
			// mixes a second space into the same key. Erosion stays keyed on life.

			const key = particleRampKey( uRampKeyMode ).toVar();

			If( uRampBlendWeight.greaterThan( 0. ), () => {

				key.assign( mix( key, particleRampKey( uRampBlendMode ), uRampBlendWeight ) );

			} );

			const col = rampColor( key ).toVar();
			col.addAssign( uEdgeCol.mul( uEdgeI ).mul( edge ).mul( shape ) );

			// material.flicker: the hashed per-step multiplier, computed on the CPU from
			// floor(layerTime * rate) so every kind that carries it steps together.

			const a = er.mul( vAlpha ).mul( uOpacity ).mul( uFlicker ).mul( softDepth() ).toVar();

			If( a.lessThan( .002 ), () => {

				Discard();

			} );

			// Alpha blending is set up premultiplied for every mode but "alpha".

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, a ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), a ) );

			} );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function trailFragment( bindings ) {
const uTrail = bindings.uTrail;
const uHasTrail = bindings.uHasTrail;
const uBlendMode = bindings.uBlendMode;
const uOpacity = bindings.uOpacity;
const uFlicker = bindings.uFlicker;
const uTrailRampMode = bindings.uTrailRampMode;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vSeed = bindings.vSeed;
const vWp = bindings.vWp;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, Discard, abs, select, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		const shape = property( 'float' );

		If( uHasTrail.equal( 1 ), () => {

			const m = uTrail.sample( vUv ).toVar();
			shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

		} ).Else( () => {

			shape.assign( smoothstep( 1., 0., abs( vUv.x.mul( 2. ).sub( 1. ) ) ) );

		} );

		// trail.ramp.space "along" (mode 1) runs the key DOWN the ribbon: vUv.y is 0
		// at the head (the particle itself) and 1 at the tail, the same key
		// trail.widthCurve tapers on. Without a trail ramp the ribbon borrows the
		// layer's ramp keyed on the particle's age, so it is one colour at a time.

		const col = rampColor( select( uTrailRampMode.greaterThan( .5 ), clamp( vUv.y, 0., 1. ), vU ) ).toVar();
		const a = shape.mul( vAlpha ).mul( uOpacity ).mul( uFlicker ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function stripFragment( bindings ) {
const uOpacity = bindings.uOpacity;
const uFlicker = bindings.uFlicker;
const uBlendMode = bindings.uBlendMode;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const vPalette = bindings.vPalette;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, Discard, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		const col = rampColor( vPalette ).toVar();
		const a = vAlpha.mul( uOpacity ).mul( uFlicker ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function sliverFragment( bindings ) {
const uOpacity = bindings.uOpacity;
const uFlicker = bindings.uFlicker;
const uBlendMode = bindings.uBlendMode;
const vUv = bindings.vUv;
const vU = bindings.vU;
const vAlpha = bindings.vAlpha;
const vRot = bindings.vRot;
const vSeed = bindings.vSeed;
const vTile = bindings.vTile;
const vWp = bindings.vWp;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, Discard, sub, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vAlpha.lessThanEqual( 0. ), () => {

			Discard();

		} );

		const col = rampColor( smoothstep( .05, .75, vUv.y ) ).toVar();
		const a = vAlpha.mul( uOpacity ).mul( uFlicker ).toVar();
		a.mulAssign( sub( 1., smoothstep( .72, 1., vUv.y ).mul( .55 ) ) );
		a.mulAssign( smoothstep( 0., .30, vUv.x ).mul( smoothstep( 1., .70, vUv.x ) ) );

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function surfaceVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uTime = bindings.uTime;
const uLength = bindings.uLength;
const uRadius = bindings.uRadius;
const uThickness = bindings.uThickness;
const uArc = bindings.uArc;
const uVertexAmp = bindings.uVertexAmp;
const uVertexFreq = bindings.uVertexFreq;
const uVertexSpeed = bindings.uVertexSpeed;
const uDisplaceShift = bindings.uDisplaceShift;
const uRoll = bindings.uRoll;
const uChannel = bindings.uChannel;
const uSplitOffset = bindings.uSplitOffset;
const uSplitGrowth = bindings.uSplitGrowth;
const uLayerU = bindings.uLayerU;
const uShell = bindings.uShell;
const uHasVertexNoise = bindings.uHasVertexNoise;
const uBillboard = bindings.uBillboard;
const uRibbon = bindings.uRibbon;
const uUseLocalZ = bindings.uUseLocalZ;
const uSlab = bindings.uSlab;
const uSlabBase = bindings.uSlabBase;
const uSlabTaper = bindings.uSlabTaper;
const uFrameV = bindings.uFrameV;
const uFrameMargin = bindings.uFrameMargin;
const uZRange = bindings.uZRange;
const uVertexBias = bindings.uVertexBias;
const vN = bindings.vN;
const vWp = bindings.vWp;
const vObj = bindings.vObj;
const vAlong = bindings.vAlong;
const vLobe = bindings.vLobe;
const vRing = bindings.vRing;
const vUv = bindings.vUv;
const uCurveF = bindings.uCurveF;
const uCurveFN = bindings.uCurveFN;
const uCurveFEase = bindings.uCurveFEase;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, mix, Loop, cross, add, normalize, sin, cos, smoothstep, sqrt, atan } = TSL;

	const gl_Position = property( 'vec4' );

	// geometry.type "frame": the card is grown by uFrameMargin metres on every side
	// so the halo around the bar has room, and vObj carries the LOCAL position in
	// metres, which is what the fragment reads the rounded-rect SDF off.








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const curveF = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveF.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveFN ), () => {

				Break();

			} );

			const a = uCurveF.element( i.sub( 1 ) ).x.toVar(), b = uCurveF.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveFEase ) );
			v.assign( mix( v, uCurveF.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );


	const main = /*@__PURE__*/ Fn( () => {

		// The object-space position: the lattice looks its cells up on it and a
		// reveal front keys on it, so both are independent of the layer's transform.

		vUv.assign( uv );
		vLobe.assign( 0. );
		vRing.assign( uv.x );
		vObj.assign( position );
		const worldPos = property( 'vec3' ), worldNormal = property( 'vec3' );

		If( uFrameV.equal( 1 ), () => {

			// A flat card standing in the layer's own XY plane, sized from the LIVE
			// geometry every frame so a track on length or radius really resizes the
			// doorway. Everything else about a frame happens per pixel.

			const half2 = vec2( uRadius.add( uFrameMargin ), uLength.mul( .5 ).add( uFrameMargin ) ).toVar();
			const local = vec3( position.xy.mul( half2 ), 0. ).toVar();
			vObj.assign( local );
			vAlong.assign( clamp( uv.y, 0., 1. ) );
			worldPos.assign( modelMatrix.mul( vec4( local, 1. ) ).xyz );
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( 0., 0., 1., 0. ) ).xyz, vec3( 0., 0., 1. ) ) );
			vWp.assign( worldPos );
			vN.assign( worldNormal );
			gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( worldPos, 1. ) ) );
			vClipPosition.assign( gl_Position );

			If( uChannel.greaterThanEqual( 0. ), () => {

				gl_Position.x.addAssign( uChannel.sub( 1. ).mul( uSplitOffset ).mul( add( 1., uSplitGrowth.mul( clamp( uLayerU, 0., 1. ) ) ) ).mul( gl_Position.w ) );

			} );

		} ).ElseIf( uSlab.equal( 1 ), () => {

			// geometry.type "slab": a VIEW-SPACE bar. The quad is rebuilt every frame
			// on the screen projection of the layer's own local +Z, so the bar runs
			// along the axis from any camera angle, never shears as the axis tilts away
			// and never goes edge-on — which is what a real tube does at a grazing
			// angle and exactly what a beam body must not do.
			//
			// Because it is built in view space the whole thing is done here in clip
			// space; the fragment's world position is the layer origin, which is all
			// the height ramp and the ground glow ever need from a billboard.

			const s = position.x.mul( .5 ).add( .5 ).toVar();

			// 0..1 along the bar

			vAlong.assign( s );
			vRing.assign( position.y.mul( .5 ).add( .5 ) );
			vUv.assign( vec2( s, position.y.mul( .5 ).add( .5 ) ) );
			const centre = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
			const mv = viewMatrix.mul( vec4( centre, 1. ) ).toVar();
			const axisV = viewMatrix.mul( modelMatrix ).mul( vec4( 0., 0., 1., 0. ) ).xyz.toVar();
			const along = select( length( axisV.xy ).greaterThan( 1e-5 ), normalize( axisV.xy ), vec2( 1., 0. ) ).toVar();
			const across = vec2( along.y.negate(), along.x ).toVar();

			// transform.scale reaches the bar the way it reaches every other kind.

			const sAxis = length( modelMatrix.element(2).xyz ).toVar();
			const sRad = mul( .5, length( modelMatrix.element(0).xyz ).add( length( modelMatrix.element(1).xyz ) ) ).toVar();
			const len = uLength.mul( sAxis ).toVar();
			const half_ = uThickness.mul( .5 ).mul( sRad ).mul( mix( 1., uSlabTaper, s ) ).toVar();
			const offAlong = select( uSlabBase.equal( 1 ), s.mul( len ), s.sub( .5 ).mul( len ) ).toVar();
			mv.xy.addAssign( along.mul( offAlong ).add( across.mul( position.y ).mul( half_ ) ) );
			vWp.assign( centre );
			vN.assign( vec3( 0., 0., 1. ) );
			gl_Position.assign( projectionMatrix.mul( mv ) );
			vClipPosition.assign( gl_Position );

			If( uChannel.greaterThanEqual( 0. ), () => {

				gl_Position.x.addAssign( uChannel.sub( 1. ).mul( uSplitOffset ).mul( add( 1., uSplitGrowth.mul( clamp( uLayerU, 0., 1. ) ) ) ).mul( gl_Position.w ) );

			} );

		} ).ElseIf( uRibbon.equal( 1 ), () => {

			// A tapered arc sweep in the local XY plane (the swoosh of a slash):
			// geometry.radius is the arc radius, geometry.length the angle it sweeps
			// (uArc, radians) and geometry.thickness its half-width at the fattest
			// point. The strip carries (u along the arc, side) in the position attribute.

			const ua = clamp( position.x, 0., 1. ).toVar();
			const side = position.y.toVar();
			vAlong.assign( clamp( uv.y, 0., 1. ) );
			const a = ua.mul( uArc ).toVar();
			const taper = safePow( max( 0., sin( mul( 3.14159265, ua ) ) ), .6 ).toVar();
			const w = max( 1e-4, uThickness.mul( taper ) ).toVar();
			const rr = uRadius.add( side.mul( w ) ).toVar();
			const local = vec3( cos( a ).mul( rr ), sin( a ).mul( rr ), mul( .12, uRadius ).mul( sin( a.mul( 2. ) ) ) ).toVar();

			If( uHasVertexNoise.equal( 1 ), () => {

				const nz = fbm3( vec3( local.xy.mul( uVertexFreq ), a.sub( uTime.mul( uVertexSpeed ) ) ) ).toVar();
				local.addAssign( vec3( cos( a ), sin( a ), 0. ).mul( nz ).mul( uVertexAmp ).mul( uRadius ).mul( curveF( vAlong ) ) );

			} );

			worldPos.assign( modelMatrix.mul( vec4( local, 1. ) ).xyz );
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( 0., 0., 1., 0. ) ).xyz, vec3( 0., 0., 1. ) ) );

		} ).ElseIf( uShell.equal( 1 ), () => {

			// Unit sphere -> teardrop: along = (1-z)/2 on the local +Z axis (z=+1 is
			// the head). The tube frame is built from the WORLD axis so that the ring
			// "up" reference and the bias push direction live in the same space no
			// matter how the layer's transform decomposes into euler angles.

			const along = clamp( sub( 1., position.z ).mul( .5 ), 0., 1. ).toVar();
			vAlong.assign( along );

			// Squaring, not pow(): a negative base would be clamped by safePow and the
			// bulbous nose would flatten into a full-radius cylinder.

			const taper = along.mul( 2. ).sub( 1. ).toVar();

			// transform.scale reaches the analytic body too: the axis column of the
			// model matrix scales nose-to-tail, the lateral columns the body radius.

			const sAxis = length( modelMatrix.element(2).xyz ).toVar();
			const sRad = mul( .5, length( modelMatrix.element(0).xyz ).add( length( modelMatrix.element(1).xyz ) ) ).toVar();
			const radius = uRadius.mul( sRad ).toVar(), length_ = uLength.mul( sAxis ).toVar();

			// The tail is a fan of coincident vertices: closing it to a point over the
			// last 5% of 'along' replaces a torn cap with a smooth taper.

			const tailCap = smoothstep( 1., .95, along ).toVar();
			const r = radius.mul( mul( 1.05, sqrt( max( 0., sub( 1., taper.mul( taper ) ) ) ) ).mul( sub( 1., along.mul( .45 ) ) ).add( .06 ) ).mul( tailCap ).toVar();
			const len = length_.mul( select( along.lessThan( .5 ), along.mul( .9 ), add( .45, along.sub( .5 ).mul( 1.1 ) ) ) ).toVar();
			const head = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
			const axis = safeDir( modelMatrix.mul( vec4( 0., 0., 1., 0. ) ).xyz, vec3( 0., 0., 1. ) ).toVar();
			const t1 = orthoOf( axis ).toVar(), t2 = cross( axis, t1 ).toVar();
			const ring = normalize( position.xy.add( vec2( 1e-5 ) ) ).toVar();
			vRing.assign( atan( ring.y, ring.x ).mul( .15915494 ).add( .5 ) );
			const radial = t1.mul( ring.x ).add( t2.mul( ring.y ) ).toVar();
			const base = head.add( axis.mul( len ) ).add( radial.mul( r ) ).toVar();
			const amp = select( uHasVertexNoise.equal( 1 ), uVertexAmp, 0. ).toVar();
			const nz = fbm3( vec3( ring.mul( uVertexFreq ).add( vec2( along.mul( 3.5 ), 0. ) ), along.mul( 4. ).sub( uTime.mul( uVertexSpeed ) ) ).add( vec3( 0., 0., along.mul( 3. ) ) ) ).toVar();
			const disp = nz.mul( amp ).mul( curveF( along ) ).mul( radius ).mul( 2.4 ).mul( tailCap ).toVar();

			// Low-frequency lobes on one side of the tube only: a few big licks lift
			// off the tail along the bias direction, so the body silhouette stays thin.

			const bias = safeDir( uVertexBias, vec3( 0., 1., 0. ) ).toVar();
			const lobe = snoise( vec3( ring.mul( uVertexFreq.mul( .36 ) ).add( vec2( along.mul( 1.4 ), 0. ) ), along.mul( 2.2 ).sub( uTime.mul( uVertexSpeed.mul( .75 ) ) ) ) ).toVar();
			const up = smoothstep( .2, .95, ring.y ).mul( smoothstep( .35, .8, along ) ).toVar();
			vLobe.assign( max( 0., lobe ).mul( up ).mul( step( 1e-4, amp ) ) );
			const lick = vLobe.mul( radius ).mul( amp.mul( 6.2 ) ).mul( tailCap ).toVar();
			worldPos.assign( base.add( radial.mul( disp ) ).add( axis.mul( nz ).mul( .25 ).mul( along ) ).add( bias.mul( length_.mul( .236 ).mul( safePow( along, 2.3 ) ).mul( add( .6, mul( .4, nz ) ) ).add( lick ) ) ) );
			worldNormal.assign( safeDir( radial, vec3( 0., 1., 0. ) ) );

		} ).ElseIf( uBillboard.equal( 1 ), () => {

			// A sprite is a camera-facing square of half-size geometry.radius: the quad
			// is rebuilt on the view's right/up axes around the layer origin, so it
			// never edges out of the shot. transform.rotation.element(2) survives as a roll.

			vAlong.assign( clamp( uv.y, 0., 1. ) );
			const centre = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
			const right = vec3( viewMatrix.element(0)[ 0 ], viewMatrix.element(1)[ 0 ], viewMatrix.element(2)[ 0 ] ).toVar();
			const up = vec3( viewMatrix.element(0)[ 1 ], viewMatrix.element(1)[ 1 ], viewMatrix.element(2)[ 1 ] ).toVar();

			// The instance's own scale still shapes the quad (a streak stays a streak).

			const half2 = vec2( length( modelMatrix.element(0).xyz ), length( modelMatrix.element(1).xyz ) ).toVar();
			const c = cos( uRoll ).toVar(), s = sin( uRoll ).toVar();
			const q = vec2( position.x.mul( half2.x ), position.y.mul( half2.y ) ).toVar();
			q.assign( vec2( q.x.mul( c ).sub( q.y.mul( s ) ), q.x.mul( s ).add( q.y.mul( c ) ) ) );
			worldPos.assign( centre.add( right.mul( q.x ) ).add( up.mul( q.y ) ) );
			worldNormal.assign( safeDir( cross( right, up ), vec3( 0., 0., 1. ) ) );

		} ).Else( () => {

			// A mesh whose axis really runs along local +Z (a bar, a crystal) keys its
			// ramp and erosion on that axis; flat cards and lathed shapes keep uv.y.

			vAlong.assign( select( uUseLocalZ.equal( 1 ), clamp( position.z.sub( uZRange.x ).div( max( uZRange.y.sub( uZRange.x ), 1e-4 ) ), 0., 1. ), clamp( uv.y, 0., 1. ) ) );
			const pos = position.toVar();

			If( uHasVertexNoise.equal( 1 ), () => {

				const nz = fbm3( vec3( position.xy.mul( uVertexFreq ), position.z.mul( uVertexFreq ).sub( uTime.mul( uVertexSpeed ) ) ) ).toVar();
				const bias = safeDir( uVertexBias, normal ).toVar();
				pos.addAssign( mix( normal, bias, .5 ).mul( nz ).mul( uVertexAmp ).mul( curveF( vAlong ) ) );

			} );

			worldPos.assign( modelMatrix.mul( vec4( pos, 1. ) ).xyz );
			worldNormal.assign( safeDir( modelMatrix.mul( vec4( normal, 0. ) ).xyz, vec3( 0., 1., 0. ) ) );

		} );

		vWp.assign( worldPos );
		vN.assign( worldNormal );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( worldPos, 1. ) ) );
		vClipPosition.assign( gl_Position );

		// material.rgbSplit: this draw is one of three per-channel copies (uChannel
		// 0/1/2), pushed apart in CLIP space so the offset is a constant fraction of
		// the frame at any depth. uChannel < 0 is the ordinary single draw.

		If( uChannel.greaterThanEqual( 0. ), () => {

			gl_Position.x.addAssign( uChannel.sub( 1. ).mul( uSplitOffset ).mul( add( 1., uSplitGrowth.mul( clamp( uLayerU, 0., 1. ) ) ) ).mul( gl_Position.w ) );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function surfaceFragment( bindings ) {
const uTime = bindings.uTime;
const uOpacity = bindings.uOpacity;
const uErodeSoft = bindings.uErodeSoft;
const uEdgeW = bindings.uEdgeW;
const uEdgeI = bindings.uEdgeI;
const uProtect = bindings.uProtect;
const uRimBias = bindings.uRimBias;
const uDisplaceShift = bindings.uDisplaceShift;
const uFresnelPower = bindings.uFresnelPower;
const uFresnelStrength = bindings.uFresnelStrength;
const uDistort = bindings.uDistort;
const uRampKeyMode = bindings.uRampKeyMode;
const uLayerU = bindings.uLayerU;
const uMaskRot = bindings.uMaskRot;
const uRadius = bindings.uRadius;
const uLength = bindings.uLength;
const uThickness = bindings.uThickness;
const uGroundY = bindings.uGroundY;
const uHeightSpan = bindings.uHeightSpan;
const uChannel = bindings.uChannel;
const uRampBlendMode = bindings.uRampBlendMode;
const uRampBlendWeight = bindings.uRampBlendWeight;
const uShell = bindings.uShell;
const uHasMask = bindings.uHasMask;
const uHasNoise = bindings.uHasNoise;
const uUseErosion = bindings.uUseErosion;
const uBlendMode = bindings.uBlendMode;
const uProcedural = bindings.uProcedural;
const uHasFresnel = bindings.uHasFresnel;
const uBolt = bindings.uBolt;
const uNoiseScale = bindings.uNoiseScale;
const uNoisePan = bindings.uNoisePan;
const uMaskScale = bindings.uMaskScale;
const uMaskPan = bindings.uMaskPan;
const uDistortPan = bindings.uDistortPan;
const uEdgeCol = bindings.uEdgeCol;
const uCam = bindings.uCam;
const uMask = bindings.uMask;
const uNoise = bindings.uNoise;
const uHasLattice = bindings.uHasLattice;
const uHasReveal = bindings.uHasReveal;
const uRevealMode = bindings.uRevealMode;
const uHasPlaneGlow = bindings.uHasPlaneGlow;
const uHasDissolve = bindings.uHasDissolve;
const uRippleN = bindings.uRippleN;
const uBand = bindings.uBand;
const uLatEdge = bindings.uLatEdge;
const uLatGap = bindings.uLatGap;
const uPulseSpeed = bindings.uPulseSpeed;
const uPhaseJitter = bindings.uPhaseJitter;
const uGrazeFade = bindings.uGrazeFade;
const uDisStart = bindings.uDisStart;
const uDisStagger = bindings.uDisStagger;
const uDisSoft = bindings.uDisSoft;
const uRevealFrom = bindings.uRevealFrom;
const uRevealTo = bindings.uRevealTo;
const uRevealWidth = bindings.uRevealWidth;
const uPlaneDist = bindings.uPlaneDist;
const uPlaneI = bindings.uPlaneI;
const uBandStripes = bindings.uBandStripes;
const uTileCol = bindings.uTileCol;
const uLatEdgeCol = bindings.uLatEdgeCol;
const uPlaneCol = bindings.uPlaneCol;
const uRipple = bindings.uRipple;
const uSlab = bindings.uSlab;
const uSlabN = bindings.uSlabN;
const uSlabTier = bindings.uSlabTier;
const uFlicker = bindings.uFlicker;
const uReflect = bindings.uReflect;
const uReflectTint = bindings.uReflectTint;
const uReflectOpacity = bindings.uReflectOpacity;
const uReflectBlur = bindings.uReflectBlur;
const uStreakOn = bindings.uStreakOn;
const uStreakRadiate = bindings.uStreakRadiate;
const uCreaseOn = bindings.uCreaseOn;
const uStreakA = bindings.uStreakA;
const uStreakB = bindings.uStreakB;
const uStreakCol = bindings.uStreakCol;
const uCrease = bindings.uCrease;
const uSymbolSeed = bindings.uSymbolSeed;
const vN = bindings.vN;
const vWp = bindings.vWp;
const vObj = bindings.vObj;
const vAlong = bindings.vAlong;
const vLobe = bindings.vLobe;
const vRing = bindings.vRing;
const vUv = bindings.vUv;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const uCurveC = bindings.uCurveC;
const uCurveCN = bindings.uCurveCN;
const uCurveCEase = bindings.uCurveCEase;
const uProcParams = bindings.uProcParams;
const uStripeA = bindings.uStripeA;
const uStripeB = bindings.uStripeB;
const uStripeN = bindings.uStripeN;
const uSymFill = bindings.uSymFill;
const uSymOutline = bindings.uSymOutline;
const uSymHigh = bindings.uSymHigh;
const uSymInk = bindings.uSymInk;
const uSymHot = bindings.uSymHot;
const uSymHotI = bindings.uSymHotI;
const uSymHotA = bindings.uSymHotA;
const uScreenPitch = bindings.uScreenPitch;
const uScreenOn = bindings.uScreenOn;
const uScreenWorld = bindings.uScreenWorld;
const uScreenCol = bindings.uScreenCol;
const uFrame = bindings.uFrame;
const uSdfN = bindings.uSdfN;
const uBeadOn = bindings.uBeadOn;
const uCorner = bindings.uCorner;
const uSdfCore = bindings.uSdfCore;
const uSdfSpine = bindings.uSdfSpine;
const uSdfInnerOff = bindings.uSdfInnerOff;
const uSdfInnerW = bindings.uSdfInnerW;
const uSdfHalo = bindings.uSdfHalo;
const uBeads = bindings.uBeads;
const uFlowOn = bindings.uFlowOn;
const uFlowN = bindings.uFlowN;
const uFlowLayer = bindings.uFlowLayer;
const uFlowCut = bindings.uFlowCut;
const uSwirlOn = bindings.uSwirlOn;
const uSwirlBands = bindings.uSwirlBands;
const uSwirlDetail = bindings.uSwirlDetail;
const uSwirlLobe = bindings.uSwirlLobe;
const uSwirlS = bindings.uSwirlS;
const uSites = bindings.uSites;
const uCells = bindings.uCells;
const uCellA = bindings.uCellA;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, smoothstep, mix, Loop, fract, atan, div, mod, cos, sin, add, exp, sign, sqrt, fwidth, mat2, log, int, Continue, distance, Discard, acos } = TSL;

	const gl_FragColor = property( 'vec4' );

	// material.lattice / reveal / planeGlow / ripples, and the band's own facing dim.
	// 0..3 are the great-circle origins, 4..7 their (speed, width, decay).
	// geometry.slab: the tiered billboard bar, and material.flicker's per-step
	// multiplier (computed on the CPU from floor(layerTime * rate), so the mesh and
	// every other kind that carries it step together).
	// 0..3 are (height, intensity); 4..7 carry the tier colour in .rgb.
	// kind "reflection": the same draw, mirrored under the ground plane, washed
	// toward uReflectTint and faded with depth below it.
	// material.streaks / material.creases: hard surface bands and the narrow dark
	// folds under them, both keyed on the body's own (angle, along) coordinates.
	// (frequency, pan, width, segmentation)
	// (intensity, fadeFrom, fadeTo, -)
	// (frequency, depth, alongStart)








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const curveC = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveC.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveCN ), () => {

				Break();

			} );

			const a = uCurveC.element( i.sub( 1 ) ).x.toVar(), b = uCurveC.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveCEase ) );
			v.assign( mix( v, uCurveC.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );

	/** Value noise on a hashed integer lattice; the swirl/fill patterns' grain. */




	/** Hex lattice with bright cell walls; density scales with material.mask.uvScale. */


	// uProcedural: 0 none (soft disc), 1 flame, 2 smoke, 3 solid, 4 hexagon,
	// 5 ice, 6 water, 7 water-streaks, 8 star, 9 sparkle, 10 portal, 11 energy-ribbon,
	// 12 star4, 13 softRadial, 14 swirlRing, 15 ringFill, 16 sigil, 17 lensFlare,
	// 18 radialRays, 19 swirlDisc (a SURFACE pattern with its own branch in
	// surfaceFragmentV2, see glslSwirl), 20 teardropStreak.
	// Modes 0-3, 12-18 and 20 are BILLBOARD SILHOUETTES: they describe a sprite's whole
	// outline, so a closed body (the analytic shell) ignores them. 4-11 are surface
	// patterns and apply everywhere; surfaceFragmentV2 gates on exactly that.
	//
	// 17 and 18 also supply their own RAMP KEY (proceduralKey below), radially, so
	// the hot core and the outer halo are two stops of one ramp rather than two
	// layers. Every other mode leaves the key alone.

	const proceduralShape = /*@__PURE__*/ Fn( ( [ p, uv, n_arg, t, fres, cell, dims, mode ] ) => {

		const portResult = property( 'float' );
		const n = n_arg.toVar();

		If( mode.equal( 3 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			const disc = smoothstep( .5, .1, length( p ) ).toVar();

			If( mode.equal( 1 ), () => {

				portResult.assign( clamp( disc.mul( add( .45, n.mul( 1.1 ) ) ), 0., 1. ) );

			} ).ElseIf( mode.equal( 2 ), () => {

				portResult.assign( clamp( disc.mul( add( .6, n.mul( .8 ) ) ), 0., 1. ) );

			} ).Else( () => {

				const q = p.mul( 2. ).toVar();

				If( mode.equal( 4 ), () => {

					portResult.assign( clamp( add( .10, mul( .90, hexCells( uv, cell ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 5 ), () => {

					// Frozen body: a fresnel-lit interior crossed by thin veins.

					const field = add( .5, mul( .5, snoise( vec3( uv.mul( vec2( 12., 6. ) ).mul( max( cell, vec2( .02 ) ) ), 1. ) ) ) ).toVar();
					const veins = sub( 1., smoothstep( .012, .03, abs( field.sub( .5 ) ) ) ).toVar();
					portResult.assign( clamp( add( .12, mul( .55, fres ).mul( fres ) ).add( mul( .80, veins ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 6 ).or( mode.equal( 7 ) ), () => {

					const bend = sin( q.y.mul( 2.8 ).sub( t.mul( 2.2 ) ) ).mul( .12 ).toVar();
					const waves = sin( q.x.add( bend ).mul( 9.5 ).add( q.y.mul( .7 ) ) ).toVar();
					const streak = smoothstep( .72, .92, waves ).mul( add( .65, mul( .35, sin( q.y.mul( 2. ).sub( t.mul( 2. ) ) ) ) ) ).toVar();
					portResult.assign( clamp( select( mode.equal( 7 ), streak, add( .88, mul( .12, streak ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 8 ), () => {

					portResult.assign( pointedShape( q, 5., .78, .31 ) );

				} ).ElseIf( mode.equal( 9 ), () => {

					portResult.assign( pointedShape( q, 4., .9, .09 ) );

				} ).ElseIf( mode.equal( 10 ), () => {

					// A framed portal: a rim of geometry.thickness metres inside the card, misty fill.

					const edgeDistance = min( sub( 1., abs( q.x ) ).mul( max( dims.x, 1e-3 ) ), sub( 1., abs( q.y ) ).mul( max( dims.y, 1e-3 ) ).mul( .5 ) ).toVar();
					const border = sub( 1., smoothstep( max( dims.z, .001 ).mul( .65 ), max( dims.z, .001 ).mul( 1.15 ), edgeDistance ) ).toVar();
					portResult.assign( clamp( border.mul( .8 ).add( add( .08, mul( .22, n ) ).mul( sub( 1., border ) ) ), 0., 1. ) );

				} ).ElseIf( mode.equal( 12 ), () => {

					// Four thin spikes plus a hot core and a halo, rolled slightly off-axis:
					// the anticipation glint. Same shape the spike drew by hand.

					const c = cos( .38 ).toVar(), sn = sin( .38 ).toVar();
					const g = vec2( q.x.mul( c ).sub( q.y.mul( sn ) ), q.x.mul( sn ).add( q.y.mul( c ) ) ).toVar();
					const r = length( g ).toVar();
					const core = safePow( max( 0., sub( 1., r ) ), 6. ).toVar();
					const ax = safePow( max( 0., sub( 1., abs( g.x ) ) ), 2. ).mul( safePow( max( 0., sub( 1., abs( g.y ).mul( 7. ) ) ), 2.5 ) ).toVar();
					const ay = safePow( max( 0., sub( 1., abs( g.y ) ) ), 2. ).mul( safePow( max( 0., sub( 1., abs( g.x ).mul( 7. ) ) ), 2.5 ) ).toVar();
					portResult.assign( clamp( core.mul( 1.2 ).add( ax ).add( ay ), 0., 2. ) );

				} ).ElseIf( mode.equal( 13 ), () => {

					portResult.assign( safePow( max( 0., sub( 1., length( q ) ) ), 2.6 ) );

				} ).ElseIf( mode.equal( 14 ), () => {

					// swirlRing: three thin strands on the same rim, each at its own offset and
					// rotating at its own multiple of uProcParams.w, plus a few short arcs
					// detached just outside the rim. uProcParams = (rim radius 0..1 of the
					// card, strand half-width, wobble amplitude, rotation rate rad/s).

					const R = max( uProcParams.x, .02 ).toVar(), hw = max( uProcParams.y, .002 ).toVar(), amp = uProcParams.z.toVar();
					const d = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const m = float( 0. ).toVar();

					Loop( 3, ( { i } ) => {

						const fi = float( i ).toVar();
						const spin = uProcParams.w.mul( t ).mul( add( 1., fi.mul( .35 ) ) ).add( fi.mul( 2.09 ) ).toVar();
						const wob = amp.mul( sin( ang.mul( add( 4., fi ) ).add( spin ) ).mul( .6 ).add( sin( ang.mul( add( 7., fi.mul( 2. ) ) ).sub( spin.mul( .7 ) ) ).mul( .4 ) ) ).toVar();
						const r = R.mul( add( 1., fi.mul( .035 ) ).sub( .035 ) ).add( wob ).toVar();

						// Squared, not safePow: safePow clamps its base to a positive epsilon,
						// which would make the whole disc inside the rim read as the rim.

						const g = d.sub( r ).div( hw ).toVar();
						m.addAssign( exp( g.negate().mul( g ) ).mul( sub( 1., fi.mul( .22 ) ) ) );

					} );

					// Detached arcs: a coarse angular comb just outside the rim, gated so only
					// a few sectors are lit at a time. They ride on the WOBBLE: a rim with no
					// wobble is a plain Gaussian ring (a shield's floor pool), and only a
					// living, breathing rim throws arcs off itself.

					const sector = floor( ang.add( 3.14159265 ).div( 6.2831853 ).mul( 9. ).add( uProcParams.w.mul( t ).mul( .5 ) ) ).toVar();
					const ga = d.sub( R.mul( 1.16 ) ).div( hw.mul( 1.4 ) ).toVar();
					const arc = step( .62, procHash21( vec2( sector, 3.7 ) ) ).mul( exp( ga.negate().mul( ga ) ) ).mul( safePow( abs( sin( ang.mul( 9. ).add( uProcParams.w.mul( t ).mul( .5 ) ) ) ), 6. ) ).mul( smoothstep( 0., .004, amp ) ).toVar();
					portResult.assign( clamp( m.add( arc.mul( .9 ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 15 ), () => {

					// ringFill: a soft radial fill with a slow pulse and a value-noise grain.
					// uProcParams = (fill radius 0..1, pulse rate rad/s, noise amount 0..1,
					// edge softness 0..1).

					const R = max( uProcParams.x, .02 ).toVar(), soft = max( uProcParams.w, .02 ).toVar();
					const d = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const pulse = add( .72, mul( .28, sin( uProcParams.y.mul( t ) ) ) ).toVar();
					const fill = smoothstep( R.add( soft ), R.mul( sub( 1., soft ) ), d ).mul( pulse ).toVar();
					fill.mulAssign( mix( 1., add( .55, mul( .45, procNoise( vec2( ang.mul( 2.4 ), d.mul( 5. ).sub( t.mul( .7 ) ) ) ) ) ), clamp( uProcParams.z, 0., 1. ) ) );
					portResult.assign( clamp( fill, 0., 1. ) );

				} ).ElseIf( mode.equal( 16 ), () => {

					// sigil: a cast circle drawn entirely in polar coordinates.
					// uProcParams = (ring count 1-6, rune cells, radial spokes, gold rim 0..1).

					const r = length( q ).toVar(), ang = atan( q.y, q.x ).toVar();
					const ta = ang.div( 6.2831853 ).add( .5 ).toVar();
					const rings = float( 0. ).toVar();
					const nr = max( uProcParams.x, 1. ).toVar();

					Loop( 6, ( { i } ) => {

						If( float( i ).greaterThanEqual( nr ), () => {

							Break();

						} );

						// Rings march inward in pairs: a bright one and a hairline beside it.

						const rr = sub( .985, float( i ).mul( .215 ) ).toVar();
						rings.addAssign( sub( 1., smoothstep( 0., .0075, abs( r.sub( rr ) ) ) ).mul( 1.15 ) );
						rings.addAssign( sub( 1., smoothstep( 0., .0045, abs( r.sub( rr ).add( .03 ) ) ) ).mul( .55 ) );

					} );

					// Rune band: each cell draws up to three ticks at hashed radii and lengths.

					const cells = max( uProcParams.y, 1. ).toVar();
					const ci = floor( ta.mul( cells ) ).toVar(), cf = fract( ta.mul( cells ) ).toVar();
					const glyph = float( 0. ).toVar();

					Loop( { start: 0, end: 3, name: 'k' }, ( { k } ) => {

						const hk = procHash21( vec2( ci.mul( 3.7 ).add( float( k ).mul( 11.3 ) ), 1. ) ).toVar();
						const rr = add( .805, mul( .135, procHash21( vec2( ci.mul( 5.1 ).add( float( k ).mul( 7.9 ) ), 2. ) ) ) ).toVar();
						const ln = add( .16, mul( .28, procHash21( vec2( ci.mul( 2.3 ).add( float( k ).mul( 19.1 ) ), 3. ) ) ) ).toVar();
						const th = add( .0055, mul( .0055, procHash21( vec2( ci.mul( 9.7 ).add( float( k ).mul( 3.1 ) ), 4. ) ) ) ).toVar();
						glyph.addAssign( step( .30, hk ).mul( sub( 1., smoothstep( 0., th, abs( r.sub( rr ) ) ) ) ).mul( smoothstep( ln, ln.mul( .55 ), abs( cf.sub( .5 ) ) ) ) );

					} );

					const spokes = max( uProcParams.z, 1. ).toVar();
					const spoke = step( .5, sub( 1., abs( fract( ta.mul( spokes ) ).sub( .5 ) ).mul( 9. ) ) ).mul( smoothstep( 0., .02, r.sub( .795 ) ) ).mul( smoothstep( .955, .94, r ) ).toVar();
					glyph.addAssign( max( spoke, 0. ).mul( .7 ) );

					// A second, finer tick row just inside the band keeps the middle busy.

					const ci2 = floor( ta.mul( cells ).mul( .62 ) ).toVar(), cf2 = fract( ta.mul( cells ).mul( .62 ) ).toVar();
					const r2 = add( .660, mul( .070, procHash21( vec2( ci2.mul( 8.3 ), 5. ) ) ) ).toVar();
					glyph.addAssign( step( .42, procHash21( vec2( ci2.mul( 2.7 ).add( 5.5 ), 6. ) ) ).mul( sub( 1., smoothstep( 0., .006, abs( r.sub( r2 ) ) ) ) ).mul( smoothstep( .30, .16, abs( cf2.sub( .5 ) ) ) ).mul( .8 ) );

					// Two panning noise layers inside the circle: the mist the sigil sits in.

					const m1 = procNoise( vec2( ta.mul( 7. ).add( t.mul( .11 ) ), r.mul( 3.2 ).sub( t.mul( .18 ) ) ) ).toVar();
					const m2 = procNoise( vec2( ta.mul( 4. ).sub( t.mul( .07 ) ), r.mul( 5.4 ).add( t.mul( .09 ) ) ) ).toVar();
					const mist = safePow( clamp( m1.mul( .72 ).add( m2.mul( .60 ) ).sub( .26 ), 0., 1. ), 1.5 ).mul( smoothstep( .80, .10, r ) ).add( mul( .34, smoothstep( .84, .10, r ) ) ).toVar();
					const gold = sub( 1., smoothstep( 0., .016, abs( r.sub( 1.030 ) ) ) ).mul( clamp( uProcParams.w, 0., 1. ) ).toVar();
					portResult.assign( clamp( rings.mul( 1.25 ).add( glyph.mul( 1.0 ) ).add( mist.mul( .72 ) ).add( gold ), 0., 1.6 ) );

				} ).ElseIf( mode.equal( 17 ), () => {

					// lensFlare: a core, two ghosts, the horizontal and vertical lens streaks
					// and four soft spikes, all on one card. uProcParams = (core tightness,
					// anisotropy >1 narrows it horizontally into a blade, spike count, halo
					// falloff). The radial cutoff at the end is what keeps the card's own
					// rectangle from ever showing.

					const tight = max( uProcParams.x, 1. ).toVar();
					const aniso = max( uProcParams.y, .05 ).toVar();
					const spikes = max( uProcParams.z, 1. ).toVar();
					const fall = max( uProcParams.w, .05 ).toVar();
					const qa = vec2( q.x.mul( aniso ), q.y ).toVar();
					const r = length( qa ).toVar(), rr = length( q ).toVar();
					const core = exp( r.negate().mul( r ).mul( tight ) ).toVar();
					const mid = exp( r.negate().mul( r ).mul( tight ).mul( .17 ) ).mul( .44 ).toVar();
					const wide = exp( r.negate().mul( r ).mul( tight ).mul( .06 ) ).mul( .12 ).toVar();
					const hstr = exp( qa.y.negate().mul( qa.y ).mul( 230. ) ).mul( exp( abs( qa.x ).negate().mul( 2.4 ) ) ).mul( .44 ).toVar();
					const vstr = exp( qa.x.negate().mul( qa.x ).mul( 330. ) ).mul( exp( abs( qa.y ).negate().mul( 1.7 ) ) ).mul( .38 ).toVar();
					const ang = atan( q.y, q.x ).toVar();
					const spk = safePow( max( 0., cos( ang.mul( spikes ).add( .4 ) ) ), 16. ).mul( exp( rr.negate().mul( fall ) ) ).mul( .16 ).toVar();
					const flick = add( .90, mul( .14, procNoise( vec2( ang.mul( 2.3 ), t.mul( 3.1 ) ) ) ) ).toVar();
					const a = core.mul( 1.35 ).add( mid ).add( wide ).add( hstr ).add( vstr ).add( spk ).mul( flick ).toVar();
					portResult.assign( clamp( a.mul( sub( 1., smoothstep( 0.62, 1., rr ) ) ), 0., 4. ) );

				} ).ElseIf( mode.equal( 18 ), () => {

					// radialRays: a fan of hashed rays turning at uProcParams.z rad/s.
					// uProcParams = (ray count, length jitter 0..1, rotation rate, sharpness).

					const n = max( uProcParams.x, 1. ).toVar();
					const jit = clamp( uProcParams.y, 0., 1. ).toVar();
					const sharp = clamp( uProcParams.w, 0., 1. ).toVar();
					const r = length( q ).toVar(), ang = atan( q.y, q.x ).add( uProcParams.z.mul( t ) ).toVar();
					const ta = ang.div( 6.2831853 ).add( .5 ).mul( n ).toVar();
					const idx = floor( ta ).toVar();
					const len = mix( sub( 1., jit ), 1., procHash21( vec2( idx, 3.1 ) ) ).toVar();
					const f = abs( fract( ta ).sub( .5 ) ).mul( 2. ).toVar();
					const ray = safePow( max( 0., sub( 1., f ) ), mix( 2., 24., sharp ) ).toVar();
					const fade = sub( 1., smoothstep( 0., max( len, .02 ), r ) ).toVar();
					portResult.assign( clamp( ray.mul( fade ).mul( fade ).mul( sub( 1., smoothstep( 0.899, 1., r ) ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 20 ), () => {

					// teardropStreak: the speed-line cap of a velocity-stretched sprite. The
					// quad's LONG axis is uv.y (that is the axis render.stretch elongates), so
					// u runs 0 at the tail to 1 at the leading point; with render.anchor "head"
					// the bright tip sits exactly on the instance and the streak trails behind.
					// uProcParams = (dash frequency, dash scroll, core tightness, halo reach).

					const u = uv.y.toVar(), v = uv.x.sub( .5 ).mul( 2. ).toVar();
					const w = mix( .30, 1., smoothstep( 0., .62, u ) ).mul( sub( 1., smoothstep( .86, 1., u ) ) ).toVar();
					const dd = abs( v ).div( max( w, 1e-3 ) ).toVar();
					const body = safePow( max( 0., sub( 1., dd ) ), 2.2 ).toVar();
					const core = safePow( max( 0., sub( 1., dd.mul( max( uProcParams.z, 1. ) ) ) ), 5. ).mul( smoothstep( .20, .80, u ) ).toVar();
					const halo = safePow( max( 0., sub( 1., length( vec2( u.sub( .80 ).mul( 2.1 ), v.mul( 1.05 ) ) ) ) ), max( uProcParams.w, .5 ) ).toVar();
					const dash = step( .62, fract( u.mul( max( uProcParams.x, 0. ) ).sub( t.mul( uProcParams.y ) ) ) ).mul( safePow( max( 0., sub( 1., dd.mul( 1.4 ) ) ), 3. ) ).mul( smoothstep( 0., .35, u ) ).mul( sub( 1., smoothstep( .55, .85, u ) ) ).toVar();
					portResult.assign( clamp( body.mul( .8 ).add( core ).add( halo.mul( 1.1 ) ).add( dash.mul( .7 ) ), 0., 2. ) );

				} ).ElseIf( mode.equal( 11 ), () => {

					// Continuous core with a narrow moving edge; no longitudinal holes.

					const edge = add( .48, mul( .035, sin( q.y.mul( 18. ).sub( t.mul( 10. ) ) ) ) ).toVar();
					const side = sub( 1., smoothstep( edge.sub( .025 ), edge.add( .025 ), abs( q.x ) ) ).toVar();
					const ends = sub( 1., smoothstep( .94, 1., abs( q.y ) ) ).toVar();
					portResult.assign( clamp( side.mul( ends ), 0., 1. ) );

				} ).Else( () => {

					portResult.assign( disc );

				} );

			} );

		} );

		return portResult;

	} );

	/**
	 * The ramp key the two radial patterns supply for themselves: 0 at the hot
	 * centre, 1 at the outer halo. Returns -1 for every other mode, which the
	 * fragment reads as "leave the key alone".
	 */

	const proceduralKey = /*@__PURE__*/ Fn( ( [ p, t, mode ] ) => {

		const portResult = property( 'float' );
		const q = p.mul( 2. ).toVar();

		If( mode.equal( 17 ), () => {

			const tight = max( uProcParams.x, 1. ).toVar(), aniso = max( uProcParams.y, .05 ).toVar();
			const qa = vec2( q.x.mul( aniso ), q.y ).toVar();
			const r = length( qa ).toVar();
			const core = exp( r.negate().mul( r ).mul( tight ) ).toVar();
			const hstr = exp( qa.y.negate().mul( qa.y ).mul( 230. ) ).mul( exp( abs( qa.x ).negate().mul( 2.4 ) ) ).mul( .44 ).toVar();
			const vstr = exp( qa.x.negate().mul( qa.x ).mul( 330. ) ).mul( exp( abs( qa.y ).negate().mul( 1.7 ) ) ).mul( .38 ).toVar();
			portResult.assign( clamp( sub( 1., clamp( core.mul( 2.2 ).add( hstr.add( vstr ).mul( 1.1 ) ), 0., 1. ) ), 0., 1. ) );

		} ).ElseIf( mode.equal( 18 ), () => {

			portResult.assign( clamp( length( q ), 0., 1. ) );

		} ).Else( () => {

			portResult.assign( - 1. );

		} );

		return portResult;

	} );

	/**
	 * material.stripes: up to three sets of hard panning bands keyed on the ALONG
	 * coordinate in world metres. The phase field is how far each circumferential RING is
	 * offset from its neighbours: 0 runs the bands straight round the body (a
	 * machine segment ladder) and 1 breaks them into independent filaments. The
	 * largest contrast in the list is the mix weight, so a core at .2 keeps a solid
	 * body and a sheath at 1 is nothing but bands.
	 */


	const stripeTerm = /*@__PURE__*/ Fn( ( [ along, ring, t ] ) => {

		const portResult = property( 'float' );

		If( uStripeN.lessThan( 1 ), () => {

			portResult.assign( 1. );

		} ).Else( () => {

			const acc = float( 0. ).toVar(), weight = float( 0. ).toVar();
			const band = floor( ring.mul( 15. ) ).toVar();

			Loop( 3, ( { i } ) => {

				If( i.greaterThanEqual( uStripeN ), () => {

					Break();

				} );

				const s = uStripeA.element( i ).toVar();
				const e = mix( .14, .008, clamp( s.w, 0., 1. ) ).toVar();

				// s.z is the RING PHASE: 0 runs the bands straight round the body (a
				// segment ladder), 1 gives every ring its own offset (filaments).

				const o = s.z.mul( stripeHash( band.mul( 3.17 ).add( 1.7 ).add( float( i ).mul( 7.3 ) ) ) ).toVar();
				const f = fract( along.mul( s.x ).sub( t.mul( s.y ) ).add( o ) ).toVar();
				acc.addAssign( smoothstep( sub( .70, e ), add( .70, e ), f ) );
				weight.assign( max( weight, uStripeB.element( i ).x ) );

			} );

			portResult.assign( mix( 1., add( .08, acc.mul( .9 ) ), clamp( weight, 0., 1. ) ) );

		} );

		return portResult;

	} );




	/* iq's star SDF: an n-pointed star whose inner radius is rf of the outer. */


	/* iq's heart SDF, cusp at the origin and lobes at y = 1. */


	/* Coverage of the four parts of a symbol: the fill, the ink outline around it,
	   the ink marks inside it, the lighter accent and the hot inner copy. */

	const symbolShape = /*@__PURE__*/ Fn( ( [ p, sd, mode, body, ring, ink, high, hot ] ) => {

		
		
		
		
		
		const aa = fwidth( p.x ).mul( 1.4 ).add( .004 ).toVar();
		const ow = max( uProcParams.x, .0 ).toVar();
		const d = float( 1e3 ).toVar(), dInk = float( 1e3 ).toVar(), dHot = float( 1e3 ).toVar(), dHigh = float( 1e3 ).toVar();

		If( mode.equal( 21 ), () => {

			d.assign( symStar( p, .70, clamp( uProcParams.y, .1, .9 ), max( uProcParams.x, 3. ) ) );

			// The hot inner copy is the SAME star at a fraction of the radius: one
			// shape, so the flash can cut without the outline moving.

			dHot.assign( symStar( p, mul( .70, clamp( uProcParams.w, .05, 1. ) ), clamp( uProcParams.y, .1, .9 ), max( uProcParams.x, 3. ) ) );
			ow.assign( max( uProcParams.z, .0 ) );

		} ).ElseIf( mode.equal( 22 ), () => {

			// A head and two ears, an expression hashed out of the instance seed, and
			// a lighter muzzle and ear inners so the disc reads as a ball.

			const ear = clamp( uProcParams.z, .05, .6 ).toVar();
			d.assign( symC( p, .58 ) );
			d.assign( min( d, symC( p.sub( vec2( - .44, .46 ) ), ear ) ) );
			d.assign( min( d, symC( p.sub( vec2( .44, .46 ) ), ear ) ) );
			const expr = floor( fract( sd.mul( 7.31 ) ).mul( max( uProcParams.x, 1. ) ) ).toVar();
			const el = p.sub( vec2( - .235, .115 ) ).toVar(), er = p.sub( vec2( .235, .115 ) ).toVar();
			const eye = property( 'float' );

			If( expr.greaterThan( .5 ).and( expr.lessThan( 1.5 ) ), () => {

				eye.assign( min( abs( symC( el, .135 ) ).sub( .042 ), abs( symC( er, .135 ) ).sub( .042 ) ) );
				eye.assign( max( eye, p.y.sub( .085 ).negate() ) );

			} ).Else( () => {

				eye.assign( min( symE( el, vec2( .088, .115 ) ), symE( er, vec2( .088, .115 ) ) ) );

			} );

			const mouth = select( expr.greaterThan( 1.5 ), symE( p.sub( vec2( 0., - .185 ) ), vec2( .115, .095 ) ), max( abs( symC( p.sub( vec2( 0., .12 ) ), .30 ) ).sub( .038 ), p.y.negate().sub( .055 ).negate() ) ).toVar();
			dInk.assign( min( eye, mouth ) );
			const inner = min( symC( p.sub( vec2( - .44, .47 ) ), ear.mul( .49 ) ), symC( p.sub( vec2( .44, .47 ) ), ear.mul( .49 ) ) ).toVar();
			dHigh.assign( min( inner, symE( p.sub( vec2( 0., - .115 ) ), vec2( .235, .175 ).mul( clamp( uProcParams.w, .1, 2. ) ) ) ) );
			ow.assign( max( uProcParams.y, .0 ) );

		} ).ElseIf( mode.equal( 23 ), () => {

			d.assign( symHeart( p.add( vec2( 0., .78 ) ).div( 1.55 ) ).mul( 1.55 ) );

		} ).ElseIf( mode.equal( 24 ), () => {

			// A comma crescent: a disc with an offset bite taken out of it.

			const bite = clamp( uProcParams.y, 0., 1. ).toVar();
			d.assign( max( symC( p.sub( vec2( - .06, .02 ) ), .56 ), symC( p.sub( vec2( add( .26, bite.mul( .2 ) ), .24 ) ), .50 ).negate() ) );

		} ).ElseIf( mode.equal( 25 ), () => {

			// A union of hashed discs: a flat cel puff, one shape and no gradient.

			const lobes = max( uProcParams.x, 1. ).toVar();
			const rr = clamp( uProcParams.y, .05, .6 ).toVar();

			Loop( 4, ( { i } ) => {

				If( float( i ).greaterThanEqual( lobes ), () => {

					Break();

				} );

				const f = float( i ).add( sd.mul( 13. ) ).toVar();
				const o = vec2( procHash21( vec2( f, 1. ) ).sub( .5 ), procHash21( vec2( f.add( 3.1 ), 2. ) ).sub( .5 ) ).mul( .72 ).toVar();
				d.assign( min( d, length( p.sub( o ) ).sub( rr.add( mul( .16, procHash21( vec2( f.add( 7.3 ), 3. ) ) ) ) ) ) );

			} );

			// A lighter cap on the upper edge, which is what makes a puff a billow.

			dHigh.assign( d.add( .16 ).sub( mul( .30, p.y.mul( .5 ).add( .5 ) ) ) );
			ow.assign( .0 );

		} ).Else( () => {

			// A three-segment polyline: the little Z bolt a comic flicks past the rim.

			const w = max( uProcParams.x, .02 ).mul( sub( 1., clamp( uProcParams.y, 0., .9 ).mul( abs( p.y ) ) ) ).toVar();
			const seg = symSeg( p, vec2( - .30, .86 ), vec2( .16, .16 ) ).toVar();
			seg.assign( min( seg, symSeg( p, vec2( .16, .16 ), vec2( - .16, - .02 ) ) ) );
			seg.assign( min( seg, symSeg( p, vec2( - .16, - .02 ), vec2( .30, - .86 ) ) ) );
			d.assign( seg.sub( w ) );
			ow.assign( .0 );

		} );

		body.assign( smoothstep( aa, aa.negate(), d ) );
		ring.assign( select( ow.greaterThan( 0. ), max( smoothstep( aa, aa.negate(), d.sub( ow ) ).sub( body ), 0. ), 0. ) );
		ink.assign( smoothstep( aa, aa.negate(), dInk ).mul( body ) );
		high.assign( smoothstep( aa, aa.negate(), dHigh ).mul( body ) );
		hot.assign( smoothstep( aa, aa.negate(), dHot ).mul( body ) );

	}, 'void' );

	/** The halftone lattice, in world metres or in the card's own UV. */

	const screentoneAt = /*@__PURE__*/ Fn( ( [ uvp, wp ] ) => {

		const portResult = property( 'float' );

		If( uScreenOn.lessThan( .5 ), () => {

			portResult.assign( 0. );

		} ).Else( () => {

			const q = select( uScreenWorld.greaterThan( .5 ), wp.xy, uvp ).toVar();
			const g = fract( q.div( max( uScreenPitch, 1e-3 ) ) ).sub( .5 ).toVar();
			portResult.assign( smoothstep( .36, .24, length( g ) ) );

		} );

		return portResult;

	} );

	// (count, speed, width)


	/* Perimeter parameter: 0 at bottom-centre, 1 at top-centre, mirrored in x. */


	/* Travelling brightness beads along the perimeter, hash-stepped so they are
	   never evenly spaced. Returns 0 when material.beads is null. */

	const beadTerm = /*@__PURE__*/ Fn( ( [ u, t ] ) => {

		const portResult = property( 'float' );

		If( uBeadOn.equal( 0 ), () => {

			portResult.assign( 0. );

		} ).Else( () => {

			const acc = float( 0. ).toVar();

			Loop( 8, ( { i } ) => {

				If( float( i ).greaterThanEqual( uBeads.x ), () => {

					Break();

				} );

				const k = float( i ).toVar();
				const bp = fract( t.mul( uBeads.y ).add( k.mul( 0.37 ) ).add( fract( sin( k.mul( 3.1 ) ).mul( 43758.5453 ) ).mul( 0.4 ) ) ).toVar();
				const g = u.sub( bp ).div( max( uBeads.z, 1e-3 ) ).toVar();
				acc.addAssign( exp( g.negate().mul( g ) ) );

			} );

			portResult.assign( acc );

		} );

		return portResult;

	} );

	// 0..3 are (scale, pan.x, pan.y, rotate); 4..7 carry the mix weight in .x.
	// One array rather than two: WebGPU allows twelve uniform buffers per stage.
	// (threshold, softness, parallax)


	/* The mixed field in 0..1. q is the surface coordinate in METRES, so a card
	   that is resized does not squash its own patches. */

	const flowField = /*@__PURE__*/ Fn( ( [ q, t, parallax ] ) => {

		const acc = float( 0. ).toVar(), weight = float( 0. ).toVar();

		Loop( 4, ( { i } ) => {

			If( i.greaterThanEqual( uFlowN ), () => {

				Break();

			} );

			const L = uFlowLayer.element( i ).toVar();
			const c = cos( L.w ).toVar(), sn = sin( L.w ).toVar();
			const r = vec2( q.x.mul( c ).sub( q.y.mul( sn ) ), q.x.mul( sn ).add( q.y.mul( c ) ) ).toVar();

			// Only the FIRST (slowest) layer takes the view offset: parallax on the
			// fine octaves reads as the whole card sliding, not as depth.

			const off = select( i.equal( 0 ), parallax.mul( uFlowCut.z ), vec2( 0. ) ).toVar();
			acc.addAssign( flowNoise( r.add( off ).mul( L.x ).add( L.yz.mul( t ) ) ).mul( uFlowLayer.element( add( 4, i ) ).x ) );
			weight.addAssign( uFlowLayer.element( add( 4, i ) ).x );

		} );

		return acc.div( max( weight, 1e-4 ) );

	} );

	// (arms, wind, width, warp)
	// (arms, wind, warp, contrast)
	// (scale1, scale2, amount)



	/* (mask, shade) for the disc at centred coordinate q (|q| <= 1 is the card). */

	const swirlDisc = /*@__PURE__*/ Fn( ( [ q, t ] ) => {

		const d = length( q ).toVar();
		const twist = uProcParams.x.toVar(), spin = uProcParams.y.toVar(), inflow = uProcParams.z.toVar();
		const arms = select( uSwirlOn.equal( 1 ).and( uSwirlBands.x.greaterThan( 0. ) ), uSwirlBands.x, max( uProcParams.w, 1. ) ).toVar();
		const rot = uSwirlS.mul( twist ).div( d.add( .08 ) ).add( t.mul( spin ).mul( 6.28318530718 ) ).toVar();
		const c = cos( rot ).toVar(), sn = sin( rot ).toVar();
		const p = vec2( q.x.mul( c ).sub( q.y.mul( sn ) ), q.x.mul( sn ).add( q.y.mul( c ) ) ).mul( add( 1., t.mul( inflow ).mul( uSwirlS ) ) ).mul( 3. ).toVar();
		const w = vec2( swirlFbm2( p.mul( 1.15 ) ), swirlFbm2( p.mul( 1.15 ).add( 31.7 ) ) ).sub( .5 ).toVar();
		const n = swirlFbm2( p.add( w.mul( 1.15 ) ) ).toVar();
		const ang = atan( q.y, q.x ).toVar();
		const wind = select( uSwirlOn.equal( 1 ), uSwirlBands.y, 1.85 ).toVar();
		const warp = select( uSwirlOn.equal( 1 ), uSwirlBands.w, 1.4 ).toVar();
		const band = add( .5, mul( .5, sin( arms.mul( ang.add( uSwirlS.mul( twist ).mul( wind ).mul( log( d.add( .09 ) ) ) ) ).add( t.mul( spin ).mul( 6.28318530718 ).mul( arms ) ).add( n.sub( .5 ).mul( warp ) ) ) ) ).toVar();
		const lobe = float( 0. ).toVar();

		If( uSwirlOn.equal( 1 ), () => {

			lobe.assign( swirlFbm3( p.mul( uSwirlLobe.x ).add( 57. ) ).sub( .5 ).add( mul( .3, swirlFbm2( p.mul( uSwirlLobe.y ).add( 91. ) ).sub( .5 ) ) ).mul( uSwirlLobe.z ) );

		} );

		const width = select( uSwirlOn.equal( 1 ), uSwirlBands.z, .5 ).toVar();
		const m = band.mul( add( .4, mul( .76, width ) ) ).add( n.mul( .46 ) ).add( lobe ).toVar();

		// The independent detail spiral: tighter, its own winding, and it only ever
		// shades — a second mask here would read as a copy of the first.

		const dArms = select( uSwirlOn.equal( 1 ), uSwirlDetail.x, 7. ).toVar();
		const dWind = select( uSwirlOn.equal( 1 ), uSwirlDetail.y, 2.75 ).toVar();
		const dWarp = select( uSwirlOn.equal( 1 ), uSwirlDetail.z, 6.2 ).toVar();
		const dCon = select( uSwirlOn.equal( 1 ), uSwirlDetail.w, .62 ).toVar();
		const fine = swirlFbm3( p.mul( 2.35 ).add( 113. ) ).toVar();
		const det = add( .5, mul( .5, sin( dArms.mul( ang.add( uSwirlS.mul( twist ).mul( dWind ).mul( log( d.add( .09 ) ) ) ) ).add( t.mul( spin ).mul( 6.28318530718 ).mul( dArms ) ).add( fine.sub( .5 ).mul( dWarp ) ) ) ) ).toVar();
		const shade = smoothstep( .10, .92, det.mul( dCon ).add( fine.mul( .54 ) ) ).toVar();

		return vec2( clamp( m, 0., 2. ), shade );

	} );


	const cellLookup = /*@__PURE__*/ Fn( ( [ nrm, site, id, edge ] ) => {

		
		
		
		const centre = int( floor( sub( 1., nrm.y ).mul( uCells ).mul( .5 ) ) ).toVar();
		const d1 = float( 9. ).toVar(), d2 = float( 9. ).toVar();
		id.assign( 0. );
		site.assign( vec3( 0., 1., 0. ) );

		Loop( { start: int( - 52 ), end: 52, name: 'k', condition: '<=' }, ( { k } ) => {

			const idx = centre.add( k ).toVar();

			If( idx.lessThan( 0 ).or( idx.greaterThanEqual( int( uCells ) ) ), () => {

				Continue();

			} );

			const q = uSites.sample( vec2( float( idx ).add( .5 ).div( 1024.0 ), .5 ) ).xyz.toVar();
			const d = distance( q, nrm ).toVar();

			If( d.lessThan( d1 ), () => {

				d2.assign( d1 );
				d1.assign( d );
				id.assign( float( idx ) );
				site.assign( q );

			} ).ElseIf( d.lessThan( d2 ), () => {

				d2.assign( d );

			} );

		} );

		edge.assign( d2.sub( d1 ).div( max( uCellA, 1e-4 ) ) );

	}, 'void' );

	const main = /*@__PURE__*/ Fn( () => {

		const V = safeDir( uCam.sub( vWp ), vec3( 0., 0., 1. ) ).toVar();
		const fres = sub( 1., abs( dot( vN, V ) ) ).toVar();

		If( uProcedural.greaterThanEqual( 21 ), () => {

			// A drawn symbol: a flat fill inside an ink outline, coloured by
			// material.symbol and never by the ramp. Mixing a gradient through it is
			// exactly what stops it reading as drawn.

			const sp = vUv.sub( .5 ).mul( 2. ).toVar();
			const body = property( 'float' ), ring = property( 'float' ), ink = property( 'float' ), high = property( 'float' ), hot = property( 'float' );
			symbolShape( sp, uSymbolSeed, uProcedural, body, ring, ink, high, hot );
			const a = body.mul( uOpacity ).add( ring ).mul( uFlicker ).toVar();

			If( body.add( ring ).lessThan( .004 ).or( a.lessThan( .003 ) ), () => {

				Discard();

			} );

			const tone = screentoneAt( vUv, vObj ).mul( body ).toVar();
			const col = mix( uSymFill, uScreenCol, tone.mul( .55 ) ).toVar();
			col.assign( mix( col, uSymHigh, high.mul( .85 ) ) );
			col.assign( mix( col, uSymHot, hot.mul( uSymHotA ) ) );
			col.assign( mix( col, uSymOutline, ring ) );
			col.assign( mix( col, uSymInk, ink ) );
			col.mulAssign( add( 1., hot.mul( uSymHotA ).mul( uSymHotI ) ) );

			If( uChannel.greaterThanEqual( 0. ), () => {

				col.mulAssign( select( uChannel.lessThan( .5 ), vec3( 1., 0., 0. ), select( uChannel.lessThan( 1.5 ), vec3( 0., 1., 0. ), vec3( 0., 0., 1. ) ) ) );

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, clamp( a, 0., 1. ) ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), clamp( a, 0., 1. ) ) );

			} );

		} ).ElseIf( uSlab.equal( 1 ), () => {

			// Hard tiers, not a gaussian: a soft-edged wide slab reads as fog, a tiered
			// one as a bar. The tiers are listed outermost first, so each later one
			// paints over the one before it and the last entry is the hot core.

			const y = abs( vUv.y.sub( .5 ) ).mul( uThickness ).toVar();

			// world metres from the axis

			const hmax = max( uSlabTier.element(0).x, 1e-4 ).toVar();
			const c = vec3( 0. ).toVar();
			const a = float( 0. ).toVar();

			Loop( 4, ( { i } ) => {

				If( i.greaterThanEqual( uSlabN ), () => {

					Break();

				} );

				const h = max( uSlabTier.element( i ).x, 1e-4 ).toVar();
				const e = h.mul( .08 ).toVar();

				// the 8% edge

				const w = sub( 1., smoothstep( h.sub( e ), h.add( e ), y ) ).toVar();
				c.assign( mix( c, uSlabTier.element( add( 4, i ) ).rgb, w ) );
				a.addAssign( w.mul( uSlabTier.element( i ).y ) );

			} );

			// A faint gaussian seat under the tiers, so the bar is not pasted on.

			a.addAssign( exp( y.div( hmax ).negate().mul( y.div( hmax ) ).mul( 1.6 ) ).mul( .10 ) );

			// Soft at both ends: a squared-off tip reads as a card, and a column must
			// never end in a flat cap.

			a.mulAssign( smoothstep( 0., .07, vAlong ).mul( sub( 1., smoothstep( .88, 1.02, vAlong ) ) ) );
			a.mulAssign( stripeTerm( vAlong.mul( uLength ), vUv.y, uTime ).mul( uOpacity ).mul( uFlicker ) );

			If( a.lessThan( .002 ), () => {

				Discard();

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( c, a ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( c.mul( a ), a ) );

			} );

		} ).ElseIf( uFrame.equal( 1 ), () => {

			// The whole rim, read off one signed distance: the drawn arc (the reveal
			// front runs the PERIMETER, so the doorway draws itself up both sides at
			// once), the solid bar, the hot spine down the middle of it, the thinner
			// parallel line inside it and the halo skirts around the lot. The ramp is
			// sampled at four fixed keys, which is what lets ONE ramp carry a rim.

			const H = vec2( max( uRadius.sub( uThickness.mul( .5 ) ), 1e-3 ), max( uLength.mul( .5 ).sub( uThickness.mul( .5 ) ), 1e-3 ) ).toVar();
			const d = sdRoundRect( vObj.xy, H, min( uCorner, min( H.x, H.y ).mul( .98 ) ) ).toVar();
			const ad = abs( d ).toVar();
			const u = framePerimeter( vObj.xy, H ).toVar();
			const front = mix( uRevealFrom, uRevealTo, clamp( uLayerU, 0., 1. ) ).toVar();
			const gate = select( uHasReveal.equal( 1 ), sub( 1., smoothstep( front, front.add( max( uRevealWidth, 1e-3 ) ), u ) ), 1. ).toVar();

			// Squared, never safePow: safePow clamps a negative base to an epsilon,
			// which would make every pixel INSIDE the frame read as the spine.

			const lk = u.sub( front ).div( max( uRevealWidth, 1e-3 ) ).toVar();
			const lead = select( uHasReveal.equal( 1 ), exp( lk.negate().mul( lk ).mul( 4. ) ).mul( step( .004, front ) ).mul( step( front, .996 ) ), 0. ).toVar();
			const bead = beadTerm( u, uTime ).toVar();
			const hw = uThickness.mul( .5 ).toVar();
			const core = sub( 1., smoothstep( hw.sub( uThickness.mul( .08 ) ), hw.add( uThickness.mul( .05 ) ), ad ) ).toVar();
			const sk = d.div( max( uSdfSpine, 1e-4 ) ).toVar();
			const ik = d.add( uSdfInnerOff ).div( max( uSdfInnerW, 1e-4 ) ).toVar();
			const spine = select( uSdfSpine.greaterThan( 0. ), exp( sk.negate().mul( sk ) ), 0. ).toVar();
			const inner = select( uSdfInnerW.greaterThan( 0. ), exp( ik.negate().mul( ik ) ), 0. ).toVar();
			const halo = float( 0. ).toVar();

			Loop( 3, ( { i } ) => {

				If( i.greaterThanEqual( uSdfN ), () => {

					Break();

				} );

				halo.addAssign( exp( ad.negate().div( max( uSdfHalo.element( i ).x, 1e-4 ) ) ).mul( uSdfHalo.element( i ).y ) );

			} );

			const lit = bead.mul( .11 ).add( lead.mul( .16 ) ).toVar();
			const col = rampColor( .22 ).mul( core ).mul( uSdfCore.add( lit ) ).add( rampColor( 0. ).mul( spine ).mul( add( 1., bead.mul( .38 ) ).add( lead.mul( 1.5 ) ) ) ).add( rampColor( .45 ).mul( inner ) ).add( rampColor( 1. ).mul( halo ) ).toVar();
			const a = clamp( core.mul( uSdfCore ).add( spine.mul( .8 ) ).add( inner.mul( .9 ) ).add( halo ), 0., 1. ).toVar();
			a.mulAssign( gate.mul( uOpacity ).mul( uFlicker ).mul( stripeTerm( u.mul( max( mul( 2., uRadius.add( uLength.mul( .5 ) ) ), 1e-3 ) ), ad.div( max( hw, 1e-4 ) ), uTime ) ) );
			col.mulAssign( gate );

			If( uReflect.equal( 1 ), () => {

				col.assign( mix( col, uReflectTint, .45 ) );
				a.mulAssign( uReflectOpacity.mul( sub( 1., smoothstep( 0., max( sub( 1., uReflectBlur ), .05 ), clamp( vUv.y, 0., 1. ) ) ) ) );

			} );

			If( uChannel.greaterThanEqual( 0. ), () => {

				col.mulAssign( select( uChannel.lessThan( .5 ), vec3( 1., 0., 0. ), select( uChannel.lessThan( 1.5 ), vec3( 0., 1., 0. ), vec3( 0., 0., 1. ) ) ) );

			} );

			If( a.lessThan( .003 ), () => {

				Discard();

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, a ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), a ) );

			} );

		} ).ElseIf( uProcedural.equal( 19 ), () => {

			// The polar swirl disc. The band mask IS the silhouette, the detail spiral
			// shades it from inside, and the ramp (space "radial") runs from the hot
			// centre out to the haze. The erosion curve raises the mask threshold, so
			// the disc TEARS from the outside in on the dissipate rather than dimming.

			const q = vUv.sub( .5 ).mul( 2. ).toVar();
			const dd = length( q ).toVar();

			If( dd.greaterThan( 1. ), () => {

				Discard();

			} );

			const sw = swirlDisc( q, uTime ).toVar();
			const outer = sub( 1., smoothstep( .78, 1.04, dd ) ).toVar();

			// The erosion curve is the mask THRESHOLD over the layer's own progress
			// (not over an along coordinate, which a disc does not have), and
			// erosion.rimBias biases it outward: the disc tears from the rim inward on
			// the dissipate instead of dimming as a whole.

			const thr = curveC( clamp( uLayerU, 0., 1. ) ).add( uRimBias.mul( .55 ).mul( smoothstep( .2, 1., dd ) ) ).toVar();
			const mask = smoothstep( thr, thr.add( max( uErodeSoft, .01 ) ), sw.x ).mul( outer ).toVar();

			// The lit term is what the ramp reads: 0 at the hot centre, 1 at the haze.

			const litR = div( 1., add( 1., safePow( dd.mul( 1.95 ), 2. ) ) ).toVar();
			const col = rampColor( clamp( sub( 1., litR ), 0., 1. ) ).toVar();

			// The detail spiral only ever SHADES: a second mask here would read as a
			// copy of the first rather than as structure inside the arms.

			col.assign( mix( col.mul( .30 ), col, sw.y ) );
			col.mulAssign( add( .62, mul( .5, litR ) ) );
			col.addAssign( rampColor( 0. ).mul( smoothstep( .86, 1., litR ) ).mul( .18 ) );
			const a = mask.mul( uOpacity ).mul( uFlicker ).toVar();

			If( uReflect.equal( 1 ), () => {

				col.assign( mix( col, uReflectTint, .45 ) );
				a.mulAssign( uReflectOpacity );

			} );

			If( uChannel.greaterThanEqual( 0. ), () => {

				col.mulAssign( select( uChannel.lessThan( .5 ), vec3( 1., 0., 0. ), select( uChannel.lessThan( 1.5 ), vec3( 0., 1., 0. ), vec3( 0., 0., 1. ) ) ) );

			} );

			If( a.lessThan( .003 ), () => {

				Discard();

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, a ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), a ) );

			} );

		} ).Else( () => {

			// material.flow: the multi-layer panning surface REPLACES material.noise as
			// the field, and (with ramp.space "surface") as the ramp key: stop t=0 is the
			// open surface and t=1 the patch that covers it.

			const flowMask = float( - 1. ).toVar();

			If( uFlowOn.equal( 1 ), () => {

				const vd = viewMatrix.mul( vec4( safeDir( uCam.sub( vWp ), vec3( 0., 0., 1. ) ), 0. ) ).xy.toVar();
				const field = flowField( vUv.mul( vec2( uRadius.mul( 2. ), uLength ) ), uTime, vd ).toVar();
				flowMask.assign( smoothstep( uFlowCut.x, uFlowCut.x.add( max( uFlowCut.y, 1e-3 ) ), field ) );

			} );

			const n = float( .5 ).toVar();

			If( uFlowOn.equal( 1 ), () => {

				n.assign( flowMask );

			} ).ElseIf( uShell.equal( 1 ), () => {

				n.assign( add( .5, mul( .5, mul( .7, snoise( vec3( vUv.x.mul( 3.0 ), vAlong.mul( 3.2 ).sub( uTime.mul( 2.0 ) ), .7 ) ) ).add( mul( .3, snoise( vec3( vUv.x.mul( 6.0 ), vAlong.mul( 6.0 ).sub( uTime.mul( 3.1 ) ), 2.3 ) ) ) ) ) ) );

				If( uHasNoise.equal( 1 ), () => {

					const ntex = uNoise.sample( vec2( vUv.x.mul( uNoiseScale.x ), vAlong.mul( uNoiseScale.y ).add( uNoisePan.y.mul( uTime ) ) ) ).r.toVar();
					n.assign( n.mul( .75 ).add( ntex.mul( .25 ) ) );

				} );

			} ).ElseIf( uHasNoise.equal( 1 ), () => {

				n.assign( uNoise.sample( vUv.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ) ).r );

			} ).Else( () => {

				n.assign( add( .5, mul( .5, fbm3( vec3( vUv.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ), 3.1 ) ) ) ) );

			} );

			const shape = float( 1. ).toVar();
			const dims = vec3( uRadius, uLength, uThickness ).toVar();

			If( uBolt.equal( 1 ), () => {

				// A bolt is a tube: its normals face the camera along the centreline and
				// graze it at the silhouette, which is exactly the falloff a hot core with
				// a soft edge wants. Without it the tube reads as a flat blown-out slab.

				shape.assign( safePow( abs( dot( vN, V ) ), .65 ) );

			} ).ElseIf( uShell.equal( 1 ).or( uHasLattice.equal( 1 ) ).or( uBand.equal( 1 ) ), () => {

				// A closed body, a lattice shell and a belt all have a silhouette of their
				// own, so a BILLBOARD silhouette (modes 0-3 and 12-16: a soft disc, a
				// flame, a glint, a sigil) has nothing to describe — applied here it would
				// cut the surface down to a disc in UV space, which on a sphere is a
				// crescent and on a strip is a blob halfway along it. Only the surface
				// patterns (4-11) apply, on (angle, distance along) for the shell and on
				// the mesh's own UV for the other two.

				If( uProcedural.greaterThanEqual( 4 ).and( uProcedural.lessThan( 12 ) ), () => {

					shape.assign( select( uShell.equal( 1 ), proceduralShape( vec2( vRing, vAlong ).sub( .5 ), vec2( vRing, vAlong ), n, uTime, fres, uMaskScale, dims, uProcedural ), proceduralShape( vUv.sub( .5 ), vUv, n, uTime, fres, uMaskScale, dims, uProcedural ) ) );

				} );

			} ).Else( () => {

				// noise.distortionPan scrolls the field that drives the distortion.

				const nd = n.toVar();
				const dp = uDistortPan.mul( uTime ).toVar();

				If( dot( dp, dp ).greaterThan( 0. ), () => {

					nd.assign( select( uHasNoise.equal( 1 ), uNoise.sample( vUv.mul( uNoiseScale ).add( uNoisePan.mul( uTime ) ).add( dp ) ).r, add( .5, mul( .5, fbm3( vec3( vUv.mul( uNoiseScale ).mul( 2. ).add( uNoisePan.mul( uTime ) ).add( dp ), 3.1 ) ) ) ) ) );

				} );

				const duv = vUv.add( nd.sub( .5 ).mul( uDistort ) ).toVar();
				const mc = cos( uMaskRot ).toVar(), ms = sin( uMaskRot ).toVar();
				const muv = mat2( mc, ms.negate(), ms, mc ).mul( duv.sub( .5 ) ).add( .5 ).mul( uMaskScale ).add( uMaskPan ).toVar();

				If( uHasMask.equal( 1 ), () => {

					const m = uMask.sample( muv ).toVar();
					shape.assign( m.a.mul( max( m.r, max( m.g, m.b ) ) ) );

				} ).Else( () => {

					shape.assign( proceduralShape( vUv.sub( .5 ), vUv, n, uTime, fres, uMaskScale, dims, uProcedural ) );

				} );

			} );

			// Every ramp space this surface understands, as one function, so
			// material.ramp.blend can ask it twice and mix the two keys.

			const key = property( 'float' );

			If( uRampKeyMode.greaterThan( 4.5 ), () => {

				key.assign( clamp( vUv.y, 0., 1. ) );

			} ).ElseIf( uRampKeyMode.greaterThan( 3.5 ), () => {

				key.assign( clamp( length( vUv.sub( .5 ).mul( 2. ) ), 0., 1. ) );

			} ).ElseIf( uRampKeyMode.greaterThan( 1.5 ).and( uRampKeyMode.lessThan( 2.5 ) ).and( flowMask.greaterThanEqual( 0. ) ), () => {

				key.assign( flowMask );

			} ).ElseIf( uRampKeyMode.greaterThan( 2.5 ), () => {

				key.assign( clamp( vWp.y.sub( uGroundY ).div( max( uHeightSpan, 1e-3 ) ), 0., 1. ) );

			} ).ElseIf( uRampKeyMode.greaterThan( 1.5 ), () => {

				key.assign( clamp( vAlong.mul( 1.08 ).add( n.sub( .5 ).mul( .35 ).mul( smoothstep( .15, .7, vAlong ) ) ), 0., 1. ) );

			} ).ElseIf( uRampKeyMode.greaterThan( 0.5 ), () => {

				key.assign( clamp( uLayerU, 0., 1. ) );

			} ).Else( () => {

				key.assign( clamp( vAlong, 0., 1. ) );

			} );

			If( uRampBlendWeight.greaterThan( 0. ), () => {

				const bkey = property( 'float' );

				If( uRampBlendMode.greaterThan( 4.5 ), () => {

					bkey.assign( clamp( vUv.y, 0., 1. ) );

				} ).ElseIf( uRampBlendMode.greaterThan( 3.5 ), () => {

					bkey.assign( clamp( length( vUv.sub( .5 ).mul( 2. ) ), 0., 1. ) );

				} ).ElseIf( uRampBlendMode.greaterThan( 1.5 ).and( uRampBlendMode.lessThan( 2.5 ) ).and( flowMask.greaterThanEqual( 0. ) ), () => {

					bkey.assign( flowMask );

				} ).ElseIf( uRampBlendMode.greaterThan( 2.5 ), () => {

					bkey.assign( clamp( vWp.y.sub( uGroundY ).div( max( uHeightSpan, 1e-3 ) ), 0., 1. ) );

				} ).ElseIf( uRampBlendMode.greaterThan( 1.5 ), () => {

					bkey.assign( clamp( vAlong.mul( 1.08 ).add( n.sub( .5 ).mul( .35 ).mul( smoothstep( .15, .7, vAlong ) ) ), 0., 1. ) );

				} ).ElseIf( uRampBlendMode.greaterThan( 0.5 ), () => {

					bkey.assign( clamp( uLayerU, 0., 1. ) );

				} ).Else( () => {

					bkey.assign( clamp( vAlong, 0., 1. ) );

				} );

				key.assign( mix( key, bkey, uRampBlendWeight ) );

			} );

			// The rim term is attenuated near the start of a mesh's axis so a bar does not
			// flare at its root; a lattice body has no such root, and its rim is exactly
			// what carries the silhouette where the cells fade out, so it is exempt.

			If( uHasFresnel.equal( 1 ), () => {

				key.addAssign( safePow( fres, uFresnelPower ).mul( uFresnelStrength ).mul( select( uHasLattice.equal( 1 ), 1., smoothstep( 0., .4, vAlong ) ) ) );

			} );

			key.addAssign( uDisplaceShift.mul( vLobe ) );

			// lensFlare and radialRays supply their own key, radially: ramp stop t=0 is
			// the hot core and t=1 the outer halo, so one card is both.

			const pk = proceduralKey( vUv.sub( .5 ), uTime, uProcedural ).toVar();

			If( pk.greaterThanEqual( 0. ), () => {

				key.assign( pk );

			} );

			key.assign( clamp( key, 0., 1. ) );
			const alpha = uOpacity.toVar();
			const rim = float( 0. ).toVar();

			If( uUseErosion.equal( 1 ), () => {

				// The field stays mostly low frequency so a tail carves into a few long
				// tongues instead of confetti.

				const low = add( .5, mul( .5, snoise( vec3( vUv.x.mul( 1.15 ), vAlong.mul( 1.25 ).sub( uTime.mul( 1.5 ) ), 4.1 ) ) ) ).toVar();
				const field = select( uShell.equal( 1 ), mul( .25, low ).add( mul( .75, n ) ).mul( .9 ).add( .2 ), shape.mul( add( .35, n.mul( .9 ) ) ) ).toVar();
				const th = max( 0., curveC( vAlong ).sub( vLobe.mul( uProtect ) ).add( fres.mul( uRimBias ).mul( .5 ) ) ).toVar();
				const er = smoothstep( th, th.add( uErodeSoft ), field ).toVar();

				If( er.lessThan( .01 ), () => {

					Discard();

				} );

				rim.assign( smoothstep( th.sub( uEdgeW ), th.add( uErodeSoft.mul( .5 ) ), field ).sub( er ) );
				alpha.mulAssign( er );

			} );

			// shape is 1 on an analytic shell with procedural "none", so this is a no-op
			// there and the procedural pattern applies everywhere else.

			alpha.mulAssign( shape );

			// material.stripes: panning hard bands on the along coordinate in world
			// metres, so a beam that extends does not squash its own bands.
			// material.flicker: the hashed per-step multiplier, computed on the CPU.

			alpha.mulAssign( stripeTerm( vAlong.mul( uLength ), vRing, uTime ).mul( uFlicker ) );

			// --- reveal / lattice / ground proximity / ripples ------------------------
			// Every one of them is a pure function of (object position, layer time), so a
			// seek lands on exactly the cells, front and rings playback would have drawn.

			const objN = safeDir( vObj, vec3( 0., 1., 0. ) ).toVar();

			// The front travels from reveal.from to reveal.to over the layer's own 0..1
			// progress; values outside 0..1 finish a reveal early inside a longer layer.

			const front = mix( uRevealFrom, uRevealTo, clamp( uLayerU, 0., 1. ) ).toVar();
			const ndv = abs( dot( vN, V ) ).toVar();

			// Analytic ground proximity: no depth texture, so it cannot flicker.

			const nearGround = select( uHasPlaneGlow.equal( 1 ), smoothstep( uPlaneDist, 0., vWp.y.sub( uGroundY ) ), 0. ).toVar();
			const latticeVis = float( 0. ).toVar();
			const latticeCol = vec3( 0. ).toVar();

			If( uHasLattice.equal( 1 ), () => {

				const site = property( 'vec3' );
				const id = property( 'float' ), edge = property( 'float' );
				cellLookup( objN, site, id, edge );
				const hc = latHash( id.mul( 1.7 ).add( .31 ) ).toVar();

				// The reveal keys on the CELL, not the pixel, so a cell lights up whole.

				const cellKey = sub( 1., site.y ).mul( .5 ).toVar();
				const on = select( uHasReveal.equal( 1 ), sub( 1., smoothstep( front.sub( .04 ), front.add( .02 ), cellKey ) ), 1. ).toVar();
				const band = select( uHasReveal.equal( 1 ), sub( 1., smoothstep( 0., max( uRevealWidth, 1e-3 ), abs( cellKey.sub( front ) ) ) ), 0. ).toVar();
				const off = select( uHasDissolve.equal( 1 ), sub( 1., smoothstep( hc.mul( uDisStagger ), hc.mul( uDisStagger ).add( uDisSoft ), max( 0., clamp( uLayerU, 0., 1. ).sub( uDisStart ) ) ) ), 1. ).toVar();
				const cellOn = on.mul( off ).toVar();

				// Outward pulse from the crown, each cell on its own hashed phase.

				const pulse = add( .5, mul( .5, sin( uTime.mul( uPulseSpeed ).sub( sub( 1., site.y ).mul( 2.6 ) ).add( hc.mul( uPhaseJitter ).mul( 6.2831853 ) ) ) ) ).toVar();

				// Expanding great-circle ripples brighten whatever they cross.

				const rip = float( 0. ).toVar();

				Loop( 4, ( { i } ) => {

					If( i.greaterThanEqual( uRippleN ), () => {

						Break();

					} );

					const age = uTime.sub( uRipple.element( i ).w ).toVar();

					If( age.lessThan( 0. ), () => {

						Continue();

					} );

					const gc = acos( clamp( dot( objN, uRipple.element( i ).xyz ), - 1., 1. ) ).toVar();
					const r = age.mul( uRipple.element( add( 4, i ) ).x ).toVar();
					rip.addAssign( sub( 1., smoothstep( 0., max( uRipple.element( add( 4, i ) ).y, 1e-3 ), abs( gc.sub( r ) ) ) ).mul( exp( age.negate().mul( uRipple.element( add( 4, i ) ).z ) ) ) );

				} );

				const gap = smoothstep( uLatGap, uLatEdge, edge ).toVar();
				const line = smoothstep( uLatEdge.mul( 1.7 ), uLatEdge, edge ).mul( gap ).toVar();
				const fill = smoothstep( uLatEdge.mul( 1.1 ), uLatEdge.mul( 2. ), edge ).toVar();

				// At grazing angles the cells compress below a pixel; fade them out and let
				// the fresnel rim carry the silhouette instead of letting them shimmer.

				const graze = smoothstep( .03, max( uGrazeFade, .04 ), ndv ).toVar();
				const soft = safePow( sub( 1., ndv ), 2.2 ).toVar();
				latticeVis.assign( add( .26, mul( .74, max( soft.mul( graze ), nearGround ) ) ).mul( graze ) );
				const bright = add( .70, mul( .55, pulse ) ).add( rip.mul( .9 ) ).mul( cellOn ).toVar();
				latticeCol.assign( uTileCol.mul( fill ).mul( bright ).mul( latticeVis ).mul( .62 ).add( uLatEdgeCol.mul( line ).mul( bright ).mul( latticeVis ).mul( .46 ) ).add( vec3( .90, 1., .98 ).mul( band ).mul( fill.mul( .35 ).add( line.mul( .8 ) ) ).mul( .55 ).mul( graze ) ) );

				// The translucent body under the cells: lit where a cell is on, dim where
				// the lattice has not arrived or has already dissolved.

				alpha.mulAssign( mix( .28, 1., max( cellOn, soft.mul( graze ) ) ) );

			} ).ElseIf( uHasReveal.equal( 1 ), () => {

				const key2 = select( uRevealMode.equal( 0 ), length( vObj.xy ), sub( 1., objN.y ).mul( .5 ) ).toVar();
				alpha.mulAssign( sub( 1., smoothstep( front.sub( .02 ), front.add( .03 ), key2 ) ) );
				alpha.mulAssign( add( 1., sub( 1., smoothstep( 0., max( uRevealWidth, 1e-3 ), abs( key2.sub( front ) ) ) ).mul( .9 ) ) );

			} );

			If( uBand.equal( 1 ), () => {

				// A spherical belt: bright stripes running ALONG it, soft edges ACROSS it,
				// and the far arc dimmed so the strip reads as one ring around the body
				// rather than as two unrelated arcs.

				alpha.mulAssign( add( .90, mul( .10, sin( vUv.x.mul( max( uBandStripes, 1. ) ).mul( 6.2831853 ).sub( uTime.mul( 2. ) ) ) ) ) );
				alpha.mulAssign( smoothstep( 0., .07, vUv.y ).mul( smoothstep( 1., .93, vUv.y ) ) );
				alpha.mulAssign( select( gl_FrontFacing, 1., .85 ) );

			} );

			const col = rampColor( key ).toVar();

			// material.creases: a second, higher-frequency surface field that darkens
			// narrow folds. A smooth body with no creases reads as plastic; the folds are
			// what make it read as a skin under tension.

			If( uCreaseOn.equal( 1 ), () => {

				const gr = add( .5, mul( .5, snoise( vec3( vRing.mul( uCrease.x ), vAlong.mul( uCrease.x ).mul( .185 ).sub( uTime.mul( .9 ) ), 5.3 ) ) ) ).toVar();
				const crease = smoothstep( .44, .30, gr ).mul( smoothstep( uCrease.z, uCrease.z.add( .09 ), vAlong ) ).toVar();
				col.mulAssign( mix( 1., sub( 1., clamp( uCrease.y, 0., 1. ) ), crease ) );

			} );

			// material.streaks: thin HARD bands on the ramp key. The radiate flag is the
			// difference between rings and speed lines — false runs them round the body,
			// true keys them on the angular coordinate so they run back from the nose,
			// which is what reads as flowing water rather than as a painted barcode.

			If( uStreakOn.equal( 1 ), () => {

				const key_ = select( uStreakRadiate.equal( 1 ), vRing.mul( uStreakA.x ).add( vAlong.mul( .9 ) ), vAlong.mul( uStreakA.x ) ).toVar();
				const band = fract( key_.sub( uTime.mul( uStreakA.y ) ).add( n.sub( .5 ).mul( .10 ) ) ).toVar();
				const stripe = smoothstep( uStreakA.z, uStreakA.z.mul( .35 ), abs( band.sub( .5 ) ) ).toVar();
				const seg = select( uStreakA.w.greaterThan( 0. ), smoothstep( .34, .58, add( .5, mul( .5, snoise( vec3( vRing.mul( uStreakA.w ).mul( 6.2831853 ), vAlong.mul( .35 ), uTime.mul( .3 ) ) ) ) ) ), 1. ).toVar();
				const fade = smoothstep( uStreakB.y, uStreakB.y.add( .12 ), vAlong ).mul( sub( 1., smoothstep( max( uStreakB.z.sub( .40 ), uStreakB.y ), uStreakB.z, vAlong ) ) ).toVar();
				col.addAssign( uStreakCol.mul( stripe ).mul( seg ).mul( fade ).mul( uStreakB.x ) );

			} );

			// The ramp carries the translucent body AND the fresnel rim, so it is only
			// dimmed by the lattice's visibility, never multiplied away by it: the rim has
			// to survive exactly where the cells compress below a pixel.

			If( uHasLattice.equal( 1 ), () => {

				col.assign( col.mul( mix( .35, 1., latticeVis ) ).add( latticeCol ) );

			} );

			If( uHasPlaneGlow.equal( 1 ), () => {

				col.addAssign( uPlaneCol.mul( nearGround ).mul( uPlaneI ) );

			} );

			If( uBand.equal( 1 ), () => {

				col.mulAssign( select( gl_FrontFacing, 1., .42 ) );

			} );

			If( uShell.equal( 1 ), () => {

				col.mulAssign( mix( add( 1., n.sub( .5 ).mul( .18 ) ), 1., smoothstep( .08, .45, vAlong ) ) );
				alpha.mulAssign( sub( 1., smoothstep( .5, 1., vAlong ).mul( .5 ) ) );

			} );

			col.addAssign( uEdgeCol.mul( uEdgeI ).mul( rim ).mul( sub( 1., smoothstep( .5, .95, vAlong ) ) ) );

			// kind "reflection": the mirrored copy is washed toward its tint and fades
			// with depth below the plane, so the floor catches a smear and not a twin.

			If( uReflect.equal( 1 ), () => {

				col.assign( mix( col, uReflectTint, .45 ) );

				// vUv.y 0 is the end of the card that meets the plane, so the smear is
				// strongest at the contact and gone by the far end.

				alpha.mulAssign( uReflectOpacity.mul( sub( 1., smoothstep( 0., max( sub( 1., uReflectBlur ), .05 ), clamp( vUv.y, 0., 1. ) ) ) ) );

			} );

			// One channel per copy: the three masked copies sum back to the original
			// colour at offset 0, so this is a split rather than a tint.

			If( uChannel.greaterThanEqual( 0. ), () => {

				col.mulAssign( select( uChannel.lessThan( .5 ), vec3( 1., 0., 0. ), select( uChannel.lessThan( 1.5 ), vec3( 0., 1., 0. ), vec3( 0., 0., 1. ) ) ) );

			} );

			If( alpha.lessThan( .002 ), () => {

				Discard();

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, alpha ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( alpha ), alpha ) );

			} );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function blobVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const vN = bindings.vN;
const vV = bindings.vV;
const vWp = bindings.vWp;
const vLobe = bindings.vLobe;
const uTime = bindings.uTime;
const uNoiseSpeed = bindings.uNoiseSpeed;
const aLobeA = bindings.aLobeA;
const aLobeB = bindings.aLobeB;
const aLobeC = bindings.aLobeC;
const aLobeP = bindings.aLobeP;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, add, clamp, smoothstep, mix, cos, sin, mat2, cross, If, mat3, cameraPosition } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const lobeR = /*@__PURE__*/ Fn( ( [ n ] ) => {

		const q = n.mul( aLobeA.z ).add( vec3( aLobeA.x.mul( 7.3 ), aLobeA.x.mul( 3.1 ).sub( uTime.mul( uNoiseSpeed ) ), aLobeA.x.mul( 11.7 ) ) ).toVar();
		const f = mul( .6, snoise( q ) ).add( mul( .3, snoise( q.mul( 2.1 ).add( 5. ) ) ) ).add( mul( .15, snoise( q.mul( 4.3 ).add( 11. ) ) ) ).toVar();

		// Positive bias: bumps push OUT of the sphere, they never dent it inward.


		return add( 1., aLobeA.y.mul( mul( .45, f ).add( mul( .55, abs( f ) ) ) ) );

	} );

	const lobeP = /*@__PURE__*/ Fn( ( [ n ] ) => {

		const p = n.mul( lobeR( n ) ).toVar();
		const s = clamp( p.y.mul( .5 ).add( .5 ), 0., 1. ).toVar();
		p.xz.mulAssign( mix( 1., sub( 1., aLobeB.y ), smoothstep( .2, 1., s ) ) );
		const a = aLobeB.x.mul( p.y ).toVar();
		p.xy.assign( mat2( cos( a ), sin( a ).negate(), sin( a ), cos( a ) ).mul( p.xy ) );
		p.xy.assign( mat2( cos( aLobeB.z ), sin( aLobeB.z ).negate(), sin( aLobeB.z ), cos( aLobeB.z ) ).mul( p.xy ) );
		p.y.mulAssign( aLobeA.w );

		return p;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		vLobe.assign( aLobeC.xyz );
		const n = safeDir( position, vec3( 0., 1., 0. ) ).toVar();
		const p = lobeP( n ).toVar();

		// Finite-difference normal in the tangent plane of the unit sphere.

		const up = select( abs( n.y ).lessThan( .9 ), vec3( 0., 1., 0. ), vec3( 1., 0., 0. ) ).toVar();
		const t1 = safeDir( cross( n, up ), vec3( 1., 0., 0. ) ).toVar();
		const t2 = safeDir( cross( n, t1 ), vec3( 0., 0., 1. ) ).toVar();
		const e = float( .055 ).toVar();
		const nrm = safeDir( cross( lobeP( safeDir( n.add( t1.mul( e ) ), n ) ).sub( p ), lobeP( safeDir( n.add( t2.mul( e ) ), n ) ).sub( p ) ), n ).toVar();

		If( dot( nrm, n ).lessThan( 0. ), () => {

			nrm.assign( nrm.negate() );

		} );

		// aLobeB.w inflates the outline hull in the lobe's own unit space.

		p.addAssign( nrm.mul( aLobeB.w ) );

		// The lobe's own placement, uniform in scale so the normal survives it.

		p.assign( p.mul( aLobeP.w ).add( aLobeP.xyz ) );
		const wp = modelMatrix.mul( vec4( p, 1. ) ).toVar();
		vWp.assign( wp.xyz );
		vN.assign( safeDir( mat3( modelMatrix ).mul( nrm ), vec3( 0., 1., 0. ) ) );
		vV.assign( safeDir( cameraPosition.sub( wp.xyz ), vec3( 0., 0., 1. ) ) );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( wp ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function blobFragment( bindings ) {
const vN = bindings.vN;
const vV = bindings.vV;
const vWp = bindings.vWp;
const vLobe = bindings.vLobe;
const uShadow = bindings.uShadow;
const uBody = bindings.uBody;
const uHigh = bindings.uHigh;
const uRimCol = bindings.uRimCol;
const uLight = bindings.uLight;
const uBands = bindings.uBands;
const uBandA = bindings.uBandA;
const uBandB = bindings.uBandB;
const uRimPow = bindings.uRimPow;
const uRimAmt = bindings.uRimAmt;
const uFlat = bindings.uFlat;
const uRampKeyMode = bindings.uRampKeyMode;
const uLayerU = bindings.uLayerU;
const uGroundY = bindings.uGroundY;
const uHeightSpan = bindings.uHeightSpan;
const uUseToon = bindings.uUseToon;
const uToonRamp = bindings.uToonRamp;
const uToonShadowScale = bindings.uToonShadowScale;
const uToonHighMix = bindings.uToonHighMix;
const uRampBlendMode = bindings.uRampBlendMode;
const uRampBlendWeight = bindings.uRampBlendWeight;
const uLightOn = bindings.uLightOn;
const uLightFall = bindings.uLightFall;
const uLightPos = bindings.uLightPos;
const uBlendMode = bindings.uBlendMode;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, length, vec3, select, vec4, float, add, div, dot, shadow, sub, pow, step } = TSL;

	const gl_FragColor = property( 'vec4' );

	// material.toon.colorSource "ramp": the BODY band's colour comes from
	// material.ramp evaluated in the ramp's own space, and the other two bands are
	// derived from it, so the cluster still posterises while its base colour grades.
	// blob.lightFrom: a fake POINT light the lobes are shaded toward instead of the
	// parallel world light material.toon carries, plus the far/near brightness bias
	// an "orbit" ring applies to whichever half is currently behind the centre.

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );


	/**
	 * The lobe's ramp key: world height over uHeightSpan (3), the layer's own
	 * progress (1), or the lobe's own 0..1 age — the only scalar a generated lobe
	 * has, which is what "life" and "surface" both fall back to. material.ramp.blend
	 * mixes a second space into it.
	 */

	const lobeRampKey = /*@__PURE__*/ Fn( ( [ mode ] ) => {

		return select( mode.greaterThan( 2.5 ), clamp( vWp.y.sub( uGroundY ).div( max( uHeightSpan, 1e-3 ) ), 0., 1. ), select( mode.greaterThan( 0.5 ).and( mode.lessThan( 1.5 ) ), clamp( uLayerU, 0., 1. ), clamp( vLobe.x, 0., 1. ) ) );

	} );

	const lobeRampKeyBlended = /*@__PURE__*/ Fn( () => {

		const key = lobeRampKey( uRampKeyMode ).toVar();

		If( uRampBlendWeight.greaterThan( 0. ), () => {

			key.assign( mix( key, lobeRampKey( uRampBlendMode ), uRampBlendWeight ) );

		} );

		return key;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		// The outline hull draws flat and unlit whatever else is set.

		If( uFlat.greaterThan( .5 ), () => {

			gl_FragColor.assign( vec4( uShadow.mul( vLobe.y ).mul( vLobe.z ), vLobe.z ) );

		} ).Else( () => {

			const c = property( 'vec3' );

			If( uUseToon.greaterThan( .5 ), () => {

				const N = safeDirLocal( vN ).toVar();

				// Half lambert against the document's FIXED toon light, posterised.
				// A point light is a direction per lobe, not per document, and it falls
				// off: that is what makes a ring of lobes read as lit by the core it
				// circles rather than by the sun.

				const L = safeDirLocal( uLight ).toVar();
				const fall = float( 1. ).toVar();

				If( uLightOn.greaterThan( .5 ), () => {

					const toLight = uLightPos.sub( vWp ).toVar();
					const dist = length( toLight ).toVar();
					L.assign( safeDirLocal( toLight ) );
					fall.assign( div( 1., add( 1., uLightFall.mul( dist ).mul( uLightFall ).mul( dist ) ) ) );

				} );

				const ndl = dot( N, L ).mul( .5 ).add( .5 ).toVar();

				// colorSource "ramp": the three fixed hexes become one continuous body
				// colour plus two derived neighbours. The BANDING is unchanged — the same
				// two thresholds on the same half-lambert — so a cluster that should read
				// cel-shaded still does; only the constant is gone.

				const body = uBody.toVar(), shadow = uShadow.toVar(), high = uHigh.toVar();

				If( uToonRamp.greaterThan( .5 ), () => {

					body.assign( rampColor( lobeRampKeyBlended() ) );
					shadow.assign( mix( body.mul( uToonShadowScale ), uShadow, .25 ) );
					high.assign( mix( body, uHigh, uToonHighMix ) );

				} );

				c.assign( select( uBands.greaterThan( 2.5 ), select( ndl.lessThan( uBandA ), shadow, select( ndl.lessThan( uBandB ), body, high ) ), select( ndl.lessThan( uBandA ), shadow, high ) ) );
				c.addAssign( uRimCol.mul( pow( max( sub( 1., clamp( dot( N, safeDirLocal( vV ) ), 0., 1. ) ), 1e-4 ), uRimPow ) ).mul( uRimAmt ) );
				c.mulAssign( mix( 1., fall, step( .5, uLightOn ) ) );

			} ).Else( () => {

				// No toon: the ramp is the colour source, keyed the usual way.

				c.assign( rampColor( lobeRampKeyBlended() ) );

			} );

			c.mulAssign( vLobe.y );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( c, vLobe.z ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( c.mul( vLobe.z ), vLobe.z ) );

			} );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function crystalVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aDir = bindings.aDir;
const aOrg = bindings.aOrg;
const aLen = bindings.aLen;
const aWid = bindings.aWid;
const aT0 = bindings.aT0;
const aSeed = bindings.aSeed;
const aAlong = bindings.aAlong;
const uTime = bindings.uTime;
const uSpan = bindings.uSpan;
const uGrowDur = bindings.uGrowDur;
const uOvershoot = bindings.uOvershoot;
const uInflate = bindings.uInflate;
const uHasCollapse = bindings.uHasCollapse;
const uCollapseStart = bindings.uCollapseStart;
const uCollapseDur = bindings.uCollapseDur;
const vN = bindings.vN;
const vW = bindings.vW;
const vAlong = bindings.vAlong;
const vSeed = bindings.vSeed;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, smoothstep, If, sin, add, cross, cos } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const main = /*@__PURE__*/ Fn( () => {

		const born = aT0.mul( uSpan ).toVar();

		// easeOutBack: the spike overshoots its length and settles.

		const u = clamp( uTime.sub( born ).div( max( uGrowDur, 1e-4 ) ), 0., 1. ).toVar();
		const s = select( u.greaterThanEqual( 1. ), 1., u.mul( u ).mul( uOvershoot.add( 1. ).mul( u ).sub( uOvershoot ) ) ).toVar();
		s.assign( max( s, 0. ) );

		If( uHasCollapse.greaterThan( .5 ), () => {

			const from = uCollapseStart.mul( uSpan ).add( aSeed.mul( uCollapseDur ).mul( 1.125 ) ).toVar();
			s.mulAssign( sub( 1., smoothstep( from, from.add( max( uCollapseDur, 1e-4 ) ), uTime ) ) );

		} );

		// A slow breath during the hold keeps the silhouette alive at rest.

		s.mulAssign( add( 1., mul( .035, sin( uTime.mul( 2.1 ).add( aSeed.mul( 6.2831853 ) ) ) ) ) );
		const up = safeDir( aDir, vec3( 0., 1., 0. ) ).toVar();
		const ref = select( abs( up.y ).greaterThan( .95 ), vec3( 1., 0., 0. ), vec3( 0., 1., 0. ) ).toVar();
		const rt = safeDir( cross( ref, up ), vec3( 1., 0., 0. ) ).toVar();
		const fw = cross( up, rt ).toVar();

		// Per-instance twist, so neighbouring spikes never share a facet layout.

		const tw = aSeed.mul( 6.2831853 ).toVar(), ct = cos( tw ).toVar(), st = sin( tw ).toVar();
		const pxz = vec2( position.x.mul( ct ).sub( position.z.mul( st ) ), position.x.mul( st ).add( position.z.mul( ct ) ) ).toVar();
		const nxz = vec2( normal.x.mul( ct ).sub( normal.z.mul( st ) ), normal.x.mul( st ).add( normal.z.mul( ct ) ) ).toVar();
		const L = aLen.mul( s ).toVar(), W = aWid.mul( add( .35, mul( .65, s ) ) ).toVar();
		const lp = vec3( pxz.x.mul( W ), position.y.mul( L ), pxz.y.mul( W ) ).toVar();

		// The hull is pushed out in the spike's own frame, scaled with its growth so
		// the line weight stays constant in world metres.

		lp.addAssign( safeDir( vec3( pxz.x, position.y.mul( .25 ), pxz.y ), vec3( 0., 1., 0. ) ).mul( uInflate ).mul( add( .6, mul( .4, s ) ) ) );
		const wp = aOrg.add( rt.mul( lp.x ) ).add( up.mul( lp.y ) ).add( fw.mul( lp.z ) ).toVar();
		const ln = safeDir( vec3( nxz.x.mul( L ), normal.y.mul( W ), nxz.y.mul( L ) ), vec3( 0., 1., 0. ) ).toVar();
		vN.assign( safeDir( rt.mul( ln.x ).add( up.mul( ln.y ) ).add( fw.mul( ln.z ) ), up ) );
		const world = modelMatrix.mul( vec4( wp, 1. ) ).toVar();
		vW.assign( world.xyz );
		vAlong.assign( aAlong );
		vSeed.assign( aSeed );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( world ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function crystalFragment( bindings ) {
const vN = bindings.vN;
const vW = bindings.vW;
const vAlong = bindings.vAlong;
const vSeed = bindings.vSeed;
const uTip = bindings.uTip;
const uFace = bindings.uFace;
const uEdge = bindings.uEdge;
const uCam = bindings.uCam;
const uFresPow = bindings.uFresPow;
const uGlintFreq = bindings.uGlintFreq;
const uGlintSpeed = bindings.uGlintSpeed;
const uOpacity = bindings.uOpacity;
const uFlat = bindings.uFlat;
const uTime = bindings.uTime;
const uBlendMode = bindings.uBlendMode;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, smoothstep, mix, normalize, clamp, add, sin, fract, Discard, If } = TSL;

	const gl_FragColor = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const main = /*@__PURE__*/ Fn( () => {

		// The outline hull draws flat and unlit, dark at the base and cooler at the
		// tip: a separator between overlapping spikes, never a second rim light.

		If( uFlat.greaterThan( .5 ), () => {

			const c = mix( uEdge.mul( .16 ), uEdge.mul( .55 ), smoothstep( .05, .85, vAlong ) ).toVar();
			gl_FragColor.assign( vec4( c.mul( uOpacity ), uOpacity ) );

		} ).Else( () => {

			const n = safeDir( vN, vec3( 0., 1., 0. ) ).toVar();
			const v = safeDir( uCam.sub( vW ), vec3( 0., 0., 1. ) ).toVar();
			const ndv = abs( dot( n, v ) ).toVar();
			const fres = safePow( sub( 1., ndv ), uFresPow ).toVar();

			// A cool ambient wrap: an unlit facet still reads pale, never black.

			const lam = add( .42, mul( .58, clamp( dot( n, normalize( vec3( .25, .9, .4 ) ) ).mul( .5 ).add( .5 ), 0., 1. ) ) ).toVar();
			const col = uFace.mul( add( .30, mul( .50, lam ) ) ).toVar();

			// Tips run vivid, the mid body stays pale, the base stays near white; the
			// tip is darkened as it saturates so ACES does not wash it back out.

			const tip = smoothstep( .18, .92, vAlong ).toVar();
			col.assign( mix( col, uTip.mul( 1.30 ), tip.mul( .94 ) ) );
			col.mulAssign( mix( 1., .55, tip ) );
			col.assign( mix( col, uEdge, fres.mul( .30 ).mul( sub( 1., tip.mul( .55 ) ) ) ) );

			// A narrow specular band sliding along the axis.

			const gb = sin( vAlong.mul( uGlintFreq ).sub( uTime.mul( uGlintSpeed ) ).add( vSeed.mul( 6.2831853 ) ).mul( 3.14159265 ) ).toVar();
			col.addAssign( uEdge.mul( safePow( max( gb, 0. ), 22. ) ).mul( add( .35, mul( .65, fres ) ) ).mul( .9 ) );

			// Internal fracture grain, plus a per-instance brightness so no two spikes
			// catch the light identically.

			const ice = add( .5, mul( .5, fbm3( vec3( vAlong.mul( 9. ).add( vSeed.mul( 17. ) ), vSeed.mul( 31. ).add( vW.y.mul( 4. ) ), 1.7 ) ) ) ).toVar();
			col.mulAssign( add( .88, mul( .26, ice ) ).mul( add( .72, mul( .56, fract( vSeed.mul( 13.7 ).add( 2.1 ) ) ) ) ) );
			col.addAssign( uTip.mul( safePow( sub( 1., ndv ), 3. ) ).mul( .60 ) );
			const a = clamp( add( .80, fres.mul( .20 ) ), 0., 1. ).mul( uOpacity ).toVar();

			If( a.lessThan( .002 ), () => {

				Discard();

			} );

			If( uBlendMode.equal( 1 ), () => {

				gl_FragColor.assign( vec4( col, a ) );

			} ).Else( () => {

				gl_FragColor.assign( vec4( col.mul( a ), a ) );

			} );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function splashVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uSliverScale = bindings.uSliverScale;
const uSliverOffset = bindings.uSliverOffset;
const uSliverRoll = bindings.uSliverRoll;
const vUv = bindings.vUv;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, vec4, vec3, cross, vec2, cos, sin, Fn } = TSL;

	const gl_Position = property( 'vec4' );

	const main = /*@__PURE__*/ Fn( () => {

		vUv.assign( uv );
		const centre = modelMatrix.mul( vec4( 0., 0., 0., 1. ) ).xyz.toVar();
		const right = vec3( viewMatrix.element(0)[ 0 ], viewMatrix.element(1)[ 0 ], viewMatrix.element(2)[ 0 ] ).toVar();
		const up = vec3( viewMatrix.element(0)[ 1 ], viewMatrix.element(1)[ 1 ], viewMatrix.element(2)[ 1 ] ).toVar();
		const fwd = cross( right, up ).toVar();
		const q = vec2( position.x.mul( uSliverScale.x ), position.y.mul( uSliverScale.y ) ).toVar();
		const c = cos( uSliverRoll ).toVar(), s = sin( uSliverRoll ).toVar();
		q.assign( vec2( q.x.mul( c ).sub( q.y.mul( s ) ), q.x.mul( s ).add( q.y.mul( c ) ) ) );
		const o = vec2( uSliverOffset.x, uSliverOffset.y ).toVar();
		const world = centre.add( right.mul( q.x.add( o.x ) ) ).add( up.mul( q.y.add( o.y ) ) ).add( fwd.mul( uSliverOffset.z ) ).toVar();
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( world, 1. ) ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function splashFragment( bindings ) {
const vUv = bindings.vUv;
const uCol = bindings.uCol;
const uDark = bindings.uDark;
const uOpacity = bindings.uOpacity;
const uBlendMode = bindings.uBlendMode;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, smoothstep, mix, vec4, If, Fn } = TSL;

	const gl_FragColor = property( 'vec4' );

	const main = /*@__PURE__*/ Fn( () => {

		// No uShade here: a sliver's backing and body colours are already darkened on
		// the CPU when the pair is built, and this program has never carried the
		// blob's orbit brightness uniform. Multiplying by it was a paste from
		// blobFragmentV2 that left the program unlinkable, so every splash layer's
		// draw was silently skipped.

		const c = mix( uDark, uCol, smoothstep( .0, .55, vUv.y ) ).toVar();

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( c, uOpacity ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( c.mul( uOpacity ), uOpacity ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function ribbonVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const uTime = bindings.uTime;
const uHead = bindings.uHead;
const uTail = bindings.uTail;
const uWidth = bindings.uWidth;
const uMorph = bindings.uMorph;
const uSpread = bindings.uSpread;
const uWidthJitter = bindings.uWidthJitter;
const uPhaseJitter = bindings.uPhaseJitter;
const uTaperHead = bindings.uTaperHead;
const uTaperTail = bindings.uTaperTail;
const uOrientPath = bindings.uOrientPath;
const vSide = bindings.vSide;
const vW = bindings.vW;
const vStrand = bindings.vStrand;
const vFade = bindings.vFade;
const uPathType = bindings.uPathType;
const uPathA = bindings.uPathA;
const uPathB = bindings.uPathB;
const uPathC = bindings.uPathC;
const uPathD = bindings.uPathD;
const uPathW = bindings.uPathW;
const uMorphPathType = bindings.uMorphPathType;
const uMorphPathA = bindings.uMorphPathA;
const uMorphPathB = bindings.uMorphPathB;
const uMorphPathC = bindings.uMorphPathC;
const uMorphPathD = bindings.uMorphPathD;
const uMorphPathW = bindings.uMorphPathW;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, sin, cos, If, cross, normalize, fract, clamp, mix, smoothstep, mat3, cameraPosition } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const pathPoint = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uPathA ).add( mul( 2., m ).mul( u ).mul( uPathB ) ).add( u.mul( u ).mul( uPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uPathD.z ).mul( u ).add( uPathD.w ).toVar();
			const r = uPathD.x.add( uPathW.x.mul( sin( uPathW.y.mul( th ) ) ) ).toVar();
			const y = uPathA.y.add( uPathD.y.mul( u ) ).add( uPathW.x.mul( .5 ).mul( sin( uPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uPathA.x.add( cos( th ).mul( r ) ), y, uPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangent = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPoint( u.add( 1e-3 ) ).sub( pathPoint( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSide = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangent( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUp = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSide( u ), pathTangent( u ) ) );

	} );

	const pathPointM = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const portResult = property( 'vec3' );

		If( uMorphPathType.equal( 1 ), () => {

			const m = sub( 1., u ).toVar();
			portResult.assign( m.mul( m ).mul( uMorphPathA ).add( mul( 2., m ).mul( u ).mul( uMorphPathB ) ).add( u.mul( u ).mul( uMorphPathC ) ) );

		} ).Else( () => {

			const th = mul( 6.28318530718, uMorphPathD.z ).mul( u ).add( uMorphPathD.w ).toVar();
			const r = uMorphPathD.x.add( uMorphPathW.x.mul( sin( uMorphPathW.y.mul( th ) ) ) ).toVar();
			const y = uMorphPathA.y.add( uMorphPathD.y.mul( u ) ).add( uMorphPathW.x.mul( .5 ).mul( sin( uMorphPathW.y.mul( th ).mul( 2. ).add( 1.3 ) ) ) ).toVar();
			portResult.assign( vec3( uMorphPathA.x.add( cos( th ).mul( r ) ), y, uMorphPathA.z.add( sin( th ).mul( r ) ) ) );

		} );

		return portResult;

	} );

	const pathTangentM = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const d = pathPointM( u.add( 1e-3 ) ).sub( pathPointM( u.sub( 1e-3 ) ) ).toVar();
		const l = length( d ).toVar();

		return select( l.greaterThan( 1e-6 ), d.div( l ), vec3( 0., 0., 1. ) );

	} );

	/* Frame at u: side is horizontal (tangent x world up), up completes it. Three
	 * plain functions rather than one with out parameters, so each stays a real
	 * WGSL function instead of being inlined at every call site. */

	const pathSideM = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathTangentM( u ), vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) );

	} );

	const pathUpM = /*@__PURE__*/ Fn( ( [ u ] ) => {

		return normalize( cross( pathSideM( u ), pathTangentM( u ) ) );

	} );


	/** The two paths blended by ribbon.morph.curve, wrapped or clamped per type. */

	const sweep = /*@__PURE__*/ Fn( ( [ u ] ) => {

		const ua = select( uPathType.equal( 1 ), clamp( u, 0., 1. ), fract( u ) ).toVar();
		const ub = select( uMorphPathType.equal( 1 ), clamp( u, 0., 1. ), fract( u ) ).toVar();

		return mix( pathPoint( ua ), pathPointM( ub ), uMorph );

	} );

	const main = /*@__PURE__*/ Fn( () => {

		const w = position.x.toVar(), side = position.y.toVar(), strand = position.z.toVar();
		const hs = ribbonHash( strand.mul( 11.7 ).add( 3.1 ) ).toVar();

		// Window: only [head - tail, head] of the path is ever drawn, offset per
		// strand so the strands braid instead of overlapping exactly.

		const u = uHead.sub( sub( 1., w ).mul( uTail ) ).add( hs.sub( .5 ).mul( uPhaseJitter ).mul( uTail ) ).toVar();
		const p = sweep( u ).toVar();
		const tangent = normalize( sweep( u.add( 1e-3 ) ).sub( sweep( u.sub( 1e-3 ) ) ).add( vec3( 0., 0., 1e-6 ) ) ).toVar();
		const sd = normalize( cross( tangent, vec3( 0., 1., 0. ) ).add( vec3( 1e-5, 0., 0. ) ) ).toVar();
		const upn = normalize( cross( sd, tangent ) ).toVar();
		p.addAssign( sd.mul( hs.sub( .5 ).mul( uSpread ) ).add( upn.mul( ribbonHash( strand.mul( 3.7 ).add( 9.1 ) ).sub( .5 ).mul( uSpread ) ) ) );
		const world = modelMatrix.mul( vec4( p, 1. ) ).toVar();

		// Taper both ends of the window: a squared-off end reads as a card.

		const fade = smoothstep( 0., max( uTaperTail, 1e-4 ), w ).mul( smoothstep( 1., sub( 1., max( uTaperHead, 1e-4 ) ), w ) ).toVar();

		// ribbon.width is the FULL width of one strand at its fattest.

		const strandW = mix( sub( 1., uWidthJitter ), 1., hs ).toVar();
		const half_ = uWidth.mul( .5 ).mul( strandW ).mul( fade ).toVar();
		const worldTangent = normalize( mat3( modelMatrix ).mul( tangent ).add( vec3( 0., 0., 1e-6 ) ) ).toVar();
		const view = normalize( cameraPosition.sub( world.xyz ) ).toVar();
		const across = select( uOrientPath.greaterThan( .5 ), normalize( mat3( modelMatrix ).mul( upn ) ), normalize( cross( worldTangent, view ) ) ).toVar();
		vSide.assign( side );
		vW.assign( w );
		vStrand.assign( hs );
		vFade.assign( fade );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( world.xyz.add( across.mul( side ).mul( half_ ) ), 1. ) ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function ribbonFragment( bindings ) {
const uTime = bindings.uTime;
const uOpacity = bindings.uOpacity;
const uCore = bindings.uCore;
const uBlendMode = bindings.uBlendMode;
const vSide = bindings.vSide;
const vW = bindings.vW;
const vStrand = bindings.vStrand;
const vFade = bindings.vFade;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, Discard, abs, exp, sin, mul, add, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vFade.lessThanEqual( 0. ), () => {

			Discard();

		} );

		// The ramp runs ACROSS the strip: stop t=0 is the core, t=1 the outer edge.

		const q = abs( vSide ).toVar();
		const core = exp( q.negate().mul( q ).mul( 9. ) ).toVar(), halo = exp( q.negate().mul( q ).mul( 1.6 ) ).toVar();
		const flicker = add( .85, mul( .15, sin( vW.mul( 40. ).add( uTime.mul( 6. ) ).add( vStrand.mul( 9. ) ) ) ) ).toVar();
		const col = rampColor( q ).mul( core.mul( 1.5 ).mul( uCore ).add( halo.mul( .30 ) ) ).mul( flicker ).toVar();
		const a = vFade.mul( uOpacity ).mul( core.mul( .9 ).add( halo.mul( .35 ) ) ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function wireBurstVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aDir = bindings.aDir;
const aSeed = bindings.aSeed;
const aKind = bindings.aKind;
const uTime = bindings.uTime;
const uLayerU = bindings.uLayerU;
const uTravel = bindings.uTravel;
const uChannel = bindings.uChannel;
const uSplitOffset = bindings.uSplitOffset;
const uSplitGrowth = bindings.uSplitGrowth;
const vA = bindings.vA;
const vK = bindings.vK;
const uCurveA = bindings.uCurveA;
const uCurveAN = bindings.uCurveAN;
const uCurveAEase = bindings.uCurveAEase;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, clamp, Break, If, mix, Loop, fract, smoothstep, add } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const curveA = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const v = uCurveA.element(0).y.toVar();

		Loop( { start: 1, end: 8 }, ( { i } ) => {

			If( i.greaterThanEqual( uCurveAN ), () => {

				Break();

			} );

			const a = uCurveA.element( i.sub( 1 ) ).x.toVar(), b = uCurveA.element( i ).x.toVar();
			const f = clamp( u.sub( a ).div( max( b.sub( a ), 1e-5 ) ), 0., 1. ).toVar();
			f.assign( mix( f, f.mul( f ).mul( sub( 3., mul( 2., f ) ) ), uCurveAEase ) );
			v.assign( mix( v, uCurveA.element( i ).y, step( a, u ).mul( f ) ) );

		} );

		return v;

	} );


	const main = /*@__PURE__*/ Fn( () => {

		const h = burstHash( aSeed.mul( 1.7 ).add( .3 ) ).toVar();
		const e = clamp( uLayerU, 0., 1. ).toVar();

		// easeOut travel: the burst is fastest on the frame it appears.

		const out1 = sub( 1., safePow( sub( 1., e ), 2.6 ) ).toVar();
		const travel = mix( .40, 1.45, h ).mul( out1 ).mul( uTravel ).mul( mix( 1., 1.50, aKind ) ).toVar();
		const scale = curveA( e ).mul( mix( .75, 1.25, h ) ).toVar();
		const p = aDir.mul( travel ).add( position.mul( scale ) ).toVar();
		vA.assign( sub( 1., smoothstep( .25, 1., e ) ) );
		vK.assign( aKind );
		gl_Position.assign( projectionMatrix.mul( modelViewMatrix ).mul( vec4( p, 1. ) ) );
		vClipPosition.assign( gl_Position );

		If( uChannel.greaterThanEqual( 0. ), () => {

			gl_Position.x.addAssign( uChannel.sub( 1. ).mul( uSplitOffset ).mul( add( 1., uSplitGrowth.mul( e ) ) ).mul( gl_Position.w ) );

		} );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function wireBurstFragment( bindings ) {
const uOpacity = bindings.uOpacity;
const uLayerU = bindings.uLayerU;
const uRampKeyMode = bindings.uRampKeyMode;
const uChannel = bindings.uChannel;
const uBlendMode = bindings.uBlendMode;
const vA = bindings.vA;
const vK = bindings.vK;
const uRamp = bindings.uRamp;
const uRampT = bindings.uRampT;
const uRampN = bindings.uRampN;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, clamp, Break, If, max, smoothstep, mix, Loop, Fn, select, vec3, Discard, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	const rampColor = /*@__PURE__*/ Fn( ( [ u_arg ] ) => {

		const u = u_arg.toVar();
		u.assign( clamp( u, 0., 1. ) );
		const c = uRamp.element(0).rgb.mul( uRamp.element(0).a ).toVar();
		const prev = uRampT.element(0).toVar();

		Loop( { start: 1, end: 6 }, ( { i } ) => {

			If( i.greaterThanEqual( uRampN ), () => {

				Break();

			} );

			const t = uRampT.element( i ).toVar();
			const ci = uRamp.element( i ).rgb.mul( uRamp.element( i ).a ).toVar();
			c.assign( mix( c, ci, smoothstep( prev, max( t, prev.add( 1e-4 ) ), u ) ) );
			prev.assign( t );

		} );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		// ramp.space "surface" keys the ramp on the population (0 outline, 1 spoke);
		// anything else keys it on the layer's own progress.

		const key = select( uRampKeyMode.greaterThan( 1.5 ).and( uRampKeyMode.lessThan( 2.5 ) ), vK, clamp( uLayerU, 0., 1. ) ).toVar();
		const col = rampColor( key ).toVar();

		// A spoke is a supporting line, not the shape: it draws a quarter dimmer.

		const a = vA.mul( uOpacity ).mul( mix( 1., .75, vK ) ).toVar();

		If( uChannel.greaterThanEqual( 0. ), () => {

			col.mulAssign( select( uChannel.lessThan( .5 ), vec3( 1., 0., 0. ), select( uChannel.lessThan( 1.5 ), vec3( 0., 1., 0. ), vec3( 0., 0., 1. ) ) ) );

		} );

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function arcVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const uTime = bindings.uTime;
const uHeight = bindings.uHeight;
const uWidthK = bindings.uWidthK;
const uSpan = bindings.uSpan;
const uWidth = bindings.uWidth;
const uMinWidth = bindings.uMinWidth;
const uJitterAmp = bindings.uJitterAmp;
const uJitterFreq = bindings.uJitterFreq;
const uJitterFold = bindings.uJitterFold;
const uSkip = bindings.uSkip;
const uRadius = bindings.uRadius;
const uPitch = bindings.uPitch;
const uPeriod = bindings.uPeriod;
const uOnTime = bindings.uOnTime;
const vUv = bindings.vUv;
const vA = bindings.vA;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, fract, clamp, mix, add, cos, sin, normalize } = TSL;

	const gl_Position = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */



	/** fbm folded toward corners: |n| kinks the wire instead of curling it. */

	const arcNoise = /*@__PURE__*/ Fn( ( [ a, b ] ) => {

		const n = fbm3( vec3( a, b, 1.7 ) ).toVar();

		return mix( n, abs( n ).mul( 2. ).sub( 1. ), clamp( uJitterFold, 0., 1. ) );

	} );

	/** One blink's worth of helix, in the layer's own space. */

	const arcPoint = /*@__PURE__*/ Fn( ( [ u, s, k ] ) => {

		const r = mix( uRadius.x, uRadius.y, arcHash( s.mul( 3.11 ).add( k.mul( 1.7 ) ) ) ).mul( uWidthK ).toVar();
		const pv = mix( uPitch.x, uPitch.y, arcHash( s.mul( 7.73 ).add( k.mul( 2.3 ) ) ) ).toVar();
		const y0 = arcHash( s.mul( 5.31 ).add( k.mul( 3.1 ) ) ).mul( uSpan ).mul( .74 ).toVar();
		const ln = uSpan.mul( add( .18, mul( .37, arcHash( s.mul( 11.1 ).add( k.mul( .7 ) ) ) ) ) ).toVar();
		const ph = arcHash( s.mul( 13.7 ).add( k.mul( 4.9 ) ) ).mul( 6.2831853 ).toVar();
		const y = y0.add( u.mul( ln ) ).mul( uHeight ).toVar();
		const a = ph.add( u.mul( pv ).mul( 6.2831853 ) ).toVar();
		const j1 = arcNoise( u.mul( uJitterFreq ), s.mul( 3.1 ).add( k.mul( .31 ) ) ).toVar();
		const j2 = arcNoise( u.mul( uJitterFreq ).mul( 2.4 ).add( 7. ), s.mul( 5.7 ).add( k.mul( .73 ) ) ).toVar();
		const j3 = fbm3( vec3( u.mul( uJitterFreq ).mul( 4.2 ).add( 3. ), s.mul( 7.9 ).add( k.mul( 1.13 ) ), .9 ) ).mul( .5 ).toVar();
		r.mulAssign( add( 1., uJitterAmp.mul( mul( .70, j1 ).add( mul( .22, j3 ) ) ) ) );
		a.addAssign( uJitterAmp.mul( mul( 1.45, j2 ).add( mul( .40, j3 ) ) ) );

		return vec3( cos( a ).mul( r ), y, sin( a ).mul( r ) );

	} );

	const main = /*@__PURE__*/ Fn( () => {

		const s = aSeed.toVar(), u = position.x.toVar(), side = position.y.toVar();

		// Per-arc stepped window: lit for onTime out of period, and some cycles are
		// skipped outright, so the set never settles into a rhythm.

		const per = mix( uPeriod.x, uPeriod.y, arcHash( s.mul( 1.71 ) ) ).toVar();
		const onT = mix( uOnTime.x, uOnTime.y, arcHash( s.mul( 2.93 ) ) ).toVar();
		const off = arcHash( s.mul( 4.37 ) ).mul( per ).toVar();
		const k = floor( uTime.sub( off ).div( max( per, 1e-4 ) ) ).toVar();
		const loc = uTime.sub( off ).sub( k.mul( max( per, 1e-4 ) ) ).toVar();
		const live = step( loc, onT ).mul( step( uSkip, arcHash( k.mul( 1.373 ).add( s.mul( 9.11 ) ) ) ) ).toVar();
		const w = sin( mul( 3.14159265, clamp( loc.div( max( onT, 1e-4 ) ), 0., 1. ) ) ).toVar();
		vA.assign( live.mul( w ).mul( add( .55, mul( .45, arcHash( k.mul( 2.11 ).add( s.mul( 5.5 ) ) ) ) ) ) );
		vUv.assign( vec2( u, side.mul( .5 ).add( .5 ) ) );
		const a3 = arcPoint( u, s, k ).toVar();
		const b3 = arcPoint( min( u.add( .035 ), 1. ).add( select( u.greaterThan( .965 ), - .070, 0. ) ), s, k ).toVar();
		const mv = modelViewMatrix.mul( vec4( a3, 1. ) ).toVar();
		const mb = modelViewMatrix.mul( vec4( b3, 1. ) ).toVar();
		const d = mb.xy.sub( mv.xy ).toVar();
		const pp = select( length( d ).greaterThan( 1e-5 ), normalize( vec2( d.y.negate(), d.x ) ), vec2( 1., 0. ) ).toVar();

		// A minimum width in SCREEN space: an arc thinner than a pixel shimmers into
		// nothing at depth, which reads as a renderer fault rather than as lightning.

		const wid = max( uWidth.mul( .5 ).mul( add( .55, mul( .45, sin( mul( 3.14159265, u ) ) ) ) ), mv.z.negate().mul( uMinWidth ) ).toVar();
		mv.xy.addAssign( pp.mul( side ).mul( wid ) );
		gl_Position.assign( projectionMatrix.mul( mv ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function arcFragment( bindings ) {
const uCore = bindings.uCore;
const uHalo = bindings.uHalo;
const uOpacity = bindings.uOpacity;
const uFlicker = bindings.uFlicker;
const uBlendMode = bindings.uBlendMode;
const vUv = bindings.vUv;
const vA = bindings.vA;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, Discard, If, abs, smoothstep, sub, vec4, Fn } = TSL;

	const gl_FragColor = property( 'vec4' );

	const main = /*@__PURE__*/ Fn( () => {

		If( vA.lessThanEqual( 0. ), () => {

			Discard();

		} );

		const y = abs( vUv.y.sub( .5 ) ).mul( 2. ).toVar();
		const core = sub( 1., smoothstep( .10, .55, y ) ).toVar();

		// the white-blue filament

		const halo = sub( 1., smoothstep( .30, 1.00, y ) ).toVar();

		// a thin sheath around it

		const ends = smoothstep( 0., .06, vUv.x ).mul( sub( 1., smoothstep( .94, 1., vUv.x ) ) ).toVar();
		const c = uCore.mul( core ).mul( 1.85 ).add( uHalo.mul( halo ).mul( .75 ) ).toVar();
		const a = vA.mul( ends ).mul( uOpacity ).mul( uFlicker ).toVar();

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( c, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( c.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function streakVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const uGrow = bindings.uGrow;
const uCurvature = bindings.uCurvature;
const uUpBias = bindings.uUpBias;
const uBundles = bindings.uBundles;
const uBundleSpread = bindings.uBundleSpread;
const uStagger = bindings.uStagger;
const uLength = bindings.uLength;
const uWidth = bindings.uWidth;
const uHueA = bindings.uHueA;
const uHueB = bindings.uHueB;
const uHueC = bindings.uHueC;
const vUv = bindings.vUv;
const vK = bindings.vK;
const vC = bindings.vC;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, fract, Fn, vec2, max, floor, mul, add, cos, sin, abs, sub, normalize, pow, mix, sign, clamp, vec4, select } = TSL;

	const gl_Position = property( 'vec4' );


	const main = /*@__PURE__*/ Fn( () => {

		const s = aSeed.toVar();
		vUv.assign( vec2( position.x, position.y.mul( .5 ).add( .5 ) ) );
		const n = max( uBundles, 1. ).toVar();
		const bi = floor( stHash( s.mul( .771 ) ).mul( n ) ).toVar();
		const bth = bi.add( mul( .65, stHash( bi.mul( 4.73 ).add( .9 ) ) ) ).div( n ).mul( 6.2831853 ).toVar();
		const bsp = uBundleSpread.mul( add( .30, mul( .70, stHash( bi.mul( 9.11 ).add( 2.3 ) ) ) ) ).toVar();
		const th = bth.add( stHash( s.mul( 1.13 ) ).sub( .5 ).mul( bsp ) ).toVar();
		const d = vec2( cos( th ), sin( th ) ).toVar();
		d.y.assign( d.y.mul( sub( 1., abs( uUpBias ).mul( .22 ) ) ).add( uUpBias ) );

		// the burst leans up (or down)

		d.assign( normalize( d.add( vec2( 1e-5, 0. ) ) ) );
		const pp = vec2( d.y.negate(), d.x ).toVar();
		const hue = stHash( s.mul( 17.1 ) ).toVar();
		const L = mix( uLength.x, uLength.y, pow( max( stHash( s.mul( 3.31 ) ), 1e-5 ), 1.5 ) ).toVar();
		const W = mix( uWidth.x, uWidth.y, stHash( s.mul( 5.17 ) ) ).toVar();
		const cv = sign( stHash( s.mul( 7.91 ) ).sub( .5 ) ).mul( uCurvature ).mul( add( .3, mul( .7, stHash( s.mul( 37.3 ) ) ) ) ).toVar();

		// Not every streak leaves at once: each lags the envelope by its own share.

		const stag = stHash( s.mul( 11.3 ) ).mul( uStagger ).toVar();
		const g = clamp( uGrow.sub( stag ).div( max( sub( 1., stag ), 1e-3 ) ), 0., 1. ).toVar();
		g.assign( sub( 1., pow( max( sub( 1., g ), 1e-5 ), 3. ) ) );
		const x = position.x.mul( L ).mul( g ).toVar();
		const root = vec2( stHash( s.mul( 23.1 ) ).sub( .5 ).mul( L ).mul( .05 ), stHash( s.mul( 29.7 ) ).sub( .5 ).mul( L ).mul( .16 ) ).toVar();
		const off = root.add( d.mul( x ) ).add( pp.mul( cv.mul( x ).mul( x ).mul( .42 ).div( max( L, 1e-3 ) ) ) ).add( pp.mul( position.y ).mul( W ) ).toVar();
		const mv = modelViewMatrix.mul( vec4( 0., 0., 0., 1. ) ).toVar();
		mv.xy.addAssign( off );
		vK.assign( add( .55, mul( .45, stHash( s.mul( 13.7 ) ) ) ) );
		vC.assign( select( hue.greaterThan( .84 ), uHueB, select( hue.greaterThan( .58 ), uHueC, uHueA ) ) );
		gl_Position.assign( projectionMatrix.mul( mv ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function streakFragment( bindings ) {
const uOpacity = bindings.uOpacity;
const uFlicker = bindings.uFlicker;
const uBlendMode = bindings.uBlendMode;
const vUv = bindings.vUv;
const vK = bindings.vK;
const vC = bindings.vC;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, smoothstep, sub, abs, max, exp, Discard, If, vec4, Fn } = TSL;

	const gl_FragColor = property( 'vec4' );

	const main = /*@__PURE__*/ Fn( () => {

		const x = vUv.x.toVar();

		// A needle, not a wedge: full width at the root, tapering only near the tip.

		const wid = sub( 1., smoothstep( .10, 1., x ) ).add( .12 ).toVar();
		const y = abs( vUv.y.sub( .5 ) ).mul( 2. ).div( max( wid, 1e-3 ) ).toVar();
		const a = exp( y.negate().mul( y ).mul( 3.2 ) ).toVar();
		a.mulAssign( smoothstep( 0., .012, x ).mul( sub( 1., smoothstep( .22, 1., x ) ) ) );

		// A speed line is a thin needle a few pixels wide: without a headroom factor
		// the additive coverage is too small to read against a lit flare, however
		// bright its hue is authored.

		a.mulAssign( vK.mul( uOpacity ).mul( uFlicker ).mul( 2. ) );

		If( a.lessThan( .002 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( vC, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( vC.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function sheetVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSheetSeed = bindings.aSheetSeed;
const aSheetAge = bindings.aSheetAge;
const vN = bindings.vN;
const vW = bindings.vW;
const vUv = bindings.vUv;
const vSeed = bindings.vSeed;
const vAge = bindings.vAge;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, vec4, mat3, normalize, Fn } = TSL;

	const gl_Position = property( 'vec4' );

	// How far through its own life this sheet is. Per SHEET, not per layer: the
	// membranes are born on staggered cadences, so a young one has barely started
	// to come apart while an old one is nearly eaten. It rides on the geometry so
	// that every sheet of a layer can still share one material.

	const main = /*@__PURE__*/ Fn( () => {

		vSeed.assign( aSheetSeed );
		vAge.assign( aSheetAge );
		vUv.assign( uv );
		const wp = modelMatrix.mul( vec4( position, 1. ) ).toVar();
		vN.assign( normalize( mat3( modelMatrix ).mul( normal ) ) );
		vW.assign( wp.xyz );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( wp ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function sheetFragment( bindings ) {
const vN = bindings.vN;
const vW = bindings.vW;
const vUv = bindings.vUv;
const vSeed = bindings.vSeed;
const vAge = bindings.vAge;
const uShadow = bindings.uShadow;
const uBody = bindings.uBody;
const uHigh = bindings.uHigh;
const uRim = bindings.uRim;
const uLight = bindings.uLight;
const uCam = bindings.uCam;
const uBands = bindings.uBands;
const uRimPow = bindings.uRimPow;
const uRimAmt = bindings.uRimAmt;
const uOpacity = bindings.uOpacity;
const uTear = bindings.uTear;
const uTearScale = bindings.uTearScale;
const uBandCount = bindings.uBandCount;
const uBlendMode = bindings.uBlendMode;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, floor, Fn, mul, sub, vec2, vec4, dot, step, min, max, float, abs, vec3, length, select, pow, add, Discard, If, normalize, clamp } = TSL;

	const gl_FragColor = property( 'vec4' );








	/** normalize() with a defined result for the zero vector. */


	/** pow() that can never see a negative base. */


	const main = /*@__PURE__*/ Fn( () => {

		// The torn edge: a low-frequency field raises the border threshold, so the
		// membrane is eaten unevenly instead of ending on a straight cut.
		// The border eats a little more as the sheet ages, so a membrane comes
		// apart rather than simply shrinking.

		If( uTear.greaterThan( 0. ), () => {

			const tear = uTear.mul( add( .7, mul( .6, vAge ) ) ).toVar();
			const fld = add( .5, mul( .5, snoise( vec3( vUv.x.mul( uTearScale ), vUv.y.mul( uTearScale ).mul( .65 ), vSeed ) ) ) ).toVar();
			const edge = min( min( vUv.x, sub( 1., vUv.x ) ).mul( 2.2 ), min( vUv.y, sub( 1., vUv.y ) ).mul( 1.5 ) ).toVar();

			If( edge.add( fld.mul( .55 ) ).lessThan( tear ), () => {

				Discard();

			} );

		} );

		const N = normalize( vN ).toVar();

		If( gl_FrontFacing.not(), () => {

			N.assign( N.negate() );

		} );

		const ndl = dot( N, normalize( uLight ) ).mul( .5 ).add( .5 ).toVar();
		const c = select( uBandCount.greaterThan( 2.5 ), select( ndl.lessThan( uBands.x ), uShadow, select( ndl.lessThan( uBands.y ), uBody, uHigh ) ), select( ndl.lessThan( uBands.x ), uShadow, uHigh ) ).toVar();
		const V = normalize( uCam.sub( vW ) ).toVar();
		const rim = pow( max( sub( 1., clamp( dot( N, V ), 0., 1. ) ), 1e-4 ), uRimPow ).toVar();
		c.addAssign( uRim.mul( rim ).mul( uRimAmt ) );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( c, uOpacity ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( c.mul( uOpacity ), uOpacity ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function crescentVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aS = bindings.aS;
const aQ = bindings.aQ;
const uEx = bindings.uEx;
const uEy = bindings.uEy;
const uN = bindings.uN;
const uR = bindings.uR;
const uPh0 = bindings.uPh0;
const uSweep = bindings.uSweep;
const uWmax = bindings.uWmax;
const uHead = bindings.uHead;
const uTail = bindings.uTail;
const uScale = bindings.uScale;
const uRadOff = bindings.uRadOff;
const uWiden = bindings.uWiden;
const uTime = bindings.uTime;
const uSeed = bindings.uSeed;
const uPeakFrom = bindings.uPeakFrom;
const uTipPower = bindings.uTipPower;
const uRootFade = bindings.uRootFade;
const uScreenSpace = bindings.uScreenSpace;
const vS = bindings.vS;
const vQ = bindings.vQ;
const vWin = bindings.vWin;
const vTipD = bindings.vTipD;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, fract, Fn, vec2, dot, floor, mul, sub, mix, float, int, If, Loop, sqrt, smoothstep, max, pow, add, cos, sin, vec4, mat3, normalize, cameraPosition, cross, sign, clamp } = TSL;

	const gl_Position = property( 'vec4' );





	/* x = cell id 0..1, y = distance to the nearest cell EDGE. */


	const crescentWidth = /*@__PURE__*/ Fn( ( [ s ] ) => {

		const dt = uHead.sub( s ).toVar();

		// behind the LIVE tip

		const rise = pow( max( smoothstep( 0., uPeakFrom.mul( .9 ), dt ), 1e-4 ), uTipPower ).toVar();
		const fall = sub( 1., smoothstep( uPeakFrom, uPeakFrom.mul( 2.6 ), dt ) ).toVar();
		const root = pow( max( smoothstep( 0., uRootFade, s.sub( uTail ) ), 1e-4 ), .8 ).toVar();

		return uWmax.mul( rise ).mul( add( .14, mul( .86, fall ) ) ).mul( root );

	} );

	const main = /*@__PURE__*/ Fn( () => {

		const ph = uPh0.add( aS.mul( uSweep ) ).toVar();
		const rad = uEx.mul( cos( ph ) ).add( uEy.mul( sin( ph ) ) ).toVar();
		const w = crescentWidth( aS ).mul( uScale ).toVar();

		// The strip widens and flutters as the tear-away grows: a blade coming apart
		// swells before it breaks, and a constant width reads as a solid ribbon.

		const flut = vorFbm( vec2( aS.mul( 7. ).sub( uTime.mul( 1.1 ) ), uSeed ) ).sub( .5 ).toVar();
		w.mulAssign( add( 1., uWiden.mul( add( .55, mul( 1.5, flut ) ) ) ).add( mul( .14, flut ) ) );
		const ctr = modelMatrix.mul( vec4( rad.mul( uR ), 1. ) ).xyz.toVar();
		const tang = normalize( mat3( modelMatrix ).mul( normalize( uEx.negate().mul( sin( ph ) ).add( uEy.mul( cos( ph ) ) ) ) ) ).toVar();
		const nrm = normalize( mat3( modelMatrix ).mul( uN ) ).toVar();
		const across = property( 'vec3' );

		If( uScreenSpace.greaterThan( .5 ), () => {

			// The across axis is built from the camera, so the blade never goes
			// edge-on however the arc plane is leaning.

			const view = normalize( cameraPosition.sub( ctr ) ).toVar();
			across.assign( normalize( cross( tang, view ) ) );
			const wr = normalize( mat3( modelMatrix ).mul( rad ) ).toVar();
			across.mulAssign( sign( dot( across, wr ) ) );

		} ).Else( () => {

			across.assign( normalize( mat3( modelMatrix ).mul( rad ) ) );

		} );

		const off = sub( .5, aQ ).mul( w ).add( uRadOff ).toVar();
		const p = ctr.add( across.mul( off ) ).toVar();

		// A short fade band at both ends of the window, so nothing pops in.

		vWin.assign( smoothstep( uTail, uTail.add( .035 ), aS ).mul( sub( 1., smoothstep( uHead.sub( .012 ), uHead, aS ) ) ) );
		vS.assign( aS );
		vQ.assign( aQ );
		vTipD.assign( clamp( uHead.sub( aS ).div( max( uPeakFrom.mul( .75 ), 1e-3 ) ), 0., 1. ) );
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( p, 1. ) ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function crescentFragment( bindings ) {
const vS = bindings.vS;
const vQ = bindings.vQ;
const vWin = bindings.vWin;
const vTipD = bindings.vTipD;
const uC0 = bindings.uC0;
const uC1 = bindings.uC1;
const uC2 = bindings.uC2;
const uC3 = bindings.uC3;
const uStreakCol = bindings.uStreakCol;
const uI = bindings.uI;
const uTime = bindings.uTime;
const uSeed = bindings.uSeed;
const uAlpha = bindings.uAlpha;
const uHead = bindings.uHead;
const uTail = bindings.uTail;
const uTear = bindings.uTear;
const uErode = bindings.uErode;
const uTipHot = bindings.uTipHot;
const uStreakOn = bindings.uStreakOn;
const uStreakFreq = bindings.uStreakFreq;
const uStreakPan = bindings.uStreakPan;
const uStreakI = bindings.uStreakI;
const uStreakSeg = bindings.uStreakSeg;
const uVorScale = bindings.uVorScale;
const uSeamWidth = bindings.uSeamWidth;
const uFrontWidth = bindings.uFrontWidth;
const uBandT = bindings.uBandT;
const uBlendMode = bindings.uBlendMode;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, fract, Fn, vec2, dot, floor, mul, sub, mix, float, int, If, Loop, sqrt, smoothstep, Discard, max, add, clamp, pow, vec4 } = TSL;

	const gl_FragColor = property( 'vec4' );

	// per-band intensities
	// the four ramp stop positions





	/* x = cell id 0..1, y = distance to the nearest cell EDGE. */


	const bandColor = /*@__PURE__*/ Fn( ( [ q ] ) => {

		const c = uC0.mul( uI.x ).toVar();
		c.assign( mix( c, uC1.mul( uI.y ), smoothstep( uBandT.x, uBandT.y, q ) ) );
		c.assign( mix( c, uC2.mul( uI.z ), smoothstep( uBandT.y, uBandT.z, q ) ) );
		c.assign( mix( c, uC3.mul( uI.w ), smoothstep( uBandT.z, uBandT.w, q ) ) );

		return c;

	} );

	const main = /*@__PURE__*/ Fn( () => {

		If( vWin.lessThanEqual( .001 ), () => {

			Discard();

		} );

		// Flow streaks running ALONG the blade, panning toward the tail. The Voronoi
		// seams are what make them read as torn sheets of flame rather than as an
		// airbrushed gradient.

		const cell = voronoi2( vec2( vS.mul( uVorScale ).add( uTime.mul( uStreakPan ) ), vQ.mul( uVorScale ).mul( .6 ).add( uSeed.mul( 3.1 ) ) ) ).toVar();
		const streak = float( 1. ).toVar();

		If( uStreakOn.greaterThan( .5 ), () => {

			const lines = mix( .60, 1.45, vorFbm( vec2( vS.mul( uStreakFreq ).mul( 3.3 ).add( uTime.mul( uStreakPan ).mul( 1.6 ) ), vQ.mul( 7. ).add( uSeed ) ) ) ).toVar();
			const seam = sub( 1., smoothstep( 0., max( uSeamWidth, 1e-3 ), cell.y ) ).toVar();
			streak.assign( lines.mul( add( .72, mul( .60, cell.x ) ) ).add( mul( .35, seam ).mul( uStreakSeg ) ) );
			streak.assign( mix( 1., streak, clamp( uStreakI, 0., 1. ) ) );

		} );

		// The erosion FRONT: cells behind it are eaten, so the tail breaks into
		// tongues instead of dimming. The front's reach is measured from the tail.

		const front = clamp( vS.sub( uTail ).div( max( .10, uFrontWidth ) ), 0., 1. ).toVar();
		const th = uErode.add( uTear.mul( sub( 1., front ) ) ).toVar();
		const n = mul( .42, cell.x ).add( mul( .58, vorFbm( vec2( vS.mul( uVorScale ).add( uSeed.mul( 5. ) ), vQ.mul( 2.1 ).sub( uTime.mul( .6 ) ) ) ) ) ).toVar();
		const a = smoothstep( th, th.add( .16 ), n ).toVar();
		const edge = smoothstep( th.sub( .02 ), th.add( .09 ), n ).mul( sub( 1., smoothstep( th.add( .09 ), th.add( .26 ), n ) ) ).toVar();
		const tip = pow( max( sub( 1., vTipD ), 1e-4 ), 2.4 ).toVar();
		const col = bandColor( vQ ).mul( streak ).toVar();
		col.addAssign( uC0.mul( tip ).mul( uTipHot ) );
		col.addAssign( uStreakCol.mul( edge ).mul( uStreakI ) );
		const al = a.mul( vWin ).mul( uAlpha ).mul( add( .86, mul( .14, sub( 1., vQ ) ) ) ).mul( add( .78, mul( .35, streak ) ) ).toVar();
		al.assign( clamp( al, 0., 1. ) );

		If( al.lessThan( .003 ), () => {

			Discard();

		} );

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, al ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( al ), al ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
// Three.js Transpiler r186

export function lickVertex( bindings ) {
const vClipPosition = bindings.vClipPosition;
const aSeed = bindings.aSeed;
const aSide = bindings.aSide;
const uAnchor = bindings.uAnchor;
const uTangent = bindings.uTangent;
const uNormal = bindings.uNormal;
const uDrift = bindings.uDrift;
const uLength = bindings.uLength;
const uWidth = bindings.uWidth;
const uStagger = bindings.uStagger;
const uLife = bindings.uLife;
const uTime = bindings.uTime;
const uSpan = bindings.uSpan;
const uCurl = bindings.uCurl;
const uFlipHz = bindings.uFlipHz;
const vS = bindings.vS;
const vA = bindings.vA;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, fract, Fn, max, floor, mix, clamp, step, pow, mul, sin, vec3, dot, vec2, normalize, sub, add, vec4 } = TSL;

	const gl_Position = property( 'vec4' );


	const main = /*@__PURE__*/ Fn( () => {

		const u = position.x.toVar(), v = position.y.toVar();
		const h1 = lickHash( aSeed.mul( 2.3 ).add( .4 ) ).toVar(), h2 = lickHash( aSeed.mul( 4.9 ).add( 6.6 ) ).toVar();

		// The flipbook: the shape holds for 1/flipbookHz of a second and then jumps.

		const k = floor( uTime.mul( max( uFlipHz, 1e-3 ) ) ).add( aSeed ).toVar();
		const L = mix( uLength.x, uLength.y, lickHash( k.mul( 1.31 ) ) ).toVar();
		const W = mix( uWidth.x, uWidth.y, lickHash( k.mul( 3.77 ).add( 2.1 ) ) ).toVar();
		const birth = mix( uStagger.x, uStagger.y, h1 ).mul( uSpan ).toVar();
		const life = mix( uLife.x, uLife.y, h2 ).toVar();
		const age = uTime.sub( birth ).toVar();
		const a01 = clamp( age.div( max( life, 1e-4 ) ), 0., 1. ).toVar();
		const alive = step( 0., age ).mul( step( a01, .999 ) ).toVar();
		vA.assign( alive.mul( sin( mul( 3.14159265, pow( max( a01, 1e-4 ), .7 ) ) ) ) );
		vS.assign( u );

		// Screen-aligned: the strip lies in the view plane, trailing back along the
		// arc and leaning up, which is what a peeling lick does.

		const R = vec3( viewMatrix.element(0)[ 0 ], viewMatrix.element(1)[ 0 ], viewMatrix.element(2)[ 0 ] ).toVar();
		const Up = vec3( viewMatrix.element(0)[ 1 ], viewMatrix.element(1)[ 1 ], viewMatrix.element(2)[ 1 ] ).toVar();
		const base = uAnchor.add( uNormal.mul( h1.sub( .5 ) ).mul( .10 ) ).add( uDrift.mul( age ) ).toVar();
		const dir = normalize( vec2( dot( uTangent.negate(), R ), dot( uTangent.negate(), Up ) ).add( vec2( 0., .55 ) ).add( vec2( 1e-4 ) ) ).toVar();
		const pr = vec2( dir.y.negate(), dir.x ).toVar();
		const w = W.mul( .5 ).mul( pow( max( u.add( .06 ), 1e-4 ), .30 ) ).mul( pow( max( sub( 1., u ), 1e-4 ), .85 ) ).toVar();
		const curl = uCurl.mul( sin( u.mul( 3. ).add( h1.mul( 6.2831853 ) ) ) ).mul( u ).mul( aSide ).toVar();
		const q = dir.mul( u.mul( L ).mul( add( .45, mul( .85, a01 ) ) ) ).add( pr.mul( aSide.mul( .045 ).add( curl ).add( v.mul( w ) ) ) ).toVar();
		const wp = base.add( R.mul( q.x ).add( Up.mul( q.y ) ).mul( alive ) ).toVar();
		gl_Position.assign( projectionMatrix.mul( viewMatrix ).mul( vec4( wp, 1. ) ) );
		vClipPosition.assign( gl_Position );

		return gl_Position;

	} );
	return main();

}
// Three.js Transpiler r186

export function lickFragment( bindings ) {
const vS = bindings.vS;
const vA = bindings.vA;
const uHot = bindings.uHot;
const uBody = bindings.uBody;
const uOpacity = bindings.uOpacity;
const uBlendMode = bindings.uBlendMode;
const { cameraProjectionMatrix: projectionMatrix, modelViewMatrix, modelWorldMatrix: modelMatrix, cameraViewMatrix: viewMatrix, normalLocal: normal, positionLocal: position, screenCoordinate, frontFacing: gl_FrontFacing } = TSL;
const uv = TSL.uv();


	const { property, Discard, If, select, vec4, Fn } = TSL;

	const gl_FragColor = property( 'vec4' );

	const main = /*@__PURE__*/ Fn( () => {

		const a = vA.mul( uOpacity ).toVar();

		If( a.lessThanEqual( .004 ), () => {

			Discard();

		} );

		// Two flat bands: cel, not gradient.

		const col = select( vS.lessThan( .42 ), uHot, uBody ).toVar();

		If( uBlendMode.equal( 1 ), () => {

			gl_FragColor.assign( vec4( col, a ) );

		} ).Else( () => {

			gl_FragColor.assign( vec4( col.mul( a ), a ) );

		} );

		return gl_FragColor;

	} );
	return main();

}
export const particleVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uStretch":{"type":"float","kind":"uniform"},"uAtlasTiles":{"type":"float","kind":"uniform"},"uMotionBlur":{"type":"float","kind":"uniform"},"uFlipFps":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uRenderMode":{"type":"int","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uFlipMode":{"type":"int","kind":"uniform"},"uAnchorHead":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"uRot":{"type":"vec2","kind":"uniform"},"uRotInit":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"}};
export const subParticleVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aPSeed":{"type":"vec4","kind":"attribute"},"aPExtra":{"type":"vec4","kind":"attribute"},"aPExtra2":{"type":"vec4","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uStretch":{"type":"float","kind":"uniform"},"uAtlasTiles":{"type":"float","kind":"uniform"},"uMotionBlur":{"type":"float","kind":"uniform"},"uFlipFps":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uRenderMode":{"type":"int","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uFlipMode":{"type":"int","kind":"uniform"},"uAnchorHead":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"uRot":{"type":"vec2","kind":"uniform"},"uRotInit":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"},"uParentPeriod":{"type":"float","kind":"uniform"},"uParentSpawnWindow":{"type":"float","kind":"uniform"},"uParentSpawnDuration":{"type":"float","kind":"uniform"},"uParentShapeLength":{"type":"float","kind":"uniform"},"uParentShapeRadius":{"type":"float","kind":"uniform"},"uParentShapeInner":{"type":"float","kind":"uniform"},"uParentShapeAngle":{"type":"float","kind":"uniform"},"uParentDrag":{"type":"float","kind":"uniform"},"uParentCurl":{"type":"float","kind":"uniform"},"uParentCurlFreq":{"type":"float","kind":"uniform"},"uParentCurlSpeed":{"type":"float","kind":"uniform"},"uParentFloorY":{"type":"float","kind":"uniform"},"uParentFloorSoft":{"type":"float","kind":"uniform"},"uParentAngle":{"type":"float","kind":"uniform"},"uParentPlanarDrag":{"type":"float","kind":"uniform"},"uParentVortexW":{"type":"float","kind":"uniform"},"uParentVortexFalloff":{"type":"float","kind":"uniform"},"uParentSpawnMode":{"type":"int","kind":"uniform"},"uParentShapeType":{"type":"int","kind":"uniform"},"uParentVelMode":{"type":"int","kind":"uniform"},"uParentHasFloor":{"type":"int","kind":"uniform"},"uParentSurfaceOnly":{"type":"int","kind":"uniform"},"uParentSpeedN":{"type":"int","kind":"uniform"},"uParentBurstN":{"type":"int","kind":"uniform"},"uParentAxis":{"type":"vec3","kind":"uniform"},"uParentDir":{"type":"vec3","kind":"uniform"},"uParentGravity":{"type":"vec3","kind":"uniform"},"uParentWind":{"type":"vec3","kind":"uniform"},"uParentBias":{"type":"vec3","kind":"uniform"},"uParentShapeSize":{"type":"vec3","kind":"uniform"},"uParentVortexAxis":{"type":"vec3","kind":"uniform"},"uParentLife":{"type":"vec2","kind":"uniform"},"uParentSpeed":{"type":"vec2","kind":"uniform"},"uParentSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uParentBurstT":{"type":"float","size":8,"kind":"uniform"},"uParentBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentInterior":{"type":"float","kind":"uniform"},"uParentAngleJitter":{"type":"float","kind":"uniform"},"uParentAngleBias":{"type":"float","kind":"uniform"},"uParentTimeShift":{"type":"float","kind":"uniform"},"uInherit":{"type":"float","kind":"uniform"},"uPathT0":{"type":"float","kind":"uniform"},"uPathDt":{"type":"float","kind":"uniform"},"uSubMode":{"type":"int","kind":"uniform"},"uSubOffset":{"type":"vec2","kind":"uniform"},"uParentPath":{"type":"vec3","size":8,"kind":"uniform"}};
export const trailVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSegments":{"type":"float","kind":"uniform"},"uSpacing":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveG":{"type":"vec2","size":8,"kind":"uniform"},"uCurveGN":{"type":"int","kind":"uniform"},"uCurveGEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"}};
export const subTrailVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aPSeed":{"type":"vec4","kind":"attribute"},"aPExtra":{"type":"vec4","kind":"attribute"},"aPExtra2":{"type":"vec4","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSegments":{"type":"float","kind":"uniform"},"uSpacing":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveG":{"type":"vec2","size":8,"kind":"uniform"},"uCurveGN":{"type":"int","kind":"uniform"},"uCurveGEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"},"uParentPeriod":{"type":"float","kind":"uniform"},"uParentSpawnWindow":{"type":"float","kind":"uniform"},"uParentSpawnDuration":{"type":"float","kind":"uniform"},"uParentShapeLength":{"type":"float","kind":"uniform"},"uParentShapeRadius":{"type":"float","kind":"uniform"},"uParentShapeInner":{"type":"float","kind":"uniform"},"uParentShapeAngle":{"type":"float","kind":"uniform"},"uParentDrag":{"type":"float","kind":"uniform"},"uParentCurl":{"type":"float","kind":"uniform"},"uParentCurlFreq":{"type":"float","kind":"uniform"},"uParentCurlSpeed":{"type":"float","kind":"uniform"},"uParentFloorY":{"type":"float","kind":"uniform"},"uParentFloorSoft":{"type":"float","kind":"uniform"},"uParentAngle":{"type":"float","kind":"uniform"},"uParentPlanarDrag":{"type":"float","kind":"uniform"},"uParentVortexW":{"type":"float","kind":"uniform"},"uParentVortexFalloff":{"type":"float","kind":"uniform"},"uParentSpawnMode":{"type":"int","kind":"uniform"},"uParentShapeType":{"type":"int","kind":"uniform"},"uParentVelMode":{"type":"int","kind":"uniform"},"uParentHasFloor":{"type":"int","kind":"uniform"},"uParentSurfaceOnly":{"type":"int","kind":"uniform"},"uParentSpeedN":{"type":"int","kind":"uniform"},"uParentBurstN":{"type":"int","kind":"uniform"},"uParentAxis":{"type":"vec3","kind":"uniform"},"uParentDir":{"type":"vec3","kind":"uniform"},"uParentGravity":{"type":"vec3","kind":"uniform"},"uParentWind":{"type":"vec3","kind":"uniform"},"uParentBias":{"type":"vec3","kind":"uniform"},"uParentShapeSize":{"type":"vec3","kind":"uniform"},"uParentVortexAxis":{"type":"vec3","kind":"uniform"},"uParentLife":{"type":"vec2","kind":"uniform"},"uParentSpeed":{"type":"vec2","kind":"uniform"},"uParentSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uParentBurstT":{"type":"float","size":8,"kind":"uniform"},"uParentBurstC":{"type":"float","size":8,"kind":"uniform"},"uParentInterior":{"type":"float","kind":"uniform"},"uParentAngleJitter":{"type":"float","kind":"uniform"},"uParentAngleBias":{"type":"float","kind":"uniform"},"uParentTimeShift":{"type":"float","kind":"uniform"},"uInherit":{"type":"float","kind":"uniform"},"uPathT0":{"type":"float","kind":"uniform"},"uPathDt":{"type":"float","kind":"uniform"},"uSubMode":{"type":"int","kind":"uniform"},"uSubOffset":{"type":"vec2","kind":"uniform"},"uParentPath":{"type":"vec3","size":8,"kind":"uniform"}};
export const stripVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uStripStep":{"type":"float","kind":"uniform"},"uStripWave":{"type":"float","kind":"uniform"},"uPalettes":{"type":"float","kind":"uniform"},"uStripCount":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uStripLen":{"type":"vec2","kind":"uniform"},"uStripWide":{"type":"vec2","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vPalette":{"type":"float","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"}};
export const sliverVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"vec4","kind":"attribute"},"aExtra":{"type":"vec4","kind":"attribute"},"aExtra2":{"type":"vec4","kind":"attribute"},"aIndex":{"type":"float","kind":"attribute"},"aSrcPos":{"type":"vec3","kind":"attribute"},"aSrcDir":{"type":"vec3","kind":"attribute"},"aEvent":{"type":"vec4","kind":"attribute"},"aSub":{"type":"float","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uSliverCurve":{"type":"float","kind":"uniform"},"uSliverTaper":{"type":"float","kind":"uniform"},"uSliverJag":{"type":"float","kind":"uniform"},"uSecondary":{"type":"float","kind":"uniform"},"uRetractOn":{"type":"float","kind":"uniform"},"uRetractStart":{"type":"float","kind":"uniform"},"uRetractEnd":{"type":"float","kind":"uniform"},"uRetractTip":{"type":"float","kind":"uniform"},"uSecLength":{"type":"float","kind":"uniform"},"uTwinkleFreq":{"type":"float","kind":"uniform"},"uTwinkleDepth":{"type":"float","kind":"uniform"},"uHasAlphaSpawn":{"type":"int","kind":"uniform"},"uSliverLen":{"type":"vec2","kind":"uniform"},"uSliverWide":{"type":"vec2","kind":"uniform"},"uSecAlong":{"type":"vec2","kind":"uniform"},"uSize":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uEPathType":{"type":"int","kind":"uniform"},"uEPathA":{"type":"vec3","kind":"uniform"},"uEPathB":{"type":"vec3","kind":"uniform"},"uEPathC":{"type":"vec3","kind":"uniform"},"uEPathD":{"type":"vec4","kind":"uniform"},"uEPathW":{"type":"vec2","kind":"uniform"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"},"uCurveB":{"type":"vec2","size":8,"kind":"uniform"},"uCurveBN":{"type":"int","kind":"uniform"},"uCurveBEase":{"type":"float","kind":"uniform"},"uCurveD":{"type":"vec2","size":8,"kind":"uniform"},"uCurveDN":{"type":"int","kind":"uniform"},"uCurveDEase":{"type":"float","kind":"uniform"},"uCurveE":{"type":"vec2","size":8,"kind":"uniform"},"uCurveEN":{"type":"int","kind":"uniform"},"uCurveEEase":{"type":"float","kind":"uniform"},"uCurveH":{"type":"vec2","size":8,"kind":"uniform"},"uCurveHN":{"type":"int","kind":"uniform"},"uCurveHEase":{"type":"float","kind":"uniform"},"uCurveT":{"type":"vec2","size":8,"kind":"uniform"},"uCurveTN":{"type":"int","kind":"uniform"},"uCurveTEase":{"type":"float","kind":"uniform"},"uPeriod":{"type":"float","kind":"uniform"},"uSpawnWindow":{"type":"float","kind":"uniform"},"uSpawnDuration":{"type":"float","kind":"uniform"},"uShapeLength":{"type":"float","kind":"uniform"},"uShapeRadius":{"type":"float","kind":"uniform"},"uShapeInner":{"type":"float","kind":"uniform"},"uShapeAngle":{"type":"float","kind":"uniform"},"uDrag":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uCurlFreq":{"type":"float","kind":"uniform"},"uCurlSpeed":{"type":"float","kind":"uniform"},"uFloorY":{"type":"float","kind":"uniform"},"uFloorSoft":{"type":"float","kind":"uniform"},"uAngle":{"type":"float","kind":"uniform"},"uPlanarDrag":{"type":"float","kind":"uniform"},"uVortexW":{"type":"float","kind":"uniform"},"uVortexFalloff":{"type":"float","kind":"uniform"},"uSpawnMode":{"type":"int","kind":"uniform"},"uShapeType":{"type":"int","kind":"uniform"},"uVelMode":{"type":"int","kind":"uniform"},"uHasFloor":{"type":"int","kind":"uniform"},"uSurfaceOnly":{"type":"int","kind":"uniform"},"uSpeedN":{"type":"int","kind":"uniform"},"uBurstN":{"type":"int","kind":"uniform"},"uAxis":{"type":"vec3","kind":"uniform"},"uDir":{"type":"vec3","kind":"uniform"},"uGravity":{"type":"vec3","kind":"uniform"},"uWind":{"type":"vec3","kind":"uniform"},"uBias":{"type":"vec3","kind":"uniform"},"uShapeSize":{"type":"vec3","kind":"uniform"},"uVortexAxis":{"type":"vec3","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uSpeed":{"type":"vec2","kind":"uniform"},"uSpeedKey":{"type":"vec2","size":8,"kind":"uniform"},"uBurstT":{"type":"float","size":8,"kind":"uniform"},"uBurstC":{"type":"float","size":8,"kind":"uniform"},"uInterior":{"type":"float","kind":"uniform"},"uAngleJitter":{"type":"float","kind":"uniform"},"uAngleBias":{"type":"float","kind":"uniform"},"uFrontC":{"type":"vec3","kind":"uniform"},"uFrontEx":{"type":"vec3","kind":"uniform"},"uFrontEy":{"type":"vec3","kind":"uniform"},"uFrontN":{"type":"vec3","kind":"uniform"},"uFrontR":{"type":"float","kind":"uniform"},"uFrontPh0":{"type":"float","kind":"uniform"},"uFrontSweep":{"type":"float","kind":"uniform"},"uFrontSpan":{"type":"float","kind":"uniform"},"uFrontStart":{"type":"float","kind":"uniform"}};
export const particleFragmentBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uMask":{"type":"sampler2D","kind":"uniform"},"uNoise":{"type":"sampler2D","kind":"uniform"},"uHasMask":{"type":"int","kind":"uniform"},"uHasNoise":{"type":"int","kind":"uniform"},"uUseErosion":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uProcedural":{"type":"int","kind":"uniform"},"uAtlasCols":{"type":"int","kind":"uniform"},"uAtlasRows":{"type":"int","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uDistort":{"type":"float","kind":"uniform"},"uErodeSoft":{"type":"float","kind":"uniform"},"uEdgeW":{"type":"float","kind":"uniform"},"uEdgeI":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uMaskRot":{"type":"float","kind":"uniform"},"uRampKeyMode":{"type":"float","kind":"uniform"},"uGroundY":{"type":"float","kind":"uniform"},"uHeightSpan":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uRampBlendMode":{"type":"float","kind":"uniform"},"uRampBlendWeight":{"type":"float","kind":"uniform"},"uNoiseScale":{"type":"vec2","kind":"uniform"},"uNoisePan":{"type":"vec2","kind":"uniform"},"uMaskScale":{"type":"vec2","kind":"uniform"},"uMaskPan":{"type":"vec2","kind":"uniform"},"uDistortPan":{"type":"vec2","kind":"uniform"},"uEdgeCol":{"type":"vec3","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"},"uCurveC":{"type":"vec2","size":8,"kind":"uniform"},"uCurveCN":{"type":"int","kind":"uniform"},"uCurveCEase":{"type":"float","kind":"uniform"},"tDepth":{"type":"sampler2D","kind":"uniform"},"uResolution":{"type":"vec2","kind":"uniform"},"uNear":{"type":"float","kind":"uniform"},"uFar":{"type":"float","kind":"uniform"},"uSoft":{"type":"float","kind":"uniform"},"uProcParams":{"type":"vec4","kind":"uniform"},"uStripeA":{"type":"vec4","size":3,"kind":"uniform"},"uStripeB":{"type":"vec4","size":3,"kind":"uniform"},"uStripeN":{"type":"int","kind":"uniform"},"uSymFill":{"type":"vec3","kind":"uniform"},"uSymOutline":{"type":"vec3","kind":"uniform"},"uSymHigh":{"type":"vec3","kind":"uniform"},"uSymInk":{"type":"vec3","kind":"uniform"},"uSymHot":{"type":"vec3","kind":"uniform"},"uSymHotI":{"type":"float","kind":"uniform"},"uSymHotA":{"type":"float","kind":"uniform"},"uScreenPitch":{"type":"float","kind":"uniform"},"uScreenOn":{"type":"float","kind":"uniform"},"uScreenWorld":{"type":"float","kind":"uniform"},"uScreenCol":{"type":"vec3","kind":"uniform"}};
export const trailFragmentBindings = {"uTrail":{"type":"sampler2D","kind":"uniform"},"uHasTrail":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uTrailRampMode":{"type":"float","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const stripFragmentBindings = {"uOpacity":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vPalette":{"type":"float","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const sliverFragmentBindings = {"uOpacity":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vU":{"type":"float","kind":"varying"},"vAlpha":{"type":"float","kind":"varying"},"vRot":{"type":"float","kind":"varying"},"vSeed":{"type":"vec3","kind":"varying"},"vTile":{"type":"vec2","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const surfaceVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uTime":{"type":"float","kind":"uniform"},"uLength":{"type":"float","kind":"uniform"},"uRadius":{"type":"float","kind":"uniform"},"uThickness":{"type":"float","kind":"uniform"},"uArc":{"type":"float","kind":"uniform"},"uVertexAmp":{"type":"float","kind":"uniform"},"uVertexFreq":{"type":"float","kind":"uniform"},"uVertexSpeed":{"type":"float","kind":"uniform"},"uDisplaceShift":{"type":"float","kind":"uniform"},"uRoll":{"type":"float","kind":"uniform"},"uChannel":{"type":"float","kind":"uniform"},"uSplitOffset":{"type":"float","kind":"uniform"},"uSplitGrowth":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uShell":{"type":"int","kind":"uniform"},"uHasVertexNoise":{"type":"int","kind":"uniform"},"uBillboard":{"type":"int","kind":"uniform"},"uRibbon":{"type":"int","kind":"uniform"},"uUseLocalZ":{"type":"int","kind":"uniform"},"uSlab":{"type":"int","kind":"uniform"},"uSlabBase":{"type":"int","kind":"uniform"},"uSlabTaper":{"type":"float","kind":"uniform"},"uFrameV":{"type":"int","kind":"uniform"},"uFrameMargin":{"type":"float","kind":"uniform"},"uZRange":{"type":"vec2","kind":"uniform"},"uVertexBias":{"type":"vec3","kind":"uniform"},"vN":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vObj":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vLobe":{"type":"float","kind":"varying"},"vRing":{"type":"float","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"uCurveF":{"type":"vec2","size":8,"kind":"uniform"},"uCurveFN":{"type":"int","kind":"uniform"},"uCurveFEase":{"type":"float","kind":"uniform"}};
export const surfaceFragmentBindings = {"uTime":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uErodeSoft":{"type":"float","kind":"uniform"},"uEdgeW":{"type":"float","kind":"uniform"},"uEdgeI":{"type":"float","kind":"uniform"},"uProtect":{"type":"float","kind":"uniform"},"uRimBias":{"type":"float","kind":"uniform"},"uDisplaceShift":{"type":"float","kind":"uniform"},"uFresnelPower":{"type":"float","kind":"uniform"},"uFresnelStrength":{"type":"float","kind":"uniform"},"uDistort":{"type":"float","kind":"uniform"},"uRampKeyMode":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uMaskRot":{"type":"float","kind":"uniform"},"uRadius":{"type":"float","kind":"uniform"},"uLength":{"type":"float","kind":"uniform"},"uThickness":{"type":"float","kind":"uniform"},"uGroundY":{"type":"float","kind":"uniform"},"uHeightSpan":{"type":"float","kind":"uniform"},"uChannel":{"type":"float","kind":"uniform"},"uRampBlendMode":{"type":"float","kind":"uniform"},"uRampBlendWeight":{"type":"float","kind":"uniform"},"uShell":{"type":"int","kind":"uniform"},"uHasMask":{"type":"int","kind":"uniform"},"uHasNoise":{"type":"int","kind":"uniform"},"uUseErosion":{"type":"int","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uProcedural":{"type":"int","kind":"uniform"},"uHasFresnel":{"type":"int","kind":"uniform"},"uBolt":{"type":"int","kind":"uniform"},"uNoiseScale":{"type":"vec2","kind":"uniform"},"uNoisePan":{"type":"vec2","kind":"uniform"},"uMaskScale":{"type":"vec2","kind":"uniform"},"uMaskPan":{"type":"vec2","kind":"uniform"},"uDistortPan":{"type":"vec2","kind":"uniform"},"uEdgeCol":{"type":"vec3","kind":"uniform"},"uCam":{"type":"vec3","kind":"uniform"},"uMask":{"type":"sampler2D","kind":"uniform"},"uNoise":{"type":"sampler2D","kind":"uniform"},"uHasLattice":{"type":"int","kind":"uniform"},"uHasReveal":{"type":"int","kind":"uniform"},"uRevealMode":{"type":"int","kind":"uniform"},"uHasPlaneGlow":{"type":"int","kind":"uniform"},"uHasDissolve":{"type":"int","kind":"uniform"},"uRippleN":{"type":"int","kind":"uniform"},"uBand":{"type":"int","kind":"uniform"},"uLatEdge":{"type":"float","kind":"uniform"},"uLatGap":{"type":"float","kind":"uniform"},"uPulseSpeed":{"type":"float","kind":"uniform"},"uPhaseJitter":{"type":"float","kind":"uniform"},"uGrazeFade":{"type":"float","kind":"uniform"},"uDisStart":{"type":"float","kind":"uniform"},"uDisStagger":{"type":"float","kind":"uniform"},"uDisSoft":{"type":"float","kind":"uniform"},"uRevealFrom":{"type":"float","kind":"uniform"},"uRevealTo":{"type":"float","kind":"uniform"},"uRevealWidth":{"type":"float","kind":"uniform"},"uPlaneDist":{"type":"float","kind":"uniform"},"uPlaneI":{"type":"float","kind":"uniform"},"uBandStripes":{"type":"float","kind":"uniform"},"uTileCol":{"type":"vec3","kind":"uniform"},"uLatEdgeCol":{"type":"vec3","kind":"uniform"},"uPlaneCol":{"type":"vec3","kind":"uniform"},"uRipple":{"type":"vec4","size":8,"kind":"uniform"},"uSlab":{"type":"int","kind":"uniform"},"uSlabN":{"type":"int","kind":"uniform"},"uSlabTier":{"type":"vec4","size":8,"kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uReflect":{"type":"int","kind":"uniform"},"uReflectTint":{"type":"vec3","kind":"uniform"},"uReflectOpacity":{"type":"float","kind":"uniform"},"uReflectBlur":{"type":"float","kind":"uniform"},"uStreakOn":{"type":"int","kind":"uniform"},"uStreakRadiate":{"type":"int","kind":"uniform"},"uCreaseOn":{"type":"int","kind":"uniform"},"uStreakA":{"type":"vec4","kind":"uniform"},"uStreakB":{"type":"vec4","kind":"uniform"},"uStreakCol":{"type":"vec3","kind":"uniform"},"uCrease":{"type":"vec3","kind":"uniform"},"uSymbolSeed":{"type":"float","kind":"uniform"},"vN":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vObj":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vLobe":{"type":"float","kind":"varying"},"vRing":{"type":"float","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"},"uCurveC":{"type":"vec2","size":8,"kind":"uniform"},"uCurveCN":{"type":"int","kind":"uniform"},"uCurveCEase":{"type":"float","kind":"uniform"},"uProcParams":{"type":"vec4","kind":"uniform"},"uStripeA":{"type":"vec4","size":3,"kind":"uniform"},"uStripeB":{"type":"vec4","size":3,"kind":"uniform"},"uStripeN":{"type":"int","kind":"uniform"},"uSymFill":{"type":"vec3","kind":"uniform"},"uSymOutline":{"type":"vec3","kind":"uniform"},"uSymHigh":{"type":"vec3","kind":"uniform"},"uSymInk":{"type":"vec3","kind":"uniform"},"uSymHot":{"type":"vec3","kind":"uniform"},"uSymHotI":{"type":"float","kind":"uniform"},"uSymHotA":{"type":"float","kind":"uniform"},"uScreenPitch":{"type":"float","kind":"uniform"},"uScreenOn":{"type":"float","kind":"uniform"},"uScreenWorld":{"type":"float","kind":"uniform"},"uScreenCol":{"type":"vec3","kind":"uniform"},"uFrame":{"type":"int","kind":"uniform"},"uSdfN":{"type":"int","kind":"uniform"},"uBeadOn":{"type":"int","kind":"uniform"},"uCorner":{"type":"float","kind":"uniform"},"uSdfCore":{"type":"float","kind":"uniform"},"uSdfSpine":{"type":"float","kind":"uniform"},"uSdfInnerOff":{"type":"float","kind":"uniform"},"uSdfInnerW":{"type":"float","kind":"uniform"},"uSdfHalo":{"type":"vec2","size":3,"kind":"uniform"},"uBeads":{"type":"vec3","kind":"uniform"},"uFlowOn":{"type":"int","kind":"uniform"},"uFlowN":{"type":"int","kind":"uniform"},"uFlowLayer":{"type":"vec4","size":8,"kind":"uniform"},"uFlowCut":{"type":"vec3","kind":"uniform"},"uSwirlOn":{"type":"int","kind":"uniform"},"uSwirlBands":{"type":"vec4","kind":"uniform"},"uSwirlDetail":{"type":"vec4","kind":"uniform"},"uSwirlLobe":{"type":"vec3","kind":"uniform"},"uSwirlS":{"type":"float","kind":"uniform"},"uSites":{"type":"sampler2D","kind":"uniform"},"uCells":{"type":"float","kind":"uniform"},"uCellA":{"type":"float","kind":"uniform"}};
export const blobVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"vN":{"type":"vec3","kind":"varying"},"vV":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vLobe":{"type":"vec3","kind":"varying"},"uTime":{"type":"float","kind":"uniform"},"uNoiseSpeed":{"type":"float","kind":"uniform"},"aLobeA":{"type":"vec4","kind":"attribute"},"aLobeB":{"type":"vec4","kind":"attribute"},"aLobeC":{"type":"vec4","kind":"attribute"},"aLobeP":{"type":"vec4","kind":"attribute"}};
export const blobFragmentBindings = {"vN":{"type":"vec3","kind":"varying"},"vV":{"type":"vec3","kind":"varying"},"vWp":{"type":"vec3","kind":"varying"},"vLobe":{"type":"vec3","kind":"varying"},"uShadow":{"type":"vec3","kind":"uniform"},"uBody":{"type":"vec3","kind":"uniform"},"uHigh":{"type":"vec3","kind":"uniform"},"uRimCol":{"type":"vec3","kind":"uniform"},"uLight":{"type":"vec3","kind":"uniform"},"uBands":{"type":"float","kind":"uniform"},"uBandA":{"type":"float","kind":"uniform"},"uBandB":{"type":"float","kind":"uniform"},"uRimPow":{"type":"float","kind":"uniform"},"uRimAmt":{"type":"float","kind":"uniform"},"uFlat":{"type":"float","kind":"uniform"},"uRampKeyMode":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uGroundY":{"type":"float","kind":"uniform"},"uHeightSpan":{"type":"float","kind":"uniform"},"uUseToon":{"type":"float","kind":"uniform"},"uToonRamp":{"type":"float","kind":"uniform"},"uToonShadowScale":{"type":"float","kind":"uniform"},"uToonHighMix":{"type":"float","kind":"uniform"},"uRampBlendMode":{"type":"float","kind":"uniform"},"uRampBlendWeight":{"type":"float","kind":"uniform"},"uLightOn":{"type":"float","kind":"uniform"},"uLightFall":{"type":"float","kind":"uniform"},"uLightPos":{"type":"vec3","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const crystalVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aDir":{"type":"vec3","kind":"attribute"},"aOrg":{"type":"vec3","kind":"attribute"},"aLen":{"type":"float","kind":"attribute"},"aWid":{"type":"float","kind":"attribute"},"aT0":{"type":"float","kind":"attribute"},"aSeed":{"type":"float","kind":"attribute"},"aAlong":{"type":"float","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uGrowDur":{"type":"float","kind":"uniform"},"uOvershoot":{"type":"float","kind":"uniform"},"uInflate":{"type":"float","kind":"uniform"},"uHasCollapse":{"type":"float","kind":"uniform"},"uCollapseStart":{"type":"float","kind":"uniform"},"uCollapseDur":{"type":"float","kind":"uniform"},"vN":{"type":"vec3","kind":"varying"},"vW":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vSeed":{"type":"float","kind":"varying"}};
export const crystalFragmentBindings = {"vN":{"type":"vec3","kind":"varying"},"vW":{"type":"vec3","kind":"varying"},"vAlong":{"type":"float","kind":"varying"},"vSeed":{"type":"float","kind":"varying"},"uTip":{"type":"vec3","kind":"uniform"},"uFace":{"type":"vec3","kind":"uniform"},"uEdge":{"type":"vec3","kind":"uniform"},"uCam":{"type":"vec3","kind":"uniform"},"uFresPow":{"type":"float","kind":"uniform"},"uGlintFreq":{"type":"float","kind":"uniform"},"uGlintSpeed":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uFlat":{"type":"float","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"}};
export const splashVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uSliverScale":{"type":"vec2","kind":"uniform"},"uSliverOffset":{"type":"vec3","kind":"uniform"},"uSliverRoll":{"type":"float","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"}};
export const splashFragmentBindings = {"vUv":{"type":"vec2","kind":"varying"},"uCol":{"type":"vec3","kind":"uniform"},"uDark":{"type":"vec3","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"}};
export const ribbonVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"uTime":{"type":"float","kind":"uniform"},"uHead":{"type":"float","kind":"uniform"},"uTail":{"type":"float","kind":"uniform"},"uWidth":{"type":"float","kind":"uniform"},"uMorph":{"type":"float","kind":"uniform"},"uSpread":{"type":"float","kind":"uniform"},"uWidthJitter":{"type":"float","kind":"uniform"},"uPhaseJitter":{"type":"float","kind":"uniform"},"uTaperHead":{"type":"float","kind":"uniform"},"uTaperTail":{"type":"float","kind":"uniform"},"uOrientPath":{"type":"float","kind":"uniform"},"vSide":{"type":"float","kind":"varying"},"vW":{"type":"float","kind":"varying"},"vStrand":{"type":"float","kind":"varying"},"vFade":{"type":"float","kind":"varying"},"uPathType":{"type":"int","kind":"uniform"},"uPathA":{"type":"vec3","kind":"uniform"},"uPathB":{"type":"vec3","kind":"uniform"},"uPathC":{"type":"vec3","kind":"uniform"},"uPathD":{"type":"vec4","kind":"uniform"},"uPathW":{"type":"vec2","kind":"uniform"},"uMorphPathType":{"type":"int","kind":"uniform"},"uMorphPathA":{"type":"vec3","kind":"uniform"},"uMorphPathB":{"type":"vec3","kind":"uniform"},"uMorphPathC":{"type":"vec3","kind":"uniform"},"uMorphPathD":{"type":"vec4","kind":"uniform"},"uMorphPathW":{"type":"vec2","kind":"uniform"}};
export const ribbonFragmentBindings = {"uTime":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uCore":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vSide":{"type":"float","kind":"varying"},"vW":{"type":"float","kind":"varying"},"vStrand":{"type":"float","kind":"varying"},"vFade":{"type":"float","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const wireBurstVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aDir":{"type":"vec3","kind":"attribute"},"aSeed":{"type":"float","kind":"attribute"},"aKind":{"type":"float","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uTravel":{"type":"float","kind":"uniform"},"uChannel":{"type":"float","kind":"uniform"},"uSplitOffset":{"type":"float","kind":"uniform"},"uSplitGrowth":{"type":"float","kind":"uniform"},"vA":{"type":"float","kind":"varying"},"vK":{"type":"float","kind":"varying"},"uCurveA":{"type":"vec2","size":8,"kind":"uniform"},"uCurveAN":{"type":"int","kind":"uniform"},"uCurveAEase":{"type":"float","kind":"uniform"}};
export const wireBurstFragmentBindings = {"uOpacity":{"type":"float","kind":"uniform"},"uLayerU":{"type":"float","kind":"uniform"},"uRampKeyMode":{"type":"float","kind":"uniform"},"uChannel":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vA":{"type":"float","kind":"varying"},"vK":{"type":"float","kind":"varying"},"uRamp":{"type":"vec4","size":6,"kind":"uniform"},"uRampT":{"type":"float","size":6,"kind":"uniform"},"uRampN":{"type":"int","kind":"uniform"}};
export const arcVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"float","kind":"attribute"},"uTime":{"type":"float","kind":"uniform"},"uHeight":{"type":"float","kind":"uniform"},"uWidthK":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uWidth":{"type":"float","kind":"uniform"},"uMinWidth":{"type":"float","kind":"uniform"},"uJitterAmp":{"type":"float","kind":"uniform"},"uJitterFreq":{"type":"float","kind":"uniform"},"uJitterFold":{"type":"float","kind":"uniform"},"uSkip":{"type":"float","kind":"uniform"},"uRadius":{"type":"vec2","kind":"uniform"},"uPitch":{"type":"vec2","kind":"uniform"},"uPeriod":{"type":"vec2","kind":"uniform"},"uOnTime":{"type":"vec2","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vA":{"type":"float","kind":"varying"}};
export const arcFragmentBindings = {"uCore":{"type":"vec3","kind":"uniform"},"uHalo":{"type":"vec3","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vA":{"type":"float","kind":"varying"}};
export const streakVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"float","kind":"attribute"},"uGrow":{"type":"float","kind":"uniform"},"uCurvature":{"type":"float","kind":"uniform"},"uUpBias":{"type":"float","kind":"uniform"},"uBundles":{"type":"float","kind":"uniform"},"uBundleSpread":{"type":"float","kind":"uniform"},"uStagger":{"type":"float","kind":"uniform"},"uLength":{"type":"vec2","kind":"uniform"},"uWidth":{"type":"vec2","kind":"uniform"},"uHueA":{"type":"vec3","kind":"uniform"},"uHueB":{"type":"vec3","kind":"uniform"},"uHueC":{"type":"vec3","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vK":{"type":"float","kind":"varying"},"vC":{"type":"vec3","kind":"varying"}};
export const streakFragmentBindings = {"uOpacity":{"type":"float","kind":"uniform"},"uFlicker":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"},"vUv":{"type":"vec2","kind":"varying"},"vK":{"type":"float","kind":"varying"},"vC":{"type":"vec3","kind":"varying"}};
export const sheetVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSheetSeed":{"type":"float","kind":"attribute"},"aSheetAge":{"type":"float","kind":"attribute"},"vN":{"type":"vec3","kind":"varying"},"vW":{"type":"vec3","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"vSeed":{"type":"float","kind":"varying"},"vAge":{"type":"float","kind":"varying"}};
export const sheetFragmentBindings = {"vN":{"type":"vec3","kind":"varying"},"vW":{"type":"vec3","kind":"varying"},"vUv":{"type":"vec2","kind":"varying"},"vSeed":{"type":"float","kind":"varying"},"vAge":{"type":"float","kind":"varying"},"uShadow":{"type":"vec3","kind":"uniform"},"uBody":{"type":"vec3","kind":"uniform"},"uHigh":{"type":"vec3","kind":"uniform"},"uRim":{"type":"vec3","kind":"uniform"},"uLight":{"type":"vec3","kind":"uniform"},"uCam":{"type":"vec3","kind":"uniform"},"uBands":{"type":"vec2","kind":"uniform"},"uRimPow":{"type":"float","kind":"uniform"},"uRimAmt":{"type":"float","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uTear":{"type":"float","kind":"uniform"},"uTearScale":{"type":"float","kind":"uniform"},"uBandCount":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"}};
export const crescentVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aS":{"type":"float","kind":"attribute"},"aQ":{"type":"float","kind":"attribute"},"uEx":{"type":"vec3","kind":"uniform"},"uEy":{"type":"vec3","kind":"uniform"},"uN":{"type":"vec3","kind":"uniform"},"uR":{"type":"float","kind":"uniform"},"uPh0":{"type":"float","kind":"uniform"},"uSweep":{"type":"float","kind":"uniform"},"uWmax":{"type":"float","kind":"uniform"},"uHead":{"type":"float","kind":"uniform"},"uTail":{"type":"float","kind":"uniform"},"uScale":{"type":"float","kind":"uniform"},"uRadOff":{"type":"float","kind":"uniform"},"uWiden":{"type":"float","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uSeed":{"type":"float","kind":"uniform"},"uPeakFrom":{"type":"float","kind":"uniform"},"uTipPower":{"type":"float","kind":"uniform"},"uRootFade":{"type":"float","kind":"uniform"},"uScreenSpace":{"type":"float","kind":"uniform"},"vS":{"type":"float","kind":"varying"},"vQ":{"type":"float","kind":"varying"},"vWin":{"type":"float","kind":"varying"},"vTipD":{"type":"float","kind":"varying"}};
export const crescentFragmentBindings = {"vS":{"type":"float","kind":"varying"},"vQ":{"type":"float","kind":"varying"},"vWin":{"type":"float","kind":"varying"},"vTipD":{"type":"float","kind":"varying"},"uC0":{"type":"vec3","kind":"uniform"},"uC1":{"type":"vec3","kind":"uniform"},"uC2":{"type":"vec3","kind":"uniform"},"uC3":{"type":"vec3","kind":"uniform"},"uStreakCol":{"type":"vec3","kind":"uniform"},"uI":{"type":"vec4","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uSeed":{"type":"float","kind":"uniform"},"uAlpha":{"type":"float","kind":"uniform"},"uHead":{"type":"float","kind":"uniform"},"uTail":{"type":"float","kind":"uniform"},"uTear":{"type":"float","kind":"uniform"},"uErode":{"type":"float","kind":"uniform"},"uTipHot":{"type":"float","kind":"uniform"},"uStreakOn":{"type":"float","kind":"uniform"},"uStreakFreq":{"type":"float","kind":"uniform"},"uStreakPan":{"type":"float","kind":"uniform"},"uStreakI":{"type":"float","kind":"uniform"},"uStreakSeg":{"type":"float","kind":"uniform"},"uVorScale":{"type":"float","kind":"uniform"},"uSeamWidth":{"type":"float","kind":"uniform"},"uFrontWidth":{"type":"float","kind":"uniform"},"uBandT":{"type":"vec4","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"}};
export const lickVertexBindings = {"vClipPosition":{"type":"vec4","kind":"varying"},"aSeed":{"type":"float","kind":"attribute"},"aSide":{"type":"float","kind":"attribute"},"uAnchor":{"type":"vec3","kind":"uniform"},"uTangent":{"type":"vec3","kind":"uniform"},"uNormal":{"type":"vec3","kind":"uniform"},"uDrift":{"type":"vec3","kind":"uniform"},"uLength":{"type":"vec2","kind":"uniform"},"uWidth":{"type":"vec2","kind":"uniform"},"uStagger":{"type":"vec2","kind":"uniform"},"uLife":{"type":"vec2","kind":"uniform"},"uTime":{"type":"float","kind":"uniform"},"uSpan":{"type":"float","kind":"uniform"},"uCurl":{"type":"float","kind":"uniform"},"uFlipHz":{"type":"float","kind":"uniform"},"vS":{"type":"float","kind":"varying"},"vA":{"type":"float","kind":"varying"}};
export const lickFragmentBindings = {"vS":{"type":"float","kind":"varying"},"vA":{"type":"float","kind":"varying"},"uHot":{"type":"vec3","kind":"uniform"},"uBody":{"type":"vec3","kind":"uniform"},"uOpacity":{"type":"float","kind":"uniform"},"uBlendMode":{"type":"int","kind":"uniform"}};
